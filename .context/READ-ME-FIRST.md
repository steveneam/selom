# READ ME FIRST → see `AGENTS.md`

Agent orientation lives in one place now: **`AGENTS.md`** at the repo root (the read
order: research vault → `CLAUDE.md`/`CODEX.md` → `agent_handoff/README.md`).

This file used to duplicate that content; it was de-duplicated so the two can't drift.
Open `AGENTS.md`.
