# Selom — CURRENT (Live State)

> **LIVE = the top sections only** (SESSIONS · LIVE · NEXT · DEFERRED · ENV · READ).
> Per-session **narrative lives in git commit messages + the linked SHA range, NOT here** —
> CURRENT carries only live pointers + the context git can't (verify deltas, NEXT, env,
> decisions). Each session is tagged **CODENAME · date · sha** and gets ONE row in the
> SESSIONS index. Update the slots in place; don't re-narrate commits. Rules:
> `agent_handoff/README.md` → "CURRENT.md shape".

## ▸ SESSIONS  (newest first — scan here; full detail = the commit range + git log)

| Tag | Date | SHA range | One-line |
|---|---|---|---|
| **AI-WRITE-PATH** | 2026-06-30 | `47d492b..66236a3` | **NEXT#1+#3 shipped; #2 spec'd.** **`47d492b` provenance chokepoint** — `/ai/apply` no longer trusts client-stamped attribution; one server fn `provenance.stamp_ai_actions` derives `actor`="ai" · `model`=active gateway · `approved_by`=verified tenant (`require_user`) · `approved_at`=server clock, keeping only the `{action_id,type,target,prompt}` delta the FE now posts. `commit_recompute` rerouted through the SAME chokepoint (no parallel path) + structure-guard. Real-engine verified: a forged delta is fully overwritten ([[selom-provenance-stamping-chokepoint]]). **`2ecee04` AI-explain follow-ons** — Copy (AI text gets an `[AI-generated]` prefix; "Copied" only on a real write) · auto refresh-on-rescore · live sweep-prose (✨, source=ai only) · shared tightened `build_explain_prompt` (labels `panel_count`, forbids "reviewers"; both live gateways, no drift). **`66236a3` cross-stage AI entry-points design spec** (#2 — build DEFERRED, owner reviews next session). Dual-reviewed: gauntlet 0-confirmed (2 refuted-but-real closed: commit_recompute path + Copy focus-ring), fe-review 1-confirmed (Copy attribution) fixed. Gates: BE fast **1025/1**, ruff clean; FE tsc/eslint clean, vitest **399**. Browser-verified on real operator `source=ai`. |
| **MIXING-METRICS** | 2026-06-30 | `58d103f` | **`mixing_metrics` skill — integration-benchmark metrics** (closes the biggest cold-drive recall gap, `docs/records/skill-gaps.md`). `skills/mixing_metrics/metrics.py` (pure numpy/sklearn): iLISI/cLISI (perplexity-calibrated inverse-Simpson) · kBET (χ² acceptance) · ARI/NMI (KMeans vs labels) · ASW batch/label. **LISI+kBET clean-room** (GPL harmonypy/kBET not used); ARI/NMI/silhouette = sklearn. **No new dep.** stub + `run_real` (obsm embedding or compute PCA; batch + optional label; bar + Stats table). Grader `infer_metric_type` types it `MT_INTEGRATION`. **gauntlet 1 HIGH + 1 MED fixed** — HIGH: `MT_INTEGRATION` `direction_close=True` short-circuited every bounded-positive metric to CLOSE (a mixing collapse scored reproduced) → set **`direction_close=False`** so the 10%/50% magnitude band gates (fixes a pre-existing iLISI/cLISI hole too) + a collapse-FAIL test; MED: dropped the generic `silhouette` routing substring (over-widened auto-extract goldens). Real-engine verified. fast **1019/1**, ruff clean |
| **AI-GATEWAY** | 2026-06-30 | `7580621` | **Live Vercel AI Gateway + operator demo stand-in wired.** `SELOM_AI_GATEWAY` = `gateway`\|`operator`\|`live`\|`null` (first match; absent credential → null). **gateway** = new `VercelAIGateway` (httpx OpenAI-compat → `ai-gateway.vercel.sh`, Groq→Bedrock, `meta/llama-3.3-70b`; **provider-agnostic — swap `ai_gateway_model`, zero feature code**) = the real gated-product path. **operator** = `OperatorActionGateway.from_recordings()` (Claude-authored, **zero credit**) = the canned demo engine for rpgrip1/jev/hani + the **build/optimize default**; per-input keying (`explain_score`→`paper_id`). Honesty unchanged (✨ only when text≠deterministic). Shared `build_action_prompt` extracted. FE `buildScorecardPayload`+`paper_id`. **gauntlet 2 low fixed** — LAUNCH-GATES Llama-license line + a FE recordings↔`REPRO_LEDGERS` parity guard. **Live-verified** operator (zero credit) + gateway (real Llama explain+sweep, source=ai). fast **1006/1**, ruff clean; FE tsc/eslint clean, vitest **399**. Spec `docs/ai-gateway-wiring/spec.md` |
| **AI-EXPLAIN** | 2026-06-30 | `a7d477c` | **explain_score + propose_sweep wired to reachable UI** ([[selom-shipped-not-reachable]]). "Explain this score" disclosure on the Score stage + public showcase (`ScoreReport showExplain` → grounded `/ai/explain`, ✨ only when `source==ai`); sweep **Suggest** → real deterministic recommender (`rank_sweep_space`) as ranked chips. `/ai/explain` gains `suggestions[]` + **source-by-what-was-produced** (a degrade can't mislabel a deterministic fallback as AI — gauntlet HIGH). Pure FE mappers `lib/ai/explain-inputs.ts` + mock + BE↔mock ranking parity. Reviewed gauntlet(4)+fe-review(2). Live-verified real engine + browser. fast 982/1 · vitest 395 |
| **P1-HOOKS** | 2026-06-30 | `dbb8a7a` | **NEXT#1 done — P1 ingest engine hooks** (`map_columns` + `apply_cleaning_step` flipped gap→wired). New `engine/columns.py` role resolver (logFC/pval/gene, override-only-never-fabricate) honoured at D1 (`compat`)/D2 (`frame_schema`)/classifier/the **volcano** runner; `_column_override` threaded via `_execute_skill_run` (clean 400 on a missing col) + **recorded** in `provenance.params` → reproduces zero-AI. Cleaning toggle **param-backed** (`STEP_PARAM` normalize→param, reuses `set_param`) + a drift-guard test. Forcing-Qs answered → spec refreshed to the real engine model. **review-gauntlet** 2 medium fixed (effective-override prune under `override=true` · STEP_PARAM drift guard + spec fix). **Live-verified** on the real engine + caught/fixed a latent volcano `Index.iloc` 500. Fast gate **975/1**, ruff clean |
| **S5-SHIP** | 2026-06-30 | `27b4836..0e0afa5` | AI-Helpers **S5 SHIPPED** (FE attribution layer: banner author-counter · ✨ proposed/staged/applied marker · AI panel [Activity+Gaps] · Reset · `/ai/apply` design-thread + `provenance.actions[]`). **Dual-reviewed** — review-gauntlet (6 bugs, 3 HIGH) + the NEW **fe-review** tool (13 findings) → all fixed, guard-tested, measured-verified. Plus the **fe-review** operating tool (V·R·D·A·R·N, peer to gauntlet/retro) + the `use-ai-helpers` hook extraction + a retro over the initiative. tsc/eslint clean · vitest 385 · BE AI tests + design test |
| **AI-HELPERS** | 2026-06-29 | `8a82d8a..6288b9a` | AI-Helpers initiative **S1–S4 SHIPPED** (Action-Gateway write-path: propose→validate(same-as-humans)→stage/approve→gated execute→actor-tagged provenance/"AI compiles away"; live PydanticAI translator gated OFF; P1 ingest helpers + 2 honest engine gaps; gap backlog+`GET /ai/gaps`+`select_skill`+`/ai/explain`); **gauntlet-reviewed twice** (fixed 1 HIGH backdoor + 2 MED audit-fidelity); **Selom Playbook + review-gauntlet + retro** operating layer; **S5 FE spec'd + next**. Fast gate 948 |
| **BE-PKGS** | 2026-06-29 | `d18afb0..61a6d42` | flat `reproduction*.py` → `reproduction/` package (§3A, re-export `__init__`) + `methods/legends/provenance/guardrails` → `companions/` (§3B), both contract-frozen; `test_structure_guard` extended (no-flat-module checks, verified both ways) → plan §3 all DONE; fast gate 848/1 |
| **FE-HOOKS** | 2026-06-29 | `c6af964..8bf5c26` | `project-workspace.tsx` → 3 hooks (view/crud/run, §3C DEFERRED→DONE, 1384→1111); +dev:mock 7c persistence handlers (un-rollback optimistic figures); `data-check-routing` e2e repointed to the figure-data stage → e2e 4/4 |
| **RECORDS-MOVE** | 2026-06-29 | `b86f0e1` | `docs/records/` physical move — 22 record docs bucketed, 78 files / 169 inbound pointers rewritten; pushed FOUNDATION-FINISH |
| **FOUNDATION-FINISH** | 2026-06-29 | `21c1177..6004a75` | docs name-collision rename + orientation de-dup; raise free-tier quota defaults (unblocks dogfood); scoped FE-3 / BE-pkgs / FE-hooks |
| STRUCTURE-REFACTOR | 2026-06-29 | `b6e3b38..c70635e` | `main.py`→`routers/` + finish lib/ feature-dirs + conventions/guards/plan; Vercel git-email fix |
| 7C-STATE | 2026-06-29 | `bc2375f..b6e3b38` | FE state → Postgres (optimistic-cache stores, client-authoritative ids) |
| DEPLOY-BACKBONE | 2026-06-28 | `…318795d` | GitHub↔AWS↔Vercel CI backbone + reflect-checkpoint + pre-7c hardening |
| AWS-MAT-1..5 | 2026-06-28 | (git) | S3 object-store seam · jobs→SQL · tenant schema · Clerk auth · step-6 prep |
| older | — | (git / `archive/`) | `git log` · `agent_handoff/archive/` |

## ▸ LIVE · AI-WRITE-PATH · 2026-06-30 20:23 +10:00 · **committed `47d492b..66236a3` on `main`, NOT pushed — 3 unpushed; owner pushes; verify `git rev-list --left-right --count origin/main...HEAD` → expect `0 3`** · Claude (FE+BE), Opus 4.8 xhigh

- **NEXT#1 provenance stamping chokepoint (`47d492b`)** — the AI-write integrity invariant ([[selom-provenance-stamping-chokepoint]]). `/ai/apply` no longer trusts the client's attribution. New `companions/provenance.py::stamp_ai_actions` is the ONE server-controlled stamp: keeps only the descriptive `{action_id,type,target,prompt}` delta and re-derives `actor`="ai" · `model`=`get_action_gateway().model_id` · `approved_by`=`ctx.user_id` (`/ai/apply` now `Depends(require_user)`) · `approved_at`=server clock. FE `approvedActions()` posts ONLY the delta (`AiActionDelta`); the mock re-derives too. `ai/execute.py::commit_recompute` (in-process / test path) rerouted through the same chokepoint (no parallel stamping) + a `test_structure_guard` "no parallel path" check (routers + execute.py). Spec `docs/provenance-chokepoint/spec.md`. **Accepted limitation:** `model` is the gateway active AT APPLY time (propose→env-flip→apply would record the apply-time model; closing fully needs a stateful token — out of scope, single-user).
- **NEXT#3 AI-explain follow-ons (`2ecee04`)** — Copy on the score explanation (AI text prefixed `[AI-generated]` so attribution travels off-screen; "Copied" only on a successful write; cleared on a re-score swap) · auto refresh-on-rescore (re-fetch open / drop stale closed) · live sweep AI-prose line (✨, rendered only `source==="ai"`; picks stay deterministic) · prompt tightening: one shared `ai/gateway.py::build_explain_prompt` + `EXPLAIN_SYSTEM_PROMPT` (labels every scorecard field — `panel_count` = figure panels, forbids "reviewers"/renaming), used by BOTH live gateways. Specs `docs/ai-explain-followups/spec.md`; deferred enhancements `docs/ai-helpers/s5-followups.md` #11–14.
- **NEXT#2 cross-stage AI entry points (`66236a3`)** — **design spec only**, `docs/ai-cross-stage-entry-points/spec.md` (ingest/route/output need entry points; analyze+grade done; one reusable composer; 5 open UX Qs; phasing output→route→ingest). **Owner reviews next session before any build.**
- **Reviews:** review-gauntlet **0 confirmed** (4 refuted; 2 refuted-but-real closed anyway: `commit_recompute` parallel path + the Copy focus-ring). fe-review **1 confirmed** (Copy stripped the AI tag → fixed with the `[AI-generated]` prefix) + 6 G1 affordance gaps (4 deferred → `s5-followups.md` #11–14).
- **Verified on real data ([[verify-on-real-data-not-mock]]):** uvicorn :8013 (operator gateway) — a FORGED `/ai/apply` delta (`actor:human, model:evil, approved_by:attacker, approved_at:1999`) came back recorded as `actor:ai, model:operator, approved_by:dev-user, approved_at:<server now>`. Browser (FE :3007 → :8013): `/reproduction/jev` "Explain this score" → operator `source=ai` → ✨ badge + Copy; clipboard read-back confirmed the `[AI-generated]` prefix; no console errors from the feature (the `/api/{projects,figures,…}` 503s = the SQL store I left off in the minimal verify backend, not a regression).
- **Owner key (now persisted):** `AI_GATEWAY_API_KEY` is saved in the **gitignored repo-root `.env`** (`.gitignore:19`; `config.py` `env_file=_REPO_ROOT/.env`). The KEY alone does NOT turn the gateway on — `SELOM_AI_GATEWAY` defaults `null`, so tests + dev stay offline/deterministic; set `SELOM_AI_GATEWAY=gateway` per-run to use it. **Live-model behavior VERIFIED** (real `meta/llama-3.3-70b` call): the tightened prompt grounds `panel_count` as "panels", says nothing about "reviewers", invents no entities — the #3 prompt fix holds end-to-end.

## ▸ NEXT — (re-ranked 2026-06-30; NEXT#1 provenance · NEXT#3 explain follow-ons DONE this session; AI-EXPLAIN · AI-GATEWAY · MIXING-METRICS DONE earlier)

1. **Cross-stage AI entry points — BUILD** (MED — design-first; the spec is written, `66236a3`). **Owner reviews `docs/ai-cross-stage-entry-points/spec.md` + answers the 5 open Qs (Q1 shared-vs-per-stage pending queue is the big one) BEFORE building.** Then phase: extract the reusable `AskAi` composer → **output** (cosmetic, lowest risk) → **route** (`select_skill` pre-select) → **ingest** (most entangled). Every figure-producing write must stamp via the NEXT#1 chokepoint. `docs/ai-helpers/s5-followups.md` #0.
2. **AI-explain enhancement backlog** (`s5-followups` #11–14, this session's fe-review G1 gaps): sweep-prose Copy (symmetry) · `approved_by` UI surface (now server-trusted, recorded, but unshown — cheap in the ✨ marker tooltip) · staged-vs-committed model-drift cue · "updated for the re-scored run" cue. Plus the older #8 "add to methods" (deferred — score-commentary ≠ figure methods).
3. _(DONE this session) Live `explain` prompt model-behavior verified against real Llama — grounds `panel_count`, drops "reviewers"._
- **Skill-combination robustness (owner-flagged 2026-06-30, AFTER the current pillar):** (a) an **ERG trace↔bar equality test** — `erg_traces` + `erg_bwave_bar` share `_erg.landmarks` but no test pins the cross-skill numeric agreement; (b) a real **`integration`→`mixing_metrics` chain** — `integration` returns a figure not a persisted corrected h5ad, so the Melody-corrected hand-off is manual/data-dependent. (Detail in `docs/mixing-metrics/spec.md`.) NB: the engine runs skills independently — `chains_with` is metadata; the multi-skill surface is the reproduction deck (robustly tested per-paper).
- **Deferred bigger rocks:** **Demo-page UI** (scripted "watch it run" + "Request access" CTA) — ON HOLD with the landing/hero AND until the engine is fully built + in AWS [[selom-pricing-demo-strategy]]. The deferred server work (7c FE-3 upload wiring · step-8 split deploy + the gateway production spend-cap). **Mechanize lessons:** a reachability-guard test [[selom-shipped-not-reachable]]. **Housekeeping:** archive older SESSIONS rows [[archive-currentmd-history-weekly]].

## ▸ DEFERRED (specced/scoped, not built)

- **`/ai/apply` actor-tag forgery-hardening — DONE `47d492b`** (the provenance chokepoint subsumed it; kept here as a pointer for one session, then prune).
- **Live AI verification** — `SELOM_AI_GATEWAY=live` + `ANTHROPIC_API_KEY` flips the gateway on; the write-path is gated OFF + degrade-clean so it works without a key, but live proposal/explain TEXT awaits the key (its own verify step).
- **Server work (after AI-Helpers): 7c-(b) FE-3 upload wiring** (~1.5–2d, FE lane) — intake→presigned PUT→confirm→parse→**run-from-`dataset_id`**; backend built+tested (`lib/uploads/api.ts` + `runSkillByDataset` + `Dataset.status`/server-id + MSW + vitest; traps: local-PUT vs S3-POST, `intake` mints the `dataset_id`, omit `content_sha256` at intake, 402 rollback; read `docs/aws-materialization/7c-frontend-state-migration.md`). **+ Step 8 (split deploy):** Lambda(API)+Fargate(compute) · Aurora min=0 ACU + Alembic head (incl `0004`) · live Clerk · S3→EventBridge→Step Functions→Fargate + cron T2 · BYPASSRLS sweep · CORS · GitHub→AWS OIDC role (owner-pending; `infra/README.md`) → kill static `selom-dev` key → flip `API_PROXY_TARGET`. Detail `docs/aws-materialization/plan.md`.

## ▸ ENV / landmines

- `:8000` = **eamos** — NEVER kill; run the BE on another port (smoke used :8010).
- BE tests: uv-3.12 PY + `PYTHONPATH=".venv/Lib/site-packages;."` (NOT `uv run` — EDR) [[selom-backend-python-exec]]; fast gate = `pytest -m "not slow"` (**now 1025 passed / 1 skipped**); ruff = `$PY -m ruff check`. The PY is `…/AppData/Roaming/uv/python/cpython-3.12.13-windows-x86_64-none/python.exe`. ⚠ run from **`app/backend`** (a bg shell starts in `D:\selom` → `.venv` not found → "No module named pytest"; `cd` first).
- **AI gateway (WIRED `7580621`):** `SELOM_AI_GATEWAY` = `gateway`\|`operator`\|`live`\|`null` (default `null`). **`gateway`** + `AI_GATEWAY_API_KEY` (`vck_…`) → live `VercelAIGateway` (Vercel AI Gateway, Llama 3.3; **same gateway eamos uses** — provider-agnostic via `SELOM_AI_GATEWAY_MODEL`). **`operator`** → recorded `ai/recordings/explain.json` (zero credit; demo + build default). **`live`** + `ANTHROPIC_API_KEY` → `PydanticAIGateway` (direct Anthropic, optional). Else degrade-clean → Null. **No new dep** — `VercelAIGateway` uses httpx (the `pydantic-ai-slim[anthropic]` extra is only for `live`). Verify: `SELOM_AI_GATEWAY=operator` (free) or `SELOM_AI_GATEWAY=gateway` (the key is now in the **gitignored repo-root `.env`** = `AI_GATEWAY_API_KEY`, auto-loaded by `config.py`; no inline key needed — NEVER commit `.env`). Gap backlog persists via `SELOM_GAP_STORE=jsonl`.
- `.claude/workflows/*` (**review-gauntlet** correctness · **fe-review** FE-interaction/V·R·D·A·R·N · **retro** reflect) + `.claude/agents/*` load at **session start** → invoke a freshly-added workflow by **`scriptPath`** (`Workflow({scriptPath:"D:/selom/.claude/workflows/fe-review.js", args:{scope}})` — use the ABSOLUTE path; the CWD-relative form resolved against `app/frontend`), not `name`. Lenses inlined (self-contained).
- FE dev: `npx next dev --webpack` (Turbopack panics) [[selom-turbopack-webpack-workaround]].
- Local dogfood: `SELOM_JOB_STORE=sql SELOM_DATABASE_URL=sqlite:///dev.db SELOM_DB_AUTO_CREATE=true` + FE `API_PROXY_TARGET=http://localhost:<port>`.
- ⚠ Quota (real-backend dogfood): `server_default` only applies at INSERT → an existing `dev.db` user row keeps the OLD caps; recreate `dev.db` or `UPDATE users SET max_projects=50, max_datasets=200, max_storage_bytes=53687091200 WHERE user_id='dev-user'` before dogfood.
- `dev:mock` now persists 7c writes (`mocks/persistence-handlers.ts`) — a run's figure survives (no rollback); e2e via `next dev --webpack --port 3011` + `npx playwright test` (config reuses an existing :3011 server).
- This repo's `git user.email` MUST stay `282747725+steveneam@users.noreply.github.com` or Vercel blocks deploys [[selom-git-commit-email-vercel]].
- Snapshot localStorage before any live-store verify [[selom-fe-probe-autopersist-gotcha]].

## ▸ READ FIRST

`docs/ai-helpers/spec.md` + `docs/ai-helpers/s5-frontend-spec.md` (the S5 build blueprint) · `docs/operating/playbook.md` · `docs/p1-ingest-engine-hooks/spec.md` · `docs/aws-materialization/{plan,7c-frontend-state-migration}.md` · memory [[selom-ai-helpers]] [[verify-on-real-data-not-mock]] [[selom-git-commit-email-vercel]] [[reflect-checkpoint-during-phase-build]] [[selom-fe-probe-autopersist-gotcha]].

<!-- prior live-state (7C-STATE, 2026-06-29 01:06 +10:00; superseded by the block above — kept for the still-active step-8 NEXT detail; full text in git @ `c70635e`):
_Last updated: 2026-06-29 01:06 +10:00 (session "7C-STATE" — **AWS MATERIALIZATION BUILD: step 7c (FE state → Postgres) SHIPPED + LIVE-VERIFIED**) · Claude (acting FE+BE) · Opus 4.8 (xhigh). **Origin/main @ `bc2375f`; local is AHEAD by this session's commits, NOT pushed — owner pushes when asked; verify vs origin at session start.** **What 7c is:** `projectStore` + `workspaceStore` rewritten behind their UNCHANGED interfaces (zero component edits) as an **optimistic local cache + background API write queue + reconcile** (sub-spec `docs/aws-materialization/7c-frontend-state-migration.md`, §10b = the full verify log). **Keystone = client-authoritative ids** (FE mints the uuid → backend stores it verbatim → no temp-id remap; idempotent retries/Undo/import). **Shipped (8 commits):** **BE-1 `7956575`** (client-id `POST /projects` + figures CRUD + new `library/` package [TenantRepo base · FigureMixin · composed `LibraryRepo`/`get_library_repo`] + `figures.guardrails`/`users.local_import_at` cols + Alembic `0004`) · **BE-2 `f533e09`** (account-library repos+endpoints — `GET /workspace` · `/workspace/gene-sets` · `/workspace/papers`(+supplements via whole-paper upsert) · `/skill-installs` [project- + workspace-scoped, client-id]; `/workspace/*` namespaced to dodge the read-only `/papers`·`/gene-sets`·`/reproduction-runs` routes; DEFERRED `cleaning_recipes` [rides `datasets.qc`] + `reproduction_runs` [BE-engine driven]) · **BE-3 `947abe8`** (idempotent atomic `POST /import/local-state` + run-from-`dataset_id`: extracted `_execute_skill_run` shared body, `POST /skills/{id}/run-dataset`+`/jobs-dataset`, byte-identical to multipart) · **project/dataset PATCH+DELETE `551ef56`** + metadata `POST /datasets` · **FE-1 `30fa35d`** (`lib/api/client.ts` + `write-queue.ts` [coalesce · grace-delete+cancel · rollback · status] + `lib/projects/sync.ts` [adapters+reconcile] + the `projectStore` rewrite; `uid()` → full uuid; `useSyncStatus()`) · **FE-2 `90a183b`** (`workspaceStore` swap + `importLocalStateOnce()` wired into app-shell) · **proxy env `97402a1`** (`API_PROXY_TARGET`, default :8000). **GATES:** backend fast gate **839 passed / 1 skipped** (`-m "not slow"`, +32), ruff clean; FE **tsc + eslint clean, vitest 351** (+11). **LIVE-VERIFIED (FE :3000 → backend :8010, SQLite + `LocalObjectStore` + dev auth):** optimistic UI create → persisted (`onServer:true`) → **reload-from-server proven** (dropped from the mirror, reappeared via reconcile with the renamed value) · figure-spec PATCH persists (cold read) · **import idempotent** (2nd pass = 0; migrated the owner's real 8-project localStorage faithfully + non-destructively) · **402 quota rollback verified live** · 0 console errors. **⚑ NEXT = step 8 (split deploy):** light zip Lambda (API) + heavy **Fargate** (compute) · **provision Aurora min=0 ACU** · live **Clerk** · S3-event→EventBridge→Step-Functions→Fargate for jobs + cron for T2 · BYPASSRLS sweep role · IAM least-priv · CORS · the **GitHub→AWS OIDC role** (owner-pending; runbook `infra/README.md`) → kill the static `selom-dev` key · flip `API_PROXY_TARGET` to the deployed API. **⚠ 7c FOLLOW-UPS (sub-spec §10b):** (1) **raise the free/dev quota defaults** (3 projects/10 datasets block real dogfood; import bypasses quota but `create_project`/`create_dataset` enforce it) · (2) **FE-3 = wire the FE upload flow** (intake→presigned PUT→confirm→parse→run-dataset) to close the upload→run loop (run-dataset endpoint is built+tested; datasets are metadata-only in 7c so the FE still uses multipart `/run`) · (3) a verify left **cosmetic fixture drift** (createdAt refreshed + dataset size_bytes zeroed on the owner's 8 localStorage fixtures; content/specs intact; test entities + import marker cleaned) — snapshot localStorage before any live-store verify [[selom-fe-probe-autopersist-gotcha]]. **ENV:** local FE+BE dogfood = `SELOM_JOB_STORE=sql SELOM_DATABASE_URL=sqlite:///dev.db SELOM_DB_AUTO_CREATE=true` + uvicorn on a non-:8000 port (⚠ eamos holds :8000 — never kill) + FE `API_PROXY_TARGET=http://localhost:8010 npx next dev --webpack` [[selom-turbopack-webpack-workaround]]; backend tests via the uv-3.12 PY + PYTHONPATH (NOT uv run) [[selom-backend-python-exec]]. All dev servers I started are KILLED; dev.db deleted. READ FIRST: `docs/aws-materialization/{plan,spec,7c-frontend-state-migration}.md` · memory [[selom-deploy-integrations-state]] [[selom-aws-materialization-decision]] [[reflect-checkpoint-during-phase-build]]._
-->

<!-- prior live-state (DEPLOY-BACKBONE, 2026-06-28 23:49 +10:00; full text in git @ `bc2375f`):
_Last updated: 2026-06-28 23:49 +10:00 (session "DEPLOY-BACKBONE" — **GitHub↔AWS↔Vercel integration backbone + a mid-build reflect-checkpoint + pre-7c hardening**; owner-directed pause to validate the AWS foundation before 7c). **Shipped this session (8 commits, PUSHED — origin/main @ `318795d`, in sync; this re-stamp is the 9th, pending push):** **(1) Integration backbone `d68fdfa`** — GitHub Actions CI (`.github/workflows/{backend,frontend}-ci.yml`, no-AWS, activate on push) · **CDK-Python** `infra/` with the `SelomGithubOidc` stack (GitHub OIDC provider + least-priv `selom-github-deploy` role, assume `cdk-*` only) · `docs/aws-materialization/integrations.md` (the triangle + env inventory + admin runbook; NOT the CodePipeline/PAT pattern of the shared Medium guide). **(2) Plan corrections `31da4a8` (plan §0)** from a 3-agent checkpoint (live-AWS inspect · 2025-26 best-practice research · arch review): **Aurora min=0 ACU, NOT a 0.5-floor** (0.5 ≈ **$73/mo = 7× the $10 budget**) · heavy compute → **Fargate (Arm+Spot)**, not a Docker/ECR Lambda (the hard 15-min/10-GB wall on real `.h5ad`) · **SSE-S3** (bucket default), drop the KMS-deny until launch · async = S3-event→EventBridge→Step Functions→Fargate→`jobs` table. **(3) README console-path fix `c358f29` + integrations API-wiring fix `1f7581c`.** **(4) Backend hardening slices 1-3 (`f5d5f98`·`ee60713`·`0f411af`):** `head_size` now re-raises non-404 ClientErrors (a throttle/403 no longer reads as "absent" → no misleading 409) + streaming `download_to_path` · `/ready` readiness probe · startup config validation (s3⇒bucket / sql⇒db / clerk⇒issuer, fail-fast) · Lambda `NullPool` (no Aurora connection storm) · `obs.py` JSON-log seam · **streaming parse** (no whole-file-in-memory) + **M6 upload integrity check** (declared-vs-actual sha → 400) · **M2 job idempotency** (`result_cache_key`/`input_sha256` stamped on every job; opt-in cross-instance reuse via `SELOM_JOB_IDEMPOTENCY`, default off = inline path unchanged). **LIVE NOW:** FE at **`selom.vercel.app`** (GitHub↔Vercel native — push `main`→prod, PR→preview; install cmd `npm install --legacy-peer-deps`; ⚠ `/api/*` rewrites to **hardcoded `localhost:8000`** so backend-dependent actions wait for step 8 — make it env-var `API_PROXY_TARGET` then) · AWS account **hardened** (account-level S3 PAB · TLS-only bucket policy · abort-incomplete-multipart 7d lifecycle · Cost Anomaly $100→$5 · IAM Access Analyzer). **GATES:** fast gate **807 passed, 1 skipped** (`-m "not slow"`, +17 vs 790: object-store/config-ready/uploads/job-store) · ruff clean. **OWNER-PENDING (NOT blocking 7c):** GitHub→AWS OIDC role (admin/root — `selom-dev` is PowerUser, no `iam:*`; **AWS-Console runbook** in `infra/README.md`) → then kill the static `selom-dev` key (top security risk); live **Clerk** keys + **Aurora** = step 8. [[selom-deploy-integrations-state]] **⚑ NEXT (owner: "do 7c sub-spec next session") = WRITE THE 7c SUB-SPEC** before any 7c code — it's ~2× the one-liner: design **(a)** the sync/async store impedance (FE `lib/projects/store.ts` is a SYNCHRONOUS `useSyncExternalStore` with value-returning mutators → an API-backed store can't be a literal "zero component edits" drop-in; design an optimistic-local-cache + background API reconcile), **(b)** the ~6 missing tenant repos+endpoints (figures/workspaces/gene_sets/papers/skill_installs/cleaning_recipes/reproduction_runs — tables already in `db/schema.py`; reuse the `UploadRepo`/`TenantQuery` short-tx pattern; a debounced-write story for live figure-spec edits), **(c)** the one-time localStorage→Postgres import (read `selom.projects.v1`/`selom.workspace.v1` → POST → mark migrated), **(d)** the **run-from-`dataset_id`** endpoint (deferred from hardening H4 — closes the upload→run loop the FE drives; reads `parquet_s3_key`/`upload_s3_key`, no multipart re-upload). Then build 7c → step 8. [[reflect-checkpoint-during-phase-build]] **(AWS-MAT-5 [step 6] full text in the demoted block below + git @ `df82118`.)**_
-->
<!-- (DEPLOY-BACKBONE full text in git @ `bc2375f`^) -->

<!-- prior live-state (session AWS-MAT-5, 2026-06-28 21:56 +10:00):
_Last updated: 2026-06-28 21:56 +10:00 (session "AWS-MAT-5" — **AWS MATERIALIZATION BUILD**: **step 6 (presigned-POST upload + parsed-matrix + the `ingest.py` temp-leak fix + T2 reconciliation) SHIPPED + VERIFIED** on SQLite + `LocalObjectStore` + a **LIVE S3 round-trip on the dev bucket**. **The `uploads/` package:** `keys.py` (server-derived, traversal-safe keys — T1; the only client-influenced segment, the filename, is reduced to a basename so `../` can't widen the prefix), `repo.py` (`UploadRepo`: projects + datasets + quota behind `TenantQuery`, mirroring `SqlJobStore` — short txs, `set_tenant`+`upsert_user`+`run_with_db_retry`), `service.py` (the leak-free server-side parse + T2 heal/sweep). **HTTP surface (`main.py`, all `Depends(require_user)`, tenant = `ctx.user_id` ONLY, no request model carries `user_id` §6.2):** `POST /projects` · `GET /projects` · `POST /uploads/intake` (row-FIRST `pending_upload` + quota vs `max_projects`/`max_datasets`/`max_storage_bytes`, THEN a presigned **POST**) · `PUT /uploads/local/{key}` (the dev stand-in for the S3 direct PUT) · `POST /uploads/{id}/confirm` (head + flip `ready` + stamp the real size) · `POST /uploads/{id}/parse` (→ `data/{sha256}.csv`) · `GET /datasets` · `GET /datasets/{id}`. **Presigned POST, not a bare PUT** — the `content-length-range` ceiling is SIGNED IN, so the size cap is enforced BEFORE bytes land (live-verified on the dev bucket: an oversized POST is **denied 400**). **Parsed output is CSV** (`data/{sha256}.csv`) per **owner gate §16 Q5** (parquet/DuckDB deferred to the substrate — `datasets.parquet_s3_key` holds the `.csv` key now, its `.parquet` target later = a one-line swap; a non-tabular AnnData payload writes no CSV — tabular-only for now, honest). **The `ingest.py:177`/`:351` `delete=False` temp leak fixed HOLISTICALLY** (NOT a naive `finally` — `bundle.path` is reused across requests by the C3 cache): ONE process-managed dir (atexit backstop) + **content-addressed** decode temps (a re-decode REUSES one file → kills the per-invocation Lambda leak) + C3-eviction-tied deletion. **T2 reconciliation built+tested** (`heal_from_key` = the S3-event path, parses the tenant FROM the key, idempotent; `sweep_orphans` = the cron — heal-landed-but-unconfirmed / prune-stale-pending-past-`SELOM_UPLOAD_TTL_HOURS` / delete-objectless; the S3-event + cron WIRING + the BYPASSRLS sweep role are deploy infra, step 8). **ObjectStore seam gained `head_size` + `list_keys`** (both backends; live-verified on real S3). Uploads require a DB (`SELOM_DATABASE_URL`); a missing one is a clean **503**. **GATES:** fast gate **790 passed, 1 skipped** (`-m "not slow"`, +18 vs 772: `test_uploads` 10 · `test_uploads_reconcile` 4 · `test_ingest_temp_lifecycle` 4) · ruff clean. **NOT PUSHED — owner pushes when asked; local is ahead of origin/main @ `21d76ea`** by this session's commits (backend `uploads/` + ingest/object_store/main + docs/handoff). **⚑ NEXT = step 7c** (FE localStorage → Postgres behind the existing `ProjectStore`/`WorkspaceStore` interfaces §10; types pre-shaped; `param-spec-cache` stays client-side) → step 8 (split deploy + **provision Aurora** scale-to-zero/0.5-ACU-floor + wire **live Clerk** + the S3-event/cron triggers + BYPASSRLS sweep role + IAM §11). **⚑⚑ TWO EXTERNAL ITEMS STILL PENDING OWNER (surface, don't block 7c):** (1) live **Clerk** keys (`SELOM_AUTH_MODE=clerk` + issuer/JWKS — verify code+tests need nothing; keys plug in at deploy); (2) **Aurora** provisioning (validates the DB-RLS backstop + JSONB live — the only unverified-on-real-PG bits; app-layer isolation IS verified + is the hard gate) — my recommendation: defer both to step 8 ($10/mo guardrail, nothing deployed). **Resend decided:** product/marketing/lifecycle email only; Clerk owns auth email; `notify/` seam near launch [[selom-resend-email-decision]]. Detail in `docs/aws-materialization/{plan,spec}.md` §4.2/§6/§7 + the Resume Prompt below. (AWS-MAT-4 [step 7b] full text in git @ `21d76ea`.)_ -->
<!-- (AWS-MAT-5 full text in git @ `df82118`^) -->

<!-- prior live-state (session AWS-MAT-4, 2026-06-28 21:11 +10:00; full text in git @ `21d76ea`): step 7b (Clerk auth + TenantQuery + RLS enforcement + the cross-tenant isolation gate) SHIPPED + VERIFIED on SQLite. **Auth behind a config seam** (`auth/context.py`, mirroring `make_object_store`/`make_job_store`): `dev` mode (a fixed offline tenant, the **unchanged default** — the inner loop is single-tenant + needs no header) + `clerk` mode (`auth/clerk.py` — **hand-rolled RS256 over the JWKS, dependency-free** so the light Lambda zip stays small [§5]; the classic JWT attacks all fail closed — `none`/HS256-confusion/tampered/wrong-iss/aud/expired/nbf/missing+unknown-kid, 18 tests). The tenant is **always the verified `sub`, never a request param** (§6.2); `ClerkVerifier` also honours a Lambda-authorizer-injected `user_id` (prod). `require_user` = the FastAPI dependency. **Tenancy (`db/`):** `TenantQuery` binds ONE tenant at construction + injects the `user_id` predicate on every select/insert/update/delete — a forgotten filter is **structurally impossible**; `insert` OVERWRITES a forged `user_id`; `update` can't reassign ownership; no unscoped method exists. `set_tenant` emits `SET LOCAL app.user_id` (Postgres-only, parameterized via `set_config`) = the RLS backstop; `upsert_user` = first-request provisioning; `db/retry.py run_with_db_retry` = the Aurora-resume transient backoff (T3). **Migration `0003`:** `skill_requests` (M-003 admin queue, folded into the unified schema) + `analysis_jobs` tightening (users FK + RLS + NOT NULL `user_id`, **Postgres-only/dialect-gated** like the 0002 RLS — the SQLite dev path stays permissive; the app layer is the primary guarantee). **Jobs are tenant-owned:** `Job.user_id`, `SqlJobStore.create` stamps it + upserts the user + `SET LOCAL` in ONE tx, `get`/`update` scope by tenant (RLS on PG, predicate on SQLite); `submit`/`execute_job`/`get_job` + the arq worker thread the tenant (default = the dev user, so the inline path is unchanged); the jobs endpoints `Depends(require_user)` + scope poll/result/events by `ctx.user_id` (a cross-tenant job id → 404; a request param can't reassign ownership). **`presign_put` seam method** (spec §1.1) on Local + S3 (`generate_presigned_post`: key bound + size ceiling signed in) — the order-independent slice ahead of step 6. **ACCEPTANCE E (`tests/test_tenant_isolation.py`) GREEN:** the app-layer isolation (§6.4 steps 1–5) over the jobs endpoints + **every** tenant table; the **DB-RLS bypass (step 6) is Postgres-gated** (`SELOM_TEST_DATABASE_URL`) — it SKIPS on the SQLite fast lane (no RLS there). **GATES:** fast gate **772 passed, 1 skipped, 357 deselected** (`-m "not slow"`, +38 vs 734: auth 18 · tenant_query 13 · isolation 6+1skip · presign_put 2) · ruff clean. **PUSHED — origin/main @ `568350f` (in sync).** This session: 7a's 2 commits (pushed at session start) + 7b's 3 (`223b7a4` foundation · `9c0806f` enforcement · `568350f` docs/handoff). **⚑ NEXT = step 6** (presigned S3 upload + parsed-parquet §4.2, now built ONCE on the real `datasets`/`users` tables + JWT `user_id`: intake/confirm endpoints · T1 tenant-prefix isolation · T2 orphan reconciliation · the C3-cache-aware temp redesign; `presign_put` already shipped) → 7c (FE localStorage → Postgres §10) → 8 (split deploy). **⚑⚑ TWO EXTERNAL ITEMS PENDING OWNER (surface, don't block):** **(1) live Clerk keys** — the issuer/JWKS (`SELOM_AUTH_MODE=clerk` + `SELOM_CLERK_ISSUER`) to flip dev→live verify (the verify CODE + tests need nothing; the keys plug in at deploy); **(2) Aurora provisioning** — provision a scale-to-zero cluster NOW to validate the DB-RLS backstop + JSONB live (those are the ONLY unverified-on-real-Postgres bits; the app-layer isolation IS verified + is the hard gate, green), vs defer to step 8 deploy — a real cost/`ask-first` call given the $10/mo guardrail. **Resend decided:** product/marketing/lifecycle email ONLY (promos · product news · waitlist nurture · billing); Clerk owns ALL auth-identity email (verify/forgot-password); a `notify/` seam near launch, nothing wired in build phase [[selom-resend-email-decision]]. Detail in the Resume Prompt + `docs/aws-materialization/{plan,spec}.md` §4.3/§6/§8.) -->
<!-- (AWS-MAT-4 full text in git @ `21d76ea`^) -->

<!-- prior live-state (session AWS-MAT-3, 2026-06-28 20:21 +10:00; full text in git @ `8153961`): _Last updated: 2026-06-28 20:21 +10:00 (session "AWS-MAT-3" — **AWS MATERIALIZATION BUILD**: **step 7a (the tenant schema) SHIPPED + VERIFIED on SQLite**. The remaining 12 tables (spec §2.3) added to `db/schema.py` (SQLAlchemy Core, portable — JSON `.with_variant(JSONB)` · `DateTime(tz)` · `server_default=func.now()` · **app-supplied uuid-hex PKs**, no `gen_random_uuid()`, so ONE portable schema · `users.user_id`/`intermediate_tables.artifact_id` natural text PKs) + Alembic **`0002_create_tenant_schema`** (created in FK-dependency order; **RLS enable+FORCE+`tenant_isolation` policy emitted POSTGRES-ONLY, dialect-gated** — SQLite has none; `skill_installs` functional unique on `COALESCE(project_id,workspace_id),skill_id`). `analysis_jobs` (0001) is left UNTOUCHED — its `user_id` stays nullable + FK/RLS-free until 7b backfills the tenant (a poll on an existing job must still resolve). **GATES:** fast gate **734 passed** (`-m "not slow"`, +6 `tests/test_tenant_schema.py`: the migration builds all 14 tables · PKs/FKs/tenant `user_id` per spec · the Alembic + `create_all` paths proven DRIFT-FREE · the functional unique rejects a dup scope · downgrade reverses) · ruff clean · `0002` is the alembic head chaining from `0001`. **⚑ REORDER (owner 2026-06-28) — the step-6-vs-7 fork resolved to SCHEMA+AUTH-FIRST:** step 6's upload *endpoints* are structurally inseparable from the `datasets`/`users` tables + JWT `user_id`, and the temp-leak rework is entangled with the C3 parsed-input cache (the `delete=False` temp is DELIBERATELY persistent for cache reuse — NOT a clean `finally` wrap); with a **Clerk account now available** there's no reason to build step 6 against a stub. So 7a shipped now; **NEXT = step 7b** (Clerk auth → `TenantQuery` + RLS enforcement + cross-tenant isolation test; **Aurora provisioned here**) → step 6 (upload, built once on the real schema; the order-independent `presign_put` seam method is a small slice ahead of it) → 7c (FE→API) → 8 (split deploy). **EXTERNAL ACCOUNTS available (owner 2026-06-28):** Clerk (7b auth) · Stripe (billing mechanism) · Cloudflare + Vercel (step 8 edge/deploy) · **Resend** (transactional email — NEW, not in the plan; parked for Clerk emails/notifications — raise where to use). **NOT PUSHED — owner pushes when asked; local is ahead of origin/main @ `747cd62` by this session's commits (backend schema `bae8793` + a docs/handoff commit; `git log` to list).**)_ -->
<!-- (AWS-MAT-3 full text in git @ `8153961`^) -->

<!-- prior live-state (session AWS-MAT-2, 2026-06-28 20:00 +10:00; full text in git @ `747cd62`): _Last updated: 2026-06-28 20:00 +10:00 (session "AWS-MAT-2" — **AWS MATERIALIZATION BUILD**: plan §6 **steps 3·4·5 (the remaining 3 stores → the ObjectStore seam) + step 2 (jobs → SQL `analysis_jobs`, the statelessness fix) SHIPPED + VERIFIED + PUSHED**. All four content-addressed stores now ride the ONE `ObjectStore` seam slice-1 introduced; each flips local↔S3 by `SELOM_OBJECT_STORE` with the local default unchanged. **step 3** = `ResultCache` durable tier (`_disk_read/_write`→`_obj_read/_write`, key `cache/result/{k}.json`; in-proc LRU/stats/deep-copy isolation untouched). **step 4** = `ArtifactStore.put/get_table/get_meta` over the seam (keys `artifacts/{id}.csv`+`.meta.json`; immutability = a `head()` check; meta-mem cache kept). **step 5** = NEW `LedgerStore` protocol + `ObjectStoreLedgerStore` (key `repro/{slug}/ledger.json`) + `get/set_ledger_store`; `save_ledger`/`load_ledger` keep the `root=` direct-filesystem path (back-compat) and route the no-`root` default through the seam. `LocalObjectStore.put_bytes` is now ATOMIC (write-temp+`os.replace`) so the cache/artifact cross-worker torn-read guarantee survives the seam. Each store kept its `root=`/`object_store=` construction → existing test seams (conftest, test_result_cache, test_lineage, the reproduction `root=` round-trips) stay green with ZERO churn. **GATES:** fast gate **720 passed** (`-m "not slow"`, +8 new parity tests vs 712); ruff clean. **§14 parity suite extended** — the 3 new adapters round-trip byte-identically over LocalObjectStore + an in-memory S3-shape + the **real boto3 path (moto)**; **LIVE round-trip on the real dev bucket `selom-dev-objectstore-apse2` PASSED for all of steps 3-5** (write→read byte-identical→cleanup, 0 keys left). **STEP 2 (statelessness fix, owner chose: build code now, DEFER Aurora; dev/test on SQLite):** new `db/schema.py` (SQLAlchemy Core `analysis_jobs`, portable JSON `.with_variant(JSONB)` + `timestamptz`) + `db/engine.py` (pooled engine, `pool_pre_ping` for the T3 Aurora-resume) + `jobs/sql_store.py` `SqlJobStore` (same `create/get/update`→`Job` surface; timestamps surfaced as EPOCH FLOATS so the FE wire shape is unchanged). Behind `make_job_store(settings)` (`SELOM_JOB_STORE=memory|sql`; default memory = unchanged) wired into `jobs/queue.py` (the bare in-memory singleton retired). The queue is already by-id + re-fetch → the SQL store is a drop-in. **Alembic scaffold** (`alembic.ini` + `alembic/env.py` reads `SELOM_DATABASE_URL`/`-x url=` + `0001_create_analysis_jobs`) — verified via the CLI AND `command.upgrade` on SQLite. Step-2 scope = `analysis_jobs` ONLY; the cross-table FKs (users/intermediate_tables) + RLS + the JWT `user_id` are STEP 7 (`user_id` ships now as a nullable forward-compat column). Deps: a `db` extra — sqlalchemy+alembic `uv pip install`'d (NOT uv sync [[selom-uv-sync-footgun]]); psycopg DECLARED for prod only (not pulled for the SQLite suite). **GATES (cumulative this session):** fast gate **728 passed** (`-m "not slow"`; +8 parity for steps 3-5, +8 for step 2 vs 712); ruff clean. **VERIFIED:** `tests/test_job_store.py` (CRUD · wire shape · the **cross-instance statelessness** proof: a fresh store on its own engine reads another's job, vs an empty in-memory store · the Alembic migration applies+reverses) + a config-driven app-path check (`SELOM_JOB_STORE=sql` → real `/skills/{id}/jobs` submit → succeeded → a cold `SqlJobStore` sees it). **PUSHED — origin/main @ `9c9131a`.** Session commits: steps 3-5 `3a27152`+`0442c74`+`74cd812`(handoff); step 2 `0cb8668`(feat)+`8f2f0a1`(docs). **⚑ ALL SEAM + STATELESSNESS WORK DONE → NEXT = plan §6 step 6 (presigned S3 upload + parsed-parquet — the ONE genuine flow rewrite §4.2: tenant-prefix isolation T1 + orphan reconciliation T2). Step 7 (auth/users/RLS+FE) is where Aurora is actually provisioned + the JWT `user_id`/RLS land.** See the Resume Prompt below + `docs/aws-materialization/{plan,spec}.md`. **The AWS-MAT-1 pointer below (step 1 — result store → S3) = PRIOR state; full text in git @ `fa82cbc`.**_ -->

<!-- prior live-state (session AWS-MAT-1, 2026-06-28 16:14 +10:00): plan §6 step 1 (result store → S3) SHIPPED + VERIFIED + PUSHED — new app/backend/storage/object_store.py ObjectStore seam (Local/S3) + storage/results.py first adapter; live round-trip on selom-dev-objectstore-apse2; moto-backed S3 parity (712 pass); R2 config retired; the 3 materialization specs (M-001/002/003) + plan committed; origin/main @ a612368. Also: ALL 530 historical commits rewritten to strip the Co-Authored-By: Claude trailer + force-pushed [[selom-authorship-no-ai-signoff]]. AI-chat "JSON too large" resolved (D5) = an LLM-token problem → TOON (md/CSV fallback) + payload-shaping (M-002, spec'd not built). Full text in git @ fa82cbc. (T29 architecture-consistency-gate = the state before that, full text @ 0a2bc43.) -->

_Last updated: 2026-06-27 23:58 +10:00 (session "T29" — **ARCHITECTURE-CONSISTENCY GATE**: **Task E1 + E2 SHIPPED** [Plotly leak hardening + render/payload telemetry + fast/slow test split; FE + a BE test-hygiene touch]) · Claude (acting FE+BE) — **MODE = architecture-consistency gate** (owner-chosen active lane; all other pillars/tangents ON HOLD). **⚑⚑ ALL DB-FREE BUCKETS NOW COMPLETE (C5✓ · D1✓ · D2✓ · D3✓ · E1✓ · E2✓) → HARD STOP: raise the data-architecture discussion with the owner BEFORE starting ANY materialization bucket (C4 R2 cache tier · D4 DuckDB/Parquet · F1–F3 Supabase/state-migration) — owner 2026-06-27 "there may be a change of plans," do NOT pre-commit to the Supabase/R2/DuckDB shape until that talk.** **PUSH STATE (2026-06-28, owner-authorized "wrap up and push"): origin/main is now IN SYNC with local HEAD — all of C1–E2 PUSHED** (prior origin tip was `58bb9dd`; this session added E1+E2 FE `0ab3a05` · E2 BE `e3db6e9` · docs `0a2bc43` · handoff). Verify vs origin at session start anyway.** **✅ SHIPPED THIS SESSION — Task E1 + E2 (owner mid-session: "you can do E2 this session too"). E1 (FE; tsc/eslint/vitest + chrome-devtools heap-snapshot diff):** Plotly leak hardening in `components/figure/figure-canvas.tsx` + new pure `lib/figure/webgl-budget.ts`. **(1) Explicit unmount teardown** — capture the Plotly graph div (`gdRef`); on unmount explicitly remove the 3 gesture listeners WE bound directly (relayout/restyle/click) + dispose every drag wiring + null the refs (the leak surface in our control). react-plotly's own `Plotly.purge` still tears down the GL context/div (proven by the Task B exit); this is idempotent on top. **(2) WebGL context budget** — `MAX_GL_CONTEXTS = 8`; each GL canvas claims a slot keyed by a stable `useId` token, freed on unmount, read via `useSyncExternalStore` (a denied overflow canvas re-upgrades when a slot frees). Under budget (the ONLY case the UI reaches today, ≤2 GL panes — Compare A/B) nothing changes; over budget the overflow canvas renders SVG (`toSvgTraces`, `scattergl`→`scatter`) instead of LOSING a context. Pure projection — canonical spec untouched. **(3) Stable data/layout `useMemo` refs** (already present, made explicit). **SSR audited** — plotly.js `import type` only, react-plotly only via `dynamic(ssr:false)`; guarded by `lib/figure/ssr-plotly-import.test.ts` (scans the whole FE tree, fails on any static non-type Plotly import — the durable ratchet for the "raw plotly import 500'd every page" lesson [[full-app-smoke-test-before-handoff]]). **E2 (FE+BE; vitest + BE fast-gate + ruff + browser perf):** **FE** `lib/figure/perf.ts` (render-timing ring buffer → p50/p95/peak + `window.__selomPerf` hook; canvas records the commit→draw delta) + `lib/figure/payload.ts` (figure byte/point audit + advisory ceilings → once-per-figure dev warning, `warnDeadKnob` discipline). `scripts/perf-audit.mjs` (Playwright + **system Chrome** `channel:chrome`, seeds a synthetic 2-figure project → no backend, drives N switches, reports cold/warm p50/p95/peak + peak heap + canvas budget) + `test:fast`/`test:perf` npm scripts. **BE** registered a `slow` pytest marker (`pyproject.toml`) + auto-tag the heavy lanes (reproduction/golden/real-engine) by FILE in `tests/conftest.py` (`pytest_collection_modifyitems`, no per-test edits) → `pytest -m "not slow"` = **695 tests ~56s** (vs ~14min full), `pytest -m slow` = 357. **GATES:** FE tsc clean · eslint 0-err (2 PRE-EXISTING baseline warns in figure-canvas: liveRef.current + handlersRef init — NOT mine; my code added 0) · **vitest 340** (+31: webgl-budget 12 · ssr-import 3 · perf 8 · payload 5 + reshuffle); BE fast gate green · ruff clean (TEMP-copy workaround [[selom-backend-python-exec]]). **✅ BROWSER-VERIFIED (chrome-devtools MCP, real volcano fixture `p_3384a01f`, FE webpack :3000, NO backend):** opened a scattergl volcano → reset telemetry → baseline heap snapshot → **20 figure switches** through real React handlers → 2nd snapshot. **Heap diff — the leak-defining counts are FLAT:** `HTMLCanvasElement` 4→4 · `Detached HTMLCanvasElement` 0→0 · `WebGLRenderingContext` 6→6 · `WebGL2RenderingContext` 1→1 · `EventListener` 1452→1452 · `V8EventListener` 1320→1320. Live DOM canvas capped at **3** throughout (never accumulated); console clean (only the expected param-spec 404s = B3 floor, no backend; NO error-boundary/Plotly/WebGL errors, no "too many WebGL contexts"). The +26 generic detached-DOM / +4.5MB = transient React-reconciliation nodes (single-GC-pass artifact), none canvases/contexts/listeners. **PERF recorded (cold/warm):** cold first draw ~4.6–8.2s (DEV-only — webpack route compile + Plotly chunk load), warm p50 60–89ms · p95 78–132ms · peak 95–138ms, peak heap ~244–276MB — measured BOTH via MCP (real fixture: p50 88.6/p95 131.8/peak 138) AND `perf-audit.mjs` (8k-pt synthetic: p50 60.1/p95 78.5/peak 94.6, peak heap 243.9MB, max canvas 3/budget 8), agreeing. **Doc `docs/architecture-consistency-gate/perf-and-test-hygiene.md`** (E1/E2 design + recorded numbers + heap-diff acceptance table). **⚑ NEXT = ⚠⚠ THE OWNER DISCUSSION (HARD STOP).** Every DB-free bucket is done. Do NOT start C4/D4/F1–F3 or pre-commit to the data-architecture (Supabase/R2/DuckDB) shape until the owner conversation — "there may be a change of plans." Candidate parked work if the owner redirects: ERG tangent · figure-editor §11 (other chart kinds) · Prism UX phases · the on-hold backlog (full index in `memory/MEMORY.md`). **ENV:** Turbopack `0xc0000142` → `npx next dev --webpack` [[selom-turbopack-webpack-workaround]] (kill stale FE servers first); BE via the uv-3.12 PY (⚠ correct path = `C:\Users\seamegdool\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\python.exe` — the T28 resume's path was MISSING `\uv\python\`) + `PYTHONPATH='D:/selom/app/backend;D:/selom/app/backend/.venv/Lib/site-packages'`, NOT `uv run` (EDR) [[selom-backend-python-exec]]; ruff via TEMP-copy; browser verify = chrome-devtools MCP on the real localStorage fixtures (NO backend needed — figures render from stored specs); perf-audit = `npm run test:perf` (system Chrome, seeds its own project). **All dev servers I started are KILLED (FE :3000); ⚠ eamos holds :8000, NOT mine — never kill.** READ FIRST: `docs/architecture-consistency-gate/{roadmap,plan,inventory,perf-and-test-hygiene}.md` · `agent_handoff/CURRENT.md` (T29) · memory [[selom-architecture-consistency-gate]] [[verify-on-real-data-not-mock]] [[stamp-prompts-with-datetime]]. _(T28 below = PRIOR session, full text in git @ `0a2bc43`.)_

<!-- prior live-state (session T28): _Last updated: 2026-06-27 22:52 +10:00 (session "T28" — **ARCHITECTURE-CONSISTENCY GATE**: **Task D D2 + D3 SHIPPED** [frame-validation at stage seams + content-addressed intermediate-table lineage; BE]) · Claude (acting FE+BE) — **MODE = architecture-consistency gate** (owner-chosen active lane; all other pillars/tangents ON HOLD). **PUSH STATE (2026-06-27 22:52 +10:00): origin/main = `58bb9dd`. Local is 8 commits ahead, NOT pushed — OWNER pushes by default; all green: C1 `c8747ca` · C2 `7f841a9` · C3 `909264e` · prior-handoff `5e46790` · C5 `1f03f06` · D1 `95fed8b` · **D2+D3 `8342a0a`** · **this handoff commit**. Verify vs origin at session start.** **✅ SHIPPED THIS SESSION — Task D2 + D3 (both BE; pytest + live-HTTP, D2 also BROWSER; ruff clean; dependency-free — NO pandera, the C1 "no diskcache" + [[selom-uv-sync-footgun]] precedent):** **D2 = frame-validation at the skill seam** (`engine/frame_schema.py`). Once D1 confirms the required columns are PRESENT, D2 checks they carry USABLE data — a present-but-empty / all-text fold-change column passes D1 yet becomes a silently-degenerate figure (or a downstream crash) in the runner. The seam check (`main.py` /run, after D1, before the runner) raises a clear **400 `frame_validation_failed`** listing every defect (lazy) — `empty_column` / `non_numeric_column` (≥1 parseable number = NOT flagged, the runner coerces) / `duplicate_column` — among the columns D1 already confirmed present. Honest (only D1-present columns; never re-reports a missing one = D1's 422), overridable. **The clean 3-layer story, one source of truth each: QC = is-clean (coarse) · D1 = columns-present (422) · D2 = columns-usable (400).** Per-skill rules DERIVED from D1's `compat._SCHEMA` (single-sourced). A named **result-seam** schema (`validate_result_table`: rectangular StatsTable) guarded by a test over the native-table skills (output-seam ratchet) — not a raising hot-path gate (a malformed output is a runner bug, not a user 400). **FE = ZERO change** — D2 rides D1's already-fixed `runSkill` `detail.message` read-once path, which is status-agnostic, so a 400 surfaces verbatim. **D3 = content-addressed intermediate-table lineage** (`engine/lineage.py`). A stage's table is materialized under its own SHA-256 (`data/artifacts/<id>.csv`) + a `<id>.meta.json` sidecar carrying **parent-hash lineage** (`ParentRef` = a source file by SHA or a prior artifact by id), the **cleaning recipe** (the plan steps), and a computed **`receipt`** ("merged from {A, B, C}" / "derived from X"). Immutable + idempotent (a re-materialize of the same bytes returns the existing record → the reproducibility guarantee); a single-cell **matrix** is recorded **meta-only** (shape + lineage, no multi-GB CSV). Wired: `/run` stamps the ingested table's `artifact` into the response (fail-soft) · `/data/combine` materializes the merged cohort + its merge-receipt (input SHAs captured BEFORE the temp uploads are cleaned in the `finally`) · new `GET /artifacts/{id}` (meta + lineage walk) + `GET /artifacts/{id}/table` (inspect the matrix the skill saw). Config `SELOM_ARTIFACTS` (default on); conftest disables it suite-wide. **The local, DB-free precursor to D4** — the local-dir backend swaps for DuckDB/Parquet behind one `materialize`/`get_table`/`get_meta`/`lineage` interface, exactly as C1's disk tier swaps for R2. **GATES:** `test_frame_schema.py` (19) + `test_frame_validation.py` (5) + `test_lineage.py` (11); **FULL backend suite 1051 passed, 1 skipped** (14m26s), ruff clean (TEMP-copy workaround [[selom-backend-python-exec]]). **✅ VERIFIED LIVE (BE :8010 real engine — NOT stub):** an empty-fold-change DE table → volcano → **400** naming `log2FoldChange` (override=true → 200); a good DE run stamped `artifact` (kind ingested, recipe "used as-is", receipt "derived from de_good.csv") and `GET /artifacts/{id}/table` returned the EXACT CSV the skill saw; `/data/combine` of 2 files → receipt **"merged from {wt.csv, ko.csv}"**. **✅ BROWSER-VERIFIED (D2 FE render):** in a NAMED throwaway project, dropped the empty-fc DE file → `/data/inspect` correctly classified it `de_results 5×3` (the "24×18,102" chip was just the transient "Classifying…" placeholder, resolved correctly — NO FE mock bug) → applied Volcano → the exact message rendered as a `role=alert` ("This data can't be analyzed by volcano: a fold-change column ('log2FoldChange') is empty — every value is missing…"), **NO figure, NO crash**, console clean (only the expected 400 log, no `[error-boundary]`/React error). The throwaway project was then **removed from localStorage** (back to 8 projects; curated fixtures untouched). **CLEANUP done (owner ask):** pruned `CURRENT.md` history — swept the stale T24↓T7 prior-live-state comments (18 of them) to `agent_handoff/archive/2026-06-27-current-history.md` via `agent_handoff/prune_current_history.py` (keep newest 2 = T27/T26 inline) [[archive-currentmd-history-weekly]]. **⚑ NEXT = E1** (Plotly leak hardening, FE — `Plotly.purge` on unmount, `useMemo` data/layout refs, cap simultaneous `scattergl`; partly proven by the Task B exit heap pass) → **E2** (telemetry + fast/slow test split, FE+BE) — **the LAST two DB-free buckets.** **⚠⚠ DISCUSSION GATE (owner 2026-06-27) — HARD STOP: after E1 + E2, EVERY DB-free bucket is done (C5✓ · D1✓ · D2✓ · D3✓ · E1 · E2) → STOP and raise it with the owner BEFORE any materialization bucket (C4 R2 tier · D4 DuckDB/Parquet · F1–F3 Supabase/state-migration) — "there may be a change of plans." Do NOT pre-commit to the Supabase/R2/DuckDB shape until that talk.** **ENV:** Turbopack `0xc0000142` → `npx next dev --webpack`; BE via uv-3.12 PY + PYTHONPATH (NOT `uv run` — EDR) [[selom-backend-python-exec]] [[selom-turbopack-webpack-workaround]]; live BE = :8010 + temp next.config proxy → **REVERTED to :8000** (⚠ eamos holds :8000, NOT mine — never kill); MCP upload sandbox = copy data into gitignored `graphify-out/scratch` (removed after). **All dev servers I started are KILLED (FE :3000, BE :8010); next.config back at :8000.** READ FIRST: `docs/architecture-consistency-gate/{roadmap,plan,inventory,skill-input-contract,frame-validation,intermediate-table-lineage}.md` · memory [[selom-architecture-consistency-gate]] [[verify-on-real-data-not-mock]] [[selom-name-test-projects]] [[full-app-smoke-test-before-handoff]] [[stamp-prompts-with-datetime]]. _(T27 below = PRIOR session, full text in git @ `8342a0a`.)_ -->
<!-- (T28 full text in git @ `0a2bc43`^) -->

<!-- prior live-state (session T27): _Last updated: 2026-06-27 22:05 +10:00 (session "T27" — **ARCHITECTURE-CONSISTENCY GATE**: **Task D D1 SHIPPED** [declared input-contract → enforced pre-run data-contract gate; cross-lane BE+FE]) · Claude (acting FE+BE) — **MODE = architecture-consistency gate** (owner-chosen active lane; all other pillars/tangents ON HOLD). **PUSH STATE (2026-06-27 22:05 +10:00): origin/main = `58bb9dd`. Local is 6 commits ahead, NOT pushed — OWNER pushes by default; all green: C1 `c8747ca` · C2 `7f841a9` · C3 `909264e` · prior-handoff `5e46790` · C5 `1f03f06` · **D1 [this commit]**. Verify vs origin at session start.** **✅ SHIPPED THIS SESSION — Task D1 (declared table/input contract per skill → enforced pre-run gate; cross-lane BE pytest + FE vitest + BROWSER; ruff/tsc/eslint clean):** the per-skill INPUT contract already existed in `engine/compat.py` (s52 data-fit scorer): `_REQS` (modality class: matrix vs table) + `_SCHEMA` (named column groups — fold-change / significance / gene, matched by the classifier synonym sets). It was **advisory** (a 0-100 score on the data-fit/intake surface + the reproduction matcher) — the own-data `POST /run` path did NOT gate on it, so a CERTAIN mismatch fell through to the runner (a `KeyError`→500, or a misleading figure). **D1 promotes it to an enforced pre-run gate + guards the declarations from drift.** **THE GATE** (`main.py` /run, after the QC gate, before the runner): compute `compat.fit(skill, assess_bundle(bundle))` ONCE; if `gated and not override` → **422 `data_contract_failed`** with `compat.contract_message(fit)` (names the missing columns + the next step) + the `data_fit` dump + routing; the same fit is reused for the response's `data_fit` (single load/score — removed the old duplicate compute). **Honest** (only `compatible is False` blocks; an unreadable / modality-unclear file stays optimistic and runs) · **overridable** (`override=true`, the same escape hatch as the QC gate) · **complementary** (a skill with no compat contract — e.g. the ERG skills — is never gated here; its runner's `ValueError→400` [T6 path] covers it). **FE** (`lib/skills-api.ts`): `runSkill` now surfaces a typed gate's `detail.message` **as-is** (self-framed, no "Couldn't run…try again" wrapper + no doubled period; a bare string detail still gets the friendly wrapper), **AND reads the 422 body ONCE** — ⚠ a double `res.json()` (readDataCheckBlock then the generic fallback) threw "body already used" and silently fell back to the generic "rejected (422)"; the vitest fetch mock's re-callable `json()` HID it → only the BROWSER caught it. RULE recorded [[fetch-body-read-once-browser-verify]]. **THE GUARD (ratchet)** `tests/test_skill_input_contract.py`: every `_REQS`/`_SCHEMA` skill ships (no drift) · a column contract declares something checkable · a contracted skill's modality admits a table class — mirrors the OUTPUT `test_skill_table_contract.py` "declared + mechanically guarded" philosophy. **GATES:** BE `test_data_contract.py` (counts→422 naming the columns · DE table→200 no false block · `override` bypasses · uncontracted skill not gated) + `test_skill_input_contract.py` + run-path regression **76** green · contract/inspect/capabilities/table-contract **70** · golden **66** unaffected · ruff clean (TEMP-copy workaround [[selom-backend-python-exec]]); FE tsc clean · eslint 0-err · **vitest 309** (+1 skills-api typed-message case). **✅ BROWSER-VERIFIED** (live Selom BE :8010 + FE webpack `npx next dev --webpack`, no mock; in the named `p_a9c58dc5`): a real **6×6 bulk-counts CSV** → volcano → **BLOCKED pre-run, NO figure created, NO stack trace**, the clear message rendered in the UI ("This data doesn't fit volcano: missing a fold-change column, a significance (p / padj) column … Swap in a file with what this analysis needs, or pick a skill that matches this data."); the eyg28 DE table → volcano → 200 (no false block, from the C5 figure). The double-read bug was found+fixed+re-verified live mid-pass. Doc `docs/architecture-consistency-gate/skill-input-contract.md`; `engine/compat.py` dangling `skill-table-schemas.md` ref repointed to it. **⚑ NEXT = D2** (frame-validation at stage seams — a named frame schema per stage output [CleanedTableSchema, a per-skill result schema], validate ingest→clean→skill boundaries `lazy=True`, failure→400, mirrors the existing ValueError→400 path; BE; ~1) → D3 (intermediate-table lineage, local: content-addressed cleaned/normalized artifacts + parent-hash lineage + cleaning recipe + merge receipt; BE; ~1–2) → E1 (Plotly leak hardening, FE) → E2 (telemetry + fast/slow test split, FE+BE) — **ALL DB-FREE.** **⚠⚠ DISCUSSION GATE (owner 2026-06-27) — HARD STOP: complete every DB-free bucket (C5✓ · D1✓ · D2 · D3 · E1 · E2), then STOP and raise it with the owner BEFORE any materialization bucket (C4 R2 tier · D4 DuckDB/Parquet · F1–F3 Supabase/state-migration) — "there may be a change of plans." Do NOT pre-commit to the Supabase/R2/DuckDB shape until that talk.** **OWNER FEEDBACK carried (2026-06-27):** "you still not naming the project folders" → name every created project at creation [[selom-name-test-projects]]. The C5 verify project `p_a9c58dc5` (named) now also holds a `raw_counts_demo.csv` bulk-counts dataset = the D1 verify input (NO figure — the blocked run created none); harmless, flag if a cleanup is wanted. **ENV:** Turbopack `0xc0000142` → `npx next dev --webpack`; BE via uv-3.12 PY + PYTHONPATH (NOT `uv run` — EDR) [[selom-backend-python-exec]] [[selom-turbopack-webpack-workaround]]; live BE = :8010 + temp next.config proxy → REVERTED to :8000 (⚠ eamos holds :8000, NOT mine — never kill); MCP upload sandbox = copy data into gitignored `graphify-out/scratch` (removed after). **All dev servers I started are KILLED (FE :3000, BE :8010).** READ FIRST: `docs/architecture-consistency-gate/{roadmap,plan,inventory,skill-input-contract}.md` · memory [[selom-architecture-consistency-gate]] [[fetch-body-read-once-browser-verify]] [[selom-name-test-projects]] [[full-app-smoke-test-before-handoff]] [[stamp-prompts-with-datetime]]. _(T26 below = PRIOR session, full text in git @ `1f03f06`.)_ -->
<!-- (T27 full text in git @ `8342a0a`^) -->

<!-- prior live-state (session T26): _Last updated: 2026-06-27 21:22 +10:00 (session "T26" — **ARCHITECTURE-CONSISTENCY GATE**: **Task C C5 SHIPPED** [param-spec caching — the LAST DB-free Task C bucket; cross-lane BE+FE]) · Claude (acting FE+BE) — **MODE = architecture-consistency gate** (owner-chosen active lane; all other pillars/tangents ON HOLD). **PUSH STATE (2026-06-27 21:22 +10:00): origin/main = `58bb9dd`. Local is 5 commits ahead, NOT pushed — OWNER pushes by default; all green: C1 `c8747ca` · C2 `7f841a9` · C3 `909264e` · prior-handoff `5e46790` · **C5 [this commit]**. Verify vs origin at session start.** **✅ SHIPPED THIS SESSION — Task C5 (param-spec caching; cross-lane BE pytest + FE vitest + BROWSER; ruff/tsc/eslint clean):** the Figure-data Inputs no longer need a live `GET /api/skills/{id}` describe round-trip on every figure open (B3's error/Retry stays the FLOOR). TWO complementary fixes. **(B, cross-lane — self-describing figure):** `provenance.build` stamps the immutable `param_spec` into the figure's `skill` block (`bundle["skill"]["param_spec"]`, grouped with id+version the FE keys by) → every run's figure carries its own spec forever; FE `SkillProvenance.skill.param_spec?` added; `useSkillParams(skillId, seed?)` takes that provenance spec as a `seed` → renders inputs **synchronously, NO fetch** (immutable per skill_version). **(A, FE-only — local cache):** new pure `lib/catalog/param-spec-cache.ts` (single key `selom.paramSpecs.v1` → `{[slug]:{version,spec}}`, SSR-safe, fail-soft); `fetchParamSpec` persists every successful describe **and falls back to the cached spec offline** (ok:true) so an already-run skill stays tunable with no backend; `useSkillParams` seeds instantly from cache (no loading flash) + warms it from the provenance seed. Wired via `FigureDataPanel` `specSeed` prop ← open figure's provenance. **GATES:** BE `test_provenance.py` (+param_spec asserts on `build` + the `/run` bundle), provenance/jobs/run-design/run-guardrail/contract/capabilities **60** green, golden **66** unaffected (provenance is OUTSIDE `_execute`/theme), ruff clean (TEMP-copy workaround [[selom-backend-python-exec]]); FE new `param-spec-cache.test.ts` + `use-skill-params.test.ts` (persist-on-success · offline cache-fallback · B3 floor when nothing cached · SSR/corrupt/quota fail-soft) → tsc clean · eslint 0-err (27 pre-existing warns, none mine) · **vitest 309** (+11). **✅ BROWSER-VERIFIED (live Selom BE :8010 + FE webpack, real eyg28 `..._DEGs_All_PDE6B_FS_d210_vs_Control_d210.csv`):** a fresh volcano run stamped `provenance.skill.param_spec` (keys fc_threshold/fdr_threshold/top_n/highlight); opening Figure-data rendered the INPUTS (thresholds 607↑/334↓/15819 n.s. + Highlight genes) with **ZERO `GET /api/skills/volcano` describe calls** (network log) and warmed `selom.paramSpecs.v1`; then **BE killed + full page reload** → the figure STILL showed tunable inputs offline (no error slot, no spinner; Re-run present, still needs the backend). Verified in a NAMED throwaway project "C5 verify — volcano param-spec cache (eyg28 PDE6B_FS d210)" (`p_a9c58dc5`); **curated fixtures untouched** (8 projects intact). **OWNER FEEDBACK (2026-06-27): "you still not naming the project folders"** → named the verify project + recorded RULE [[selom-name-test-projects]] (name every created project/folder at creation; the name field commits on blur). **⚑ NEXT = D1** (declared table contract per skill — a pre-run "missing column X for this skill" message, extends the skill-table-contract guard; BE; ~1–2) → D2 (frame-validation at stage seams) → D3 (intermediate-table lineage, local) → E1 (Plotly leak hardening) → E2 (telemetry + fast/slow test split) — **ALL DB-FREE.** **⚠⚠ DISCUSSION GATE (owner 2026-06-27) — HARD STOP: complete every DB-free bucket (C5✓ · D1–D3 · E1–E2), then STOP and raise it with the owner BEFORE any materialization bucket (C4 R2 tier · D4 DuckDB/Parquet · F1–F3 Supabase/state-migration) — "there may be a change of plans." Do NOT pre-commit to the Supabase/R2/DuckDB shape until that talk.** **ENV:** Turbopack `0xc0000142` → `npx next dev --webpack`; BE via uv-3.12 PY + PYTHONPATH (NOT `uv run` — EDR) [[selom-backend-python-exec]] [[selom-turbopack-webpack-workaround]]; live BE = :8010 + temp next.config proxy → REVERTED to :8000 (⚠ eamos holds :8000, NOT mine — never kill); upload sandbox = copy data into gitignored `graphify-out/scratch` (MCP upload restricted to the workspace root). **All dev servers I started are KILLED (FE :3000, BE :8010).** READ FIRST: `docs/architecture-consistency-gate/{roadmap,plan,inventory}.md` · memory [[selom-architecture-consistency-gate]] [[selom-name-test-projects]] [[selom-fe-probe-autopersist-gotcha]] [[full-app-smoke-test-before-handoff]] [[stamp-prompts-with-datetime]]. _(T25 below = PRIOR session, full text in git @ `5e46790`.)_ -->
<!-- (T26 full text in git @ this commit^) -->

<!-- prior live-state (session T25) archived 2026-06-27 → `archive/2026-06-27-current-history.md` (full text also in git log). -->
<!-- (T25 full text in git @ `5e46790`^) -->

<!-- prior live-state (sessions T24 ↓ T7) archived 2026-06-27 → `archive/2026-06-27-current-history.md` (full text also in git log). -->

<!-- prior live-state (sessions T6 ↓ 55) archived 2026-06-23 → `archive/2026-06-23-current-history.md` (full text also in git log). -->

<!-- prior live-state (sessions 54 ↓ 41) archived 2026-06-23 → `archive/2026-06-23-current-history.md` (full text also in git log). -->

<!-- prior live-state (sessions 40 ↓ B4) archived 2026-06-21 → `agent_handoff/archive/2026-06-21-current-history.md` (full text also in git log). -->

_Sessions 8 and earlier → `agent_handoff/archive/2026-06-17-pre-session9-history.md`._

**Model:** **Opus 4.8 (xhigh effort)** for all Selom work — owner-directed 2026-06-13,
pinned in `.claude/settings.json` (`model=claude-opus-4-8`, `effortLevel=xhigh`); applies on
session restart. Supersedes the prior Fable-5 default.

## Resume Prompt (next session)

> Stamped 2026-06-29 01:18 +10:00. Paste at session start; re-stamp date+time before reuse. (The 7C-STATE live-state at the top is authoritative.)

```
Resume · 2026-06-29 01:18 +10:00 (Australia/Sydney, AEST) · Selom · Claude (acting FE+BE) · Opus 4.8 (xhigh) · [re-stamp date+time]

⚑⚑ FIRST — DON'T START BUILDING. Owner wants to **discuss the repo folder & file structure before any further work** (step 8 included). Do a quick read-only survey + come back with options, don't reorganize anything yet: map `app/backend/` (the package sprawl — `uploads/` · `library/` · `db/` · `auth/` · `jobs/` · `storage/` · `engine/` · `extract/` · `skills/` · `litsynth/` + the flat `main.py`/`provenance.py`/`guardrails.py`/`legends.py`/`methods.py`/`papers_api.py`/`paper_metadata.py` modules), `app/frontend/lib/` (many feature dirs + flat `*-api.ts`), `docs/` (growing per-feature spec tree), `agent_handoff/`. Surface: what's inconsistent, what a clean target layout could be, migration cost/risk, and whether to do it now (cheapest before step 8 adds deploy/infra files) or defer. ASK the owner before moving files — a reorg touches imports across both lanes.

MODE: AWS MATERIALIZATION — BUILD PHASE. **Step 7c (FE state → Postgres) SHIPPED + LIVE-VERIFIED 2026-06-29** (the 7C-STATE live-state at the top is authoritative; sub-spec `docs/aws-materialization/7c-frontend-state-migration.md` §10b = the verify log). Through 7c DONE: ObjectStore seam · jobs→SQL · 7a schema · 7b auth/RLS · 6 presigned upload · **7c FE optimistic-cache stores + client-authoritative ids + figures/account-library CRUD + idempotent localStorage import + run-from-`dataset_id` endpoint**. **FE LIVE at `selom.vercel.app`. Origin/main @ `bc2375f`; local AHEAD by 7c's commits, NOT pushed — owner pushes when asked; verify vs origin at start.** GATES: backend fast gate **839 pass/1 skip**, ruff clean; FE tsc/eslint clean, vitest 351. **⚑ NEXT = STEP 8 (split deploy).** **PENDING OWNER (still):** the GitHub→AWS OIDC role (admin/root; AWS-Console runbook in `infra/README.md`) → kill the static `selom-dev` key after; live **Clerk** + **Aurora** provisioning. **Resend:** product/lifecycle email only; Clerk owns auth email [[selom-resend-email-decision]].

⚠ 7c FOLLOW-UPS (do before/alongside step 8 — sub-spec §10b):
  • RAISE the free/dev quota defaults — `users.max_projects=3`/`max_datasets=10` BLOCK new creates for a real dogfood account (already has 10 projects). Import bypasses quota (correct), but `create_project`/`create_dataset` enforce it → 402 (verified live; the FE rolls back the optimistic create). Bump the defaults or defer the gate.
  • FE-3 (close the upload→run loop): wire the FE upload flow (intake → presigned PUT → confirm → parse → run-from-`dataset_id`). The run-dataset endpoint is BUILT + tested (byte-identical to multipart); datasets are metadata-only in 7c (no S3 upload wired) so the FE still drives skills via multipart `/run`.
  • A verify left COSMETIC fixture drift (createdAt refreshed + dataset size_bytes zeroed on the owner's 8 localStorage fixtures; content/specs intact; test rows + import marker cleaned). RULE: snapshot localStorage before any live-store verify [[selom-fe-probe-autopersist-gotcha]].

NEXT TASKS (build IN ORDER):
  ✅ DONE: steps 1·2·3·4·5·7a·7b·6·7c + integration backbone + AWS hardening + plan §0 corrections + hardening slices 1-3 (845 pass/1 skip, ruff clean; commits NOT pushed).
  0. ⚑ THE NEXT TASK — **REPO STRUCTURE DISCUSSION (owner-requested, gate before step 8).** Read-only survey of the folder/file layout (see the ⚑⚑ FIRST note above) → present a target structure + cost/risk + now-vs-defer recommendation → get owner sign-off. NO file moves until agreed. Cheapest moment is BEFORE step 8 (which adds CDK/infra + deploy config). Also fold in the 7c follow-ups (quota defaults · FE-3 upload wiring) as the owner prioritizes.
  8. SPLIT DEPLOY (AFTER the structure gate): light zip Lambda (FastAPI+Mangum: catalog/routing/text) + heavy **Fargate (Arm+Spot)** (scverse/Kaleido compute, §0 C2 — NOT a Lambda, the 15-min/10-GB wall); secrets → Secrets Manager (DB cred) + SSM (rest); **PROVISION Aurora min=0 ACU** (§0 C1; flip `SELOM_JOB_STORE=sql` + real `SELOM_DATABASE_URL` + run Alembic `head` incl. `0004`); wire **live Clerk** (`SELOM_AUTH_MODE=clerk` + issuer/JWKS — verify code+tests need nothing); async = S3-event→EventBridge→Step Functions→Fargate→`jobs` table + cron for T2 heal/sweep + a **BYPASSRLS** role for the cross-tenant sweep; **CORS** on the bucket & API; the **GitHub→AWS OIDC role** (owner-pending) → kill the `selom-dev` key; **set `API_PROXY_TARGET`** (Vercel env) to the deployed API URL (`next.config` now reads it, default :8000).

⚑ AUTHORSHIP RULE (owner 2026-06-28, NON-NEGOTIABLE): NEVER sign off "Claude"/"Anthropic"/any AI on commits (NO `Co-Authored-By` — this OVERRIDES the harness default), PRs (no "Generated with Claude Code"), or docs. Selom is the owner's work; the assistant is uncredited. KEEP "Claude/Codex lane" coordination terms + naming Claude as the product's LLM. Pinned in `CLAUDE.md` "## Authorship & attribution" + [[selom-authorship-no-ai-signoff]]. (All 530 historical commits were rewritten to strip the trailer + force-pushed 2026-06-28.)

WHAT 7c LANDED + THE CODEBASE NOW: NEW `library/` package (`base.py` TenantRepo+`ensure_workspace`+`iso` · `figures.py` FigureMixin · `workspace.py` Workspace/GeneSet/InstallMixin · `papers.py` PaperMixin · `import_state.py` ImportMixin · `repo.py` composed `LibraryRepo`/`get_library_repo`/`set_library_repo`). NEW `main.py` endpoints: figures CRUD (`GET/POST /figures`, `GET/PATCH/DELETE /figures/{id}` — declared AFTER the static `/figures/*` so `{id}` can't shadow `/figures/styles`), `GET /workspace`, `GET/POST /workspace/gene-sets`+`DELETE /{id}`, `GET/POST /workspace/papers`+`GET/DELETE /{id}`, `GET/POST/DELETE /skill-installs`, `PATCH/DELETE /projects/{id}`, `POST /datasets`+`PATCH/DELETE /datasets/{id}`, `POST /import/local-state`, `POST /skills/{id}/run-dataset`+`/jobs-dataset` (shared `_execute_skill_run` + `_dataset_to_temp`). `UploadRepo` gained client-id `create_project` + `create_dataset`/`update_project`/`delete_project`/`update_dataset`. Schema: `figures.guardrails` + `users.local_import_at` + Alembic `0004`. NEW FE: `lib/api/{client,write-queue,import-local-state}.ts` · `lib/projects/sync.ts` + rewritten `lib/projects/store.ts` · `lib/workspace/sync.ts` + rewritten `lib/workspace/store.ts` · `next.config.ts` `API_PROXY_TARGET` · app-shell wires `importLocalStateOnce()`. Tests: BE `test_figures`/`test_library`/`test_import_state`/`test_run_dataset` + uploads/schema updated; FE `lib/api/write-queue.test`/`lib/projects/store.test` + workspace test now mocks the api client. KEY DESIGN: client-authoritative ids (no temp-id remap) · the write queue coalesces figure-spec PATCHes + grace-delays deletes (Undo cancels) + rolls back permanent 4xx · reconcile is union-merge (server wins by id, local-only kept, gated on queue idle). Prior step-6/7b detail in git @ `bc2375f`/`8153961`.

STEP 8 NOTES (the implementer's checklist — READ `docs/aws-materialization/{plan,spec,integrations}.md` first):
  • The DB-RLS backstop + JSONB are still UNVERIFIED on real Postgres (SQLite has neither) — validate when Aurora/a local PG lands (`SELOM_TEST_DATABASE_URL` runs `test_tenant_isolation`'s PG-gated step). The cross-tenant sweep + worker reads/writes need the tenant set OR a **BYPASSRLS** role on Postgres.
  • Run Alembic to `head` on Aurora (chain `0001→0004`); `db_auto_create` is dev-only (SQLite). Flip `SELOM_JOB_STORE=sql` + the real `SELOM_DATABASE_URL`.
  • Clerk: the verify CODE + tests need nothing — just `SELOM_AUTH_MODE=clerk` + `SELOM_CLERK_ISSUER`/`_JWKS_URL`/`_AUDIENCE`. The FE api client has a pluggable `setAuthHeader()` hook (`lib/api/client.ts`) — wire the Clerk bearer there (one-liner).
  • Heavy lane = **Fargate** (scverse/Kaleido), light zip Lambda for catalog/routing/text (§0 C2). Async = S3-event→EventBridge→Step Functions→Fargate→`jobs` table; cron for T2 heal/sweep.
  • Set `API_PROXY_TARGET` (Vercel env) to the deployed API URL; add **CORS** on the bucket & API; the **GitHub→AWS OIDC role** is still owner-pending (runbook `infra/README.md`) → kill the static `selom-dev` key after.
  • Raise the `users` quota defaults (3/10) — see the 7c follow-up above. Prior 7b/7a/steps-1–6 detail in git @ `bc2375f`/`8153961`.

AI-CHAT "JSON TOO LARGE" (M-002, spec'd not built): resolved (plan D5) to be an LLM-token problem, not bytes → format = TOON (Token-Oriented Object Notation), markdown/CSV fallback, hand-rolled encoder (no venv dep) + payload-shaping (schema + sampled/aggregated slice, never the whole matrix; honest "what you're NOT seeing" framing). Parquet/Arrow/MsgPack explicitly REJECTED (they fix bytes, not the token problem). Detail in `docs/ai-chat-context/spec.md`.

LIVE AWS: S3 dev bucket `selom-dev-objectstore-apse2` (ap-southeast-2, public-access-blocked, SSE-S3) — the single bucket all four content-addressed stores share. Account 417673081852 · IAM user selom-dev (PowerUserAccess). Live round-trip (write→presign GET→byte-identical→get→delete) PASSED via the real store code. [[selom-aws-access-setup]] holds the bucket + the build progress.

ENV: DRIVE AWS via boto3 + `py -3` (system Python; boto3 is pip --user'd there) — there is NO `aws` CLI [[selom-aws-access-setup]]. Backend tests via the uv-3.12 PY `C:\Users\seamegdool\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\python.exe` (note the `.13`) + `PYTHONPATH='D:/selom/app/backend;D:/selom/app/backend/.venv/Lib/site-packages'` from app/backend, NOT `uv run` (EDR) [[selom-backend-python-exec]]; fast gate = `pytest -m "not slow"` (~1m40s, **790 pass / 1 skip** after step 6 — the skip = the PG-gated DB-RLS isolation step); ruff = `$PY -m ruff check`. Alembic via `$PY -m alembic -x url=sqlite:///dev.db upgrade head` (env reads `SELOM_DATABASE_URL` or `-x url=`; head is `0003_tenant_auth` — step 6 added NO migration: it rides the 7a `datasets`/`projects` tables; the FE-local dev DB auto-creates via `SELOM_DB_AUTO_CREATE=true`). Live S3 driving for the upload seam uses boto3 + `py -3` + `requests` (both present) — see the scratchpad verify script pattern. Add a venv dep with `uv pip install` NOT `uv sync` [[selom-uv-sync-footgun]] (note: installing moto's botocore data tree was SLOW under EDR — minutes, looked hung but wasn't). Docker OK for the AWS/ECR image only [[ask-before-docker-wsl]]. FE dev = `npx next dev --webpack` [[selom-turbopack-webpack-workaround]]. ⚠ eamos holds :8000 — never kill.

GUARDRAILS: spec-driven — READ `docs/aws-materialization/spec.md` (M-001) before each slice; build in the order (7a✓ → 7b✓ → 6✓ → 7c → 8, see plan §6); specs are amended in place as we go. Live-verify each slice on real AWS/data before "done" [[verify-on-real-data-not-mock]] [[full-app-smoke-test-before-handoff]]. Owner PUSHES (push when asked — **local is AHEAD of origin/main @ `21d76ea` by this session's step-6 commits, NOT yet pushed**). NAME every bucket/project at creation [[selom-name-test-projects]]. Kill dev servers you start [[kill-dev-servers-on-handoff]]. Stamp prompts with the real clock [[stamp-prompts-with-datetime]]. Every lesson → a durable artifact [[the-ratchet-durable-artifacts]]. PENDING owner security (deferred): a persistent CloudTrail trail once the PROD S3 bucket exists (the dev bucket isn't it). When building the schema: fold M-003 `skill_requests` into M-001's unified schema; M-002 owns LLM-context redaction (vs M-003's queue redaction). READ FIRST: `docs/aws-materialization/plan.md` (§6 order + §10 resolved decisions) · `.../spec.md` (M-001 build detail + §13–16) · `agent_handoff/{README,CURRENT}.md` · memory [[selom-aws-materialization-decision]] [[selom-aws-access-setup]] [[selom-authorship-no-ai-signoff]] [[selom-backend-python-exec]].
```

## Active Status

| Agent | Role | Lane | Status |
|---|---|---|---|
| Claude | **Frontend + Backend (acting)** — UI/design/product copy **and** APIs/skill runners/data/tests | `app/frontend` + `app/backend` + both plans | IDLE — **T18 (2026-06-25): §11 generalization SHIPPED + VERIFIED LIVE + COMMITTED — VOLCANO threshold flagship + global gesture flip + GENE LABELLING (canvas click-to-label toggle + Statistics-table Label column, one shared annotation set) + paintbrush figure-styling icon. Live-verified on REAL DE data (`D:\selom-data\eyg28`). Tree CLEAN. GATES: FE tsc · eslint 0-err · vitest 218; BE full suite + ruff. NEXT (owner-flagged §11 roadmap): extend the capability/staged-edit model to the next chart kinds (heatmap · UMAP · bar/box) — ASK the owner which first. See the T18 live-state ↑ + `docs/figure-data-capabilities/generalization-spec.md` + [[selom-figure-edit-ux-pattern]].** _Prior context — s55: Slice 4 (auto-drive regression fixtures) SHIPPED — this CLOSES the "Reproduction — dogfood-ready" phase (Slice 4 was the only open slice). Pure BE + docs, NO FE. Local commits PROPOSED, NOT pushed — ASK before push; origin ↔ main @ `7efdab2`; tree = the new BE files + this docs; no dev servers (Eamos :3001 untouched).** New `reproduction_fixtures.py` = the reusable framework (`DriveSnapshot` + `snapshot()` projects a `DriveResult` → per-panel status-shape + driven metric values; `diff()`/`assert_reproduces()` = the regression contract: same panel set · same status per panel · 0 Selom defects · every driven metric within its golden's `resolve_tolerances` band — the exact s46 grader, so a tolerated engine delta stays green + a broken reader flips red) + `scripts/regen_reproduction_fixtures.py` (bless/refresh generator + shared `FIXTURE_PAPERS` registry) + committed `tests/fixtures/reproduction/{jev,hani,dorgau}.json` + README + `tests/test_reproduction_fixtures.py` (3 tiers). **MEASURED:** cold-driving JEV/Hani/Dorgau drives **0 panels** (all `no_golden`/`data_unmatched`/`run_failed` — the Slice-0/1 data-availability+recall insight on the hand-ledger papers; **0 Selom defects**) → the win locked today is the **honest classification shape**; metrics auto-populate as recall rises. Tier a (cheap CI, no data) = framework unit tests on a synthetic drive (status flip→red · within-band→green · out-of-band→red) + characterization over each snapshot; tier b (opt-in, owner machine) = real `reproduce()` re-drive asserting the snapshot, verified green here on all 3. **4 hand ledgers untouched/byte-identical**; RPGRIP1 = a documented `FIXTURE_PAPERS` slot (no PDF here). **Gates: fixtures 15 + 1 skip · 4 ledgers+drive+honest 63 + 1 skip (no regression) · ruff clean · 780 collect clean.** **NEXT: dogfood phase CLOSED; the owner's ~2–3-session TANGENT PROJECT runs next (wait for the owner to open it). Before the ⚑ pillars-complete flag, 2 LIGHT post-phase items remain — P4-4a contract audit + the P3 routing-confidence surface.** [[selom-live-reproduction-drive]] [[selom-reproducibility-score]] [[layered-deterministic-extraction]] [[the-ratchet-durable-artifacts]] [[selom-backend-python-exec]] _Prior — s54: Slice 5B (deposit-data handoff) + Slice 3 (skill-gap signal) SHIPPED + PUSHED (`6659092`/`3c11f5c`/`7efdab2`). s53: Slice 2 FINISHED — per-panel data PICKER + Product-A data-fit band, SHIPPED + PUSHED (`fad4a62`/`b77ebd0`/`baefbae`). s52: Slice 2 engine `engine/compat.py` + matcher honesty + before/after panel, PUSHED (`932010a`/`36bf560`/`3029006`). s51: Slice 0 (2nd baseline Yoshimura) + Slice 1 n_cells (`9794b8c`) + Slice 5 accessions (`8de466f`). s50: phase spec + Slice 0 diagnostic. s49: circle-back FE/UX — Product-A workbench + L3 Statistics node. s48: is-my-data-clean guardrail. s47: figure-legend layer + `engine/match.py`. Older (s46 ↓ s13) in git log + archive → `agent_handoff/archive/2026-06-21-current-history.md`._ |
| Codex | Backend (APIs/skill runners/data/tests) | `app/backend` + `plans/v2-backend.md` | **AWAY** (busy on another project). Backend role **temporarily covered by Claude** as of 2026-06-11 22:10. B1 + **B2 backend done by Claude** in the interim (see `## Codex` section). May reclaim at any time — state is drop-in-ready; review the arq #6 lock + the B1/B2 commits on return. |

Roles are explicit; any swap is written here before work proceeds.

> **Role swap (2026-06-11 22:10 +10:00).** Codex is away on another project; **Claude is acting backend owner** in addition to frontend, so a single agent now drives both lanes. **Takeover discipline (so Codex or any new agent can drop in cleanly):** (1) keep the `## Codex — Last Task & Resume` section accurate to *true* backend state — what's done, the exact next command, the resume pointer; (2) **explicit git staging only** — never `git add -A`; stage `app/backend/**` and `app/frontend/**` in **separate commits** with `feat(backend:)` / `feat(frontend:)` scopes so lane history stays cleanly attributable; (3) keep `plans/v2-backend.md` current as the BE backlog; (4) honour the backend-lane decisions already locked (arq #6) until Codex reclaims and reviews. The disjoint-lane locks in `README.md` still apply — when wearing the BE hat I follow the BE rules.

## Pillar / Phase Status (bird's-eye, refreshed s55)

Map of `docs/pillars/plan.md` (the shared engine spine P1–P4 + reproduction P5) + the now-CLOSED phase
`docs/records/reproduction-dogfood/spec.md`. **The engine spine is functionally complete; the reproduction
dogfood phase CLOSED in s55 (Slice 4 shipped). Only 2 LIGHT post-phase items stand between here and
the "pillars complete" milestone — both in the spec's "Remaining phases" table, neither a dogfood slice.**

| Pillar | State | Remaining |
|---|---|---|
| **P1 Ingest & Clean** | ✅ **complete** | — (1a/1b/1c done; 1d via `engine.classify`) |
| **P2 Table Joining & Synthesis** | ✅ **complete** | 2a–2e done (2d = picker + honest `data_unmatched`, s53) |
| **P3 Routing & Guidance** | ~95% | **3c data-side skill-gap signal SHIPPED s54** (`skill_gaps.py` + `docs/records/skill-gaps.md`); remaining = the routing-confidence "not sure" surface (a UX feature) |
| **P4 Analysis & Output** | ~90% | **4a** contract-uniformity audit (checked list + guard test — light); minor FE polish (Pro/AI legend tier · own-run → Library artifact) |
| **P5 Reproduction ("dogfood-ready" — CLOSED s55)** | ✅ **complete** | 5A/5B + Slices 0–4 all shipped; **Slice 4 (auto-drive regression fixtures) SHIPPED s55** closed it |
| **P6 Parking lot** | parked by design | untouched until an explicit owner decision names the pillar it rejoins |

**Phase — Reproduction dogfood-ready (`docs/records/reproduction-dogfood/spec.md`): CLOSED s55.** Slices **0 ✅
1 ✅ 2 ✅ 3 ✅ 4 ✅ 5A ✅ 5B ✅** — all shipped. Slice 4 (the 4 hand-ledger papers → auto-drive
regression fixtures) was the only open slice; it landed s55 (`reproduction_fixtures.py` framework +
committed JEV/Hani/Dorgau snapshots + 3-tier tests; the 4 hand ledgers untouched). The Phase-B2
auto-fetch infra stays on hold in `docs/on-hold/README.md`.

**⚑ Milestone flags (owner asked to be told):**
- **ACTIVE phase COMPLETE** — the reproduction dogfood phase is **CLOSED** (Slice 4 shipped s55). Every
  planned slice (0–5) is done.
- **Nearing ALL planned pillars' completion** — P1/P2/P5 complete; P3/P4 each have **one light item left**
  (P3 routing-confidence surface · P4-4a contract-uniformity audit). Once those two land, **P1–P5 are
  essentially done** → Claude flags **"pillars complete."** **Owner steer (s53): BEFORE we strategise the
  next pillars, a TANGENT PROJECT (the owner's actual work, ~2–3 sessions) runs first.** So the sequence
  is: dogfood phase CLOSED (now) → [optional: the 2 light items → "pillars complete"] → the tangent
  project (2–3 sessions) → THEN the next-pillars strategy session. Candidates already in the docs for that later session: the **Prism UX phases**
  ([[selom-prism-pillar-phases]]), **commercial/launch gates** (`LAUNCH-GATES.md`, deferred-by-policy),
  **Product-A daily-driver maturity** (spine-complete but thin on UX), **breadth-by-collaboration**
  (the skill-audit steer, [[selom-clawbio-bioskills-shortlist]]). Not started — flagged.

## Log Edit-Lock

UNLOCKED
<!-- session-55 update by Claude 2026-06-22 19:35 +10:00 (Slice 4 — auto-drive regression fixtures SHIPPED; this CLOSES the "Reproduction — dogfood-ready" phase [Slice 4 was the only open slice]. Local commits PROPOSED, NOT pushed — ASK before push; origin ↔ main @ 7efdab2, working tree = the new files + this docs, no dev servers [Eamos :3001 untouched]. Pure BE + docs, no FE. Built: app/backend/reproduction_fixtures.py [the reusable framework — DriveSnapshot + snapshot() projects a DriveResult to status-shape + driven metric values; diff()/assert_reproduces() = the regression contract: same panel set · same status per panel · 0 Selom defects · every driven metric within its golden's resolve_tolerances band, graded with the exact s46 grader so an engine delta inside the metric-type band stays green + a broken reader flips red] + scripts/regen_reproduction_fixtures.py [bless/refresh generator + shared FIXTURE_PAPERS registry] + committed tests/fixtures/reproduction/{jev,hani,dorgau}.json + README + tests/test_reproduction_fixtures.py [3 tiers]. MEASURED reality drove the design: cold-driving JEV/Hani/Dorgau drives 0 panels — all no_golden/data_unmatched/run_failed [the Slice-0/1 data-availability+recall insight, confirmed on the hand-ledger papers; every panel still classified honestly, 0 Selom defects] — so the win locked today is the honest classification shape; metrics empty + auto-populate as recall rises. Tier a [cheap CI no data] = framework unit tests on a synthetic drive [status flip->red · within-band metric->green · out-of-band->red = the break-a-reader check] + characterization over each committed snapshot. Tier b [opt-in, owner machine] = real reproduce() re-drive asserting the committed snapshot; skips without the staged PDF; verified green here on all 3. 4 hand ledgers untouched/byte-identical; RPGRIP1 = a documented FIXTURE_PAPERS slot [no staged PDF here]. Gates: fixtures 15 + 1 skip · 4 ledgers+drive+honest 63 + 1 skip [no regression] · ruff clean · 780 collect clean. Memory selom-live-reproduction-drive updated. NEXT: dogfood phase CLOSED; before the pillars-complete milestone 2 LIGHT post-phase items remain — P4-4a contract-uniformity audit + P3 routing-confidence "not sure" surface; then a ~2-3-session owner TANGENT PROJECT runs BEFORE any next-pillars strategy.); released. -->
<!-- session-53 update by Claude 2026-06-22 09:05 +10:00 (Slice 2 FINISHED — per-panel data PICKER + Product-A data-fit band; SHIPPED + PUSHED [owner-authorized], 3 lane-separated commits feat(backend:) fad4a62 + feat(frontend:) b77ebd0 + this docs; origin ↔ main in sync, tree clean, dev servers stopped [Eamos :3001 untouched]. Owner: "ok do 1 and 2". (1) Picker components/reproduction/data-picker.tsx — each data_unmatched panel on the Score stage gets a Radix file Select annotated w/ the engine fit band; pick -> persist SavedPaper.dataMap [filenames not bytes; workspaceStore.setPaperDataMap] -> "Re-run with these picks" re-drives w/ a data_map override. panel_drives now rides RunRecord+reproduction_runs.public; POST /papers/{id}/reproduce gained a data_map form field resolved server-side [_resolve_data_map] to each file's saved path; match_data honours it. usePaperRun.start(dataMap?) explicit OR persisted; useReproductionRun exposes panelDrives; fixed paper-shell passing run.start directly as onClick. (2) Product-A band: /skills/{id}/run data_fit -> SkillRunResponse.dataFit -> persisted on Figure -> DataFitVerdict [single-fit sibling of DataFitPanel] on the own-data figure; mockDataFit for dev:mock. (3) Fix: _save_capped saves uploads into a per-file subdir keeping the ORIGINAL name [no uuid prefix] -> filenames match + the picker round-trips [affects /reproduce + /assess-data; /skills/run + /data/inspect use _save_upload, untouched]. Gates: BE run 17 [+3] + drive/compat/diagnose 43 + 4 hand ledgers 30 byte-identical; FE tsc/eslint 0-err/vitest 121 [+5]; real-data round-trip graphify-out/scratch/s53_picker_roundtrip.py [override left data_unmatched, fed the pick, run_failed honestly, 0 Selom defects]; /p/[id] + /paper/?stage=score compile + serve 200. NOT DONE: live interactive browser-verify [chrome-devtools profile locked by another session]. This session CLOSED P2 slice 2d; added a bird's-eye Pillar/Phase Status block + milestone flags [active phase ~3 slices from done; P1–P5 nearing overall completion]. Memory selom-data-fit-scorer updated. NEXT: GATED Slice 5B accession fetch [ASK] + Slice 3 skill-gap doc + Slice 4 regression fixtures.); released. -->
<!-- session-51 update by Claude 2026-06-22 02:30 +10:00 (dogfood phase advanced — Slice 0 2nd-baseline + Slice 1 + Slice 5 SHIPPED + PUSHED [owner-authorized mid-session]; origin ↔ main in sync, tree clean, no dev servers. Owner steer: "do slice zero and slice one" + "also slice 5". (0) Slice 0 re-run COLD on Yoshimura [kidney-organoid multiome, 2nd calibration paper] -> docs/records/reproduction-dogfood/findings-yoshimura.md: independently confirmed the Harmony gaps [0 goldens, 0 driven, run_failed/oos, 0 Selom defects]; meta-finding generalizes [data = GEO accessions, only a QC table attached]. (1) Slice 1 n_cells extractor [9794b8c]: extract.golden.extract_dataset_size lifts the analyzed dataset size on TIGHT result/QC anchors ["resulted in 56,865 cells after filtering"], 0-false-positive vs Harmony's 28 benchmark cell-counts; backfill n_cells->umap_scrna [engine.match] + L1 read-back readers._read_umap [UMAP point count]; drive test proves extract->grade; 4 hand ledgers byte-identical [30/30]; n_clusters/correlation deferred. (2) Slice 5 accession recognizer Phase A [8de466f]: extract/accessions.py deterministic no-network regex over bundle.text [availability-weighted] -> Accession{repo,id,access:open|raw|controlled,ingestable,url,section,note} [GEO/SRA/ENA/ArrayExpress/GSA/PRIDE/MetaboLights+MW/Zenodo/Figshare/Dryad/dbGaP/EGA]; raw->needs-quantification, controlled->requires-application; traps handled [citation markers, ST-table refs]; wired into reproduction_diagnose [header line + Cited-datasets table]; 10 tests on the real Yoshimura statement; Phase B GATED. Gates: full BE 722 passed; ruff clean. Live re-run lift: Yoshimura report blank -> n_cells golden + 4-GEO Cited-datasets table, still 0 Selom defects. STRATEGIC INSIGHT: two papers one wall — both deposit accessions + a QC table not matrices -> binding constraint for driven = DATA-AVAILABILITY not extractor recall. NEXT s52: Slice 2 matcher honesty [QC-table != counts-matrix -> data_unmatched not forced run_failed] + per-panel picker [flips auto_grade off 0%]; THEN GATED Slice 5 Phase B fetch. Memory: selom-accession-ingest [Phase A SHIPPED] + selom-live-reproduction-drive [Slice 0/1/5 + strategic insight] updated.); released. -->
<!-- session-50 update by Claude 2026-06-22 01:41 +10:00 (next phase chosen + KICKED OFF — "Reproduction — dogfood-ready" [owner's why: dogfood many papers to TRAIN the engine, limited own omics data]. 3 commits LOCAL [feat(backend:) 7939a48 + docs c36efc6 + this handoff], NOT pushed — ASK before push; origin ↔ main @ 9e2e8b8, working tree clean, no dev servers started. (0) Phase spec docs/records/reproduction-dogfood/spec.md [P5 b/c/d + P2 2d + P3 3c; agile Slices 0–5, decisions D1–D6 incl. D6 accession-fetch-gated; remaining-phases roadmap; pillars/plan.md points at it]. (1) Slice 0 cold-drive diagnostic app/backend/reproduction_diagnose.py — projects a DriveResult -> a gap report [per-panel status/skill/data/goldens-expected/metrics-read/reason + rollup + auto_grade_rate], JSON+markdown; pure projection, 0 Selom defects, no silent caps; test_reproduction_diagnose 4 green, ruff clean, drive test 12/12. (2) Ran it COLD on the real Harmony paper -> docs/records/reproduction-dogfood/findings-harmony.md: 0/8 gradable, 0 goldens extracted, all 8 run_failed, yet 0 Selom defects. Causes: golden extractor knows DE/PCA only not integration benchmarks [Slice 1]; matcher forced lone xlsx onto all panels -> run_failed not data_unmatched [Slice 2]; META: famous methods papers deposit code+.rda+GEO/GSA accessions not tabular supplements. NEXT BUILD s51 [owner-approved]: Slice 5 accession recognizer Phase A [deterministic, no network]; Phase-B fetch GATED. Memory: new selom-accession-ingest, selom-live-reproduction-drive updated.); released. -->
<!-- session-49 update by Claude 2026-06-22 01:03 +10:00 (the circle-back FE/UX pass — tasks 1 & 2 of the s48 NEXT list — SHIPPED + browser-verified vs the LIVE BE; 3 commits LOCAL [feat(backend:) 3878a29 + feat(frontend:) 13e4c83 + this handoff], ASK before push; origin/main @ 1201438 [the 3 s48 commits pushed at session start, owner-authorized], working tree clean, dev servers killed. (1) Product-A guided workbench [P1c/P3a/P3b]: lib/skills-api DataCheck/QcFlag/DataRouting/FigureLegend types + figure_legend->legend / data_check->dataCheck mapping + override run option + typed DataCheckError from the 422; new DataCheckPanel [verdict w/ modality chip + QC flags + clickable suggested pipeline · blocked w/ subordinate "Review & run anyway" override]; project-workspace block card + suggested-step install/preselect + legend/dataCheck persisted on the Figure. (2) L3 Statistics node + Methods/legend [table-synthesis §8 step 4 + workspace-library §11]: BE POST /skills/{id}/run attaches table=table or synthesize_table(...) for tableless skills [test_run_synthesized_table 3]; StatsPanel "Computed by Selom" badge+explainer; PublishConfidence paste-ready Figure-legend section. Gates: BE 3+4; FE tsc clean · vitest 114 [+4] · eslint 0 errors [+1 inert React-Compiler note]. Browser-verified on :3001 vs live BE [clean verdict + steps · 422 block + override->figure · synthesized node · methods+legend; screens graphify-out/scratch/s49-*]. Docs: table-synthesis §8 step 4 DONE, workspace-library §11 own-data legend DONE, pillars plan P3b + circle-back FE pass DONE; memory selom-engine-spine-pillars updated. Note: umap_scrna has its own SELOM_UMAP_ENGINE selector [ignored stub], 500'd on a CSV — use pca/deg for stub own-data runs.); released. -->
<!-- session-48 update by Claude 2026-06-22 00:16 +10:00 (two backend ships, 3 commits LOCAL [2 feat(backend:) + this handoff] — ASK before push; origin/main @ 10b6005, working tree otherwise clean: items 1 & 2 of the s47 NEXT list = the Product-A engine front door. (1) is-my-data-clean? guardrail + RAW-routing surfaced for own data [P1c/P3a]: POST /skills/{id}/run ingests via engine.ingest -> run_qc -> blocks block-severity unless override=true [D-e5, structured 422 + fix hints], runs from the same bundle, surfaces data_check [kind+QC+routing]; fail-soft so the proven path never breaks; async .../jobs not gated yet. (2) runner takes a DataBundle [P1 §1b / E4]: skills.contract.run_bundle/run_bundle_with_table [byte-identical to the path runner, duck-typed no engine import], engine.ingest sets DataBundle.path, both products load through engine.ingest -> bundle -> run end-to-end [reproduction _default_runner fail-soft + /skills/{id}/run]. Gates: test_run_guardrail 4 + test_run_bundle 6; engine+golden+table+registry+routing 153; full reproduction set 116 passed [5:08] BYTE-IDENTICAL; ruff clean. Docs: engine-spine §6 ANALYZE + qc-gate prose + §9 step 5, pillars plan P1b/P1c/P3a + inventory all DONE; memory selom-engine-spine-pillars gate-MET note. Every engine-core skeleton now exists -> FE pass next); released. -->
<!-- session-47 update by Claude 2026-06-21 23:39 +10:00 (two backend ships, both PUSHED — origin ↔ main in sync @ 61034b2: (1) figure-legend layer 959344d [P4c] — new legends.py per-skill caption sibling of methods.build_body, DE-split enriched via the new canonical readers.de_counts [_read_de_table routes through it too], surfaced as figure_legend on /skills/{id}/run + GET /papers/{slug}/legends via litsynth/legends_from_ledger; additive, figure goldens unchanged; test_legends 24. (2) engine/match.py 61034b2 [P1 step 5] — lifted merge_ledger/match_data/tabular_paths out of reproduction_drive into the engine package, reproduction_drive consumes + re-exports them, byte-identical, no import cycle. Gates: test_legends 24 + readers; golden/methods/litsynth/synthesize 132; drive+drive_honest+4 ledgers+runs+sweep = 59 UNCHANGED; score/guards/tolerance/papers_api/citations/contract/provenance 75; ruff clean. Docs: pillars plan P4c+P1-step-5 DONE, engine-spine §6/§9, workspace-library §11); released. -->
<!-- session-46 update by Claude 2026-06-22 00:34 +10:00 (two backend ships, both PUSHED — origin ↔ main in sync: (1) proteomics_de native de_table 424ea54, the last at-source L3 gap [table-synthesis §8 step 5] — canonical de_table in real engine + stub, registered to the shared de_table L1 reader _read_de_table so de_total=up+down; only the node-link trio stays L4-only. (2) metric-type-aware tolerance grader 359c7f2 [P5a] — metric_type on Golden → family band [METRIC_TYPE_TOLERANCES + infer_metric_type/resolve_tolerances], explicit tolerance still wins, drive auto-types its goldens; non-regressing by construction. Gates: 24+12 new; golden/registry/route/synthesize 101; reproduction/score/guards 57; lighter engine 46; drive+drive_honest+4 ledgers UNCHANGED across both regression runs [46 each]; ruff clean. Docs: table-synthesis spec §8 step 5 + pillars plan P5a marked DONE); released. -->
<!-- session-45 update by Claude 2026-06-21 22:16 +10:00 (P2 Tier-B LOSSY set boxplot/heatmap/scorecard/violin SHIPPED — 72410b2, completing the full L3 synthesizer set (16 skills); each behind a faithfulness gate, all four faithful so none punted to L4; new shared _heatmap_grid gate, refactored corr_heatmap onto it; reader auto-extended via _SYNTHESIZERS. Gates: test_synthesize 28 (+10) + test_readers green; drive + drive_honest + 4 ledgers UNCHANGED [46 passed, 6m34s]; ruff clean. PUSHED @ 78eede1, origin ↔ main in sync); released. -->
<!-- session-44 update by Claude 2026-06-21 21:49 +10:00 (P2 Tier-B clean trio corr_heatmap/sankey/upset SHIPPED — fe079be, each behind a faithfulness gate, reader auto-extended via _SYNTHESIZERS; tests green [synth+readers 36 / drive 16 / golden 6]; ruff clean. + CURRENT.md weekly archive: stale s40↓ live-state comments + the Active-Status _Prior_ stack → agent_handoff/archive/2026-06-21-current-history.md. Local UNPUSHED — ASK before push); released. -->
<!-- session-43 (cont.) update by Claude 2026-06-21 21:33 +10:00 (pushed the 10 s43 commits + P2 commit c215299; integrated The Ratchet + restructured MEMORY.md into How-I-work vs Selom-project buckets under cap; P2 L3 table synthesis Tier A COMPLETE 9/9 [+umap_scrna/annotate] + reader L3 integration [read_metric -> synthesize_table fallback, tagged L3/synthesized, score-eligible]; full suite 632 passed; ruff clean; no dev servers); released. -->
<!-- session-36 update by Claude 2026-06-20 20:09 +10:00 (Skill Match flow polish SHIPPED + verified — widen/equal-height, metadata-header swap on drop, single dropzone + no bottom Run, Selom-themed routing progress sweep, Copy(TSV)/CSV export [lib/skill-match/export.ts +11 vitest → FE 74]; Workspace Library SPEC written + owner-signed-off model [single home · promote papers+gene sets+skill installs · SavedPaper=compact summary] + expanded umbrella vision §10/§11 [fold Skill Match+Reproduction+Recover data over a Paper object · reproduction AI-gating = AI-proposes/deterministic-disposes · Methods+Figure-legend layer ~70% built on lit-synth]; 6 commits LOCAL UNPUSHED — push at next session start; FE 918527e/1d645a6 + docs c71526a/79962eb/56fd77d/36504e3; dev servers killed); released. -->
<!-- session-35 update by Claude 2026-06-20 18:37 +10:00 (extraction↔recovery gap CLOSED — 2 production caption forms added to L2 recovery: Wiley/JEV PUA-glyph inline + bioRxiv line-numbered manuscript; JEV 0→8/Ishii 0→4/Hani 5→5, L3 inventory unchanged; _jev_text.txt regenerated from production + 2 unit fixtures; BE 538. FE per-figure UX: all-skill chips · Overview/Per-figure tabs · scrollable · chip→SkillDetail popout · rail no-auto-collapse; tsc+vitest 63+lint green; browser-verified real JEV. Commits BE 42dbe3a / docs 97382aa / FE 56543b4 ALL PUSHED @ 56543b4); released. -->
<!-- session-33 update by Claude 2026-06-20 03:08 +10:00 (Skill Keyword Index legend-segmentation hardening → 4-layer deterministic core: de-spaced/glyph markers + ordinal recovery, header-less citation-cluster refs exclusion, token-canonical relaxed matcher, L3 paper skill-inventory + per-figure tier/confidence, canonical-term dedup; validated on real JEV — L3 recall 8/8 in-scope skills, 8/8 garbled captions recovered, full bibliography excluded; flagship proprietary module; pattern distilled to memory layered-deterministic-extraction; BE 518; scope fed1ba2 / code ae2e149 / docs edeaaa7 / handoff de480d8 ALL PUSHED @ de480d8); released. -->
<!-- session-32 update by Claude 2026-06-20 02:01 +10:00 (Skill Keyword Index v1 shipped: deterministic keyword->skill/oos router in extract/routing/ — registry+curated vocab validated vs the live registry, refs-excluded section segmentation, alternation matcher, per-figure FeasibilityMap, POST /papers/route; backtested vs all 4 ledgers + live JEV; BE 509; spec 0fde152 / code 9834f09 / record eb73b64 ALL PUSHED @ eb73b64); released. -->
<!-- session-31 update by Claude 2026-06-20 00:49 +10:00 (Dorgau 4th figure-repro ledger built + validated: 94/100, Melody dogfooded live on GSE234963 0.146→0.490 mixing; new MODALITY_UNSUPPORTED scope; BE 495; all s30+s31 PUSHED @ `9017fd9`; Skill Keyword Index sequenced next); released. -->
<!-- session-30 update by Claude 2026-06-19 23:43 +10:00 (Harmony2 mode A+B folded into Melody behind a default-off flag; validated vs the R harmony 2.0.5 oracle; BE 485); released. -->
<!-- session-29 update by Claude 2026-06-19 22:35 +10:00 (Selom Melody shipped: clean-room pure-numpy Harmony replaces harmonypy on the integration path, GPL off the shipped path, validated vs oracle; harmonypy→oracle extra; BE 480; license mis-record corrected); released. -->

## Shared File Locks

None held.

## Cross-Agent Requests

> **Coverage note (2026-06-11 22:10):** while Codex is away, the **FE → Codex** asks below are now **Claude's own backend backlog** (acting BE) — I action them in-lane rather than waiting. They're kept here verbatim as the running BE contract/spec so Codex can read the full thread and reclaim cleanly. No new cross-lane asks are needed while one agent holds both lanes.

- **GitHub: connected ✓** Pushed to **github.com/steveneam/selom** (private); `main` ↔
  `origin/main` in sync as of 2026-06-11 01:41 +10:00. Latest: `afaafd2` (command-center
  scaffold + design) atop `e63f38b`/`5c4a1fd` (figure editor). Commit identity is local
  `Steven <mactechdish@gmail.com>` — adjust if GitHub commit attribution should differ.

- **FE → Codex — ✅ RESOLVED 2026-06-11 23:25 (B1, Claude acting BE):** asks 1 & 2 below are done.
  1. **✅ Backend booted + real engine wired.** `uv python install 3.12` + `uv sync --extra scrna`
     (light scRNA path, no scvi-tools/torch) on the user-managed py3.12; `uvicorn main:app` on :8000.
     `run_scanpy.py` now runs the real pipeline behind `umap_scrna/run` and returns an editable
     Plotly spec. Verified: real 2700-point / 11-cluster UMAP, direct and through the FE proxy.
  2. **✅ Canonical P0 contract = ONE-SHOT** multipart `POST /skills/{id}/run` (field `matrix` +
     params as query). The two-step upload-then-run idea is dropped for P0; `plans/v2-frontend.md`
     should be updated to match. `run.py` dispatches stub vs. real via `SELOM_UMAP_ENGINE`.
  3. **FYI — no backend action needed:** FE deps were re-pinned. `plotly.js@^2.37` did not exist
     (install hard-failed); bumped to `plotly.js@^3.6` + `react-plotly.js@^3.0` (now natively
     supports React 19, so `--legacy-peer-deps` is no longer strictly required). This is FE-lane
     only — the wire format (Plotly `{data, layout}` JSON) is unchanged, so the contract is
     unaffected. Worth updating RISKS #1/#3 (Codex/shared) at some point.

- **FE → Codex (NEW 2026-06-11 01:03 — command-center backend; full spec in `docs/command-center/design.md` §3–§9):**
  The frontend is building the project-first command center **mock-first** (no backend dependency to
  build). When the backend lane resumes, these are the new contracts to own/finalize — all **additive**;
  the figure wire format (Plotly `{data, layout}` JSON) is unchanged:
  1. **Skill registry API (B1).** `GET /skills` (filterable list of `SkillCatalogEntry`) +
     `GET /skills/{id}` (detail/SkillSpec). Seed from the ingested **ClawBio `skills/catalog.json`**
     (maps ~1:1) + **bioSkills** categories/`SKILL.md` frontmatter. The full ~600-skill catalog is
     browsable; each entry carries `tier` (verified|community), `engine`, `status`, `input_formats`,
     `provenance`, `license`. Shape = design §3.1.
  2. **Ingest + intake (B2).** `POST /upload` returns a dataset handle **plus** an ingest/QC report
     (detected modality, n_obs/n_var, guardrail flags). `POST /intake` takes questionnaire answers +
     dataset shape → schema-constrained **`IntakeProposal`** (cleaning steps + 1–3 proposed skills with
     pre-filled params + rationale/confidence) via the **shared AI gateway**. LLM proposes, never
     auto-runs. Shapes = design §4.3 / §7.
  3. **Verified runner expansion (B3).** Generalize `POST /skills/{id}/run` beyond `umap_scrna` to any
     Verified skill; async (arq) for heavy ones. **Skill Foundry (manual):** hand-port the wedge
     (~6–8: UMAP/cluster/DEG/volcano/heatmap/enrichment) + selected ClawBio runnable skills into the
     SkillSpec contract, each emitting an editable Plotly spec + provenance/golden-image test.
  4. **Supabase persistence (B4).** Tables = design §5 (`projects`, `datasets`, `skill_installs`,
     `intake_sessions`, `figures`, `figure_history`, `skill_catalog`; RLS by owner). FE persists through
     a `ProjectStore` interface (localStorage now) designed to match this schema, so the swap is a
     backend-impl change, not a FE rewrite.
  5. **Skill execution toolchain (design §6.5).** What we download/install to actually test+run skills:
     **(A)** catalog ingest — `git clone --depth 1` ClawBio + bioSkills, parse `catalog.json` + `SKILL.md`
     → seed manifest (cheap, do-now, an `scripts/ingest-catalog` job). **(B)** Verified runners — the
     existing `[omics]` extra (`uv sync --extra omics`: scverse + plotly/kaleido). **(C)** ClawBio runnable
     — `pip install clawbio` + **Miniforge/mamba** for per-skill `environment.yml` + bioconda. **(D)**
     bioSkills long tail — bioconda CLI toolchain (samtools/bcftools/STAR/salmon/GATK4/… + Snakemake/
     Nextflow) consumed via the Skill Foundry. **(E)** Community sandbox — Docker + mamba images +
     network policy + compute (v2, deferred). A+B are immediate; C is the contained next step; D/E grow
     over time.
  No action needed now — FE is unblocked and mock-first; this is the spec to build against when the
  backend lane resumes. North star (owner): grow runnable coverage to the full 500+ via the Skill
  Foundry (manual now → LLM-assisted = the v2 Extract-Skills moat).

- **FE → Codex (NEW 2026-06-11 21:56 — decision ratified, BE-lane confirm needed):** the owner directed
  ratifying all 5 pending vault `[DECISION]`s. One is backend-lane: **DECISIONS.md #6 — async job queue =
  `arq` + Redis (not Dramatiq).** It's locked to the vault's recommended default (asyncio-native, matches the
  FastAPI stack + every manifest/schematic); recorded by FE per the owner directive so it isn't left
  un-ratified. **Codex: confirm this, or supersede-by-append in `DECISIONS.md` if a Dramatiq-specific need
  surfaces** when the queue goes load-bearing (charter Bucket B3 / ledger P2). No action needed before then.
  _Update 2026-06-11 22:10 — Codex away; Claude (acting BE) proceeds on **arq** as locked; left for Codex to review on reclaim._

## Current State

- **Scaffold complete and committed:** git repo at `D:/selom`, branch `main`, commit
  **`4ccb6b6`** ("chore: scaffold Selom build repo (skeleton + agent workflow)"),
  40 files, clean tree. **Pushed to github.com/steveneam/selom (private); main ↔ origin/main in sync.** Identity is set LOCALLY
  to `Steven <mactechdish@gmail.com>` (correct it if that's not the intended GitHub
  identity).
- **Skeletons that ALREADY EXIST — do NOT re-scaffold:**
  - **Backend** (`app/backend`): FastAPI `main.py` (`GET /skills/{id}`,
    `POST /skills/{id}/run`, `GET /health`) + `skills/contract.py` (SkillSpec loader)
    + `skills/umap_scrna/` = `skill.json`, `run.py` (offline **STUB** — returns a
    valid Plotly dict with zero heavy deps; **verified runs on Python 3.10**),
    `run_scanpy.py` (real Scanpy engine, lazy import). `pyproject.toml` = light core
    deps + an `[omics]` extra for the scverse stack. `.python-version` = 3.12.
    `tests/test_contract.py`.
  - **Frontend** (`app/frontend`): Next.js 16 / React 19 / TS / Tailwind 4 shell —
    `app/page.tsx` (file upload → `POST /api/skills/umap_scrna/run` → render via
    `react-plotly.js`), `layout.tsx` (Inter via next/font), `next.config.ts`
    (`/api/*` → `:8000` proxy), tsconfig/postcss/globals.
- **P0 hello-UMAP = DONE (B1, 2026-06-11).** Backend venv built (`uv sync --extra scrna`
  on user-managed py3.12); `run_scanpy.py` real engine wired behind one-shot
  `POST /skills/umap_scrna/run`; `scripts/make_demo.py` writes the pbmc3k `demo.h5ad`
  fixture (gitignored, ~21MB). Verified in a real browser: upload → run UMAP → editable
  Plotly figure (11 Leiden clusters / ~2700 cells) renders against live :8000 (MSW off).
  Backend contract test green (`python -m pytest`, stub-pinned). FE proxy body cap raised
  to 512mb (RISKS #8). **Pushed: `main` ↔ `origin/main` @ `7d58e92`.**
- **Still NOT done (by scope):** no cloud (`.env` + `.mcp.json` are TODO placeholders),
  no Docker, no Supabase/arq. (**Kaleido journal export DONE 2026-06-15 — native, no Docker.**)
  Those remaining are later buckets.
- **Next milestone = B2 (Steven's real skills) — SIGNED OFF 2026-06-11, not started.** Build 6
  backend runners (cluster · violin · DEG · volcano · heatmap · GSEA/enrichment), each a pure-Python
  skill emitting an editable Plotly spec + a golden-image snapshot test. Locked: public proxy
  datasets now (real data later); GSEA via Reactome/GO (DECISIONS #9). Then B3 (jobs/queue) / B4
  (publish-confidence). See `docs/build-charter.md`.

## Claude — Last Task & Resume

- **Last (2026-06-18 01:19 +10:00, session 18) — REPRODUCTION ENGINE R4: figure-extraction subsystem substantially completed + a 3rd real ledger.** Owner approved building all four remaining items in one pass (R5 deferred to next session). **(b)** E7 two-input ingest (`extract/ingest.py`: `ingest_paper(main, supplements=[…])`→`PaperBundle`, format-plural PDF/xlsx/csv; `build_extracted_spec` bundle-aware) — `edc1624`. **(a)** X1 slice-2 vision layer (`extract/vision.py`: Claude-as-gateway `OperatorVisionGateway` behind the existing `VisionClassifier`; `associate_counts`/`augment_with_vision`/`venn3_totals`/`segment_panels`; VERIFIED LIVE on the real RPGRIP1 Fig 6E Venn → pixel-only pairwise 13/10/2, per-rod totals 102/119/74) — `2c110a5`. **(d)** `reproduction_hani.py` + test (Kim 2023, GSE201356; mmc2 Cepo matrix re-derived exactly via `drive_live_markers`; scorecard 96/100 Verified, 0 Selom bugs, 1 wet-lab out-of-scope) — `5f8db38`. **(c)** X2 Track-B reconstruction (`extract/reconstruct.py`: pure-numpy SSIM + editable Plotly redraw + gated Kaleido render; verified live SSIM 1.0 / 0.924) — `912615a`. The 3 ledgers now span the score spectrum RPGRIP1 63 / JEV 86 / Hani 96. **pytest 307 (+34); ruff clean; library-only (D12); all LOCAL — ASK before push.** Memories: [[selom-claude-acts-as-ai-gateway]], [[compound-capability-each-task]]; updated [[selom-repro-edge-cases]] (#10 pixel-only vision recovery), [[selom-reproducibility-score]] (Hani 96 + the spectrum), [[selom-backend-python-exec]] (pandas 3.0 `.map`). **NEXT (owner picks): R5 FE Reproduction view · SSIM-band calibration · X3/X4 · panel-segment automation · a 4th ledger.**
- _Older Claude task-log entries (through 2026-06-18) archived → `archive/2026-06-23-current-history.md` (full text also in git log)._

## Codex — Last Task & Resume

> **Role coverage (2026-06-11 22:10):** Codex is away on another project; **Claude is acting backend owner.**
> This section is maintained by Claude *on Codex's behalf* and kept accurate to true backend state so Codex (or
> any agent) can drop in and take over from the `**Next**` line below with zero re-derivation. When Codex
> returns, take this section back and review the backend-lane decisions locked in the interim (arq #6).

- **Last (done by Claude, acting BE, 2026-06-12):** **B1 + B2 backend complete.** B1 = real scanpy UMAP behind
  the one-shot `POST /skills/umap_scrna/run` (`6e37829`). **B2 = all 6 wedge skills** (`eded0db`):
  `cluster · violin · deg · volcano · heatmap · enrichment`, each `skills/<slug>/{skill.json, run.py (stub) +
  run_real.py}`, dispatched by `SELOM_SKILLS_ENGINE` (auto/stub/real) via `skills/_engine.py`; output via the
  shared `skills/_plotly.jsonable` (factored out of `run_scanpy`). DEG: scRNA `rank_genes_groups` + bulk pyDESeq2
  (CPM fallback). **Enrichment: in-house hypergeometric ORA + BH FDR vs a bundled GO/Reactome `gene_sets.json` —
  NO gseapy/MSigDB (DECISIONS #9).** `main.py` run-endpoint generalized to ANY Verified skill (query params +
  preserved upload suffix). Tests `tests/test_skills_golden.py` + `tests/golden/*.json` (regen `tests/regen_golden.py`)
  → `python -m pytest` = 14 passed; real engines smoke-verified on synthetic data.
- **Last (done by Claude, acting BE, 2026-06-12) — B3 backend DONE + integration quick wins; ALL PUSHED (`main` @ `5138aec`).**
  **Registry:** `skills/registry.py` maps each `skill.json` (+ a new optional `catalog` block on `SkillSpec`) to the FE
  `SkillCatalogEntry` shape; `GET /skills` returns the live Verified set. **Async jobs API:** `POST /skills/{id}/jobs` →
  `GET /jobs/{id}` (poll) / `/events` (SSE) / `/result`; new `config.py`, `jobs/{store,queue,worker}.py`,
  `storage/results.py`; one `execute_job` shared by the inline executor and the arq worker. **Default = inline + local
  filesystem result store (ZERO infra).** `SELOM_QUEUE=arq`+Redis (DECISIONS #6) and `SELOM_R2_*`+R2 (boto3 + the RISKS #5
  checksum fix) are **wired but DORMANT**; new optional `[jobs]` extra. **heatmap** got scipy hierarchical row-ordering
  (real-engine only); **`gseapy` DROPPED** from `[omics]` (DECISIONS #9 / RISKS #6/#9; uv re-locked). `pytest` = **21 passed**.
  arq #6 confirmed in your absence — review on return. **`/run` stays synchronous** (the proven light-skill path); only
  heavy skills use `/jobs`. The one open BE item: **arq cross-process job *status*** needs a Redis-backed JobStore
  (`jobs/worker.py` documents the caveat — success propagates via the shared result store, but queued/running/error states don't yet).
- **Last (done by Claude, acting BE, 2026-06-12 02:45) — B4 publish-confidence backend slice-1 DONE; COMMITTED `2ab7c43`, NOT pushed**
  (`main` ahead 2 of `origin/main`). New `provenance.py` = per-figure reproducibility bundle (skill+version, resolved+typed params,
  input **SHA-256**+size, environment = python/platform/engine-policy + scientific-stack versions via `importlib.metadata`). New
  `methods.py` = auto methods-text (deterministic per-skill templates quoting params + canonical citations; generic fallback).
  `contract.resolved_params()` = typed param coercion (runners untouched). **Additive wire change:** `POST /skills/{id}/run` +
  `GET /jobs/{id}/result` now return `{figure, provenance, methods}` (same shape both paths); `Job` gained `filename`, `submit`/
  `execute_job` thread it, the shared `execute_job` stores the full bundle (`result_store.put` param figure→payload). `pytest` =
  **28 passed** (21 + `test_provenance.py` + `test_methods.py` + jobs-bundle asserts); ruff clean; live uvicorn smoke green.
  **No new deps** (stdlib hashlib/platform/importlib.metadata). FE half is committed separately (`84ae54e`).
- **Last (done by Claude, acting BE, 2026-06-16) — Pillar-1 S2.1: skills emit a Statistics `table` (`af03583`, NOT pushed; `main` ahead 9).** Decision D7, additive. A runner attaches `figure["table"]`; `contract._execute` **pops it BEFORE theming** so the goldens stay **byte-identical**; new `run_skill_with_table(skill_id, path, params) → (figure, table)`; `/run` (`main.py`) + the job bundle (`jobs/queue.py`) gain `"table"` (None for purely-visual skills). `skills/_table.py` = shared `table()`/`de_table()` (JSON-safe cell coercion). Wired: **volcano** (full DE table gene·log2FC·padj·direction, most-significant-first, capped 300) · **deg** (gene·log2FC) · **enrichment** (terms + up/down split). **markers DEFERRED** (the FE fallback `lib/lineage/derive-table.ts` covers it). New `tests/test_table.py` (5; stub-pinned via the `_force_stub` fixture). **pytest 171 (166+5), ruff clean, goldens unchanged.** ⚠️ **Running backend pytest/ruff FROM THE AGENT TOOL CONTEXT:** the project `.venv\Scripts\python.exe` launcher is **EDR-blocked** (`Access is denied — os error 5`); plain `uv run` from the agent also fails (it spawns that stub). Workaround (the owner's own interactive `uv run` is unaffected): `$uvpy="C:\Users\seamegdool\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\python.exe"; $env:PYTHONPATH="D:\selom\app\backend\.venv\Lib\site-packages"; Set-Location D:\selom\app\backend; & $uvpy -m pytest -q; & $uvpy -m ruff check .` (memory `selom-backend-python-exec`).
- **Last (done by Claude, acting BE, 2026-06-17) — Reproduction Engine first slice R0+R1+R2 (`e69d695`, LOCAL/push-pending).** New `app/backend/reproduction.py` (typed Pydantic Ledger + verdict/tolerances **D4** + the blame decision procedure **D10** + findings-first scorecard + JSON load/save under `settings.data_dir/repro/<slug>/`) + `reproduction_guards.py` (the 14 SOP edge-case guards as a registry, **D11**) + `run_panel` (run via `run_skill_with_table` + `provenance.build` + `methods.build` → validate + blame, rebuild scorecard). **Headless library — NO new routes/contract change** (v1 internal-first, **D12**). Composes existing modules; **no new deps**. pytest **219** (+28 in `tests/test_reproduction*.py`); ruff clean. Spec + sub-spec live in `docs/reproduction-engine/` (`spec.md` APPROVED, build plan R0–R5). Next backend repro step = **R3** oracle runner (R edgeR/fgsea, dev-only/ADR 0002) + sweep, or **R4** extraction `extract/` package (X1).
- **Next (finish B4, then integration backlog):** **B4 remaining** — (a) **statistical-guardrail expansion DONE** by Claude
  (`guardrails.py` adds a `guardrails` list to the bundle: multiple-testing / FDR method checks + best-effort `.h5ad` low-cell /
  pre-normalized / uncorrected-batch; cluster silhouette stays in the runner; commit `edd3615`), (b) **Kaleido journal export**
  PNG/SVG/PDF — needs **Docker + system Chromium** (RISKS #2 / DECISIONS #7), so it lands with the deploy image (B8-adjacent);
  scope a CPU-Docker path or defer — **ASK OWNER before any Docker/WSL work**. **Integration backlog (`docs/integrations.md` + `plans/v2-backend.md`):**
  OmicVerse **isolated** 2nd engine/Foundry source — pins `pandas<3`, CANNOT share this venv (RISKS #9); run out-of-process (its MCP
  server / a worker), SCA-gate 50 deps; the **arq Redis status store**; R4DS/Quarto + R-oracle (RISKS #7); full GO/Reactome GMT;
  catalog-count true-up (≈418). sklearn is core (silhouette in `cluster`). Explicit staging only (never `git add -A`); `feat(backend:)` scope.
- **Resume:**
  ```
  # Resume · 2026-06-12 02:45 +10:00 · Selom · Codex (backend) — reclaiming from Claude
  Selom build repo D:/selom. CODEX.md auto-loads. Read agent_handoff/README.md + CURRENT.md + DECISIONS.md + RISKS.md + docs/build-charter.md + plans/v2-backend.md + docs/integrations.md + git status.
  Delta: B1 + B2 + B3 + B4-slice-1 backend DONE by Claude, ALL PUSHED (main ↔ origin/main in sync; B4 backend 2ab7c43 + FE half 84ae54e). B4 = provenance.py (per-figure reproducibility bundle: skill+ver, resolved+typed params, input SHA-256+size, env=python/platform/engine-policy+pkg versions) + methods.py (auto methods-text, per-skill templates + citations) + contract.resolved_params(). /run + /jobs/{id}/result now return {figure, provenance, methods}; Job.filename added; shared execute_job stores the full bundle; result_store.put param figure→payload. No new deps. pytest 28 passed; ruff clean; uvicorn smoke green.
  Open BE items: (1) arq cross-process job STATUS needs a Redis-backed JobStore (jobs/worker.py caveat). Next: finish B4 — Kaleido export ONLY (guardrail expansion DONE — guardrails.py edd3615; Kaleido needs Docker+Chromium, RISKS #2 — ASK OWNER before any Docker/WSL) — then integration backlog (OmicVerse isolated worker, R-oracle, full GMT, catalog true-up). Env: user-managed py3.12 (uv); system 3.10 IT-locked; `uv run --directory app/backend python -m pytest`/`-m uvicorn`. Stay on Opus 4.8 (xhigh). End clear-safe.
  ```
