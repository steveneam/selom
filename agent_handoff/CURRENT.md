# Selom — CURRENT (Live State)

> ## ▶ BOOT: Steven types **`gogogo`** — that IS the whole resume prompt.
>
> **Agent, on `gogogo` (or any greeting with no task): do this, unprompted.**
> He cannot copy text out of the terminal, and he may be sending it from a
> Telegram topic on his phone (the swordfish hermes relay cold-starts this
> session in tmux — no one types `claude` first). So there is no prompt for him
> to paste: **the prompt is this file.** Read, in order, then act:
> 1. this whole file (newest SESSION slot → the live pointers below it)
> 2. `CLAUDE.md` + `agent_handoff/README.md` (the coordination home) and memory
> 3. `git log --oneline -8` and `git status` — trust the repo, not the stamp
>
> Then **state the next action in one sentence, say what you are starting, and
> start it.** Do not ask "shall I?" — the next action IS the standing approval.
> Stop only at a founder gate (spend · irreversible · anything the protocol
> names a founder decision).
>
> _Boot block added 2026-07-13 at the founder's direction, so a relay-cold-started
> session resumes with no chat history (mechanism: the Swordfish hermes relay).
> Keep it at the top when you overwrite this file in place._

> Thin, slot-based pointer — **overwrite in place, never append** (README "CURRENT.md shape" + hard
> rule 1). Per-session NARRATIVE lives in commit messages + `archive/`, not here.

## ▸ SESSIONS  (newest first — scan here; detail = the commit range + git log)

| Tag | Date | SHA range | One-line |
|---|---|---|---|
| **PHASE0-COMPLETE** | 2026-07-25 | `main` `dddf5d6..621875f` (**1 unpushed**) | **Phase 0 of the next-session plan is DONE — the 3-lane sprint is ready to fork.** Owner cleared all four founder gates in one pass. **Pushed `main`** (`60df628..dddf5d6`, 27 commits, Vercel deploy triggered) and deleted `campaign/parallel-lanes` after verifying containment. Built the missing groundwork: **`scripts/verify.sh`** — Selom's first single gate of record, 7 gates at CI parity (it adds the `fe-build` the plan's list omitted, the only gate that catches SSR breaks), raw output, exit-code gated, **70s** — and **`scripts/worktree-setup.sh`**, because lane provisioning was **PowerShell-only on a Linux box**, so no lane could have been forked at all. Validated on a real throwaway worktree, which **measured** the lane limits: eslint/tsc/vitest all pass in-lane, only Turbopack cannot, so `fe-build` + browser checks are merge-train-only. **Froze `GET /cloud/providers`** as an executable ratchet (verified to fail on a renamed FE key, an added BE key, and a leaked secret), pinned before any lane forks and named outside Lane 2's glob. Fixed **L1-10** so the gate is green on a clean tree. Decisions: **lucide stays** (DECISIONS #12, closes A30, drops L3-03); **unparked** arq+Redis (executes decision #6) + journal style packs; on-hold folded into **one** register with true reasons. Owner's broader ask answered in `docs/integration-robustness/proposal.md` — top recommendation a **reachability ratchet**. |
| **REVIEW-MERGE** | 2026-07-25 | `main` `60df628..bee9e66` (**22 unpushed**) | Milestone review of the whole campaign branch (`review-gauntlet` 30 confirmed / 3 blockers + `fe-review` 24 confirmed, 31 of 35 user tasks with no affordance) → fix pass → **owner approved → `main` FF-merged**, gates re-run on merged main. **Both blockers fixed:** WS3.1 had flipped DE significance from adjusted p to RAW p across 5 runners while the axis/table/methods still said "adjusted" (`b73bd9b`, 2835 vs 1008 significant genes on the real EYG_28 export; the guard test had been inverted to bless it) · a gene-label click deleted the user's annotation (`b1a49e8`). HIGH honesty set fixed (`1d2aa81`): FACS cites FlowIO+FlowUtils not the absent FlowKit + states the real compensation outcome · assemble records lineage · GSEA's bare `t` no longer matches `entrezgene_id` · Nango ELv2 recorded. **Annotation layer FLAGGED OFF** (owner call) → ~20 findings unreachable. **Google Drive + Dropbox cloud connections now LIVE + verified** end-to-end. |
| **LAUNCH-CAMPAIGN** | 2026-07-23 | `60df628..e90203a` — **merged into `main` 2026-07-25** | Owner-directed parallel launch campaign. **Committed:** WS3.1 skill-runner vocab converge (`7440f56`) · Pillar-2 figure-editor **canvas shell** slice-0 (`3db8f0b`) + **annotation/drawing** slice-5 (`d06a28b`) · **ERG** OP+PhNR+flicker-FFT+robust-a/b (`a84636c`) · fe-review drops `frontend-design` (`5ab0103`) · **cloud-storage + self-host Nango** foundation (`8fb2bad`) · **multi-sample scRNA assemble** (`11a115f`) · **FACS `facs_gating` now REAL, IN-PROCESS on pandas-3.0** — clean-room FlowIO+FlowUtils (FlowKit dropped, bokeh/tornado gone from the lock), RISKS #12 **RESOLVED**, `e90203a`. **Nango is LIVE** at `nango.swordfish.cfd` (syd2, swordfish-provisioned) — loading Google/Dropbox integrations blocked ONLY on the env **secret key** (asked swordfish in ASK-BACKS). **ERG Fig-1E n=5 mean±SEM** figure+data staged; delivery is via the Nango cloud channel (owner's choice — not rclone/export). |
| **HOST-PORTABILITY / W-003** | 2026-07-19 | `c512dbc..96d7296` | First session on **Linux host syd4**. Host-portable: review workflows resolve `git` from cwd; `config.py` reads `SELOM_DATASETS_DIR`/`SELOM_PAPERS_DIR`; hygiene-scan 5th class (drive-paths). Closed W-003 (M-007/M-008). Pushed to `origin/main`. |
| **PORT-MERGED** | 2026-07-09 | `24c6797..2cb4cb9` | PR #1 FF-merged to `main`; two `ci.yml` trigger-event fixes. [[verify-ci-in-its-target-event]]. |
| older | — | `git log` / `archive/` | ENG-PORT · CI-GREEN · PARALLEL-SPRINT-1 · RESTRUCTURE 01–08 · AWS materialization · deploy backbone. |

## ▸ LIVE · PHASE0-COMPLETE · 2026-07-25 17:30 +1000 · branch `main` (**1 commit unpushed**) · Claude (FE+BE, solo)

- **State: Phase 0 is COMPLETE — every row DONE. The 3-lane sprint is approved and ready to fork; nothing blocks it.** `main` is linear at `621875f`. The 27-commit push landed (`60df628..dddf5d6`, Vercel deploy triggered) and `campaign/parallel-lanes` is deleted (containment verified first). Only `621875f` (docs) is unpushed — **that push is its own founder gate**, per the plan's rule that each push is separate.
- **⚑ THE ONE GATE OF RECORD IS NOW `scripts/verify.sh`** — use it, do not hand-assemble gates, and **never pipe it through `| tail`** (a pipe returns tail's status and throws the gate's failure away). 7 gates at CI parity, raw output, ledger + non-zero exit, fails CLOSED on a missing tool, **70s**. `--fast` skips `fe-build`; `--be`/`--fe` narrow; `--list` prints the commands. Current: **7/7 PASS** (BE 1294 passed / 6 skipped · FE tsc 0 · eslint 0 · vitest 541 · hygiene 952 files / 0 hits).
- **Lanes can now actually be forked:** `scripts/worktree-setup.sh` (the PowerShell provisioner was unusable on Linux — no lane could have been forked with working deps). **Measured, not predicted:** eslint + tsc + vitest all pass in-lane; **only Turbopack cannot**, so `fe-build` *and* the real-browser check are **merge-train steps on the main checkout**, never in-lane. Both dep sets are **shared symlinks** — never `npm install` in a lane, and a lane needing a new BE dep **re-plans** rather than syncing the shared venv (`uv run` auto-syncs and would mutate every lane).
- **`GET /cloud/providers` is FROZEN** (`docs/cloud-providers-contract/spec.md`) — the one cross-lane surface, pinned before any fork. Guard is `app/backend/tests/test_contract_cloud_providers.py`, deliberately **outside** Lane 2's `test_cloud*.py` glob, so editing the freeze is a visible scope breach. Verified to fail on a renamed FE key, an added BE key, and a leaked secret. Changing the shape is a **re-plan**, never a wave-through.
- **Cloud channel is real but still unreachable:** Google Drive (`c878e8db…`, `drive.file`) + Dropbox (`5f45a106…`, 4 scopes) connected on swordfish's syd2 Nango, refresh verified, live calls 200. Three closed gates remain = Lane 2 (`SELOM_CLOUD_*` absent from `.env` · no providers route · FE hardcodes `comingSoon`).
- **TWO NANGO INSTANCES — never confuse them:** LIVE = `nango.swordfish.cfd` on **syd2** (holds the integrations + connections); DEV = `127.0.0.1:3003` on **syd4** (`deploy/nango/`, own DB + key, **stopped**, used by nothing). Selom's secret key returns `unknown_account` on the wrong one — that one command is the tell. Run `bash deploy/nango/preflight.sh [BASE]` before ever asking a human to consent.
- **Gates are fully green — `L1-10` is fixed** (it was a bad test *fixture*: no obs/var → auto-generated indices that pandas-3 makes arrow-backed and anndata cannot write; production already handled this via `assemble._prepare_for_write`). There is no longer any known-red test.
- **selom-data IS on this host:** full corpus at `/home/deploy/migration/selom-migration-staging/selom-data/` → `SELOM_DATASETS_DIR`. Export it or the real-data tests silently skip and `verify.sh` will say so. [[selom-machine-migration]].

## ▸ NEXT  — **entry point = `docs/next-session-plan/plan.md`** (phased, ID-tracked). **Phase 0 is DONE; start at §Lanes.** Run them as ISOLATED worktree lanes, never in-context fan-out (a prior Mode-A default auto-compacted mid-sprint; [[parallel-agent-lanes]])

- **① FORK THE 3 APPROVED LANES — this is the next action.** Owner approved the partition (`P0-05`), so forking, building in-lane and the **local** merges are all autonomous; **each push stays a separate founder gate.** Per lane: `git worktree add <path> -b agent/<bucket>/<slug>` → `scripts/worktree-setup.sh <path>` → write the lane a **kickoff FILE** (scope · frozen-contract pointer · definition of done · verify command · the landmines, **inlined** — a worktree is a separate memory namespace and recalls none of this) → drive it in the shared tmux server.
  - **Lane 1** backend integrity — 9 open rows (`L1-01`…`L1-09`; `L1-10` already done). **Lane 2** cloud reachability — 6 rows, sole owner+consumer of the frozen contract. **Lane 3** FE polish — 3 rows (`L3-03` dropped by the icon decision).
  - **Merge train `Lane 1 → Lane 3 → Lane 2`**, strictly serial and **lead-driven — a lane never merges itself**: rebase onto current `main` → run **`scripts/verify.sh` (no args) on the rebased result** → merge → next lane rebases onto the new `main`. Contract-shaped conflict ⇒ **send the lane back**, never hand-resolve at the train.
  - **In-lane gate is `scripts/verify.sh --fast`** and cap parallelism (`SELOM_PYTEST_WORKERS=2`, never `auto` — 3 lanes × 6 workers on 6 vCPU oversubscribes). `fe-build` + the real-browser check are **train-only, on the main checkout** (measured: Turbopack rejects the out-of-root symlink).
- **② AWAITING OWNER REACTION — `docs/integration-robustness/proposal.md`** (answers the owner's "more features + tighten integration/layout/workflow" directive). Top recommendation: build a **reachability ratchet** — every user-facing backend capability is either reached by a declared FE surface or explicitly waived with a reason + ID, exit-code gated — **shipped green** with waivers for today's known set, each lane removing its own. It is the remedy [[selom-shipped-not-reachable]] already named and that was never built. Also proposes 4 features and explicitly **no new analysis skills** (the constraint is reachability, not breadth). Nothing in it is scheduled until the owner picks.
- **③ THEN the two just-unparked items** (`P0-07`, owner-decided, deliberately in **no** lane): **arq + Redis job-status store** — executes locked decision **#6**, Redis already on `:6380`, closes the `jobs/worker.py` cross-process-status gap — and **journal style packs**, whose spec is already written and which is the natural vehicle for the proposal's `F3` one-click regenerate-at-journal-spec.
- **Backlog of record:** `docs/milestone-review-2026-07-25/findings.md` (A = gauntlet · B = fe-review · C = the G1 task walkthrough · D = layout predictions still needing a real browser). Both blockers and the whole HIGH honesty set are fixed; the annotation layer is **flagged off** (`NEXT_PUBLIC_ANNOTATION_LAYER`), so ~20 of its findings are unreachable until Plan C's spec — which should be **led by** "re-run must not silently discard the user's annotations", not by more drawing tools.

## ▸ DEFERRED

- **OneDrive/Microsoft** cloud provider (owner on hold until a machine that logs into Azure cleanly).
- **Public Selom backend on syd2** (swordfish scoping note, FROM-SWORDFISH top): needs a backend **Dockerfile + GHCR image-CI** (mine) + 5 data-plane answers (DB/object-store/datasets-mount/heavy-jobs/auth). Owner-gated on any syd2 resize (spend). Reply in ASK-BACKS.
- WS6 AWS deploy — owner chose **"this box first, AWS later"**. Owed WS1/restructure reviews fold into the campaign milestone review.

## ▸ ENV / landmines (Linux · syd4)

- **selom-data IS here** at `/home/deploy/migration/selom-migration-staging/selom-data/` → `SELOM_DATASETS_DIR`. **Docker installed** — in a fresh shell use `sudo docker` until the `deploy` docker-group login refreshes.
- **Gate of record = `scripts/verify.sh`** (7 gates, 70s, raw + exit-code gated). Do NOT hand-assemble gates and do NOT pipe it through `| tail` — a pipe returns tail's status and discards the failure [[read-gate-output-raw-not-piped]]. Servers: backend `uv run uvicorn main:app --reload`; frontend `npm install --legacy-peer-deps` **in the MAIN checkout only**. Derive the FE dev-lane port (Selom FE=3152) to avoid the shared-box `:3000` collision; `:8000` is eamos — never bind it.
- Every commit runs `.githooks/pre-commit` (hygiene-scan, 5 classes). `git user.email` MUST stay the noreply (`282747725+steveneam@…`) or Vercel blocks deploys [[selom-git-commit-email-vercel]].
- **Cloud OAuth creds** staged in `app/backend/.env` (gitignored): `GOOGLE_*` ✓ · `DROPBOX_*` ✓ · `MS_*` empty (on hold). The syd4 **Nango dev copy** (`deploy/nango/`) is **STOPPED** as of 2026-07-25 — containers + volumes intact, restart with `sudo docker compose -f deploy/nango/docker-compose.yaml start`. A dead `localhost:3003` is EXPECTED; the live broker is swordfish's on **syd2** (`nango.swordfish.cfd`) and is unaffected. Verify any time with `bash deploy/nango/preflight.sh`.
- **Worktree lanes share deps by SYMLINK** (`scripts/worktree-setup.sh`): never `npm install` in a lane (it writes through the link and clobbers the main tree — `guard-worktree-install.mjs` refuses it), and a lane needing a new BE dep **re-plans** rather than syncing, because `uv run` auto-syncs the SHARED `.venv`. Turbopack cannot run in a lane at all, so `next build`/`next dev` and browser checks belong on the main checkout.
- **The agent cannot marshal binary/large files through chat** (base64 reproduction corrupts, even ~20 KB) — deliver files via a real channel (scp/SFTP/rclone/the cloud integration), never by pasting base64 into a tool call. Emailing via the Gmail MCP is draft-only + attachment-limited; Drive `create_file` needs valid inline base64 (same wall).

## ▸ READ FIRST

**`docs/next-session-plan/plan.md`** (the entry point — **Phase 0 done, start at §Lanes**) · **`docs/integration-robustness/proposal.md`** (awaiting owner reaction) · **`docs/cloud-providers-contract/spec.md`** (the FROZEN cross-lane contract — read before touching cloud) · **`docs/milestone-review-2026-07-25/findings.md`** (54-finding backlog + status ledger) · `agent_handoff/DECISIONS.md` (#12 = lucide stays) · `docs/on-hold/README.md` (now the ONE on-hold register) · `docs/next-session-plan/lane-mechanics-from-thalon.md` · `docs/restructure/plan.md` (WS3 done) · `docs/fe-review/spec.md` (now impeccable-only) · CLAUDE.md · `deploy/nango/` (once built). Memory: [[parallel-agent-lanes]] · [[selom-machine-migration]] · [[ask-before-docker-wsl]] · [[selom-fe-review-framework]] · [[verify-on-real-data-not-mock]] · [[selom-git-commit-email-vercel]].

## Codex — Last Task & Resume

Codex is away; Claude covers both lanes ([[claude-covers-both-selom-lanes]]). Keep the BE handoff drop-in-ready. Last Codex-lane state of record = `docs/restructure/plan.md` + `plans/v2-backend.md`.
