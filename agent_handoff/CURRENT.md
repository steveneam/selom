# Selom — CURRENT (Live State)

> **LIVE STATE ONLY.** The current state of the two-agent build. History lives in
> each agent's rolling log, not here. This file is **replaced, never stacked** —
> update at MAJOR boundaries only (rule 9 in `agent_handoff/README.md`).

_Last updated: 2026-06-23 01:30 +10:00 (TANGENT session "T4" — ERG figure-editor restructure, owner's real work) · Claude (acting FE+BE) — **P2.5 (skill-DEPENDENT param richness) SHIPPED + PUSHED (origin ↔ main @ `696ef6e`; tree clean; no dev servers — FE :3000 stopped). Then the owner refocused: the param/metadata work is a SIDE QUEST — don't drift; the MAIN plan is getting the ERG traces / publication figure done. P2.5(c) + skill role/stage metadata are PARKED with their designs captured (below); ERG is the headline for next session.** **P2.5(a)+(b) — DONE (`696ef6e`, verified LIVE on `demo-pbmc`):** `showWhen` conditional `ParamField`s + a shared `visibleParamFields(schema, params)` filter wired into BOTH ParamControl surfaces (Workbench + Figure-data), so method-specific knobs reveal identically in each. New `integration` (Melody) FE schema: `batch_key` · a Melody-BRANDED engine `select` (keyed to the real `harmony2` param via to_bool-compatible `"true"/"false"` values — labels say "Selom Melody — 2019 method" / "— Harmony2 mode", NEVER literal GPL "Harmony") · `theta` · `alpha` GATED to Harmony2 mode (`showWhen`) · `n_hvg` · `normalize`; keys/ranges mirror `skills/integration/skill.json` `param_spec`. Enriched `umap_scrna` (dropped the DEAD `resolution` knob — the runner's Leiden ignores it; surfaced the honoured `normalize`/`n_hvg`/`n_neighbors`/`n_pcs`) + `cluster` (kept honoured `resolution` + added `n_neighbors`/`n_pcs`). Added `selom.integration` to the Store seed catalog (installable + selectable). `param-control.tsx` range readout now honours step precision ("15" not "15.0"; α reads "0.20"). **Files:** `lib/catalog/params.ts` (+`showWhen`, +`visibleParamFields`, enriched schemas) · `params.test.ts` (+11) · `param-control.tsx` · `workbench-panel.tsx` · `figure-data-panel.tsx` · `seed.ts`. **Gates: vitest 150 (+11) · eslint clean.** **LIVE-verified:** Store→install Melody→`demo-pbmc` Workbench→select Melody shows the rich scRNA knobs; switch engine to Harmony2 mode → α appears (`graphify-out/scratch/melody-harmony2-reveal.png`). **PARKED side quests (designs captured, both cross the BACKEND lane → coordinate with Codex):** **(c) backend `param_spec` → FE (kill the hand-maintained `params.ts`):** the contract source already EXISTS — every `skill.json` has a `param_spec`, served verbatim by `GET /skills/{id}` (`load_skill().model_dump()`; `SkillSpec.param_spec: dict`). DESIGN = the FE derives the param SET + types/defaults/min-max from the backend spec (so dead/missing knobs become impossible — the drift we just caught), with a THIN FE *presentation overlay* for the polish the backend spec lacks (friendly labels, `select`/options, `step`, `showWhen`, hidden-advanced). Backend run path ALREADY fills defaults server-side (`contract.resolved_params` = `{**defaults, **caller}` coerced) → the FE need only send CHANGED params, so one-click quick-apply can send `{}`. BUILD shape: `paramFieldsFromSpec(runtimeId, spec)` (pure, testable) + move `SCHEMAS`→`PRESENTATION` overlay + async `loadSkillParamSpec(id)` (cache) + `useSkillParams(id)` hook (loading state) + MSW mock for `GET /api/skills/:id` + wire both components + live-verify vs the lightweight backend (the skills endpoint needs no omics stack). **(skill role/stage metadata):** add `stage`/`role` (qc · prep · integration/correction · analysis · post-hoc/filter · visualization) + `produces` (figure · table · transformed-data) to the SkillSpec → a card BADGE + routing (where a skill lands) + "what to run next" recommendations (the data-check card already does adjacent suggest-a-skill). NOTE corrected for the owner: Selom's Melody `integration` skill DOES run a full pipeline + emits a UMAP (read→normalize→PCA→Melody→neighbours→Leiden→UMAP) — it's a mid-pipeline CORRECTION step, not pure post-hoc filtering; the role badge just makes that legible. **════ MAIN NEXT = ERG traces / publication figure (the real deliverable):** the figure-editor restructure P1+P2+P2.5 is scaffolding; now finish the actual figure. Open items: **(1)** the **b-wave reconciliation BLOCKER** (`docs/erg-module/spec.md` §"Extraction result… OPEN") — extracted b-wave-per-condition reads far too high vs Fig 1E's printed bar (flat conditions ≈ 88–146 µV vs bar ≈ 15–20 µV); reconcile against the printed Fig 1E values (LabScribe `marks.txt`/`Views.txt` landmarks + smoothed-landmark fallback) BEFORE trusting representative selection (R16/R19). **(2)** export a publication figure (open from T3). **(3)** owner's call: the PARKED display-smoothing default (skill's notch+300 Hz leaves flats noisier than the team's `render_preview.py` 16 ms moving-average). **(4)** Phase 2 skills (`erg_bwave_bar`, `erg_intensity_response`, Naka-Rushton, ISCEV `methods._erg_traces`) + Phase 3 FE polish. **(5)** figure-editor P3 (primitives — scale-bar show/hide/resize as a tagged `layout.meta.selom.primitives` primitive · threshold/annotation edit · click-a-line→recolour [`seriesForTrace` exported] · trace-grid BE opt-in emits `legendgroup`+`layout.meta.selom`) + P4 design polish (`/impeccable`, `ui-ux-pro-max`) — insofar as they serve the ERG figure. _[prior T3 state ↓]_

<!-- prior live-state (session T3): _Last updated: 2026-06-23 01:00 +10:00 (TANGENT session "T3" — ERG figure-editor restructure, owner's real work) · Claude (acting FE+BE) — **P1 + P2 of the skills-agnostic figure-editor restructure (`docs/figure-editor-contract/spec.md`) SHIPPED + verified LIVE on the real ERG figure. 2 FE-lane commits LOCAL — ASK before push (origin ↔ main @ `0fbe096`; `fdbbbb4` P1 + `3d22fa4` P2). No dev servers (FE :3000 + BE :8000 stopped; Eamos :3001 untouched).** **P1 — adaptive agnostic editor:** new `lib/figure-model.ts` = inference-first `deriveFigureModel(spec)` (per-trace kind + the CORRECT colour channel [`line.color` vs `marker.color`] + capabilities + series grouping: 42 ERG traces → 6 conditions, the two grey arms PDE6B/stuffer kept apart by name-prefix; reads optional `meta.selom`/`legendgroup` hints). `normalizeSpec` no longer force-injects `marker` on non-marker traces; StylePanel renders control groups CONDITIONALLY (Markers/Lines/Colour-scale/Palette/Background/Colour-bar; palette-apply rewrites explicitly-coloured series); DataPanel lists SERIES (swatch writes the right channel to all traces in the series). LIVE: Style showed Lines (not Markers), Data showed SERIES·6, recolouring Untreated wrote `line.color` to all 7 of its panels (blue→green ×7, read back from the Plotly div). +15 `figure-model.test.ts`. **P2 — "Figure data" stage + figure-forward:** new `components/project/figure-data-panel.tsx` (renders the figure's `param_spec` prefilled → Re-run → new "edited inputs" version, original kept; data-check + data-fit RELOCATED here off the artboard) + `param-control.tsx` (EXTRACTED from `workbench-panel`, shared). `workrail.tsx` got a `figuredata` RailView + a contextual action node between Statistics and Figure (enabled only with a figure open); `project-workspace.tsx` routes it + `rerunFigureWithParams` + a "Figure data" toolbar button + removed the inline checks from the figure view (figure-forward). LIVE: edited scale-bar 200→400 µV + Re-run → a new version re-rendered at 400 µV, original kept (2-version family). Scoping call: the home 4-step teaching Pipeline (`pipeline.tsx`) was left unchanged; the stage lives in the rail/working pipeline. **Gates (both phases): tsc clean · eslint 0 (pre-existing advisory warns only) · vitest 139 (+15).** **NEXT = P2.5** (owner steer this session — skill-DEPENDENT param richness: the Figure-data panel is the agnostic shell, but the per-skill schemas declare too little; build `showWhen` conditional fields + enrich the scRNA/integration schemas [Melody method+knobs — Melody is the shippable engine, NOT GPL Harmony]; figure = rich single card now, multi-step pipeline deferred) → **P3** (primitives — scale-bar show/hide/resize as a tagged primitive + threshold/annotation edit + click-a-line→recolour [`seriesForTrace` already exported from figure-model.ts]; backend opt-in: trace-grid emits `legendgroup` + `layout.meta.selom`) → **P4** design polish (`/impeccable`, `ui-ux-pro-max`). Real data: `D:\selom-data\erg-fig1e\erg_waveforms_long.csv` (gitignored upload copy re-staged at `graphify-out/scratch/erg_waveforms_long.csv` — a page reload loses the in-memory File → re-upload via Data ▸ Add another dataset before a live re-run; a few duplicate ERG datasets were left in the `p_7821cab4` browser localStorage — harmless dev-only state). Scratch screenshots: `graphify-out/scratch/p1-*`, `p2-*`. [[selom-figure-editor-architecture]] [[selom-erg-module]] [[the-ratchet-durable-artifacts]] [[selom-backend-python-exec]] [[kill-dev-servers-on-handoff]]_ -->

<!-- prior live-state (session T2): _Last updated: 2026-06-23 00:05 +10:00 (TANGENT session "T2" — ERG figure module, owner's real work) · Claude (acting FE+BE) — **Tangent project (Eamegdool PDE6B *Gene Therapy* revision ERG figure) CONTINUED. Phase 1 was already shipped+pushed (`7a67e3e`). This session: ran the `erg_traces` skill LIVE end-to-end on REAL data through the full FE (project → data → workbench → figure editor) and acted on owner design feedback. COMMITTED + PUSHED (origin ↔ main @ `185e007`; 3 lane-separated commits `2aad6b0` feat(backend) + `903809f` feat(frontend) + `185e007` docs; tree clean). Owner deferred the editor RESTRUCTURE to next session.** SHIPPED this session: **(1) owner colour scheme** on `app/backend/skills/_erg.py::COLORS` — Control **black** · Untreated **BLUE** (`#0072B2`) · AAV8-RK-PDE6B **grey** · GFP-polyA-stuffer **grey** · CMV-GFP **AMBER** (`#E69F00`) · PDE6B-3'UTR **red** (the 3-way story = healthy/disease/rescue; golden regenerated, `test_skills_golden -k erg_traces` + table-contract green). Column ORDER already matched the owner's bar graph (Control→Untreated→PDE6B→stuffer→CMV-GFP→3'UTR), no change needed. **(2) figure-editor artboard top-clip FIX** (`app/frontend/components/figure/editor-workspace.tsx`): the scroll region used `items-center`, so a figure taller than the box had its TOP pushed out of view AND unreachable by scroll → changed to `items-start`. **(3) `erg_traces` is now a first-class FE catalog citizen** so it actually RUNS in the workbench (it was seed-only `getSkill`-gated → treated as non-verified/non-runnable): added the seed entry + a param schema (filter/lowpass/scale-bar) + an Activity icon branch + an "Electrophysiology" Store shelf in `app/frontend/lib/catalog/{seed,params,modality}.ts`. **(4) BIG — the steer that ended the session:** the live run surfaced that the figure INSPECTOR is implicitly MARKER-centric (normalizeSpec injects `marker` onto every trace; Style reads `/data/0/marker/*`; the colour editor writes `marker.color` → a NO-OP on the ERG line traces) and the SKILLS don't talk to the editor (params never reach it; scale-bar/primitives invisible). Survey: 11 figure archetypes, ~40% of skills don't fit the marker controls. → Wrote **`docs/figure-editor-contract/spec.md`** (review gate): a skills-AGNOSTIC editor + skill↔pipeline wiring — FigureModel **inference-first** (+ optional `layout.meta.selom` hints) · adaptive inspector (per-channel colour, conditional control groups, 42→6 series grouping, click-a-line→recolour) · a new **"Figure data" pipeline box** (`param_spec`→re-run, between Statistics and Figure) · **figure-forward** layout (relocate the routing/data-fit/publish chrome off the artboard) · scale-bar as a managed primitive. **Owner APPROVED the plan and explicitly said BUILD IT NEXT SESSION.** **NEXT (next session): build per the spec — P1 adaptive agnostic editor (line.color fix, conditional controls, 42→6 series grouping, click-a-line→recolour) → P2 "Figure data" box + figure-forward relocate → P3 primitives + trace-grid `legendgroup`/hints → P4 design-skill polish (`/impeccable`, `ui-ux-pro-max`). Verify LIVE on the real CSV via the FE. COMMIT this session's dirty tree FIRST.** Real data: `D:\selom-data\erg-fig1e\erg_waveforms_long.csv` (the FE upload tool is workspace-root-gated, so a gitignored copy is staged at `graphify-out/scratch/erg_waveforms_long.csv`). PARKED finding: the skill's notch+300 Hz display filter leaves flat conditions noisier than the team's `render_preview.py` (a 16 ms moving-average ≈ genuinely flat flats); owner's call whether to switch the default smoothing. No dev servers (FE :3000 + BE :8000 stopped; Eamos :3001 untouched). [[selom-erg-module]] [[selom-figure-editor-architecture]] [[the-ratchet-durable-artifacts]] [[selom-backend-python-exec]] [[kill-dev-servers-on-handoff]] [[claude-covers-both-selom-lanes]]_ -->

<!-- prior live-state (session 56): _Last updated: 2026-06-22 21:05 +10:00 (session 56) · Claude (acting FE+BE) — **Owner authorized the push + the 2 post-phase items this session. ALL SHIPPED + PUSHED (origin ↔ main @ `06174fb` + a docs commit; tree clean; no dev servers — my :3002 + Playwright's :3011 stopped, Eamos :3001 untouched). ⚑ ALL PILLARS + the dogfood phase + both post-phase items are now COMPLETE.** **(1) Slice 4 (auto-drive regression fixtures) — committed + PUSHED** (`40eca7c` feat(backend) + `50e0703` docs): closes the "Reproduction — dogfood-ready" phase (was the only open slice). `reproduction_fixtures.py` (DriveSnapshot + snapshot/diff/assert_reproduces = the regression contract, graded with the s46 metric-type grader) + `scripts/regen_reproduction_fixtures.py` (bless generator + FIXTURE_PAPERS) + committed `tests/fixtures/reproduction/{jev,hani,dorgau}.json` + README + `tests/test_reproduction_fixtures.py` (3 tiers). **(2) P4-4a contract-uniformity audit — SHIPPED** (`cca0c27`): `docs/reproduction-engine/skill-table-contract.md` + the guard `tests/test_skill_table_contract.py` partition EVERY shipped skill by table source — **native 11 · L3 16 (== `_SYNTHESIZERS`) · L4-only 3 (`go_graph`/`pathway`/`string_network`)** — completeness + disjointness asserted, so a new tableless skill with no synthesizer that isn't a reviewed L4-only entry fails CI (no silent tableless graded path). NATIVE is grounded in source (skills attaching `spec["table"]`), not a hand list; classification measured vs reality (the native attach is split `run.py`/`run_real.py`/conditional — the stub alone undercounts). **(3) P3 routing-confidence "not sure" surface — SHIPPED** (`06174fb`): the FE data-check panel now honestly surfaces `DataRouting.confident === false` (modality unclear / coming-soon / unclassifiable — `engine/route.py` already returned it) as a calm dashed "Not sure how to analyze this" card (Compass + the engine note + "Exploratory starting points" + a "Choose a skill yourself" hand-off to the workbench) instead of presenting a guess as a recommendation. New `UncertainRouting` in `data-check.tsx`; mock `?data_check=unsure|comingsoon` variants; `e2e/data-check-routing.spec.ts`. **Gates: BE — fixtures 15+1 skip · table-contract 10 · 4 hand ledgers + drive + drive_honest 63+1 skip (byte-identical) · ruff clean. FE — tsc clean · eslint 0 err (16 pre-existing advisory warns) · vitest 124 · 2 e2e pass (Playwright/system Chrome) · screenshot `graphify-out/scratch/s56-routing-not-sure.png`.** **NEXT: the owner's standing steer holds — a ~2–3-session TANGENT PROJECT (owner's real work) runs NEXT, BEFORE any next-pillars strategy. WAIT for the owner to open it; do NOT start strategising next phases.** [[selom-live-reproduction-drive]] [[layered-deterministic-extraction]] [[selom-reproducibility-score]] [[the-ratchet-durable-artifacts]] [[selom-backend-python-exec]]_ -->

<!-- prior live-state (session 55): _Last updated: 2026-06-22 19:35 +10:00 (session 55) · Claude (acting FE+BE) — **Slice 4 (auto-drive regression fixtures) SHIPPED — this CLOSES the "Reproduction — dogfood-ready" phase (Slice 4 was the only open slice). Local commits PROPOSED, NOT pushed — ASK before push; origin ↔ main @ `7efdab2`; working tree = the new BE files + this docs; no dev servers (Eamos :3001 untouched). Pure BE + docs, NO FE.** New `app/backend/reproduction_fixtures.py` = the reusable framework: `DriveSnapshot` + `snapshot()` projects a `DriveResult` → per-panel status-shape + driven metric values; `diff()`/`assert_reproduces()` = the regression contract (**same panel set · same status per panel · 0 Selom defects · every driven metric within its golden's `resolve_tolerances` band**, graded with the exact s46 grader so a tolerated engine delta inside the metric-type band stays green and a broken reader flips red). Plus `scripts/regen_reproduction_fixtures.py` (the bless/refresh generator + the shared `FIXTURE_PAPERS` registry), committed `tests/fixtures/reproduction/{jev,hani,dorgau}.json` + a `README`, and `tests/test_reproduction_fixtures.py` (3 tiers). **MEASURED reality drove the design:** cold-driving JEV/Hani/Dorgau drives **0 panels** — all `no_golden`/`data_unmatched`/`run_failed` (the Slice-0/1 data-availability+recall insight, now confirmed on the hand-ledger papers; every panel still classified honestly, **0 Selom defects**) — so the win these fixtures lock today is the **honest classification shape**; `metrics` are empty and auto-populate as recall rises (no fixture-code change — each future paper is a copy-paste). **Tier a (cheap CI, no data):** the framework's unit tests over a synthetic stub-runner drive (status flip → red · within-band metric → green · out-of-band → red = the "break a reader" check) + characterization over each committed snapshot (statuses honest · summary == tally · 0 defects). **Tier b (opt-in, owner machine):** the genuine engine-regression guard — `reproduce()` each blessed paper, `assert_reproduces(committed_snapshot, result)`; skips without the staged PDF (CI stays green); verified green here on all 3. **The 4 hand ledgers are untouched/byte-identical** (this only ADDS the auto-drive fixtures); **RPGRIP1** is a documented `FIXTURE_PAPERS` slot (no PDF staged on this machine). **Gates: fixtures 15 + 1 skip · 4 hand ledgers + drive + drive_honest 63 + 1 skip (no regression) · ruff clean · 780 tests collect clean.** **PROPOSED commits (lane-separated, on go-ahead): feat(backend:) (the 3 .py + fixtures/README) · docs (spec + this).** **NEXT: dogfood phase CLOSED; before the ⚑ pillars-complete milestone, 2 LIGHT post-phase items remain — P4-4a contract-uniformity audit + P3 routing-confidence "not sure" surface; then the owner's ~2–3-session TANGENT PROJECT runs BEFORE any next-pillars strategy.** [[selom-live-reproduction-drive]] [[selom-reproducibility-score]] [[layered-deterministic-extraction]] [[the-ratchet-durable-artifacts]] [[selom-backend-python-exec]]_ -->

<!-- prior live-state (session 54): _Last updated: 2026-06-22 13:14 +10:00 (session 54) · Claude (acting FE+BE) — **Slice 5B (deposit-data HANDOFF) + Slice 3 (skill-gap signal) — SHIPPED + PUSHED (owner-authorized 2026-06-22); 3 lane-separated commits (feat(backend:) `6659092` + feat(frontend:) `3c11f5c` + this docs); origin ↔ main in sync; tree clean; no dev servers (Eamos :3001 untouched).** **(1) Slice 5B — deposit-data HANDOFF (un-gated, NO fetch infra):** each recognized accession now carries a per-repo `download_hint` ("which file, how" — GEO=processed matrix not SRA reads · PRIDE=quant tables not *.raw · Zenodo=.xlsx/.csv/.h5ad · raw/controlled=honest can't-drop-in). `accessions` ride the run contract now (`DriveResult.accessions` ← `find_accessions(bundle.text)` in `drive_bundle` → `RunRecord` → `public()`); new FE `components/reproduction/cited-datasets.tsx` renders on the Score stage ABOVE the picker (repo link-out + download instructions + adaptive lead-in when panels are `data_unmatched`). Diagnostic markdown's Cited-datasets table gained a "how to get the data" column. **(2) Slice 3 — skill-gap signal:** new `app/backend/skill_gaps.py` + committed `docs/skill-gaps.md` (the Ratchet, D4). `update_skill_gaps_doc` accumulates each cold-drive's *buildable* gaps (needs_recipe=read-back · no_skill=routing · oos+modality_unsupported=modality) into a ranked doc **idempotently** (paper ids + examples de-dup, `papers_seen` accumulates); auto-region (machine JSON + table) the updater owns + a hand-authored **curated** region it never clobbers. Seeded from REAL cold drives of Harmony + Yoshimura → both all data-availability gaps (Harmony now `no_golden×8`+`data_unmatched×1` — Slice 2 honesty cleared its old `run_failed×8`; Yoshimura `no_golden×3`+`oos×1`+`data_unmatched×1`) → auto-region honestly "no buildable skill gaps yet" while the curated region holds the real signal (LISI/kBET integration mixing-metrics, from `findings-harmony.md`). Diagnostic markdown also shows each run's own "Skill gaps (this run)". **Gates: BE — affected 47 (test_accessions +6 · test_skill_gaps NEW 6 · diagnose +5 · runs +1) + drive/honest 18 + 4 hand ledgers 30 byte-identical; ruff clean (binary copied OUT of the EDR-blocked `.venv` → `$env:TEMP`, then run). FE — tsc clean · eslint 0 · vitest 124 (+3). Real-data: both cold drives ran on staged PDFs + seeded the doc; updater idempotent + curated-preserving on the real doc (byte-identical re-render).** **PROPOSED commits (lane-separated, on go-ahead): feat(backend:) · feat(frontend:) · docs.** **NEXT: Slice 4 (4 ledgers → auto-drive regression fixtures) → P1–P5 essentially done → FLAG "pillars complete"; a ~2–3-session owner TANGENT PROJECT runs BEFORE any next-pillars strategy.** [[selom-accession-ingest]] [[selom-live-reproduction-drive]] [[layered-deterministic-extraction]] [[selom-data-fit-scorer]] [[the-ratchet-durable-artifacts]] [[selom-backend-python-exec]]_ -->

<!-- prior live-state (session 53): _Last updated: 2026-06-22 09:05 +10:00 (session 53) · Claude (acting FE+BE) — **Slice 2 FINISHED: the per-panel data PICKER + the Product-A data-fit band — SHIPPED + PUSHED (owner-authorized 2026-06-22); 3 lane-separated commits (feat(backend:) `fad4a62` + feat(frontend:) `b77ebd0` + this docs); origin ↔ main in sync; working tree clean; my dev servers stopped (Eamos :3001 untouched).** Owner directive: "ok do 1 and 2" = (1) the explicit per-panel data picker writing the `data_map` override + (2) a Product-A FE for the `data_fit` band. **(1) PICKER (`components/reproduction/data-picker.tsx`):** on the Score stage, each `data_unmatched` panel gets a file `Select` (Radix) annotated with the engine's per-candidate fit band; pick → persist on `SavedPaper.dataMap` (panel_key→filename, not bytes; `workspaceStore.setPaperDataMap`) → "Re-run with these picks" re-drives with the override. Wiring: `panel_drives` now rides the run contract (`reproduction_runs.public` + `RunRecord`); `POST /papers/{id}/reproduce` gained a `data_map` form field (JSON {panel_key: filename}) resolved server-side to each file's saved path (`_resolve_data_map`); `match_data` already honours the override (wins over the auto-heuristic). `usePaperRun.start(dataMap?)` takes an explicit map (picker) or falls back to the paper's persisted picks (plain Run); `useReproductionRun` exposes `panelDrives`. Honest: poor-fit pick is informed (band + caution shown), un-picked panel stays grey, unknown/stale filename = no-op (never a 4xx). **(2) PRODUCT-A BAND:** `POST /skills/{id}/run`'s `data_fit` parsed onto `SkillRunResponse.dataFit`, persisted on the `Figure`, rendered as a new `DataFitVerdict` (single-fit sibling of `DataFitPanel`, reuses the same band primitives) on the own-data figure view; MSW `mockDataFit` added so it renders in `dev:mock`. **(3) Fix folded in:** `_save_capped` now saves each upload into a per-file subdir keeping the ORIGINAL name (no uuid prefix) → data-fit filenames match what the user dropped AND the picker round-trips cleanly. **Gates: BE run tests 17 (3 new) + drive/compat/diagnose 43 green; ruff n/a (no new ruff issues); FE tsc clean + eslint 0 errors + vitest 121 (run/store +5); real-data BE round-trip (`graphify-out/scratch/s53_picker_roundtrip.py`) — override pulled the unmatched `umap_scrna` panel out of `data_unmatched`, fed the picked xlsx, `run_failed` honestly, 0 Selom defects (a compatible pick would `drive`); `/p/[id]` + `/paper/?stage=score` compile + serve 200 (new components bundle). 4 hand ledgers (RPGRIP1/JEV/Hani/Dorgau) 30 passed — byte-identical, no regression.** **NOT DONE: the live interactive browser-verify (click the picker / see the band against the live BE) — the chrome-devtools profile was locked by another session; remaining manual check.** **NEXT (s54): finish the dogfood phase straight through (owner decision s53: no gated infra) — Slice 5B = deposit-data HANDOFF (per-accession link + download instructions → user downloads → drops into the picker; un-gated, auto-fetch on hold) + Slice 3 skill-gap doc + Slice 4 regression fixtures. Then FLAG "pillars complete" — but a TANGENT PROJECT (owner's real work, ~2–3 sessions) runs BEFORE any next-pillars strategy.** [[selom-data-fit-scorer]] [[selom-live-reproduction-drive]] [[layered-deterministic-extraction]] [[selom-accession-ingest]] [[selom-figure-repro-mission]] [[ask-before-docker-wsl]] [[kill-dev-servers-on-handoff]]_ -->

<!-- prior live-state (session 52): _Last updated: 2026-06-22 03:20 +10:00 (session 52) · Claude (acting FE+BE) — **Slice 2 — data-fit scoring + matcher honesty — SHIPPED: 3 commits (feat(backend:) + feat(frontend:) + docs) PUSHED (owner-authorized 2026-06-22); origin ↔ main in sync; working tree clean; my dev servers stopped (the unrelated Eamos :3001 left alone).** Owner steered the scope live (5 refinements, all folded in): (a) "rank/score the dropped supplementary data — we have a filter for this"; (b) "visible BEFORE Run so they can swap a file, and after"; (c) "recognize the right COLUMNS / format, layered like our extractor"; (d) "the score should MEAN something — Confident / Not confident"; (e) "it's translatable to the main feature too (own data)." **Built `engine/compat.py`** — the bridge composing the two filters the engine already had (`engine.classify` modality + `engine.qc` is-it-clean) against *what a panel's skill needs* → one rankable **fit score 0-100 + a confidence band** (Confident / Usable / Uncertain / Not a fit / Unreadable). **Layered** ([[layered-deterministic-extraction]]): L2 payload-class gate (a flat table can never be a single-cell matrix — the *certain* gate) → L1 per-skill **column schema** (volcano needs logFC+padj, gsea a ranked list — precise + actionable) → L3 coarse modality. **Honesty rule:** gate only on a *positively-determined* mismatch; an unloadable/unclear file stays optimistic → every fake-path drive test unaffected, **4 hand ledgers byte-identical**. **Matcher honesty:** `engine.match.match_data` picks the best-fit *compatible* file, returns `None` (→ honest `data_unmatched`) when every candidate is a certain mismatch — **on real Yoshimura this flipped the QC-table single-cell panels from `run_failed×3` (h5py crash) → honest `data_unmatched`/`no_golden`, 0 Selom defects.** **Surfaced before+after+product-agnostic:** pre-run `POST /papers/{id}/assess-data` (no skills run); post-run on the run contract + gap report; own-data product gets the same band on `POST /data/inspect` + `POST /skills/{id}/run` (`data_fit`). **FE:** reusable `DataFitPanel` wired into the Reproduce stage (before) + Score stage (after). **Gates: full BE 745 + targeted 40 green; ruff clean; FE tsc + eslint + vitest (4 new) green; showcase ScoreReport browser-verified unbroken on :3002; live `assess-data` HTTP returns `not_a_fit` on the real QC xlsx.** [[selom-live-reproduction-drive]] [[layered-deterministic-extraction]] [[selom-accession-ingest]] [[selom-figure-repro-mission]] [[ask-before-docker-wsl]]_ -->

<!-- prior live-state (session 51): _Last updated: 2026-06-22 02:30 +10:00 (session 51) · Claude (acting FE+BE) — **Dogfood phase advanced: Slice 0 (2nd baseline) + Slice 1 + Slice 5 SHIPPED. 3 code/docs commits + this handoff PUSHED (owner-authorized 2026-06-22; origin ↔ main in sync; working tree clean; no dev servers).** Owner steer this session: "do slice zero and slice one" + "also slice 5" (order my call). **(0) Slice 0 re-run on Yoshimura** (2nd calibration paper, kidney-organoid multiome) → `docs/reproduction-dogfood/findings-yoshimura.md`: **independently confirmed** the Harmony gaps — 0 goldens, 0 driven, all `run_failed`/`oos`, **0 Selom defects**; the meta-finding generalizes (real data = GEO accessions, only a QC table attached). **(1) Slice 1 — extractor recall, `n_cells` family (`9794b8c`):** `extract.golden.extract_dataset_size` lifts the analyzed dataset size on TIGHT result/QC anchors ("resulted in 56,865 cells after filtering"), **0-false-positive vs Harmony's 28 benchmark cell-counts** (precision-first E2). Closed end-to-end: backfill `n_cells→umap_scrna` (`engine.match`) + L1 read-back `readers._read_umap` (UMAP point count); drive test proves extract→grade. 4 hand ledgers **byte-identical** (30/30). `n_clusters`/correlation deferred (no clean signal in the 2 papers — measure-before-build). **(2) Slice 5 — accession recognizer Phase A (`8de466f`):** `extract/accessions.py` — deterministic, no-network recognizer over the availability statement → typed `Accession{repo, id, access: open|raw|controlled, ingestable, url, section, note}` (GEO/SRA/ENA/ArrayExpress/GSA/PRIDE/MetaboLights+MW/Zenodo/Figshare/Dryad/dbGaP/EGA); honest raw→"needs quantification" / controlled→"requires application"; precision traps handled (citation markers, ST-table refs). Wired into the diagnostic gap report (header line + Cited-datasets table). 10 tests over the real Yoshimura statement. **Phase B (fetch) GATED.** **Live re-run lift:** Yoshimura report went from blank → `goldens(exp): n_cells` + a 4-GEO Cited-datasets table, still 0 Selom defects. **Gates: full BE 722 passed; ruff clean.** **THE STRATEGIC INSIGHT:** two papers, one wall — both deposit accessions + a QC table, NOT matrices → the binding constraint for `driven` is **data-availability, not extractor recall**. **NEXT: Slice 2 (matcher honesty — stop force-feeding a QC table onto a matrix skill → `data_unmatched` + per-panel picker; flips auto_grade off 0%) + Slice 5 Phase B (GATED fetch of the open GEO data) — NOT more golden families.** [[selom-live-reproduction-drive]] [[selom-accession-ingest]] [[layered-deterministic-extraction]] [[selom-figure-repro-mission]] [[selom-bam-ingest]] [[ask-before-docker-wsl]]_ -->

<!-- prior live-state (session 50): _Last updated: 2026-06-22 01:41 +10:00 (session 50) · Claude (acting FE+BE) — **Next phase chosen + KICKED OFF: "Reproduction — dogfood-ready". Phase spec APPROVED + Slice 0 (cold-drive diagnostic) SHIPPED + run on the real Harmony paper. 3 commits (feat(backend:) `7939a48` + docs `c36efc6` + handoff `9044ab1`).** (0) Phase spec `docs/reproduction-dogfood/spec.md` (agile Slices 0–5, decisions D1–D6). (1) Slice 0 `app/backend/reproduction_diagnose.py`: projects a `DriveResult` → a gap report (per-panel status/reason + `auto_grade_rate`), pure projection, 0 Selom defects, no silent caps. (2) Ran COLD on Harmony → `findings-harmony.md`: 0/8 gradable, 0 goldens, all `run_failed`, yet 0 Selom defects. Causes: golden extractor knows DE/PCA only [Slice 1]; matcher forced lone xlsx onto every panel [Slice 2]; META: papers deposit code+`.rda`+GEO/GSA accessions [Slice 5]._ -->

<!-- prior live-state (session 49): _Last updated: 2026-06-22 01:03 +10:00 (session 49) · Claude (acting FE+BE) — **The single circle-back FE/UX pass (tasks 1 & 2 of the s48 NEXT list) SHIPPED + browser-verified vs the LIVE BE. 3 commits PUSHED 2026-06-22 (owner-authorized; origin ↔ main @ `9e2e8b8`; working tree clean; dev servers killed).** It consumes the s48 run contract (`data_check` + `figure_legend` + synthesized `table`). **(1) Product-A guided workbench (P1c/P3a/P3b):** `lib/skills-api` gained `DataCheck`/`QcFlag`/`DataRouting`/`FigureLegend` types, maps the run response's `figure_legend`→`legend` + `data_check`→`dataCheck`, adds an `override` run option, and parses the 422 `data_check_failed` into a typed `DataCheckError`. New `DataCheckPanel` — a **verdict** variant (modality chip + QC flags w/ fix hints + a clickable suggested pipeline) shown with a run, and a **blocked** variant (calm tone, fix-first, a deliberately-subordinate "Review & run anyway" override that re-runs with `override=true` — the user owns their data). `project-workspace` catches the block → block card, lets a suggested step install+preselect into the workbench, and persists `legend`+`dataCheck` on the `Figure`. **(2) L3 Statistics node + Methods/legend (table-synthesis §8 step 4 + workspace-library §11):** backend `POST /skills/{id}/run` now attaches `table = table or synthesize_table(skill_id, figure)` for a tableless skill (symmetric with the reproduction reader; `test_run_synthesized_table` — pca synthesizes, deg's native table untouched, go_graph stays L4-only); `StatsPanel` shows a "Computed by Selom" badge + a read-not-recompute explainer; `PublishConfidence` renders a paste-ready "Figure legend" section beside Methods (Copy + draft note). **Gates: BE test_run_synthesized_table (3) + test_run_guardrail (4) green; FE tsc clean · vitest 114 (+4 new skills-api.test) · eslint 0 errors (+1 advisory React-Compiler memoization note on a pre-existing run callback, behaviorally inert — same category as 14 already in the file). Browser-verified on :3001 vs the live BE: clean verdict + suggested steps (deg) · the 422 block card + override → figure (pca) · the synthesized "Computed by Selom" stats node (pca `[component, variance %]`) · the Methods + Figure-legend panel — screenshots `graphify-out/scratch/s49-*`.** **NEXT: push the 3 s49 commits (ASK); then optional small follow-ups — gate the async `…/jobs` path on QC too · a per-skill opt-in to consume `DataBundle.payload` (skip the re-load) · the Pro/AI legend-polish tier · make an own-data run a saved Library artifact.** [[selom-engine-spine-pillars]] [[selom-live-reproduction-drive]] [[layered-deterministic-extraction]] [[selom-reproducibility-score]] [[selom-uv-sync-footgun]] [[ask-before-docker-wsl]]_ -->

<!-- prior live-state (session 48): _Last updated: 2026-06-22 00:16 +10:00 (session 48) · Claude (acting FE+BE) — **TWO backend ships (COMMITTED, NOT yet pushed — ASK before push; origin/main @ `10b6005`, 3 commits ahead = 2 feat(backend:) + this handoff, working tree otherwise clean, no dev servers): items 1 & 2 of the s47 NEXT list = the Product-A engine front door. Skeleton-first, additive (E4), NO FE.** **(1) is-my-data-clean? guardrail + RAW-routing surfaced for own data (P1c/P3a):** `POST /skills/{id}/run` now ingests the upload through `engine.ingest` → `run_qc` → **blocks a `block`-severity problem unless `override=true`** (D-e5: warn + require-override, structured 422 carrying the QC flags + fix hints — never a silent misleading figure; the native moat for non-bioinformaticians), then runs from the same bundle. Every successful run also surfaces `data_check` = kind + QC verdict + the P3 routing pipeline. **Fail-soft:** an upload the engine can't load yields no bundle and runs the path-based way, so the proven path is never broken. **(2) runner takes a `DataBundle` (P1 §1b / E4):** new `skills.contract.run_bundle`/`run_bundle_with_table` = the engine-spine ANALYZE entry (runs from `DataBundle.path`, BYTE-IDENTICAL to the path runner); `engine.ingest` sets `DataBundle.path`; both products now load through `engine.ingest` → bundle → run end-to-end. Gates: test_run_guardrail (4) + test_run_bundle (6); reproduction set 116 BYTE-IDENTICAL; ruff clean._ -->


<!-- prior live-state (session 47): _Last updated: 2026-06-21 23:39 +10:00 (session 47) · Claude (acting FE+BE) — **TWO backend ships, both PUSHED (origin ↔ main in sync @ `61034b2`; clean tree, no dev servers): (1) the figure-legend layer (P4c, `959344d`) · (2) the `engine/match.py` JOIN/MATCH consolidation (P1 step 5, `61034b2`).** **(1) Figure-legend layer:** the methods half already shipped (lit-synth); this is the LEGEND half — new top-level `legends.py`, the per-skill caption sibling of `methods.build_body` (31 templates + an honest generic fallback), honest from resolved params alone and ENRICHED with the run's real DE up/down split when the figure/table is present — read through a new canonical `readers.de_counts` that `_read_de_table` now also routes through (one source of truth, no drift). Surfaced two ways mirroring methods: `figure_legend` on `POST /skills/{id}/run` (the own-data headline path) + per-panel `GET /papers/{slug}/legends` via `litsynth/legends_from_ledger.compose_ledger_legends` (the reproduction twin of `/papers/{slug}/methods`, numbering "Figure ⟨n⟩⟨panel⟩." from the ledger + enriching from each driven `ReproRun`). Additive (E4): figure goldens unchanged (they snapshot the figure, not the run response). New `test_legends` (24). **(2) `engine/match.py`:** lifted the spine's JOIN/MATCH stage out of reproduction-internal plumbing into the product-agnostic engine package (engine-spine §6/§9 step 5) — `merge_ledger` (routed skeleton + extracted goldens → drivable ledger) + `match_data` + `tabular_paths`; `reproduction_drive` now consumes them and keeps only the orchestration, re-exporting the names for back-compat. **Byte-identical — no recognizer semantics changed; no import cycle (`engine` never imports reproduction; `match` not in `engine/__init__`, so `import engine` stays light).** Remaining increment (deferred, E4): make the runner take a `DataBundle` so `reproduce()` + Product A load through `engine.ingest` end-to-end. **Gates: test_legends 24 + readers · golden/methods/litsynth/synthesize 132 · drive + drive_honest + 4 ledgers (RPGRIP1/JEV/Hani/Dorgau) + runs + sweep = 59 UNCHANGED · score/guards/tolerance/papers_api/citations/contract/provenance 75 · ruff clean.** **NEXT (skeleton-first, NO FE yet):** P1c/P3a is-my-data-clean? guardrail + RAW-data routing (the Product-A path / native moat) · P1 §1b unify paper-supplement + own-data ingest onto `DataBundle` · THEN the single circle-back FE/UX pass across all pillars (incl. the L3 FE Statistics-node wiring §8 step 4 + the Methods/legend FE panel). [[selom-engine-spine-pillars]] [[selom-lit-synthesizer]] [[layered-deterministic-extraction]] [[selom-reproducibility-score]] [[selom-uv-sync-footgun]] [[ask-before-docker-wsl]]_ -->

<!-- prior live-state (session 46): _Last updated: 2026-06-22 00:34 +10:00 (session 46) · Claude (acting FE+BE) — **TWO backend ships, both PUSHED (origin ↔ main in sync — s46 commits `424ea54` + `359c7f2` + this handoff; clean tree, no dev servers): (1) `proteomics_de` native `de_table` — the LAST at-source L3 gap (table-synthesis §8 step 5); (2) the metric-type-aware tolerance grader (P5a).** **(1) `proteomics_de` de_table (`424ea54`):** it computed real per-protein logFC+padj but attached no Statistics table (the figure title's tested-protein count was easily mistaken for a DE total) — it now attaches the canonical `de_table` the volcano is drawn from, restricted to the tested/kept proteins, in BOTH the real engine and the stub, exactly as `volcano` does. Registered `proteomics_de` to the shared `de_table` L1 reader (renamed `_read_volcano`→`_read_de_table`) so `de_total` reads as up+down, NOT the tested-protein row count the L2 generic reader would return. The only L4-only skills are now the node-link trio (`go_graph`/`pathway`/`string_network`). **(2) Metric-type tolerance grader (`359c7f2`):** the band a golden is graded at now follows its metric TYPE — exact counts/ID-sets stay strict, engine-sensitive families (GSEA term counts gseapy↔fgsea · integration mixing Melody↔Harmony) get a wide band so a *measured* engine-delta isn't mislabelled irreproducible. `metric_type` on `Golden` supplies the DEFAULT band (`METRIC_TYPE_TOLERANCES` + `infer_metric_type`/`resolve_tolerances`); an explicit per-golden tolerance still WINS (type=default, explicit=override). `validate_panel` grades via `resolve_tolerances(gold)`; the drive's auto-goldens (`extract.golden.to_golden`) stamp the inferred type (today's DE counts → strict `de_count`, a tolerance no-op, but a future GSEA/integration extraction is engine-sensitive for free). **Non-regressing by construction: an untyped golden resolves to its own fields verbatim, so the 4 hand-ledgers grade byte-identically.** **Gates: new test_proteomics_de + test_readers (24) + test_reproduction_tolerance (12); golden/registry/route/synthesize (101); reproduction/score/guards (57); lighter engine set (46); drive + drive_honest + 4 ledgers (RPGRIP1/JEV/Hani/Dorgau) UNCHANGED across both regression runs (46 each); ruff clean; proteomics_de figure golden unchanged (table popped, D7).** **NEXT (skeleton-first, NO FE yet):** P4 methods/figure-legend layer wiring (~70% on lit-synth) · P1 step-5 consolidation (fold reproduction onto engine/ingest + a new `engine/match.py`, re-verify the 4 ledgers) · is-my-data-clean? + RAW-data routing — THEN the single circle-back FE/UX pass across all pillars (incl. the L3 FE Statistics-node wiring, §8 step 4). [[selom-live-reproduction-drive]] [[selom-engine-spine-pillars]] [[layered-deterministic-extraction]] [[selom-gsea-engine-sensitivity]] [[selom-reproducibility-score]] [[selom-uv-sync-footgun]] [[ask-before-docker-wsl]]_ -->

<!-- prior live-state (session 45): _Last updated: 2026-06-21 22:24 +10:00 (session 45) · Claude (acting FE+BE) — **P2 / L3 table synthesis — the Tier-B LOSSY set SHIPPED, completing the full synthesizer set (16 skills). All PUSHED (origin ↔ main @ `78eede1`, clean tree).** Added `boxplot`/`heatmap`/`scorecard`/`violin` synthesizers to `extract/synthesize.py` (`72410b2`), each behind a **faithfulness gate** → `None` on a non-conforming shape. All four had a faithful deterministic table, so **none was punted to L4** (only the node-link trio `go_graph`/`pathway`/`string_network` stays L4-only): `boxplot` → `[group,n,min,q1,median,q3,max]` (the five-number summary the box encodes — n/min/median/max exact, q1/q3 use Plotly's default *linear* quartile method via pure-Python `_percentile`, so the table == the drawn box; reads y/x); `heatmap` → wide `[gene,<group…> z]` (the row-z-scores drawn; new shared `_heatmap_grid` gate, dendrogram trace ignored; refactored the shipped `_corr_heatmap` onto it); `scorecard` → long `[condition,metric,score]` from either layout (radar `scatterpolar` closing-point-dropped · or the heatmap grid); `violin` → `[gene,pubmed hits,verdict]` from the `annotate=pubmed` annotation, else `None` (un-annotated distributions not tabulated — S4). Registered in `_SYNTHESIZERS` → **auto-extends the L1/L2 reader fallback** (no reader edit). S1–S5 honoured; verified on each skill's live stub. **Gates: test_synthesize 28 (+10) + test_readers green · drive + drive_honest + the 4 ledgers (RPGRIP1/JEV/Hani/Dorgau) UNCHANGED (46 passed, 6m34s) · ruff clean.** **No dev servers.** **NEXT (skeleton-first, NO FE yet):** cross-lane FE Statistics-node wiring + `proteomics_de` native `de_table` (§8 steps 4–5) · P5 metric-type-aware tolerance grader · P4 methods/legend wiring · P1 step-5 consolidation — THEN the single circle-back FE/UX pass across all pillars. [[selom-live-reproduction-drive]] [[selom-engine-spine-pillars]] [[layered-deterministic-extraction]] [[selom-figure-repro-mission]] [[selom-uv-sync-footgun]] [[ask-before-docker-wsl]]_ -->

<!-- prior live-state (session 44): _Last updated: 2026-06-21 21:49 +10:00 (session 44) · Claude (acting FE+BE) — **P2 Tier-B clean trio SHIPPED + the CURRENT.md weekly archive. Local commits UNPUSHED — ASK before push (origin ↔ main @ `43a3052`).** **(1) P2 / L3 table synthesis — Tier B (clean trio) DONE (`fe079be`):** added `corr_heatmap`/`sankey`/`upset` synthesizers to `extract/synthesize.py`, each behind a **faithfulness gate** → `None` on a non-conforming shape — corr_heatmap → long-form `[row,col,r]` (upper-triangle when square-symmetric); sankey → `[source,target,value]` resolving link indices→labels (out-of-range → None); upset → `[intersection,sets,size]` from the size bars + member sets read back off the “present” dots. Registering the three in `_SYNTHESIZERS` **auto-extends `extract/readers.py`** (it already falls back to `synthesize_table` when a skill emits no native table) — no reader edit. Read-only/recomputes-nothing (S1); tagged `synthesized:True` (S3); score-eligible (S2); never forced → L4 (S4). Each verified on its skill’s own live stub figure. **Gates: test_synthesize + test_readers 36 · reproduction drive 16 · the 3 skills’ golden 6 · ruff clean.** The **lossy** Tier-B set (scorecard/boxplot/violin/heatmap) → a gated follow-up or L4. **(2) Hygiene:** CURRENT.md weekly-archived (s40↓) → `agent_handoff/archive/2026-06-21-current-history.md`. **NEXT (skeleton-first, NO FE yet):** the **lossy** Tier-B synthesizers (each gated or → L4) · cross-lane FE Statistics-node wiring + `proteomics_de` native `de_table` · P5 tolerance grader · P4 methods/legend wiring · P1 step-5 consolidation. -->


<!-- prior live-state (session 43): _Last updated: 2026-06-21 20:12 +10:00 (session 43) · Claude (acting FE+BE) — **RE-PLAN + engine-spine SKELETON build. Owner reset the plan: organize ALL work around ONE shared engine spine; defer ALL frontend until every pillar's BACKEND skeleton exists, then circle back for a single FE/UX pass. 9 commits LOCAL, UNPUSHED — ASK before push (origin ↔ main still @ `991e0cc`).** **(0) Re-plan (`a670383`):** `docs/pillars/plan.md` (6 pillars — P1 Ingest · P2 Join/Synthesis · P3 Routing · P4 Analysis+Output = the shared SPINE; P5 Reproduction = spine+grading; P6 parking lot), `docs/engine-spine/spec.md` (keystone; owner-approved D-e1..5: wrap-don't-coerce · new `engine/` package · strangler · 7 Kinds · QC warn+override), `docs/on-hold/README.md` (parking-lot registry incl. the ClawBio/bioSkills/OmicVerse audit strategy — native picks carved out). Both products SHARE the spine: Product A "my own data" = spine−grade (was under-built); Product B "reproduce a paper" = spine+grade. **(1) P1 — Layered Ingest & Clean (4 commits `52f60f6`/`a48d8cb`/`7cf817a`/`c8a9f03`):** new `app/backend/engine/` — `models.py` (Kind taxonomy + SourceRef/Design/QCFlag/QCReport pydantic) · `databundle.py` (DataBundle wraps the live AnnData/DataFrame payload + layered `classify()`) · `ingest.py` (registry .h5ad/10x/.xlsx/.csv/.tsv → classified DataBundle, provenance-not-bytes) · `qc.py` (`run_qc` honest modality-aware flags: non-integer/negative counts→block, proteomics all-missing/high-miss→warn+MNAR hint, de p-range, sc normalized/empty/mito>20%→warn) · `POST /data/inspect` (file → kind + QC + routing = Product A's front door). 31 tests; a test caught a real classifier bug. **(2) P3 — RAW-data router (`86e81db`):** `engine/route.py` `route_data(bundle)` → Kind→registry-validated skill pipeline (no fabricated steps; metabolomics=coming-soon honest); wired into /data/inspect. 7 tests. **(3) P2 — L3 table synthesis SIGNED OFF + Tier-A part 1 (`39b44b3`/docs `c98e3ea`):** owner accepted spec defaults (synthesized MAY feed score, tagged). `extract/synthesize.py` `synthesize_table(skill_id, figure)` re-shapes a tableless skill's OWN figure→canonical table (reads-not-recomputes; None→L4). 7/9 Tier-A built (pca/composition/cluster/pvca/regression/integration/trajectory) from the `skill-table-schemas.md` shapes; 9 tests. **Gates: all new BE tests green (47 new) + regression smoke 56, ruff clean, zero regression (net-new engine pkg; main.py only ADDED /data/inspect + a QCReport computed_field). No dev servers started.** **NEXT (skeleton-first, NO FE yet): finish P2 = the 2 trace-length synthesizers (`umap_scrna`/`annotate`, verify on a live panel) + READER INTEGRATION (build step 2: `extract/readers.py` tries `synthesize_table` when native table is None, tagged lower-confidence) + Tier B; then P4/P5 skeleton gaps (methods/legend wiring; metric-type-aware tolerance grader); THEN the single circle-back FE/UX pass across all pillars. P1 step 5 (fold reproduction onto the registry + `engine/match.py`, re-verify the 4 ledgers) = deferred consolidation. ASK before push (9 commits) + before any Redis/arq/Docker.** [[layered-deterministic-extraction]] [[selom-figure-repro-mission]] [[selom-reproducibility-score]] [[compound-capability-each-task]] [[selom-uv-sync-footgun]] [[ask-before-docker-wsl]]_ -->

<!-- prior live-state (session 42): _Last updated: 2026-06-21 04:58 +10:00 (session 42) · Claude (acting FE+BE) — **THREE ships, all PUSHED (origin ↔ main @ `991e0cc`, clean tree): (1) Live-reproduction Phase 3 FE wiring · (2) the external skill AUDIT · (3) proteomics_de MNAR hardening.** **(1) Live-reproduction drive (umbrella step c) — Phase 3 FE WIRING SHIPPED + browser-verified e2e + PUSHED. The umbrella is now usable end-to-end: drop a paper → attach supplements → Run reproduction → graded two-axis Score, wired against the s41 backend.** Committed `2bb37a8` (14 FE files: 5 new + 9 modified). **NEW:** `lib/paper/run-files.ts` (session File cache = the I5 bytes home; `paperFiles` + `usePaperFiles`, non-persistent, dedup on lowercased filename) · `lib/reproduction/run.ts` (run client `startReproduction`/`fetchRun` w/ the house detail-error convention + `usePaperRun` controller [POST→stamp `reproductionRunId`→navigate to `?stage=score`; inline-terminal + forward-compat poll] + `useReproductionRun` Score loader) · `components/reproduction/score-report.tsx` (the graded body EXTRACTED from `paper-detail.tsx` so the showcase detail AND the live Score stage render byte-identically). **MODIFIED:** `lib/workspace/types.ts` (reserved field → `reproductionRunId`) · `store.ts` (`setPaperReproductionRun`) · `paper-detail.tsx` (renders `<ScoreReport>`) · `panel-table.tsx` (additive "Computed — not auto-validated" section + `#panel-` anchors so in-scope-but-ungraded live cells have a home+scroll target; empty for the showcase) · `score-stage.tsx` (live states loading / succeeded→ScoreReport / expired-or-failed→honest banner / no-run→the s40 ghost) · `reproduce-stage.tsx` (captures supplement bytes into `paperFiles` + "The paper" attach/re-attach row + per-supplement Ready vs Re-attach + run readiness/progress/error card w/ the I5 note) · `paper-shell.tsx` (one `usePaperRun(id)` drives the pipeline "Run reproduction" forward button, enabled when main+≥1 supplement bytes present) · `skill-match.tsx` (stash the dropped PDF File → `paperFiles.setMain` on the Reproduce handoff so the main bytes carry without a re-drop). Tests: `run-files.test.ts`, `run.test.ts` (stubbed fetch; next/navigation mocked), store-test addition. **Gates: tsc clean · vitest 110 (+11) · eslint 0 err / 0 warn.** Browser-verified on :3001 vs the live BE (real Hani PDF + its mmc2.csv): Skill Match metadata-enrich (PMID 36630901/DOI) + 14 skills → Reproduce carries the PDF ("Attached for this run") → supplement "Ready" → Run reproduction (inline ~2.5s) → auto-nav to Score → real driven ledger via ScoreReport (two-axis —/100 · Out of scope, 0 scored / 5 in-scope · 5 form-only, 0 Selom defects, heatmap 5 grey cells) → honest `run_failed` (Hani's marker matrix ≠ the raw data those scRNA skills need) stays grey, never a defect → I5 reload clears bytes → "Re-attach the paper PDF" + Run disabled. Screenshot `graphify-out/scratch/phase3-score-verify.png`. **PUSHED** (origin ↔ main was @ `cd1c64d`; this push carries the 2 s41 BE commits `32f2632`/`793a4b6` + Phase 3 `2bb37a8` + this docs commit). **(2) External skill AUDIT SHIPPED + PUSHED** (`420fcc0`) → `docs/skill-audit/external-skill-audit.md`: 8 sub-agents over the real ~650-skill universe — **bioSkills 547** (MIT method GUIDES, not runnable) + **ClawBio 88** (MIT runnable) + **OmicVerse** 25 modules (**GPL-3.0** = in-process SaaS blocker → arms-length worker/clean-room only). **OWNER STEER #1: don't compete on breadth → COLLABORATE** (Selom = the no-code IDE / editable-figure frontend / repro-scoring / deploy layer for partners' packages). **OWNER STEER #2 (PRIMARY product goal): a NON-bioinformatician's OWN data → Selom cleans+guides → the output they want** (figure-repro = the proving ground; this = the daily product) → the cleaning/QC + guided-routing layer is **native MOAT**, not "platform." ~60% of the universe = off-thesis SKIP. **(3) `proteomics_de` MNAR hardening SHIPPED + PUSHED** (`991e0cc`, the audit's #1 native pick): the moderated empirical-Bayes test (`stats=moderated`, Smyth 2004) was ALREADY shipped, so I closed the genuine remaining gap — **left-censored missing-value imputation** (mean-impute biased FCs toward zero; MNAR = below detection limit). New `missing` param: `mean` (default, byte-identical) · `mindet` (per-sample 1% detection-tail) · `minprob` (seeded downshifted-normal, Perseus). v0.1.0→v0.2.0; proteomics_de 7/7, golden+registry 63 (default unchanged), ruff clean. **NEXT (owner picks — audit-aligned, own-data-serving lead):** (1) ★ `metabolomics_de` skill (audit #2 native pick, pure whitespace: Pareto/UV → OPLS-DA + VIP + volcano + mummichog); (2) ★ "is-my-data-clean?" guardrail pass + extend the skill-keyword router to RAW data (the cleaning/guidance moat that earns the "for non-bioinformaticians" claim); (3) metric-type-aware tolerance grader (engine-delta ≠ irreproducible in `validate_panel`); (4) HOST a collaboration slice — ClawBio affinity-proteomics (Olink/SomaScan, MIT, shovel-ready) [ASK before worker/Docker]; (5) L3 table-synthesis (awaiting sign-off, 4 decisions); (6) umbrella remainder (a) pdf.js region-capture, (d) Methods/legend layer. ASK before any Redis/arq/Docker. [[selom-figure-repro-mission]] [[selom-clawbio-bioskills-shortlist]] [[layered-deterministic-extraction]] [[selom-reproducibility-score]] [[selom-command-center-architecture]] [[compound-capability-each-task]]_ -->

<!-- prior live-state (session 41): _Last updated: 2026-06-21 02:50 +10:00 (session 41) · Claude (acting FE+BE) — **Live-reproduction drive (umbrella step c) — BACKEND COMPLETE (Phases 1+2+4), live-smoked on the real JEV PDF, all green; + the proprietary table-synthesis (L3) spec written for sign-off. 5 commits LOCAL (incl. the still-unpushed s40 `9721077`) — ASK before push.** Owner steered mid-session: inventory EVERY skill's Statistics-table shape via subagents → use a LAYERED extractor (L1 skill-specific / L2 generic / L3 proprietary synthesis / L4 Pro AI), chose **"reader now, spec L3 next"**. Delivered: **(1)** fan-out inventory of all ~30 skills (5 parallel subagents) + my own grep-verification → `docs/reproduction-engine/skill-table-schemas.md` (`4eeea42`): **only 10 skills emit a Statistics table** (volcano/deg/diff_abundance/markers/normalization_qc/pseudotime_genes/gsea/ssgsea/enrichment/cepo); the rest carry numbers in figure title/subtitle/axis/annotation strings or trace arrays — so the extractor must be **multi-source** (table→figure-string regex→trace). Key findings: **volcano is the only clean DE-count source** (de_table `direction` col; deg can't — no padj/direction), and **proteomics_de computes DE stats but attaches no table** (Foundry gap). **(2)** `extract/readers.py` (`657c8f8`) — the layered reader: **L1** skill-specific (volcano DE-count tallies+title-total; pca axis-title variance) + **L2** generic (directional row counts, named-cell key→value, figure-string %). A metric no layer reads → `None` → drive marks `needs_recipe` (reproducibility-axis only, **0 Selom-confidence defects**). 12 unit tests over the real output shapes. **(3)** `reproduction_drive.py` (`1535f55`) — the orchestration `reproduce()`/`drive_bundle()`: ingest → **MERGE** routed figure→skill skeleton + extracted printed-number goldens into one drivable Ledger (gap #1; backfills skill from the golden metric — DE→volcano, var→pca) → match a data file per panel (gap #2; per-panel `data_map` override + honest `data_unmatched`) → run the matched skill (INJECTABLE `runner=` so it's unit-testable stack-free) + read the golden back → grade. **Honest classification (L2/L4):** out_of_scope/no_golden/data_unmatched/needs_recipe/run_failed are greyed, excluded, **0 defects**; every panel gets a heatmap cell (no silent caps). 12 tests; **full BE subset green, no regression** (routing/golden/engine/JEV 123 passed). **(4)** `docs/table-synthesis/spec.md` (`2cc77a8`) — the L3 moat: synthesize a canonical table for tableless skills from their OWN output (read-only, recompute-nothing; synthesized≠digitized so it MAY feed the score; don't force node-link/raw-dist skills → L4 AI). Registry (Tier A clean / B lossy / L4) + 8 invariants + 4 sign-off decisions + phased plan. **NOT built — awaiting owner sign-off.** **(5) Phase 2 endpoints SHIPPED + live-smoked** (`cc9e538` + override fix `866fa98`): `reproduction_runs.py` (FastAPI-free run store + lifecycle, inline drive, `drive_fn` injectable) + `POST /papers/{id}/reproduce` (multipart main+supplements[]+design → per-run temp dir + configurable size cap [413] + cleanup → `start_run`) + `GET /reproduction-runs/{id}` (driven Ledger+scorecard, GET-/papers-shape so the Score stage reuses showcase components) + SSE `.../events` (light ticks). `config.max_upload_mb` (50, env). 8 tests. **Live-smoked end-to-end on the REAL JEV PDF**: succeeded, 8 panels, honest 2 `data_unmatched`+6 `no_golden`, **0 Selom defects**, 18-skill inventory rode along; smoke caught + fixed a real bug — DE figures mis-routed to `cluster`, so the drive now OVERRIDES the route to `volcano` for DE-count goldens (only volcano's de_table yields counts). **Owner authorised the 6 s41-floor commits PUSHED (origin @ `4712953`); +2 NEW local (Phase 2 + override) — ASK before push.** **(6) Phase 4 honest-edge hardening SHIPPED** (`32f2632`): `tests/test_reproduction_drive_honest.py` drives deliberately-irreproducible inputs END-TO-END + locks the two-axis guard — (1) figure prints a number its data can't reproduce (JEV Fig4e shape) → reproducibility 50, NOT discrepant, 0 Selom bugs; (2) data unattached → all grey, 0 scored, 0 defects, no silent drops; (3) structural limit → reproducibility 38 but selom_confidence 100 (textbook two-axis split); (4) needs_recipe never invents a FAIL. `selom_engine_bugs == 0` across all. **NEXT: Phase 3 FE wiring (the last piece to make it usable e2e): enable the s40 "Run reproduction →" trigger → session File bytes (PDF re-read from the dropped File + the attached supplements) → `POST /papers/{id}/reproduce` → SSE progress on the Reproduce stage → on `succeeded` stamp `SavedPaper.reproductionRunId` + switch to the Score stage, which GETs `/reproduction-runs/{run_id}` and fills the skeleton (zero reflow per U6); honest banner if a reload lost the bytes [I5]. Use the design skills + browser-verify on :3001 vs the live BE. Then build L3 once the spec is signed off (4 decisions in `docs/table-synthesis/spec.md`).** ASK before any Redis/arq/Docker. **+2 local commits (Phase 4 `32f2632` + this) unpushed — ASK.** [[selom-figure-repro-mission]] [[layered-deterministic-extraction]] [[selom-reproducibility-score]] [[selom-proprietary-skills-folder]] [[compound-capability-each-task]] [[ask-before-docker-wsl]]_ -->

<!-- prior live-state (sessions 40 ↓ B4) archived 2026-06-21 → `agent_handoff/archive/2026-06-21-current-history.md` (full text also in git log). -->

_Sessions 8 and earlier → `agent_handoff/archive/2026-06-17-pre-session9-history.md`._

**Model:** **Opus 4.8 (xhigh effort)** for all Selom work — owner-directed 2026-06-13,
pinned in `.claude/settings.json` (`model=claude-opus-4-8`, `effortLevel=xhigh`); applies on
session restart. Supersedes the prior Fable-5 default.

## Resume Prompt (next session)

> Stamped 2026-06-23 01:00 +10:00. Paste at session start; re-stamp date+time before reuse.

```
Resume · 2026-06-23 01:00 +10:00 (Australia/Sydney) · Selom · Claude (FE+BE, acting) · Opus 4.8 (xhigh)

MODE: TANGENT PROJECT continues — the ERG figure module for the owner's Eamegdool PDE6B *Gene Therapy* revision (real work). Do NOT resume the Selom pillars (complete + parked). The skills-agnostic figure-editor restructure is the active build. P1 + P2 are DONE; **P2.5 (skill-dependent param richness — owner-requested this session) is NEXT**, then P3/P4.

STATUS: P1 (fdbbbb4) + P2 (3d22fa4) are COMMITTED LOCAL, NOT pushed — ASK before push (origin ↔ main @ 0fbe096). Both verified LIVE on the real ERG figure; tree otherwise clean; no dev servers. Start building P3 directly.

PLAN (owner-APPROVED 2026-06-22, build now): docs/figure-editor-contract/spec.md — a skills-AGNOSTIC figure editor + skill↔pipeline wiring. Phases:
- P1 ✅ DONE (fdbbbb4) Adaptive cosmetic editor — lib/figure-model.ts inference + adaptive Style/Data panels + line.color fix + 42→6 series grouping + normalizeSpec no longer injects marker on non-marker traces. (click-a-line→recolour deferred to P3; seriesForTrace already exported.)
- P2 ✅ DONE (3d22fa4) "Figure data" pipeline box — components/project/figure-data-panel.tsx (param_spec → Re-run → new version) + relocated data-check/data-fit + figure-forward; workrail figuredata node + project-workspace routing. (Kept pipeline.tsx's home 4-step teaching flow unchanged — the stage lives in the rail.)
- P2.5 ⬅ NEXT — skill-DEPENDENT param richness (owner-requested + design agreed this session). The Figure-data panel is the agnostic SHELL; the knobs inside are PER-SKILL (each skill's param_spec). Today the scRNA skills declare too few. See the dedicated P2.5 block below.
- P3 primitives (scale-bar show/hide/resize as a tagged primitive; threshold/annotation editing; click-a-line→recolour) + trace-grid emits legendgroup + layout.meta.selom hints (backend, opt-in).
- P4 design-skill polish: /impeccable audit + ui-ux-pro-max on the new surfaces.
Owner §9 decisions: inference-first ✔ · add legendgroup/meta to trace-grid ✔ · scale-bar skill-emitted + editor-managed ✔ · build P1→P2→(P2.5)→P3 ✔.

DONE this session (T3 — 2 commits LOCAL, unpushed; no dev servers):
- P1 (fdbbbb4): lib/figure-model.ts inference engine + adaptive Style/Data panels + normalizeSpec kind-aware fix + 15 figure-model.test.ts. LIVE on real ERG: Lines section (not Markers), Data SERIES·6, click-recolour wrote line.color to all 7 panels of the Untreated condition.
- P2 (3d22fa4): figure-data-panel.tsx ("Figure data" stage: param_spec → Re-run → new version; data-check/data-fit relocated here) + param-control.tsx (extracted, shared) + workrail figuredata node + project-workspace routing/handler/toolbar + figure-forward. LIVE: scale-bar 200→400 µV re-run → new "edited inputs" version at 400 µV, original kept.
- STILL OPEN: export a publication figure for the revision; PARKED display-smoothing decision — the erg_traces notch+300Hz leaves flats noisier than the team's render_preview.py 16ms moving-average; owner's call whether to switch the default.

NEXT — P2.5 (skill-DEPENDENT param richness). WHY (owner steer this session): the Figure-data stage's params must be specific to the skill/data type — scRNA shows QC filters + integration (Melody) knobs; ERG shows filter/scale (already auto). The panel + ParamControl are ALREADY the agnostic shell that renders "whatever the active skill declares" — the gap is the SCHEMAS declare too little, NOT the rendering. Build, FE-first:
  (a) `showWhen` CONDITIONAL fields on ParamField (lib/catalog/params.ts) + ParamControl (components/project/param-control.tsx) — a `method` SELECT reveals method-specific knobs (e.g. integration: "Melody" | "Melody — harmony2 mode" → its α / iterations). Because ParamControl is SHARED (P2 extracted it), this lights up BOTH the Workbench (add/run a skill) AND the Figure-data stage at once.
  (b) ENRICH the per-skill schemas: scRNA (umap_scrna / cluster — QC mins + normalize + resolution) + a NEW `integration` entry (method + params). ⚠ LICENSE/LANE: Melody is the SHIPPABLE clean-room engine; Harmony is GPL + OFF the shipped path ([[license-decision-framework]]) → expose "Melody / Melody-harmony2 mode", NEVER literal Harmony. Defining skill params crosses into the BACKEND lane — the source of truth is the real runner (skills/integration/melody.py — harmony2 mode α=0.2); coordinate before hardcoding names/knobs.
  (c) (LATER, the true end-state) serve param_spec from the backend GET /skills/{id} SkillSpec instead of the hardcoded lib/catalog/params.ts → every skill (incl. future/third-party) gets correct knobs in both surfaces with ZERO FE edits (the registry-driven goal in [[selom-figure-editor-architecture]]). Its own slice.
  DECISIONS agreed this session: figure = ONE terminal skill → a RICH single card now (the terminal skill's param_spec carries all its data-shaping knobs). The genuine multi-step editable pipeline (a card per upstream step: QC→normalize→integrate→cluster→embed) is DEFERRED until a figure's provenance carries the whole pipeline (a backend contract change; the panel is already structured to render N cards). Optionally also surface the dataset's EXISTING cleaning steps (Data tab) as the "data" half of "data & skills related".
  BUILD ORDER: P2.5 (a)+(b) FE-first (verify LIVE: a scRNA figure's Figure-data stage shows QC/integration/Melody knobs + method-conditional reveal) → P3 → P4. (c) = a later backend slice.

THEN — P3 (primitives + hints): scale-bar show/hide/resize as a tagged primitive (layout.meta.selom.primitives) · threshold/annotation list+edit · click-a-line→recolour in figure-canvas.tsx (seriesForTrace already exported from lib/figure-model.ts) · trace-grid BACKEND opt-in: _tracegrid/erg_traces emit legendgroup (=condition) + layout.meta.selom (render-inert hints). THEN P4 design polish (/impeccable audit + ui-ux-pro-max on the new surfaces). Verify LIVE on the real ERG CSV each phase.

READ FIRST: agent_handoff/README.md + CURRENT.md + docs/figure-editor-contract/spec.md (THE plan) + docs/erg-module/spec.md + app/frontend/components/figure/{editor-workspace,figure-canvas}.tsx + app/frontend/components/figure/panels/{style,data}-panel.tsx + app/frontend/lib/figure-spec.ts (normalizeSpec marker injection) + app/frontend/components/{pipeline,project/workrail,project/project-workspace}.tsx + app/frontend/lib/catalog/{seed,params,modality}.ts + app/frontend/lib/figure-model.ts (the P1 inference engine) + app/frontend/components/figure/panels/{style,data}-panel.tsx + app/frontend/components/figure/figure-canvas.tsx (P3 click-to-select goes here) + app/frontend/components/project/{figure-data-panel,param-control,workrail,project-workspace}.tsx (P2) + app/frontend/components/project/param-control.tsx + lib/catalog/params.ts (P2.5 schema + showWhen go here) + app/backend/skills/integration/melody.py (P2.5 integration knobs = source of truth) + app/backend/skills/{_erg,_tracegrid}.py + proprietary/erg_traces (P3 backend hints) + memory ([[selom-erg-module]], [[selom-figure-editor-architecture]], [[license-decision-framework]], [[selom-harmony-reimplementation]], [[selom-backend-python-exec]], [[claude-covers-both-selom-lanes]], [[kill-dev-servers-on-handoff]]) + git log (2 local unpushed commits: fdbbbb4 P1, 3d22fa4 P2).

ENV: BE = the uv 3.12 PY + PYTHONPATH (NOT uv run — the .venv stub is EDR-blocked): PY=C:\Users\seamegdool\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\python.exe. Run the skill engine LIVE: from app/backend, PYTHONPATH="D:\selom\app\backend;D:\selom\app\backend\.venv\Lib\site-packages" SELOM_SKILLS_ENGINE=real & $PY -m uvicorn main:app --port 8000 --host 127.0.0.1 (NO --reload; restart after a backend edit). Tests: & $PY -m pytest tests/test_skills_golden.py -k erg_traces tests/test_skill_table_contract.py -q. Regen golden after an intentional stub-output change: SELOM_SKILLS_ENGINE=stub & $PY tests/regen_golden.py. FE: npm run dev in app/frontend (:3000; /api→:8000 via next.config rewrite; plain `npm run dev` = real backend, dev:mock = MSW offline). Real ERG data: D:\selom-data\erg-fig1e\erg_waveforms_long.csv — the chrome-devtools upload_file is workspace-root-gated, so a gitignored copy lives at graphify-out/scratch/erg_waveforms_long.csv (upload THAT). After a page reload the in-memory File is lost → re-upload before re-running. Scratch screenshots: graphify-out/scratch/erg_*.png.

ASK before: any Redis/arq/Docker/WSL. Use the design skills for FE work (/impeccable, ui-ux-pro-max, frontend-design) per CLAUDE.md. Kill every dev server you start before handoff.
```

## Active Status

| Agent | Role | Lane | Status |
|---|---|---|---|
| Claude | **Frontend + Backend (acting)** — UI/design/product copy **and** APIs/skill runners/data/tests | `app/frontend` + `app/backend` + both plans | IDLE (clear-safe) — **SESSION 55: Slice 4 (auto-drive regression fixtures) SHIPPED — this CLOSES the "Reproduction — dogfood-ready" phase (Slice 4 was the only open slice). Pure BE + docs, NO FE. Local commits PROPOSED, NOT pushed — ASK before push; origin ↔ main @ `7efdab2`; tree = the new BE files + this docs; no dev servers (Eamos :3001 untouched).** New `reproduction_fixtures.py` = the reusable framework (`DriveSnapshot` + `snapshot()` projects a `DriveResult` → per-panel status-shape + driven metric values; `diff()`/`assert_reproduces()` = the regression contract: same panel set · same status per panel · 0 Selom defects · every driven metric within its golden's `resolve_tolerances` band — the exact s46 grader, so a tolerated engine delta stays green + a broken reader flips red) + `scripts/regen_reproduction_fixtures.py` (bless/refresh generator + shared `FIXTURE_PAPERS` registry) + committed `tests/fixtures/reproduction/{jev,hani,dorgau}.json` + README + `tests/test_reproduction_fixtures.py` (3 tiers). **MEASURED:** cold-driving JEV/Hani/Dorgau drives **0 panels** (all `no_golden`/`data_unmatched`/`run_failed` — the Slice-0/1 data-availability+recall insight on the hand-ledger papers; **0 Selom defects**) → the win locked today is the **honest classification shape**; metrics auto-populate as recall rises. Tier a (cheap CI, no data) = framework unit tests on a synthetic drive (status flip→red · within-band→green · out-of-band→red) + characterization over each snapshot; tier b (opt-in, owner machine) = real `reproduce()` re-drive asserting the snapshot, verified green here on all 3. **4 hand ledgers untouched/byte-identical**; RPGRIP1 = a documented `FIXTURE_PAPERS` slot (no PDF here). **Gates: fixtures 15 + 1 skip · 4 ledgers+drive+honest 63 + 1 skip (no regression) · ruff clean · 780 collect clean.** **NEXT: dogfood phase CLOSED; the owner's ~2–3-session TANGENT PROJECT runs next (wait for the owner to open it). Before the ⚑ pillars-complete flag, 2 LIGHT post-phase items remain — P4-4a contract audit + the P3 routing-confidence surface.** [[selom-live-reproduction-drive]] [[selom-reproducibility-score]] [[layered-deterministic-extraction]] [[the-ratchet-durable-artifacts]] [[selom-backend-python-exec]] _Prior — s54: Slice 5B (deposit-data handoff) + Slice 3 (skill-gap signal) SHIPPED + PUSHED (`6659092`/`3c11f5c`/`7efdab2`). s53: Slice 2 FINISHED — per-panel data PICKER + Product-A data-fit band, SHIPPED + PUSHED (`fad4a62`/`b77ebd0`/`baefbae`). s52: Slice 2 engine `engine/compat.py` + matcher honesty + before/after panel, PUSHED (`932010a`/`36bf560`/`3029006`). s51: Slice 0 (2nd baseline Yoshimura) + Slice 1 n_cells (`9794b8c`) + Slice 5 accessions (`8de466f`). s50: phase spec + Slice 0 diagnostic. s49: circle-back FE/UX — Product-A workbench + L3 Statistics node. s48: is-my-data-clean guardrail. s47: figure-legend layer + `engine/match.py`. Older (s46 ↓ s13) in git log + archive → `agent_handoff/archive/2026-06-21-current-history.md`._ |
| Codex | Backend (APIs/skill runners/data/tests) | `app/backend` + `plans/v2-backend.md` | **AWAY** (busy on another project). Backend role **temporarily covered by Claude** as of 2026-06-11 22:10. B1 + **B2 backend done by Claude** in the interim (see `## Codex` section). May reclaim at any time — state is drop-in-ready; review the arq #6 lock + the B1/B2 commits on return. |

Roles are explicit; any swap is written here before work proceeds.

> **Role swap (2026-06-11 22:10 +10:00).** Codex is away on another project; **Claude is acting backend owner** in addition to frontend, so a single agent now drives both lanes. **Takeover discipline (so Codex or any new agent can drop in cleanly):** (1) keep the `## Codex — Last Task & Resume` section accurate to *true* backend state — what's done, the exact next command, the resume pointer; (2) **explicit git staging only** — never `git add -A`; stage `app/backend/**` and `app/frontend/**` in **separate commits** with `feat(backend:)` / `feat(frontend:)` scopes so lane history stays cleanly attributable; (3) keep `plans/v2-backend.md` current as the BE backlog; (4) honour the backend-lane decisions already locked (arq #6) until Codex reclaims and reviews. The disjoint-lane locks in `README.md` still apply — when wearing the BE hat I follow the BE rules.

## Pillar / Phase Status (bird's-eye, refreshed s55)

Map of `docs/pillars/plan.md` (the shared engine spine P1–P4 + reproduction P5) + the now-CLOSED phase
`docs/reproduction-dogfood/spec.md`. **The engine spine is functionally complete; the reproduction
dogfood phase CLOSED in s55 (Slice 4 shipped). Only 2 LIGHT post-phase items stand between here and
the "pillars complete" milestone — both in the spec's "Remaining phases" table, neither a dogfood slice.**

| Pillar | State | Remaining |
|---|---|---|
| **P1 Ingest & Clean** | ✅ **complete** | — (1a/1b/1c done; 1d via `engine.classify`) |
| **P2 Table Joining & Synthesis** | ✅ **complete** | 2a–2e done (2d = picker + honest `data_unmatched`, s53) |
| **P3 Routing & Guidance** | ~95% | **3c data-side skill-gap signal SHIPPED s54** (`skill_gaps.py` + `docs/skill-gaps.md`); remaining = the routing-confidence "not sure" surface (a UX feature) |
| **P4 Analysis & Output** | ~90% | **4a** contract-uniformity audit (checked list + guard test — light); minor FE polish (Pro/AI legend tier · own-run → Library artifact) |
| **P5 Reproduction ("dogfood-ready" — CLOSED s55)** | ✅ **complete** | 5A/5B + Slices 0–4 all shipped; **Slice 4 (auto-drive regression fixtures) SHIPPED s55** closed it |
| **P6 Parking lot** | parked by design | untouched until an explicit owner decision names the pillar it rejoins |

**Phase — Reproduction dogfood-ready (`docs/reproduction-dogfood/spec.md`): CLOSED s55.** Slices **0 ✅
1 ✅ 2 ✅ 3 ✅ 4 ✅ 5A ✅ 5B ✅** — all shipped. Slice 4 (the 4 hand-ledger papers → auto-drive
regression fixtures) was the only open slice; it landed s55 (`reproduction_fixtures.py` framework +
committed JEV/Hani/Dorgau snapshots + 3-tier tests; the 4 hand ledgers untouched). The Phase-B2
auto-fetch infra stays on hold in `docs/on-hold/README.md`.

**⚑ Milestone flags (owner asked to be told):**
- **ACTIVE phase COMPLETE** — the reproduction dogfood phase is **CLOSED** (Slice 4 shipped s55). Every
  planned slice (0–5) is done.
- **Nearing ALL planned pillars' completion** — P1/P2/P5 complete; P3/P4 each have **one light item left**
  (P3 routing-confidence surface · P4-4a contract-uniformity audit). Once those two land, **P1–P5 are
  essentially done** → Claude flags **"pillars complete."** **Owner steer (s53): BEFORE we strategise the
  next pillars, a TANGENT PROJECT (the owner's actual work, ~2–3 sessions) runs first.** So the sequence
  is: dogfood phase CLOSED (now) → [optional: the 2 light items → "pillars complete"] → the tangent
  project (2–3 sessions) → THEN the next-pillars strategy session. Candidates already in the docs for that later session: the **Prism UX phases**
  ([[selom-prism-pillar-phases]]), **commercial/launch gates** (`LAUNCH-GATES.md`, deferred-by-policy),
  **Product-A daily-driver maturity** (spine-complete but thin on UX), **breadth-by-collaboration**
  (the skill-audit steer, [[selom-clawbio-bioskills-shortlist]]). Not started — flagged.

## Log Edit-Lock

UNLOCKED
<!-- session-55 update by Claude 2026-06-22 19:35 +10:00 (Slice 4 — auto-drive regression fixtures SHIPPED; this CLOSES the "Reproduction — dogfood-ready" phase [Slice 4 was the only open slice]. Local commits PROPOSED, NOT pushed — ASK before push; origin ↔ main @ 7efdab2, working tree = the new files + this docs, no dev servers [Eamos :3001 untouched]. Pure BE + docs, no FE. Built: app/backend/reproduction_fixtures.py [the reusable framework — DriveSnapshot + snapshot() projects a DriveResult to status-shape + driven metric values; diff()/assert_reproduces() = the regression contract: same panel set · same status per panel · 0 Selom defects · every driven metric within its golden's resolve_tolerances band, graded with the exact s46 grader so an engine delta inside the metric-type band stays green + a broken reader flips red] + scripts/regen_reproduction_fixtures.py [bless/refresh generator + shared FIXTURE_PAPERS registry] + committed tests/fixtures/reproduction/{jev,hani,dorgau}.json + README + tests/test_reproduction_fixtures.py [3 tiers]. MEASURED reality drove the design: cold-driving JEV/Hani/Dorgau drives 0 panels — all no_golden/data_unmatched/run_failed [the Slice-0/1 data-availability+recall insight, confirmed on the hand-ledger papers; every panel still classified honestly, 0 Selom defects] — so the win locked today is the honest classification shape; metrics empty + auto-populate as recall rises. Tier a [cheap CI no data] = framework unit tests on a synthetic drive [status flip->red · within-band metric->green · out-of-band->red = the break-a-reader check] + characterization over each committed snapshot. Tier b [opt-in, owner machine] = real reproduce() re-drive asserting the committed snapshot; skips without the staged PDF; verified green here on all 3. 4 hand ledgers untouched/byte-identical; RPGRIP1 = a documented FIXTURE_PAPERS slot [no staged PDF here]. Gates: fixtures 15 + 1 skip · 4 ledgers+drive+honest 63 + 1 skip [no regression] · ruff clean · 780 collect clean. Memory selom-live-reproduction-drive updated. NEXT: dogfood phase CLOSED; before the pillars-complete milestone 2 LIGHT post-phase items remain — P4-4a contract-uniformity audit + P3 routing-confidence "not sure" surface; then a ~2-3-session owner TANGENT PROJECT runs BEFORE any next-pillars strategy.); released. -->
<!-- session-53 update by Claude 2026-06-22 09:05 +10:00 (Slice 2 FINISHED — per-panel data PICKER + Product-A data-fit band; SHIPPED + PUSHED [owner-authorized], 3 lane-separated commits feat(backend:) fad4a62 + feat(frontend:) b77ebd0 + this docs; origin ↔ main in sync, tree clean, dev servers stopped [Eamos :3001 untouched]. Owner: "ok do 1 and 2". (1) Picker components/reproduction/data-picker.tsx — each data_unmatched panel on the Score stage gets a Radix file Select annotated w/ the engine fit band; pick -> persist SavedPaper.dataMap [filenames not bytes; workspaceStore.setPaperDataMap] -> "Re-run with these picks" re-drives w/ a data_map override. panel_drives now rides RunRecord+reproduction_runs.public; POST /papers/{id}/reproduce gained a data_map form field resolved server-side [_resolve_data_map] to each file's saved path; match_data honours it. usePaperRun.start(dataMap?) explicit OR persisted; useReproductionRun exposes panelDrives; fixed paper-shell passing run.start directly as onClick. (2) Product-A band: /skills/{id}/run data_fit -> SkillRunResponse.dataFit -> persisted on Figure -> DataFitVerdict [single-fit sibling of DataFitPanel] on the own-data figure; mockDataFit for dev:mock. (3) Fix: _save_capped saves uploads into a per-file subdir keeping the ORIGINAL name [no uuid prefix] -> filenames match + the picker round-trips [affects /reproduce + /assess-data; /skills/run + /data/inspect use _save_upload, untouched]. Gates: BE run 17 [+3] + drive/compat/diagnose 43 + 4 hand ledgers 30 byte-identical; FE tsc/eslint 0-err/vitest 121 [+5]; real-data round-trip graphify-out/scratch/s53_picker_roundtrip.py [override left data_unmatched, fed the pick, run_failed honestly, 0 Selom defects]; /p/[id] + /paper/?stage=score compile + serve 200. NOT DONE: live interactive browser-verify [chrome-devtools profile locked by another session]. This session CLOSED P2 slice 2d; added a bird's-eye Pillar/Phase Status block + milestone flags [active phase ~3 slices from done; P1–P5 nearing overall completion]. Memory selom-data-fit-scorer updated. NEXT: GATED Slice 5B accession fetch [ASK] + Slice 3 skill-gap doc + Slice 4 regression fixtures.); released. -->
<!-- session-51 update by Claude 2026-06-22 02:30 +10:00 (dogfood phase advanced — Slice 0 2nd-baseline + Slice 1 + Slice 5 SHIPPED + PUSHED [owner-authorized mid-session]; origin ↔ main in sync, tree clean, no dev servers. Owner steer: "do slice zero and slice one" + "also slice 5". (0) Slice 0 re-run COLD on Yoshimura [kidney-organoid multiome, 2nd calibration paper] -> docs/reproduction-dogfood/findings-yoshimura.md: independently confirmed the Harmony gaps [0 goldens, 0 driven, run_failed/oos, 0 Selom defects]; meta-finding generalizes [data = GEO accessions, only a QC table attached]. (1) Slice 1 n_cells extractor [9794b8c]: extract.golden.extract_dataset_size lifts the analyzed dataset size on TIGHT result/QC anchors ["resulted in 56,865 cells after filtering"], 0-false-positive vs Harmony's 28 benchmark cell-counts; backfill n_cells->umap_scrna [engine.match] + L1 read-back readers._read_umap [UMAP point count]; drive test proves extract->grade; 4 hand ledgers byte-identical [30/30]; n_clusters/correlation deferred. (2) Slice 5 accession recognizer Phase A [8de466f]: extract/accessions.py deterministic no-network regex over bundle.text [availability-weighted] -> Accession{repo,id,access:open|raw|controlled,ingestable,url,section,note} [GEO/SRA/ENA/ArrayExpress/GSA/PRIDE/MetaboLights+MW/Zenodo/Figshare/Dryad/dbGaP/EGA]; raw->needs-quantification, controlled->requires-application; traps handled [citation markers, ST-table refs]; wired into reproduction_diagnose [header line + Cited-datasets table]; 10 tests on the real Yoshimura statement; Phase B GATED. Gates: full BE 722 passed; ruff clean. Live re-run lift: Yoshimura report blank -> n_cells golden + 4-GEO Cited-datasets table, still 0 Selom defects. STRATEGIC INSIGHT: two papers one wall — both deposit accessions + a QC table not matrices -> binding constraint for driven = DATA-AVAILABILITY not extractor recall. NEXT s52: Slice 2 matcher honesty [QC-table != counts-matrix -> data_unmatched not forced run_failed] + per-panel picker [flips auto_grade off 0%]; THEN GATED Slice 5 Phase B fetch. Memory: selom-accession-ingest [Phase A SHIPPED] + selom-live-reproduction-drive [Slice 0/1/5 + strategic insight] updated.); released. -->
<!-- session-50 update by Claude 2026-06-22 01:41 +10:00 (next phase chosen + KICKED OFF — "Reproduction — dogfood-ready" [owner's why: dogfood many papers to TRAIN the engine, limited own omics data]. 3 commits LOCAL [feat(backend:) 7939a48 + docs c36efc6 + this handoff], NOT pushed — ASK before push; origin ↔ main @ 9e2e8b8, working tree clean, no dev servers started. (0) Phase spec docs/reproduction-dogfood/spec.md [P5 b/c/d + P2 2d + P3 3c; agile Slices 0–5, decisions D1–D6 incl. D6 accession-fetch-gated; remaining-phases roadmap; pillars/plan.md points at it]. (1) Slice 0 cold-drive diagnostic app/backend/reproduction_diagnose.py — projects a DriveResult -> a gap report [per-panel status/skill/data/goldens-expected/metrics-read/reason + rollup + auto_grade_rate], JSON+markdown; pure projection, 0 Selom defects, no silent caps; test_reproduction_diagnose 4 green, ruff clean, drive test 12/12. (2) Ran it COLD on the real Harmony paper -> docs/reproduction-dogfood/findings-harmony.md: 0/8 gradable, 0 goldens extracted, all 8 run_failed, yet 0 Selom defects. Causes: golden extractor knows DE/PCA only not integration benchmarks [Slice 1]; matcher forced lone xlsx onto all panels -> run_failed not data_unmatched [Slice 2]; META: famous methods papers deposit code+.rda+GEO/GSA accessions not tabular supplements. NEXT BUILD s51 [owner-approved]: Slice 5 accession recognizer Phase A [deterministic, no network]; Phase-B fetch GATED. Memory: new selom-accession-ingest, selom-live-reproduction-drive updated.); released. -->
<!-- session-49 update by Claude 2026-06-22 01:03 +10:00 (the circle-back FE/UX pass — tasks 1 & 2 of the s48 NEXT list — SHIPPED + browser-verified vs the LIVE BE; 3 commits LOCAL [feat(backend:) 3878a29 + feat(frontend:) 13e4c83 + this handoff], ASK before push; origin/main @ 1201438 [the 3 s48 commits pushed at session start, owner-authorized], working tree clean, dev servers killed. (1) Product-A guided workbench [P1c/P3a/P3b]: lib/skills-api DataCheck/QcFlag/DataRouting/FigureLegend types + figure_legend->legend / data_check->dataCheck mapping + override run option + typed DataCheckError from the 422; new DataCheckPanel [verdict w/ modality chip + QC flags + clickable suggested pipeline · blocked w/ subordinate "Review & run anyway" override]; project-workspace block card + suggested-step install/preselect + legend/dataCheck persisted on the Figure. (2) L3 Statistics node + Methods/legend [table-synthesis §8 step 4 + workspace-library §11]: BE POST /skills/{id}/run attaches table=table or synthesize_table(...) for tableless skills [test_run_synthesized_table 3]; StatsPanel "Computed by Selom" badge+explainer; PublishConfidence paste-ready Figure-legend section. Gates: BE 3+4; FE tsc clean · vitest 114 [+4] · eslint 0 errors [+1 inert React-Compiler note]. Browser-verified on :3001 vs live BE [clean verdict + steps · 422 block + override->figure · synthesized node · methods+legend; screens graphify-out/scratch/s49-*]. Docs: table-synthesis §8 step 4 DONE, workspace-library §11 own-data legend DONE, pillars plan P3b + circle-back FE pass DONE; memory selom-engine-spine-pillars updated. Note: umap_scrna has its own SELOM_UMAP_ENGINE selector [ignored stub], 500'd on a CSV — use pca/deg for stub own-data runs.); released. -->
<!-- session-48 update by Claude 2026-06-22 00:16 +10:00 (two backend ships, 3 commits LOCAL [2 feat(backend:) + this handoff] — ASK before push; origin/main @ 10b6005, working tree otherwise clean: items 1 & 2 of the s47 NEXT list = the Product-A engine front door. (1) is-my-data-clean? guardrail + RAW-routing surfaced for own data [P1c/P3a]: POST /skills/{id}/run ingests via engine.ingest -> run_qc -> blocks block-severity unless override=true [D-e5, structured 422 + fix hints], runs from the same bundle, surfaces data_check [kind+QC+routing]; fail-soft so the proven path never breaks; async .../jobs not gated yet. (2) runner takes a DataBundle [P1 §1b / E4]: skills.contract.run_bundle/run_bundle_with_table [byte-identical to the path runner, duck-typed no engine import], engine.ingest sets DataBundle.path, both products load through engine.ingest -> bundle -> run end-to-end [reproduction _default_runner fail-soft + /skills/{id}/run]. Gates: test_run_guardrail 4 + test_run_bundle 6; engine+golden+table+registry+routing 153; full reproduction set 116 passed [5:08] BYTE-IDENTICAL; ruff clean. Docs: engine-spine §6 ANALYZE + qc-gate prose + §9 step 5, pillars plan P1b/P1c/P3a + inventory all DONE; memory selom-engine-spine-pillars gate-MET note. Every engine-core skeleton now exists -> FE pass next); released. -->
<!-- session-47 update by Claude 2026-06-21 23:39 +10:00 (two backend ships, both PUSHED — origin ↔ main in sync @ 61034b2: (1) figure-legend layer 959344d [P4c] — new legends.py per-skill caption sibling of methods.build_body, DE-split enriched via the new canonical readers.de_counts [_read_de_table routes through it too], surfaced as figure_legend on /skills/{id}/run + GET /papers/{slug}/legends via litsynth/legends_from_ledger; additive, figure goldens unchanged; test_legends 24. (2) engine/match.py 61034b2 [P1 step 5] — lifted merge_ledger/match_data/tabular_paths out of reproduction_drive into the engine package, reproduction_drive consumes + re-exports them, byte-identical, no import cycle. Gates: test_legends 24 + readers; golden/methods/litsynth/synthesize 132; drive+drive_honest+4 ledgers+runs+sweep = 59 UNCHANGED; score/guards/tolerance/papers_api/citations/contract/provenance 75; ruff clean. Docs: pillars plan P4c+P1-step-5 DONE, engine-spine §6/§9, workspace-library §11); released. -->
<!-- session-46 update by Claude 2026-06-22 00:34 +10:00 (two backend ships, both PUSHED — origin ↔ main in sync: (1) proteomics_de native de_table 424ea54, the last at-source L3 gap [table-synthesis §8 step 5] — canonical de_table in real engine + stub, registered to the shared de_table L1 reader _read_de_table so de_total=up+down; only the node-link trio stays L4-only. (2) metric-type-aware tolerance grader 359c7f2 [P5a] — metric_type on Golden → family band [METRIC_TYPE_TOLERANCES + infer_metric_type/resolve_tolerances], explicit tolerance still wins, drive auto-types its goldens; non-regressing by construction. Gates: 24+12 new; golden/registry/route/synthesize 101; reproduction/score/guards 57; lighter engine 46; drive+drive_honest+4 ledgers UNCHANGED across both regression runs [46 each]; ruff clean. Docs: table-synthesis spec §8 step 5 + pillars plan P5a marked DONE); released. -->
<!-- session-45 update by Claude 2026-06-21 22:16 +10:00 (P2 Tier-B LOSSY set boxplot/heatmap/scorecard/violin SHIPPED — 72410b2, completing the full L3 synthesizer set (16 skills); each behind a faithfulness gate, all four faithful so none punted to L4; new shared _heatmap_grid gate, refactored corr_heatmap onto it; reader auto-extended via _SYNTHESIZERS. Gates: test_synthesize 28 (+10) + test_readers green; drive + drive_honest + 4 ledgers UNCHANGED [46 passed, 6m34s]; ruff clean. PUSHED @ 78eede1, origin ↔ main in sync); released. -->
<!-- session-44 update by Claude 2026-06-21 21:49 +10:00 (P2 Tier-B clean trio corr_heatmap/sankey/upset SHIPPED — fe079be, each behind a faithfulness gate, reader auto-extended via _SYNTHESIZERS; tests green [synth+readers 36 / drive 16 / golden 6]; ruff clean. + CURRENT.md weekly archive: stale s40↓ live-state comments + the Active-Status _Prior_ stack → agent_handoff/archive/2026-06-21-current-history.md. Local UNPUSHED — ASK before push); released. -->
<!-- session-43 (cont.) update by Claude 2026-06-21 21:33 +10:00 (pushed the 10 s43 commits + P2 commit c215299; integrated The Ratchet + restructured MEMORY.md into How-I-work vs Selom-project buckets under cap; P2 L3 table synthesis Tier A COMPLETE 9/9 [+umap_scrna/annotate] + reader L3 integration [read_metric -> synthesize_table fallback, tagged L3/synthesized, score-eligible]; full suite 632 passed; ruff clean; no dev servers); released. -->
<!-- session-36 update by Claude 2026-06-20 20:09 +10:00 (Skill Match flow polish SHIPPED + verified — widen/equal-height, metadata-header swap on drop, single dropzone + no bottom Run, Selom-themed routing progress sweep, Copy(TSV)/CSV export [lib/skill-match/export.ts +11 vitest → FE 74]; Workspace Library SPEC written + owner-signed-off model [single home · promote papers+gene sets+skill installs · SavedPaper=compact summary] + expanded umbrella vision §10/§11 [fold Skill Match+Reproduction+Recover data over a Paper object · reproduction AI-gating = AI-proposes/deterministic-disposes · Methods+Figure-legend layer ~70% built on lit-synth]; 6 commits LOCAL UNPUSHED — push at next session start; FE 918527e/1d645a6 + docs c71526a/79962eb/56fd77d/36504e3; dev servers killed); released. -->
<!-- session-35 update by Claude 2026-06-20 18:37 +10:00 (extraction↔recovery gap CLOSED — 2 production caption forms added to L2 recovery: Wiley/JEV PUA-glyph inline + bioRxiv line-numbered manuscript; JEV 0→8/Ishii 0→4/Hani 5→5, L3 inventory unchanged; _jev_text.txt regenerated from production + 2 unit fixtures; BE 538. FE per-figure UX: all-skill chips · Overview/Per-figure tabs · scrollable · chip→SkillDetail popout · rail no-auto-collapse; tsc+vitest 63+lint green; browser-verified real JEV. Commits BE 42dbe3a / docs 97382aa / FE 56543b4 ALL PUSHED @ 56543b4); released. -->
<!-- session-33 update by Claude 2026-06-20 03:08 +10:00 (Skill Keyword Index legend-segmentation hardening → 4-layer deterministic core: de-spaced/glyph markers + ordinal recovery, header-less citation-cluster refs exclusion, token-canonical relaxed matcher, L3 paper skill-inventory + per-figure tier/confidence, canonical-term dedup; validated on real JEV — L3 recall 8/8 in-scope skills, 8/8 garbled captions recovered, full bibliography excluded; flagship proprietary module; pattern distilled to memory layered-deterministic-extraction; BE 518; scope fed1ba2 / code ae2e149 / docs edeaaa7 / handoff de480d8 ALL PUSHED @ de480d8); released. -->
<!-- session-32 update by Claude 2026-06-20 02:01 +10:00 (Skill Keyword Index v1 shipped: deterministic keyword->skill/oos router in extract/routing/ — registry+curated vocab validated vs the live registry, refs-excluded section segmentation, alternation matcher, per-figure FeasibilityMap, POST /papers/route; backtested vs all 4 ledgers + live JEV; BE 509; spec 0fde152 / code 9834f09 / record eb73b64 ALL PUSHED @ eb73b64); released. -->
<!-- session-31 update by Claude 2026-06-20 00:49 +10:00 (Dorgau 4th figure-repro ledger built + validated: 94/100, Melody dogfooded live on GSE234963 0.146→0.490 mixing; new MODALITY_UNSUPPORTED scope; BE 495; all s30+s31 PUSHED @ `9017fd9`; Skill Keyword Index sequenced next); released. -->
<!-- session-30 update by Claude 2026-06-19 23:43 +10:00 (Harmony2 mode A+B folded into Melody behind a default-off flag; validated vs the R harmony 2.0.5 oracle; BE 485); released. -->
<!-- session-29 update by Claude 2026-06-19 22:35 +10:00 (Selom Melody shipped: clean-room pure-numpy Harmony replaces harmonypy on the integration path, GPL off the shipped path, validated vs oracle; harmonypy→oracle extra; BE 480; license mis-record corrected); released. -->

## Shared File Locks

None held.

## Cross-Agent Requests

> **Coverage note (2026-06-11 22:10):** while Codex is away, the **FE → Codex** asks below are now **Claude's own backend backlog** (acting BE) — I action them in-lane rather than waiting. They're kept here verbatim as the running BE contract/spec so Codex can read the full thread and reclaim cleanly. No new cross-lane asks are needed while one agent holds both lanes.

- **GitHub: connected ✓** Pushed to **github.com/steveneam/selom** (private); `main` ↔
  `origin/main` in sync as of 2026-06-11 01:41 +10:00. Latest: `afaafd2` (command-center
  scaffold + design) atop `e63f38b`/`5c4a1fd` (figure editor). Commit identity is local
  `Steven <mactechdish@gmail.com>` — adjust if GitHub commit attribution should differ.

- **FE → Codex — ✅ RESOLVED 2026-06-11 23:25 (B1, Claude acting BE):** asks 1 & 2 below are done.
  1. **✅ Backend booted + real engine wired.** `uv python install 3.12` + `uv sync --extra scrna`
     (light scRNA path, no scvi-tools/torch) on the user-managed py3.12; `uvicorn main:app` on :8000.
     `run_scanpy.py` now runs the real pipeline behind `umap_scrna/run` and returns an editable
     Plotly spec. Verified: real 2700-point / 11-cluster UMAP, direct and through the FE proxy.
  2. **✅ Canonical P0 contract = ONE-SHOT** multipart `POST /skills/{id}/run` (field `matrix` +
     params as query). The two-step upload-then-run idea is dropped for P0; `plans/v2-frontend.md`
     should be updated to match. `run.py` dispatches stub vs. real via `SELOM_UMAP_ENGINE`.
  3. **FYI — no backend action needed:** FE deps were re-pinned. `plotly.js@^2.37` did not exist
     (install hard-failed); bumped to `plotly.js@^3.6` + `react-plotly.js@^3.0` (now natively
     supports React 19, so `--legacy-peer-deps` is no longer strictly required). This is FE-lane
     only — the wire format (Plotly `{data, layout}` JSON) is unchanged, so the contract is
     unaffected. Worth updating RISKS #1/#3 (Codex/shared) at some point.

- **FE → Codex (NEW 2026-06-11 01:03 — command-center backend; full spec in `docs/command-center/design.md` §3–§9):**
  The frontend is building the project-first command center **mock-first** (no backend dependency to
  build). When the backend lane resumes, these are the new contracts to own/finalize — all **additive**;
  the figure wire format (Plotly `{data, layout}` JSON) is unchanged:
  1. **Skill registry API (B1).** `GET /skills` (filterable list of `SkillCatalogEntry`) +
     `GET /skills/{id}` (detail/SkillSpec). Seed from the ingested **ClawBio `skills/catalog.json`**
     (maps ~1:1) + **bioSkills** categories/`SKILL.md` frontmatter. The full ~600-skill catalog is
     browsable; each entry carries `tier` (verified|community), `engine`, `status`, `input_formats`,
     `provenance`, `license`. Shape = design §3.1.
  2. **Ingest + intake (B2).** `POST /upload` returns a dataset handle **plus** an ingest/QC report
     (detected modality, n_obs/n_var, guardrail flags). `POST /intake` takes questionnaire answers +
     dataset shape → schema-constrained **`IntakeProposal`** (cleaning steps + 1–3 proposed skills with
     pre-filled params + rationale/confidence) via the **shared AI gateway**. LLM proposes, never
     auto-runs. Shapes = design §4.3 / §7.
  3. **Verified runner expansion (B3).** Generalize `POST /skills/{id}/run` beyond `umap_scrna` to any
     Verified skill; async (arq) for heavy ones. **Skill Foundry (manual):** hand-port the wedge
     (~6–8: UMAP/cluster/DEG/volcano/heatmap/enrichment) + selected ClawBio runnable skills into the
     SkillSpec contract, each emitting an editable Plotly spec + provenance/golden-image test.
  4. **Supabase persistence (B4).** Tables = design §5 (`projects`, `datasets`, `skill_installs`,
     `intake_sessions`, `figures`, `figure_history`, `skill_catalog`; RLS by owner). FE persists through
     a `ProjectStore` interface (localStorage now) designed to match this schema, so the swap is a
     backend-impl change, not a FE rewrite.
  5. **Skill execution toolchain (design §6.5).** What we download/install to actually test+run skills:
     **(A)** catalog ingest — `git clone --depth 1` ClawBio + bioSkills, parse `catalog.json` + `SKILL.md`
     → seed manifest (cheap, do-now, an `scripts/ingest-catalog` job). **(B)** Verified runners — the
     existing `[omics]` extra (`uv sync --extra omics`: scverse + plotly/kaleido). **(C)** ClawBio runnable
     — `pip install clawbio` + **Miniforge/mamba** for per-skill `environment.yml` + bioconda. **(D)**
     bioSkills long tail — bioconda CLI toolchain (samtools/bcftools/STAR/salmon/GATK4/… + Snakemake/
     Nextflow) consumed via the Skill Foundry. **(E)** Community sandbox — Docker + mamba images +
     network policy + compute (v2, deferred). A+B are immediate; C is the contained next step; D/E grow
     over time.
  No action needed now — FE is unblocked and mock-first; this is the spec to build against when the
  backend lane resumes. North star (owner): grow runnable coverage to the full 500+ via the Skill
  Foundry (manual now → LLM-assisted = the v2 Extract-Skills moat).

- **FE → Codex (NEW 2026-06-11 21:56 — decision ratified, BE-lane confirm needed):** the owner directed
  ratifying all 5 pending vault `[DECISION]`s. One is backend-lane: **DECISIONS.md #6 — async job queue =
  `arq` + Redis (not Dramatiq).** It's locked to the vault's recommended default (asyncio-native, matches the
  FastAPI stack + every manifest/schematic); recorded by FE per the owner directive so it isn't left
  un-ratified. **Codex: confirm this, or supersede-by-append in `DECISIONS.md` if a Dramatiq-specific need
  surfaces** when the queue goes load-bearing (charter Bucket B3 / ledger P2). No action needed before then.
  _Update 2026-06-11 22:10 — Codex away; Claude (acting BE) proceeds on **arq** as locked; left for Codex to review on reclaim._

## Current State

- **Scaffold complete and committed:** git repo at `D:/selom`, branch `main`, commit
  **`4ccb6b6`** ("chore: scaffold Selom build repo (skeleton + agent workflow)"),
  40 files, clean tree. **Pushed to github.com/steveneam/selom (private); main ↔ origin/main in sync.** Identity is set LOCALLY
  to `Steven <mactechdish@gmail.com>` (correct it if that's not the intended GitHub
  identity).
- **Skeletons that ALREADY EXIST — do NOT re-scaffold:**
  - **Backend** (`app/backend`): FastAPI `main.py` (`GET /skills/{id}`,
    `POST /skills/{id}/run`, `GET /health`) + `skills/contract.py` (SkillSpec loader)
    + `skills/umap_scrna/` = `skill.json`, `run.py` (offline **STUB** — returns a
    valid Plotly dict with zero heavy deps; **verified runs on Python 3.10**),
    `run_scanpy.py` (real Scanpy engine, lazy import). `pyproject.toml` = light core
    deps + an `[omics]` extra for the scverse stack. `.python-version` = 3.12.
    `tests/test_contract.py`.
  - **Frontend** (`app/frontend`): Next.js 16 / React 19 / TS / Tailwind 4 shell —
    `app/page.tsx` (file upload → `POST /api/skills/umap_scrna/run` → render via
    `react-plotly.js`), `layout.tsx` (Inter via next/font), `next.config.ts`
    (`/api/*` → `:8000` proxy), tsconfig/postcss/globals.
- **P0 hello-UMAP = DONE (B1, 2026-06-11).** Backend venv built (`uv sync --extra scrna`
  on user-managed py3.12); `run_scanpy.py` real engine wired behind one-shot
  `POST /skills/umap_scrna/run`; `scripts/make_demo.py` writes the pbmc3k `demo.h5ad`
  fixture (gitignored, ~21MB). Verified in a real browser: upload → run UMAP → editable
  Plotly figure (11 Leiden clusters / ~2700 cells) renders against live :8000 (MSW off).
  Backend contract test green (`python -m pytest`, stub-pinned). FE proxy body cap raised
  to 512mb (RISKS #8). **Pushed: `main` ↔ `origin/main` @ `7d58e92`.**
- **Still NOT done (by scope):** no cloud (`.env` + `.mcp.json` are TODO placeholders),
  no Docker, no Supabase/arq. (**Kaleido journal export DONE 2026-06-15 — native, no Docker.**)
  Those remaining are later buckets.
- **Next milestone = B2 (Steven's real skills) — SIGNED OFF 2026-06-11, not started.** Build 6
  backend runners (cluster · violin · DEG · volcano · heatmap · GSEA/enrichment), each a pure-Python
  skill emitting an editable Plotly spec + a golden-image snapshot test. Locked: public proxy
  datasets now (real data later); GSEA via Reactome/GO (DECISIONS #9). Then B3 (jobs/queue) / B4
  (publish-confidence). See `docs/build-charter.md`.

## Claude — Last Task & Resume

- **Last (2026-06-18 01:19 +10:00, session 18) — REPRODUCTION ENGINE R4: figure-extraction subsystem substantially completed + a 3rd real ledger.** Owner approved building all four remaining items in one pass (R5 deferred to next session). **(b)** E7 two-input ingest (`extract/ingest.py`: `ingest_paper(main, supplements=[…])`→`PaperBundle`, format-plural PDF/xlsx/csv; `build_extracted_spec` bundle-aware) — `edc1624`. **(a)** X1 slice-2 vision layer (`extract/vision.py`: Claude-as-gateway `OperatorVisionGateway` behind the existing `VisionClassifier`; `associate_counts`/`augment_with_vision`/`venn3_totals`/`segment_panels`; VERIFIED LIVE on the real RPGRIP1 Fig 6E Venn → pixel-only pairwise 13/10/2, per-rod totals 102/119/74) — `2c110a5`. **(d)** `reproduction_hani.py` + test (Kim 2023, GSE201356; mmc2 Cepo matrix re-derived exactly via `drive_live_markers`; scorecard 96/100 Verified, 0 Selom bugs, 1 wet-lab out-of-scope) — `5f8db38`. **(c)** X2 Track-B reconstruction (`extract/reconstruct.py`: pure-numpy SSIM + editable Plotly redraw + gated Kaleido render; verified live SSIM 1.0 / 0.924) — `912615a`. The 3 ledgers now span the score spectrum RPGRIP1 63 / JEV 86 / Hani 96. **pytest 307 (+34); ruff clean; library-only (D12); all LOCAL — ASK before push.** Memories: [[selom-claude-acts-as-ai-gateway]], [[compound-capability-each-task]]; updated [[selom-repro-edge-cases]] (#10 pixel-only vision recovery), [[selom-reproducibility-score]] (Hani 96 + the spectrum), [[selom-backend-python-exec]] (pandas 3.0 `.map`). **NEXT (owner picks): R5 FE Reproduction view · SSIM-band calibration · X3/X4 · panel-segment automation · a 4th ledger.**
- **Last (2026-06-18 00:21 +10:00, session 17 cont.) — REPRODUCTION ENGINE R4/X1 slice-1: the FIGURE-EXTRACTION SUBSYSTEM.** Owner picked **R4** (after the Reproducibility Score). Presented the X1 scope (charter rule); owner picked **"Build slice-1 as scoped"** — the text-derivable target-extraction core, vision gated. Built the new **`app/backend/extract/`** package: turns just a paper PDF into the typed target spec `reproduction_{rpgrip1,jev}.build_ledger()` hand-encode today (SOP steps 1–4).
  - **`ingest.py`** — thin typed wrapper over `papers.py` (PDFium text + lazy rasters; BSD, E1). **`models.py`** — `GoldenTarget` / `MethodsDigest` / `PanelDraft` / `ExtractedSpec`. **`golden.py`** — **text-layer-exact** DE-count extraction (E2; **sentence-bounded** so a neighbouring count sentence can't bleed in, `total = up+down` not a 'top 50' neighbour, figure ref searched one sentence wider since it often precedes the count) + the **word-boundary** methods-digest lexicon (so `RLE` doesn't fire inside `Morley`) + guard-2 `find_figures_vs_methods` + `to_golden`/`to_engine_panels`. **`classify.py`** — rule scope-classify (guard 7) + a pluggable `Classifier`; the vision LLM is **gated** (`VisionUnavailable`, dev-only, R-oracle pattern — open-Q#2, no gateway wired).
  - **Verified on the REAL JEV PDF** (integration test, skips where absent): recovers Fig 1 miRNA **12/23/35** + Fig 4 protein **61/119/180** (== `reproduction_jev`'s figure goldens), the threshold (p<0.05) + filter (|logFC|>0.5), and the methods recipe (edgeR/limma/voom/fgsea/Seurat/SCTransform + TMM). Panel-letter assignment is **best-effort** (the deferred segmentation/vision job, open-Q#1). **pytest 275 (+14, `tests/test_extract.py`); ruff clean.** Library-only (D12). **Commit `c195d38` (`feat(backend)`) + this `docs(handoff)` — PUSH owner-gated.**
  - **Owner corrections/steers:** (1) `…/Data/THL/mmc1.pdf` is the **RPGRIP1 main paper + supplement combined** (I'd wrongly read it as supplement-only) — a 2nd extraction target. (2) **Intake (E7):** paper extraction should take a **main-paper drop + a separate supplementary drop** (two inputs) until auto-detection improves — main = figures/text, supplement (PDF/xlsx) = the golden tables (ST2/ST6) + extended methods. Recorded in the sub-spec; slice-1 ingest is single-file, generalize before R5/FE. **NEXT = X1 slice-2** (live vision + panel-segment) **or X2 reconstruction or a 3rd ledger.**
- **Last (2026-06-17 23:51 +10:00, session 17) — REPRODUCTION ENGINE: THE REPRODUCIBILITY SCORE SHIPPED.** Owner pre-authorized the session-16 push as the first action → pushed (`main ↔ origin/main`, was ahead 3; `4e88113`/`9bf0473`/`cc437e9`). Then presented the build scope (charter rule) — owner picked **"Build as specified"** — and built **the Reproducibility Score**, a Selom-unique graded 0–100 layer over the engine's existing verdict/blame/provenance (the proposal in [[selom-reproducibility-score]]). All in `reproduction.py` (~80 lines) + `tests/test_reproduction_score.py`.
  - **Implementation.** `score_panel(panel, validation, sweep) → PanelScore` (out-of-scope → grey/None; else the panel takes its WORST in-scope metric; then a **Deposit-faithful overlay** — a panel that matches its deposit but diverges from the figure caps at 58 with selom_confidence intact, D14; and a **Recoverable overlay** for a value reachable only at an engine-recovered setting) + a weighted `score_paper(ledger, panel_scores) → PaperScore` rollup + coverage. `build_scorecard` computes + attaches `panel_scores` + `score` → **both ledgers auto-backfill with zero driver edits**. New: `Panel.weight` (numeric "heart" panels > form re-plots), `score_to_tier` band table, `TIER_COLORS` (hex, FE-ready), `ATTR_*` chips, `format_score`/`format_panel_scores`.
  - **The load-bearing rule (the proposal's whole point): two axes kept SEPARATE** — `reproducibility` ("can the figure be regenerated?", a paper+data property → the red→green heatmap headline) vs `selom_confidence` ("is Selom's reconstruction trustworthy?", an our-tool property). A paper-irreproducible figure (a WIN to detect) scores LOW reproducibility but HIGH confidence → never reads as a Selom failure. Tiers: Verified 95–100 · Reproduced 80–94 · Recoverable 65–79 · Deposit-faithful 50–64 (JEV 4e) · Irreproducible 30–49 · Discrepant 1–29 · Out-of-scope grey.
  - **Backfilled fixtures the engine PRODUCES (pinned as tests): JEV 86/100 (Reproduced · 100 confidence · one amber 4e Deposit-faithful 📄 `ST6+ Fig4e−`) · RPGRIP1 63/100 (Deposit-faithful · 92 confidence · 0 Selom defects)** — the 63-vs-92 gap is the headline (paper hard to reproduce, Selom did its job). Note: panel 6E renders Irreproducible 🗄 because its worst metric (the 52-core `upstream-delta`) masks the `engine-delta` rod-term count; the Recoverable tier is still exercised at the metric level + a dedicated unit test.
  - **Docs:** `spec.md` += **D14** (source-provenance) + **D15** (Reproducibility Score) + the `PanelScore`/`PaperScore` models, the `scope` enum / `Panel.weight` / `sources[]` fields, and the report-step/invariant updates. **pytest 261 (+16); ruff clean** (EDR-workaround uv-3.12). Library-only (D12). Dev CLI prints the score block (`python -m reproduction_{rpgrip1,jev}`). **Commit `145acc9` (`feat(backend)`) + this `docs(handoff)` — committed locally, PUSH owner-gated (per-push rule; NOT pre-authorized).** **NEXT (owner picks): R4 extraction X1 · a 3rd ledger / driver harness · or R5 FE Reproduction view (the score is its natural headline).** Memory: [[selom-reproducibility-score]] updated → **SHIPPED**.
- **Last (2026-06-17 22:39 +10:00, session 16) — REPRODUCTION ENGINE: SECOND REAL LEDGER (Cioanca/JEV 2023) + SOURCE-PROVENANCE (+/−) TAGGING (D14).** Owner picked option (b) — drive a 2nd paper through the engine before R4 (cross-paper overfit check). Built **`app/backend/reproduction_jev.py`** (Cioanca/Natoli, *J Extracell Vesicles* 2023, doi 10.1002/jev2.12393; 11 panels — EV proteomics + miRNA + single-cell). The opposite shape from RPGRIP1: a wall of FAITHFUL reproductions of the deposited supplementary tables.
  - **Two engine extensions** (surfaced by JEV; in `reproduction.py`): **`DATA_NOT_DEPOSITED` scope** (Fig 8 single-cell — the study's own scRNA PRJNA990691 was never deposited → pipeline demo on the Fadl GSE153674 reference; out-of-scope, distinct from wet-lab; `OUT_OF_SCOPE_SCOPES`) + a **`reproduced` (faithful) scorecard finding** (so a paper that DOES reproduce reads as a win; `FAILURE_BLAMES`).
  - **Owner-driven KEY FINDING → D14.** Across a back-and-forth, owner challenged my first "paper-irreproducible" call on Fig 4e. Diligence: ST6 is the Key-sheet-confirmed COMPLETE DE result for the same DR-vs-PD contrast; the published Fig 4e (61 up/119 down/180) is NOT reconstructable from it (172/275/447 @p<0.05; no single threshold gives 61/119 — up & down pin different p; verified by a full sweep + a fine 2D p×|logFC| grid). Built an **interactive Fig 4e volcano** (`D:/selom-data/jev/figures/fig4e_interactive.html` — draggable significance/FC lines + live up/down counter + the paper's labelled hits B2M/HK2/TAOK3/…) → owner confirmed the labelled proteins sit at MATCHING positions ⇒ Fig 4e is most likely a **different biological/experimental replicate**. Owner steer: **reproduce the DEPOSIT faithfully (the win), record figure divergence as neutral provenance, NOT a blame.** → Adopted **source-provenance (+/−) tagging**: `SourceTag {ref, faithful, note}` on every panel + `Panel.provenance`/`.diverges_from` + `Scorecard.provenance_divergences`. JEV 4e = `ST6+ Fig4e−`.
  - **Scorecard the engine PRODUCES (JEV):** reproduced 14 · paper-irreproducible 0 · engine/upstream/structural 0 · **SELOM-ENGINE BUGS 0** · out-of-scope 4 (Fig 8) · **1 figure-divergence shown-not-blamed (4e: ST6+ Fig4e−)**. RPGRIP1's `reproduced` now shows 9 (the new finding; its `format_scorecard` prints the line).
  - **Tests:** `tests/test_reproduction_jev.py` (+6) + engine (+2: data-not-deposited scope, provenance) + RPGRIP1 (+1: reproduced). **pytest 245 (+9); ruff clean** (EDR-workaround uv-3.12). Library-only (D12). Dev CLI `python -m reproduction_jev [--live --xlsx <s001.xlsx>]`. **Commits: `feat(backend)` (reproduction.py + reproduction_jev.py + reproduction_rpgrip1.py + 3 test files) + this `docs(handoff)` — committed locally; PUSH owner-gated (per-push rule).**
  - **NEXT SESSION (owner-AGREED): build the Reproducibility Score** (0–100 heatmap red→green; tiers Verified/Reproduced/Recoverable/Deposit-faithful/Irreproducible/Discrepant + grey out-of-scope; attribution chip Selom/engine/paper/data + provenance badge; panel→figure→paper rollup + coverage; secondary Selom-confidence number) — per the accepted proposal in [[selom-reproducibility-score]] — **THEN R4** (extraction X1; now has 2 validated ledgers as its target/test set). Paper + supplement at the Adrian/ share (resume prompt). New/updated memory: [[selom-reproducibility-score]] (next build), [[selom-jev-reproduction-plan]] (driven through engine + D14 + paper location).
- **Last (2026-06-17 21:04 +10:00, session 15) — REPRODUCTION ENGINE: FIRST END-TO-END INTEGRATION ON A REAL PAPER (RPGRIP1 Fig 5/6 → R0+R1+R2+R3), VERIFIED LIVE.** Owner picked option (b) (integration dogfood) over R4 (charter rule = present scope, owner picks). Built **`app/backend/reproduction_rpgrip1.py`** = the first REAL reproduction ledger:
  - **`build_ledger()`** — hand-encodes the **13-panel** RPGRIP1 ledger (Fig 5: 5sig/5A/5B/5C + wet-lab 5D/5E/5F; Fig 6: 6A/6C/6D/6E/6F/6G) with goldens, method-subs, scopes, structural flags, methods digest. The structured form of `docs/rpgrip1-figrepro.md` + `D:/tmp-thl/rpgrip1_target_spec.md` that **R4's extraction subsystem will auto-generate** from the PDF.
  - **`drive_captured()`** (pure/CI-safe, no data/R) — replays the verified session-11/12 dogfood observations (the logged `*_oracle.log` numbers) through `validate_panel`→`build_oracle_result`→`build_scorecard`, so the engine *produces* the by-hand verdicts. **Findings-first scorecard: paper-irreproducible 2 · structural-limit 2 · engine-delta 1 · upstream-delta 1 · SELOM-ENGINE BUGS 0** (13 panels / 10 in scope; verdicts exact 2 / close 8 / fail 8).
  - **`drive_live_fig5()`** — re-runs the Fig 5 count loop on the real **GSE293982** deposit through actual engine code (`sweep.signature_count` on Selom's real DE rows → `sweep_panel` → **gated edgeR oracle** → `revalidate_panel`). **VERIFIED LIVE (oracle ON):** universe **1133** / signature.count **19** / down_both **13** — matches the edgeR oracle (`signature.adj` 19, `down_both.adj` 13) EXACTLY; sweep proves the printed **78 irreproducible** (stated adj-p<0.05 = 19); `revalidate` upgrades blame to **paper-irreproducible**. **Live == captured.**
  - **Two engine gaps the dogfood surfaced + FIXED in `reproduction.py`:** (1) **`Golden.ints_exact`** (added + threaded into `validate_panel`) — a continuous integer-valued golden (the 6D "2×" fold) was tripping `classify_metric`'s strict-int path (1.8× read EXACT, masking the miss); now uses the float bands. (2) **`Panel.note`** — form/claim panels (5A/5B/6C: no printed number) record why there's no golden. Also: **"down in both" has two readings** — the paper/de.py/edgeR mean *sig+down in the primary contrast (MS-VUS) + down-by-direction in the secondary (LCA-1)* = 13, NOT a strict set-intersection (= 11); the live driver uses the faithful definition (`sweep.intersect=` keeps its stricter semantics for its own callers).
  - **Tests:** `tests/test_reproduction_rpgrip1.py` (+5; ledger shape, form/claim notes, captured scorecard = the dogfood verdicts, per-metric blame disambiguation, ledger round-trip). **pytest 236 (+5); ruff clean** (EDR-workaround uv-3.12). Library-only (no HTTP, D12); oracle gated (ADR 0002, off by default). Dev CLI `python -m reproduction_rpgrip1 [--live-fig5 --oracle]`. No dev servers. **Commit `eb156b6` (`feat(backend)`) + this `docs(handoff)` commit — NOT pushed (per-push rule).**
  - **Owner Qs at session end (answered):** (1) the **Hani** (Kim 2023, GSE201356) and **Cioanca** (the JEV multiomics paper, JEV 2023 doi 10.1002/jev2.12393) papers are also dogfood targets — both were **reproduced as Selom figures earlier** (Hani drove `upset`/`corr_heatmap`; Cioanca/JEV reproduced in the 2026-06-14 JEV dogfood) but **NOT yet driven through the Reproduction Engine** (only RPGRIP1 has) → option (b) next. (2) The **2 ClawBio horizontal skills are NOT built/reverse-engineered yet:** **data-extractor** (figure-image→CSV, vision+OpenCV) is the **clean-room blueprint for R4** (figure-extraction-subsystem.md decision **E6**), and **lit-synthesizer** (PubMed/bioRxiv) → a future **auto-methods-text** backend. The shipped ClawBio-derived work was the *vertical* skills (proteomics_de/gsea/upset/corr_heatmap/normalization_qc/scorecard), per [[selom-clawbio-bioskills-shortlist]]. **NEXT = R4 (extraction X1, clean-rooming the ClawBio data-extractor) OR more dogfood papers through the engine (Hani/Cioanca) OR generalize the per-paper driver.** No new memory needed — [[selom-figure-repro-mission]] / [[selom-clawbio-bioskills-shortlist]] / [[selom-real-datasets]] cover the substance.
- **Last (2026-06-17 19:38 +10:00, session 14) — REPRODUCTION ENGINE R3 (ORACLE + SWEEP) SHIPPED + VERIFIED LIVE ON REAL DATA.** Owner picked R3 over R4 (my recommendation) + "go go, can commit and push". (1) **Pushed** session-13's 3 commits first (`9f35398`/`e69d695`/`b676821`) — `main` ↔ `origin/main` synced. (2) Built **R3** = the two blame **instruments** that feed the already-shipped R0+R1+R2 core (the blame procedure + verdicts were already built/tested; R3 produces the `OracleResult`/`Sweep` they consume):
  - **`sweep.py`** (loop stage 9, guard 1) — pure-Python, no heavy deps; `signature_count`/`signature_set` (universe∩significant gate) + `run_sweep` over the contrast × stat × thr × direction × lfc grid → typed `Sweep` (`reproducing_setting` pins the authors' *unstated* choice, or `irreproducible=True`) + the `methods_vs_numbers` `Inconsistency` when the printed number ≠ the stated method's yield; `sweep_panel` wires both onto the ledger. Codifies the manual `D:/tmp-thl/fig5-real/fig5_sweep*.py`. Supports the down-in-both intersection (the 49 case).
  - **`oracle.py`** (loop stage 7 — the R blame instrument; **validation-only ADR 0002, gated `SELOM_ORACLE=off` by default**) — `resolve_rscript` (config `r_oracle_bin` → known R-4.6 install; raises `OracleUnavailable` when disabled so the product profile degrades to `delta-unmeasured`, never crashes), `run_r` (runs a bundled template, **redirects output to a `.log`** per guard 9, raises on non-zero), `read_result` (parse `result.json`), `run_edger_signature`/`run_fgsea_terms` (gated runners), `build_oracle_result` (pure → `OracleResult` via the new `oracle_agreement`), + a dev CLI (`python -m oracle edger|fgsea`). **`oracle_templates/{edger_signature,fgsea_terms}.R`** = the dogfood scripts generalized to read argv + emit `result.json`. Design split: **running** R is gated+isolated; **parsing + verdict** are pure → the fast suite needs no R.
  - **`reproduction.py`** += typed **`Sweep`/`SweepCell`** (flipped `Ledger.sweeps: list[Sweep]`), **`oracle_agreement`** (turns oracle-vs-golden-vs-Selom into the two booleans `assign_blame` reads, reusing `classify_metric` — engine-sensitive counts declare a wide `close_tol` per D4/RISKS #10, no new heuristic), **`revalidate_panel`** (re-runs blame from a panel's stored run once an oracle lands → upgrades `delta-unmeasured`). **`config.py`** += `oracle`/`r_oracle_bin` + `oracle_enabled`.
  - **VERIFIED LIVE (gated dev CLI, real data):** `edger_signature.R` on **GSE293982** (`C:/Temp/selom-geo/GSE293982/...tsv.gz` + scratch `universe.txt`) → universe **1133** / tested 15927 / signature.adj **19** / signature.raw **125** / down-both **13** — reproduces `edger_oracle.log` EXACTLY ⇒ edgeR (authors' tool) misses golden 181/78/49 at the stated adj-p ⇒ **paper-irreproducible** assigned automatically. `fgsea_terms.R` on the real `D:/tmp-thl/fig6-real/ranked_Rod{1,2,3}.rnk` + `c5_go.gmt` → Rod **62/95/85** + `all_three` **0** — matches `fgsea_oracle.log` EXACTLY ⇒ **engine-delta** (fgsea ≈ paper, gseapy 24/36/1 ≪; RISKS #10) + **upstream-delta** (52-core unreachable even with fgsea). Smoke outputs in scratch `D:/tmp-thl/r3-oracle-smoke{,-fgsea}/`.
  - **Tests:** `tests/test_reproduction_sweep.py` (count gate, find-unstated-setting, prove-irreproducible = Fig 5 shape, down-in-both intersection, ledger wiring) + `tests/test_reproduction_oracle.py` (gating off-by-default, result parse, + the dogfood blame verdicts: Fig 5 → paper-irreproducible, 6E count → engine-delta, 52-core → upstream-delta, raw selom-bug vs engine-delta split). **pytest 231 (+12); ruff clean** (EDR-workaround uv-3.12 interp). Library-only (NO HTTP — internal-first D12; R5/FE deferred). No dev servers started. **Commits (session 14): docs `9f35398`+engine `e69d695`+handoff `b676821` PUSHED; then `feat(backend)` R3 + `docs(reproduction)` spec + `docs(handoff)` this — see git log.** **NEXT = R4** (extraction subsystem X1: PDF → panel inventory + methods digest + golden-target table; `papers.py` + AI gateway, PDFium — removes the manual front-half) **or** drive the REAL RPGRIP1 Fig 5/6 ledger end-to-end (both instruments now work). No new memory needed — [[selom-figure-repro-mission]] / [[selom-repro-edge-cases]] / [[selom-gsea-engine-sensitivity]] / [[selom-r-validation-oracle]] cover the substance.
- **Last (2026-06-17 19:03 +10:00, session 13) — REPRODUCTION ENGINE: SPEC REVISED + SUB-SPEC SPLIT + BUILD PLAN APPROVED, FIRST SLICE R0+R1+R2 SHIPPED.** Owner-steered (boot prompt B): review/approve the Reproduction Engine spec, split the figure-extraction sub-spec, fold in ALL Fig 5+6 learnings, then plan the build (charter rule = present scope + save for owner review **before** coding). Did all four:
  1. **Reviewed `docs/reproduction-engine/spec.md`** (was written 02:06 mid-session-12, *predating* the codified Fig 6 learnings) against both completed real dogfoods (Fig 5 bulk GSE293982 + Fig 6 scRNA GSE293984, per `docs/rpgrip1-figrepro.md` + the 14-guard SOP).
  2. **Split the figure-extraction front-half** (PDF → panel inventory → methods digest → golden-target table; **PDFium/`papers.py`, NOT poppler/fitz** = decision E1) into a new sub-spec **`docs/reproduction-engine/figure-extraction-subsystem.md`** — two jobs (target-extraction vs reconstruction), the stage table, the typed **GoldenTarget/Inconsistency** model (the structured form of the hand-written `*_target_spec.md`), Track A (vector-faithful)/Track B (editable+SSIM), honest ceiling, `extract/` module layout, X1–X4 build phases. spec.md keeps a tight pointer + the ceiling summary.
  3. **Folded the learnings into spec.md as first-class engine stages:** (a) **guard registry** — the 14 SOP edge-case guards as named checks, each mapped to a loop stage + the blame/action it produces (D11 = a verdict is invalid until applicable guards ran; `guards_fired[]` recorded); (b) **Validation & Blame Assignment** section — blame taxonomy `selom-engine | engine-delta | upstream-delta | paper-irreproducible | structural-limit | out-of-scope`, the **R-oracle as the gated disambiguation instrument** (ADR 0002, validation-only, never shipped; product profile degrades to `delta-unmeasured`, D9) with the dogfood worked examples (Fig 5 78/181/49 → paper-irreproducible; 6E counts → engine-delta, 52-core → upstream-delta; 6D → structural-limit); (c) **hardened D5** method-substitution discipline = record the substitution + **measure** the delta vs the oracle, never assume (proven both ways: edgeR≈pyDESeq2, gseapy≪fgsea); (d) restructured the **loop 6→10 stages** (added anchor / prepare / oracle / sweep); (e) data model gained `blame`, `method_subs[]`, `Oracle`, `Sweep`, `Inconsistency`, `scope`, `totals_by_blame`; (f) updated decisions D9–D11, invariants, testing, and the proving-ground to the **actual** dogfood verdicts.
  4. **Build plan R0–R5** (phased, in spec.md): **R0** ledger+verdict+blame core · **R1** guard registry · **R2** run+validate over existing skills on Fig 5 · **R3** oracle+sweep (dev profile) · **R4** extraction X1 (removes the manual front-half) · **R5** FE read-only Reproduction view + reconstruction. **Recommended first buildable slice = R0+R1+R2** (deterministic engine over the skills that already exist, validated on Fig 5; no novel extraction needed yet).
  Docs (`spec.md` rewritten + `figure-extraction-subsystem.md` new) committed `9f35398` (`docs(reproduction):`). Owner then approved the 4 confirming Qs (internal-first v1 **D12** · findings-first scorecard **D10** · 1% rel-tol per-metric **D4** · sibling-feeds-Foundry **D13**) + the first slice → folded the answers into the spec (status APPROVED) and **built the engine**: `app/backend/reproduction.py` (R0: typed Ledger + verdict + blame procedure + findings-first scorecard + JSON load/save) · `reproduction_guards.py` (R1: 14-guard registry + pure helpers) · `tests/test_reproduction.py` + `tests/test_reproduction_guards.py` (R2 `run_panel` + 28 tests; **blame cases = the two real dogfoods as fixtures** — Fig 5 paper-irreproducible, 6E engine/upstream-delta, 6D structural-limit). **pytest 219 (+28); ruff clean** (via the EDR-workaround uv-3.12 interp). Committed `feat(backend): … R0+R1+R2` = `e69d695` (LOCAL). **NO HTTP surface yet** (v1 internal-first per D12 → R5/FE deferred); engine is library-only, driven programmatically. **Session-12 commits PUSHED (through `3da9ccc`); docs `9f35398` + engine `e69d695` + this handoff push pending owner OK** (per-push rule). No dev servers started. **NEXT = R3** (oracle runner + sweep — R edgeR/fgsea, dev-only/ADR 0002 — the blame instrument that lets the engine assign engine-delta/upstream-delta/paper-irreproducible on real data) **or R4** (extraction subsystem X1: PDF → panel inventory + methods digest + golden-target table). Memory [[selom-figure-repro-mission]] / [[selom-repro-edge-cases]] / [[selom-scrna-batch-genotype-confound]] / [[selom-gsea-engine-sensitivity]] cover the substance — no new memory needed.
- **Last (2026-06-15, session 4) — FIGURE-EDITOR IN-CANVAS REPOSITIONING SHIPPED + e2e harness (2 FE commits, NOT pushed).** Owner-queued reposition feature; presented scope first (charter rule), owner picked **"both parts, defer dendrogram."** **(1)** Plotly `edits` in `figure-canvas.tsx` (drag legend / colour bar / annotations, double-click titles + axis titles — granular flags ONLY, never blanket `editable`, which would let users drag data points = a server-side data edit). New **`lib/plotly-edits.ts`** translates `plotly_relayout`/`plotly_restyle` event keys → `/layout` & `/data` set-ops: allowlist-only (legend/annotation/shape/title/colorbar/name) so zoom/pan/autosize/resize are NEVER persisted, no-op values skipped (one undoable commit per gesture). **(2)** structured Inspector controls — Axes → tick-label side (X bottom/top, Y left/right; serves the heatmap labels-right layout); Style → Colour bar position presets (right/bottom/left) + length, shown only when `findColorbarTrace` (figure-spec.ts) finds a colour bar. **Two real bugs found while browser-verifying + fixed:** (a) a colour-bar drag emits `colorbar.x` even when the trace has no `colorbar` object → fast-json-patch threw on the missing parent and **white-screened** the editor; fixed with **ancestor-creating** set-ops (`pushSet`). (b) **react-plotly v3 only binds its `onRelayout`/`onRestyle` props on the first *componentDidUpdate*, not on mount** — so a drag on a brand-new figure silently no-op'd until the user touched a property control; `revision`-bump tricks failed (next/dynamic delays mount past the bump); **fix that works = bind listeners straight onto the graph div via `onInitialized`+`onUpdate`** (both fire right after `Plotly.react` resolves), idempotent (`removeListener` then `on`). **Tooling** (owner: "you need to create a helper… make it a rule") `48f3d2a`: a dev `?demo=<skillId>` deep-link (mock-only; `project-workspace.tsx`) that lands you on a live editable figure in ONE step + a **Playwright** regression harness (`npm run test:e2e`; `playwright.config.ts` uses **system Chrome `channel:"chrome"`** so no Chromium download; MSW webServer on :3011; `e2e/figure-gestures.spec.ts` encodes both bugs) — **2/2 green** on a clean Playwright-managed server (no HMR flakiness). `e2e/`+`playwright.config.ts` excluded from the app tsconfig; report/output gitignored. Verified in a real browser (chrome-devtools MCP, desktop): first-touch legend drag + trace rename persist + survive re-render, colour-bar gesture no crash + Colour bar section appears, tick-label side + colorbar presets commit, undo/redo peels edits one at a time, console clean. tsc + `next build` (5 routes) + e2e all green. **Commits `81af08a` (feat) + `48f3d2a` (test/helper) — NOT pushed; `main` ahead of origin by 2.** Memory updated: [[selom-figure-editor-architecture]] (shipped + the react-plotly gotcha + verify-with-the-harness), new RULE [[step-back-build-helpers-when-stuck]]. **Also ran 3 Prism UI/UX research subagents** (workspace-IA · graphing-editor UX · analysis/guided-trust) — full reports captured this session; **synthesis awaits owner triage**, top candidates listed in NEXT below. **NEXT (owner direction 2026-06-15 — NEW PROCESS):** the 3 Prism pillars are each their own **phase**, because each changes the *experience* of using Selom (not shallow feature-copies). Each phase = **deep research → a thorough owner INTERVIEW → design → a spec saved to `docs/<phase>/spec.md` → a plan**, BEFORE building. Pillars (sequential, order TBD in interview): **(1) liveness & lineage** (hot-linked data→skill→figure→publish · stale/re-run · freeze · lineage rail), **(2) direct-manipulation editor** (dbl-click-element→Inspector-tab · significance brackets w/ snap · "Magic" apply-style-to-N · color schemes vs per-series · color mapping — extends the session-4 drag work), **(3) guided trust** (data-shape-driven Skill Store · per-skill Analysis Checklist with auto-checked vs user-attested tiers — extends publish-confidence). **First, next session: complete the current figure-editor phase** — push the 3 unpushed commits + tidy loose ends (journal-styles undo-label polish · parked export preview) — **then start the pillar process with Pillar-1 research + the owner interview.** Owner wants me to "think more about it" fresh next session. Full 3 Prism research reports captured this session; framing + open decisions in [[selom-prism-pillar-phases]]. Present scope before building (charter rule).
- **Last (2026-06-15, session 3 cont.) — JOURNAL STYLES v1 SHIPPED (3 commits, NOT pushed).** Owner: "push then start scoping" (pushed the export commits → `main` reached sync), then picked v1 = engine + built-ins + apply, applied as export-skin + **live editor preview (WYSIWYG)**. Wrote `docs/journal-styles/spec.md` (owner approved "build it" + the 5-style set). **BE** (`dd2e68f`): `skills/styles.py` registry (Style dataclass + 5 packs; `selom` = today's tokens verbatim → golden-pinned byte-identical) + `theme.apply(spec, skill_id, style=)` refactor (threads the style, re-application-safe) + `GET /figures/styles` + `POST /figures/style/apply` + optional export `style` param. pytest **166** (+10; goldens unchanged) + ruff. **FE** (`ae221c6`): `lib/styles-api.ts` + a Figure-tab toolbar `Style ▾` picker that restyles the live figure as ONE undoable JSON-Patch edit (`replace /data` + `/layout`); export menu's Style slot reflects the active style; MSW mock. **Owner UX feedback mid-build → export popover focus treatment:** page dims+blurs behind the popover but the **figure artboard stays crisp** above the scrim (lifted via `EditorWorkspace elevated` ↔ `ExportMenu onOpenChange`; figure z-45 < popover z-50); style dropdown intentionally un-scrimmed. Browser-verified at desktop (Grayscale live restyle; undo reverts the figure; figure crisp under scrim; console clean). spec `2fe1e20`. **Known v1 gap:** undo doesn't rewind the picker *label* (figure is correct) — fix = stamp style id into the spec. **NEXT — journal-styles Phase 2:** Style **Store** (browse/install) + **import-your-own** + **capture-current-figure-as-a-style**; **Phase 3:** workspace default style; **parked:** export **preview** (figure shows the chosen journal crop/aspect). Memory: [[selom-journal-styles-feature]] (v1 shipped). Other open backlog: figure-editor in-canvas repositioning; arq Redis; B8 deploy image (Kaleido's only Docker touch).
- **Last (2026-06-15, session 3) — JOURNAL FIGURE EXPORT (B4) SHIPPED via Kaleido, NO Docker (2 commits, PUSHED).** Owner opened the Kaleido window + authorized Docker/WSL "only when you absolutely have to". Probed first → **Docker was never needed**: Kaleido v1 drives the already-installed Windows Chrome over CDP (verified with a `uv run --with kaleido` smoke render of PNG/SVG/PDF before building). **BE** (`c963de6`): `export.py` (journal `PRESETS` Nature/Cell/Science/PLOS = column-width + DPI; pure `resolve_dimensions` math — mm→css-px, DPI→Kaleido `scale`, retina 2× for "fit"; lazy-kaleido `render` → clean `ExportUnavailable`→503) + `POST /figures/export` (off-thread via `asyncio.to_thread`, binary download) + `GET /figures/export/presets` in `main.py` + a light `export = ["kaleido>=1.3"]` extra (omics now its superset). pytest **156** (preset math always runs; real PNG/SVG/PDF skip when Chrome absent) + a **live uvicorn smoke** (all 3 formats real-rendered, correct download headers). **FE** (`87dfb02`): `lib/export-api.ts` (presets + blob download) + `components/figure/export-menu.tsx` (Export popover: PNG/SVG/PDF segmented × journal size `Select` + disabled **"Style" slot** = forward-compat for journal styles) wired into the Figure-tab toolbar + MSW mock (`mocks/export-fixture.ts`). tsc + `next build` clean; **browser-verified** at desktop (mock :3010) — open → SVG toggle updates action/note → Export → `POST /figures/export` 200, console clean. Killed both dev servers. **NEXT (owner-sequenced): JOURNAL STYLES** — installable/importable visual style packs (refactor `theme.py` → named style registry; `style` param on export + live editor preview; cite-don't-gate + open-font mapping). Memory: [[selom-journal-styles-feature]], [[ask-before-docker-wsl]] (Docker NOT needed for this feature — only the B8 deploy image). Also still open: figure-editor in-canvas repositioning (prior session's queued item); arq Redis; B8 deploy image (where Kaleido's only Docker touch lives).
- **Last (2026-06-15, session 2) — OWNER-PICKS ④②③①⑤ + heatmap row-dendrogram SHIPPED & PUSHED.** Owner picked the 5-item dogfood backlog (order ④→②→③→①→⑤), presented per-item scope first (charter rule), then ran it. **④** `scorecard` `invert_metrics` — lower-is-better metrics (e.g. `off_target`) inverted so higher always reads as better; surfaced dogfooding the real Hani benchmark CSV (`37a2087`/`7bc4cb1`). **②** `enrichment` `direction=split` — up/down genes scored by ORA separately → diverging dotplot (Suppl Fig 6/5C); refactored to a shared `_ora`, combined path byte-identical (`e1e4f4e`/`1afcc75`). **③** `proteomics_de` `stats=moderated` — limma-style empirical-Bayes variance shrinkage (Smyth 2004 fitFDist + trigamma-inverse Newton solve; numpy/scipy, no R); on the real JEV proteome (5v5, 2180 proteins, FDR≤0.05) moderated calls **32** vs Welch **14**, and on a synthetic n=3 matrix recovers 8 true DE where Welch recovers 0 (`b42adad`/`4dae816`). **①** JEV **Fig 1c** (miRNA DE, ST1×ST2), **Fig 5D** (EV-mediator proteins, ST10×ST5) + **Fig 4d** (top-50 DE proteins) heatmaps via the existing `heatmap` skill, all with **row dendrograms**, wired into the JEV deck + browser-verified (artifacts in external `D:/selom-data/jev`, no repo change). **⑤** `annotate` **`pbmc`** panel — 8 canonical immune types, all resolved on `data/pbmc3k_raw.h5ad` across 11 clusters (`c5164be`/`3d5044d`). **NEW heatmap `dendrogram=row`** (owner figure-review request): draws the SciPy clustering tree (tree LEFT / labels RIGHT via `yaxis.side` / small horizontal z-score key at the bottom — paper-faithful); `theme.py` now preserves `categoryorder`/`categoryarray`/`side` axis keys (it had silently dropped them — the categoryorder fix also corrected `markers`/`upset` ordering) (`941b7b0`/`f145820`/`56f4364`). pytest **143**; ruff + tsc + `next build` (5 routes) clean. **11 commits PUSHED; `main` ↔ `origin/main` in sync.** **NEXT SESSION (owner-queued — plan + build): figure-editor in-canvas repositioning.** `figure-canvas.tsx` does NOT enable Plotly `editable`/`edits` today (edits flow only through the property panel as JSON-Patch). Plan: (1) **quick win** — enable Plotly `edits` (drag legend/colorbar/annotations/axis-titles + resize) and persist moved positions via `onRelayout`→JSON-Patch (undo via existing history); (2) **structured controls** — property-panel knobs for the non-draggable layout: label side (L/R), dendrogram on/off + side, colorbar orientation/position presets. Memory: [[selom-jev-reproduction-plan]] (Fig 1c/4d/5 + dendrograms done), [[selom-figure-editor-architecture]] (reposition plan).
- **Last (2026-06-15 02:29) — C–G BACKLOG SHIPPED & PUSHED + first graphify semantic pass.** Owner: "do A, B, then C-G." **(A)** Ran graphify's **first LLM semantic pass** — clean **3,726-node** graph (`.claude`/`.impeccable` vendored tooling excluded via tracked `.graphifyignore`; owner reversed the prior "keep it" call after seeing the god nodes were dominated by impeccable internals like `modern-screenshot.umd.js`); also found the AST commit-hook had been silently *refused* (node-drop guard) so `graph.json` was stale — refreshed with `force=True`. Pushed the held Hani batch → `main` reached sync. **(B)** Browser-verified the 4 Hani Store cards at mock :3010 (correct skill-specific metadata; console clean bar the favicon 404). **(C–G, 10 commits, PUSHED):** **C** `scorecard` `radar|heatmap` layout (`01751d6`/`cddde44`); **D** `annotate` **`retinal_cepo`** Cepo panel + **Microglia** class (`b07f311`/`9e38cb3`); **E** **Ensembl→HGNC** ingest relabel (`b72a5a6`); **F** **`sankey`** (`765f682`/`9d6fba9`) + **`string_network`** live STRING PPI (`f1e9c99`/`5c61f0b`); **G** `proteomics_de` synthetic-DE-recovery test (`f76ce2a`) + a `trajectory` real-Hani smoke (10k cells/PAGA). **`flow_cytometry` DEFERRED** (owner: FlowJo dominates / likely in ClawBio community). pytest **136**; ruff + tsc + `next build` clean. **22 verified skills.** **NEXT (owner picks):** Fig 1c / Fig 5 JEV heatmaps via existing `heatmap`; an `enrichment` up/down-split option; `scorecard` heatmap on the real Hani benchmark CSV; promote more mmc2 panels; a `proteomics_de` moderated/limma stats MODE (not just the test) for better small-N power; Kaleido export (Docker-gated — ASK). Memory: [[selom-jev-reproduction-plan]] updated (string_network done, flow_cytometry deferred); [[graphify-keep-current]] updated (semantic pass done + `.claude` excluded).
- **Last (2026-06-15) — HANI PAPER BATCH: 4 new skills + ingest fix (5 commits LOCAL/UNPUSHED).** Owner dropped a 2nd real CMRI paper (Kim et al. 2023; `…/Desktop/Claude code and website tips/Data/Hani`); recon via 2 subagents → built **`corr_heatmap`** + **`upset`** (`d2237a7`; upset ingests the mmc2 boolean marker matrix) · **`scorecard`** radar + **`normalization_qc`** QC-violin panel (`2b0785a`) · FE Store seed + MSW mocks (`c8dcedb`, `4a15ee0`). **Ingest capability** `skills/_genes.read_anndata` (conservative genome-prefix strip + dedupe, `bd736fd`) wired into all 8 scRNA real engines — surfaced from the `GRCh38_` symbol prefix on **GSE201356** (Selom's first GEO pull / 10x-mtx ingest; staged `D:/selom-data/hani`, incl. `processed/hani_irpe_subset.h5ad` = 10k rod-dominant organoid cells). All validated on real Hani data (full single-cell chain + ingest fix end-to-end + mmc2 enrichment→cilium terms). EAMOS recon (`D:/eamos`) = nothing to copy (clinical-variant-grade, off-thesis). pytest **118**; ruff clean; FE tsc + build green. **NEXT (owner picks):** (1) **push** the 5 commits; (2) browser-verify the 4 new Store cards at mock :3010 (desktop); (3) `scorecard` heatmap-layout variant (Fig 6F's other form); (4) promote mmc2 → a `retinal_cepo` `annotate` panel + add a **Microglial** type (cite Kim 2023); (5) Ensembl-ID→symbol ingest mapping (sibling of the prefix fix); (6) backlog — Sankey/alluvial skill, JEV `string_network` / `flow_cytometry` (memory `selom-jev-reproduction-plan`).

- **Last (2026-06-11 21:56):** Re-oriented from the research vault (Me.md · Business/selom.md · Selom/Wiki
  semantic-index + selom-build-kickoff + build-ledger + SESSION). Confirmed the staged goal (personal tool →
  EAMOS engine → SaaS) and pinned the **core decision = publish-confidence** ("is THIS the right, trustworthy,
  reproducible figure to put in my paper?"). Presented a **build charter (B0–B9)** that reorders the P0–P8
  ledger to honour both, folding already-built work in as done; **owner APPROVED it → filed to `docs/build-charter.md`.**
  **Ratified the 5 pending build [DECISION]s** into `DECISIONS.md`: #5 custom figure editor · #6 arq+Redis
  (BE-lane, flagged to Codex) · #7 web-first · #8 trade-secret IP (repo = `D:/selom` was already #2). Then presented the **Bucket-1 plan** (close the P0 gate) for owner review — not started. The
  charter + deep ADRs `0004–0007` still need mirroring into the vault by the wiki agent (read-only here).
- **Prior session:** Designed + documented the **command-center expansion** and scaffolded its **frontend shell**
  (mock-first). Docs (keystone): `docs/command-center/design.md` — research synthesis (bioSkills 540 +
  ClawBio 88 + Hermes gateway), project-first IDE architecture, Skill Store catalog model (hybrid-tiered
  Verified/Community), guided-intake flow, auto-clean/QC, **execution toolchain §6.5** (what to install to
  run skills: clone+ingest → `[omics]` runners → `clawbio`+Miniforge → bioconda long tail → Docker
  sandbox), and phased plan **C1–C3 (FE) / B1–B4 (BE)**. Framework locked into PRODUCT.md, README.md,
  ROADMAP.md (C/B phases + toolchain), plans/v2-frontend.md (C1–C3), and CURRENT.md → Cross-Agent Requests
  (backend spec for Codex). Memory: added `selom-command-center-architecture`. North star (owner): all 500+
  skills runnable via the Skill Foundry (manual now → LLM-assisted = v2 Extract-Skills moat).
  FE scaffold (`app/frontend`, mock-first, **no backend dependency**):
  - **Data layer** — `lib/projects/{types,store}.ts` (localStorage `ProjectStore` via `useSyncExternalStore`,
    schema-aligned to the planned Supabase tables), `lib/catalog/{types,seed}.ts` (~32-skill seed standing in
    for the full ~600), `lib/intake/mock.ts` (adaptive questionnaire + deterministic `IntakeProposal` + QC).
  - **Shell** — `components/shell/{app-shell,sidebar}.tsx` (project-first rail + header; wraps every route via
    root `layout.tsx`), `components/ui/badge.tsx`.
  - **Screens** — `app/page.tsx` (Home dashboard, replaces the old single-surface flow), `app/store/page.tsx`
    + `components/store/*` (Skill Store: browse/filter/install, Verified/Community tiers, honest coverage
    meter), `app/p/[id]/page.tsx` + `components/project/*` (workspace: Overview/Data/Workbench/Figure),
    `components/intake/*` (questionnaire + proposal plan). The existing figure editor is **reused unchanged**
    as Project ▸ Figure (run a skill → `runSkill` MSW mock → editable Plotly figure).
  **Verified:** `tsc --noEmit` clean + **`next build` clean** (all 4 routes compile/type-check/prerender).
  Live browser click-through was blocked by a stale Chrome profile lock (env, not code); mock dev server is up
  on **:3001** (`npm run dev:mock`; :3000 already in use). **Committed `afaafd2` + pushed**; `main` ↔
  `origin/main` in sync (the push also carried `e63f38b`/`5c4a1fd`). Removed orphaned
  `components/app/top-bar.tsx` + `components/upload/upload-hero.tsx` (superseded by the new shell).
- **B1 (done 2026-06-11):** P0 hello-UMAP closed end-to-end — real scanpy UMAP behind the one-shot
  `POST /skills/umap_scrna/run`, browser-verified; pushed (`main` @ `7d58e92`). See git history `6e37829`/`396ca66`.
- **Last (2026-06-12 00:44) — FRONTEND AUDIT + B2 DONE locally (NOT pushed).**
  - **Frontend audit** (all 3 skills): ran `/impeccable init` → wrote **`DESIGN.md`** + `.impeccable/design.json`
    sidecar (captured the existing dark-IDE token system). Audited Home / Store / project tabs / intake /
    figure editor across ui-ux-pro-max + frontend-design + impeccable, **browser-verified on the live app**
    (mock mode, :3010). Fixes applied (commit `506d1e6`): removed the banned skill-card left side-stripe; made
    the skill-card detail keyboard-operable (`role=button` + Enter/Space + focus ring); skill-detail modal focus
    management; bumped a low-contrast meta line; friendlier run-error copy (no raw "HTTP 500"); rail collapses
    to a drawer below `lg` so the **desktop** app degrades gracefully in narrow/split windows. The FE was already
    strong (landmarks, focus rings, role=dialog/alert, tabular nums, guardrails-first IA, light artboard).
  - **B2 — all 6 wedge skills** (`feat(backend:)` `eded0db`): `cluster · violin · deg · volcano · heatmap ·
    enrichment`, each `skills/<slug>/{skill.json, run.py (dep-free stub) + run_real.py}`, dispatched by
    `SELOM_SKILLS_ENGINE` (auto/stub/real) via new `skills/_engine.py`; all emit the editable plain-array Plotly
    spec via the shared **`skills/_plotly.jsonable`** (factored out of run_scanpy; umap now reuses it). DEG does
    scRNA `rank_genes_groups` + bulk pyDESeq2 (CPM-log2FC fallback when pydeseq2 absent). **Enrichment = in-house
    hypergeometric ORA + BH FDR** against a bundled GO/Reactome gene-set sample (`gene_sets.json`) — NO
    gseapy/MSigDB (DECISIONS #9). Golden-image tests `tests/test_skills_golden.py` (+ `tests/golden/*.json`, regen
    via `tests/regen_golden.py`) — `python -m pytest` = **14 passed**; real engines smoke-verified on synthetic
    h5ad/CSV (all 8 paths). `main.py` generalized: run-endpoint takes any Verified skill (query-string params +
    preserved upload suffix). FE `selom.violin` entry added (`feat(frontend:)` `d1b9533`; coverage → 14/628).
- **Last (2026-06-12 01:48) — B3 DONE + integration quick wins; ALL PUSHED (`main` @ `5138aec`).** Pushed
  the 7 prior commits first, then landed B3 in 4 scoped commits:
  - **`021af8c` `feat(backend)` — live `GET /skills` + async jobs API.** Registry: `skills/registry.py` maps each
    `skill.json` (+ a new optional `catalog` block on `SkillSpec`) to the FE `SkillCatalogEntry` shape; `GET /skills`
    returns the live Verified set. Jobs: `POST /skills/{id}/jobs` → `GET /jobs/{id}` (poll) / `/events` (SSE,
    `text/event-stream`) / `/result` ({figure}, same shape as `/run`). New modules `config.py` (pydantic-settings),
    `jobs/{store,queue,worker}.py`, `storage/results.py`. **One `execute_job`** is shared by the inline executor and
    the arq worker. Default = **inline + local filesystem result store** (zero infra); `SELOM_QUEUE=arq`+Redis and
    `SELOM_R2_*`+R2 (boto3 + RISKS #5 checksum fix) are **wired but dormant**. New optional `[jobs]` extra.
  - **`5b38f9e` `feat(backend)` — heatmap hierarchical row-ordering** (scipy correlation-distance + average linkage,
    real-engine only; stub/golden unchanged) **+ dropped `gseapy`** from `[omics]` (DECISIONS #9 / RISKS #6/#9; uv re-locked).
  - **`5138aec` `feat(frontend)` — registry-driven Store.** `lib/catalog/registry.ts` fetches `/api/skills`, merges live
    Verified over the seed's Community tail, falls back to seed offline; `CoverageMeter` shows the live count; MSW mocks
    `/api/skills`; seed `enrichment` relabelled MIT (DECISIONS #9).
  - **Verified:** `pytest` 21 passed; ruff clean; live uvicorn smoke (skills/submit/poll/result/SSE) green; FE `tsc` +
    `next build` clean; browser (mock :3010) — `/api/skills` 200, meter live **13/~628**, 32 shown, `proteomics_volcano`
    dropped, console clean (only an unrelated favicon 404).
- **Last (2026-06-12 02:45) — B4 publish-confidence slice-1 DONE (both lanes); COMMITTED locally, NOT pushed** (`main`
  ahead 2 of `origin/main` @ `4216935`). Two scoped commits:
  - **`2ab7c43` `feat(backend)` — reproducibility bundle + auto methods-text.** New `provenance.py` (per-figure bundle:
    skill+version, resolved+typed params, input **SHA-256**+size, environment = python/platform/engine-policy + scientific-stack
    versions via `importlib.metadata`) + `methods.py` (deterministic per-skill templates quoting the exact params + canonical
    citations — Scanpy/Leiden/UMAP/PyDESeq2/DESeq2/BH/GO/Reactome/SciPy; generic fallback). `contract.resolved_params()` does
    typed coercion (runners untouched). **Additive wire change:** `POST /skills/{id}/run` + `GET /jobs/{id}/result` now return
    `{figure, provenance, methods}` (same shape both paths); jobs thread the upload `filename` (new `Job.filename`) + store the
    full bundle via the shared `execute_job`; `result_store.put` param renamed figure→payload. `pytest` **28 passed** (21 + new
    `test_provenance.py` + `test_methods.py` + jobs-bundle asserts); ruff clean; live uvicorn smoke green.
  - **`84ae54e` `feat(frontend)` — "Publish confidence" panel.** `components/project/publish-confidence.tsx` (collapsed by
    default, in the Figure tab between toolbar + editor): **Methods** (prose + Copy[text+numbered citations] + citation list) +
    **Reproducibility** (skill v, param chips, input filename/size/sha, env Python + package chips). `runSkill` now returns the
    full bundle (`SkillProvenance`/`SkillMethods`, both optional); `project-workspace` captures it on run + clears on "New figure";
    MSW mock serves a representative bundle (`mocks/stub-bundle.ts`). `tsc` + `next build` clean (4 routes); **browser-verified**
    (mock :3010) — ran umap_scrna, panel expands with methods + repro record, Copy present, `POST /run` 200, console clean (only the pre-existing favicon 404).
- **Next (finish B4, then integration backlog). B4 remaining:** (a) **statistical-guardrail expansion** — silhouette already on
  `cluster`; add batch-effect / normalization / multiple-testing / low-cell warnings surfaced in the bundle (a `guardrails` field
  or figure subtitle), (b) **Kaleido journal export** — PNG/SVG/PDF presets; needs **Docker + system Chromium** (RISKS #2 / DECISIONS #7),
  so it lands with the deploy image (B8-adjacent) — scope a CPU-Docker path or defer. Integration backlog (`plans/v2-backend.md` →
  `docs/integrations.md`): **OmicVerse isolated worker** (RISKS #9), **arq cross-process job *status*** (Redis-backed JobStore — `jobs/worker.py`
  caveat), R4DS/Quarto + R-oracle (RISKS #7), full GO/Reactome GMT, catalog-count true-up (≈418 via a live ingest job), OmicVerse/JARVIS MCP
  (only AFTER its isolated env). `plans/v2-frontend.md` P0 still says two-step upload.
- **Last (2026-06-13) — pushed + research/planning, no lane code.** Pushed the held B4 slice-1 (BE `2ab7c43` + FE `84ae54e` +
  handoff `3d76bf1`) → `main` ↔ `origin/main` in sync. Filed the **OmicsBox competitor teardown** (`docs/competitors/omicsbox.md`,
  `6a139ad`); threaded its **P1→P4 skill priorities** into `docs/build-charter.md` as a prioritization lens (`7c3189e`); landed
  **DECISIONS #10** (skill-scope bet — win the editable-figure last mile + own the proteomics/metabolomics whitespace, don't chase
  upstream NGS). **Model switched off Fable 5 → Opus 4.8 (xhigh)**, owner-directed (`267ab50`; pinned in `.claude/settings.json`,
  applies on session restart). No app/ code touched (research/planning).
- **Last (2026-06-13) — B4 slice-2 (statistical guardrails) DONE, both lanes; committed, push pending.** New bundle-side
  `app/backend/guardrails.py` adds a `guardrails` list to every `/run` + job result: method/param checks (multiple-testing note
  + lax / no-FDR-filtering warnings for deg/volcano/enrichment) + best-effort `.h5ad` data checks (low-cell, pre-normalized-input,
  uncorrected-batch; backed read, handle released so the Windows temp-unlink works; CSV/stub skip cleanly). Cluster silhouette
  stays in the runner. FE: a **"Quality checks"** section in the Publish-confidence panel (warnings first, level icons) + an amber
  warn-count header chip; `runSkill` / bundle state / MSW mock all thread `guardrails`. `pytest` **40 passed** (28 + 12); ruff clean;
  real-engine smoke fired low-cell+batch / pre-normalized / clean-raw correctly; FE `tsc` + `next build` clean; browser-verified
  (mock :3010 — bulk DEG run → Quality checks shows the multiple-testing check, console clean). Commits BE `edd3615` + FE `56da2a6`;
  charter B4 updated. **Open: push the 3 local commits (+ this doc) on owner's word.** Kaleido export is the only B4 remainder
  (Docker+Chromium — RISKS #2; ASK OWNER before any Docker/WSL — see memory `ask-before-docker-wsl`).
- **Last (2026-06-13) — real RPGRIP1 data ingested + dogfooding caught a guardrail bug.** Owner re-interview set the
  direction: **build P1→P4**, **keep the staged order** (dogfood-first), validate on real data. The real data
  (`\\cmri.com.au\…\RPGRIP1_Hani`, owner-provided) is **ShinyCell exports**, not h5ad. Built `scripts/shinycell_to_h5ad.py`
  (dev tool; MIT pure-Python `rdata`, no R / no AGPL) → merged `.h5ad` (83,659 cells × 33,538 genes; 9 samples WT/C3/PT/FS;
  log-normalized) at `D:/selom-data/rpgrip1/processed/` (raw bundles copied to `…/raw/` as a share-loss hedge; the whole
  `D:/selom-data` tree is OUTSIDE the repo). Extended B4 guardrails to **bulk/CSV** (`133fb79`) and **fixed a backed-sparse
  h5ad read bug** (`78afdcb`) that only surfaced on real sparse scRNA; guardrails validated live on the real data
  (pre-normalized + uncorrected-batch fire). Commits `133fb79` / `78afdcb` / `5dc9750`. **Open: push the 6 local commits on
  owner's word.** Memory: `selom-rpgrip1-real-data`. **Next:** dogfood the existing skills on the merged h5ad (WT-vs-mutant
  DEG / UMAP / cluster), then build **P1** (pathway/go-graph + deg/enrichment breadth).
- **Last (2026-06-13) — 2nd real dataset (EYG_28 bulk) reviewed + dogfooded; volcano hardened.** EYG_28 = human retinal-organoid
  **bulk RNA-seq DE-results** (RUVseq/limma; PDE6B {FS/V/pt} + RPGRIP1_cpdHet vs Control across d120–d210; 5 norm methods incl.
  TMM-K0 baseline), staged at `D:/selom-data/eyg28/raw` (110 CSVs, 283.7 MB; ~840 MB HTML reports left on the share). **No raw
  counts exported** → Selom fit = **volcano + enrichment** (NOT `deg`, which would recompute). Tool fix: broadened `volcano`
  gene-column detection (GeneID/gene_name/gene_symbol/feature) so real DE tables label with gene symbols not the row index —
  `8f832db` + `test_volcano_columns.py`; validated on RPGRIP1_cpdHet/TMM-K0 (327 up / 962 down). **Dogfooding findings → hardening
  backlog:** (1) the scRNA skills assume raw counts + recompute embeddings, so **pre-normalized** ShinyCell data (rpgrip1) needs a
  **skip-normalization / use-existing-embedding** option before honest scRNA figures (the B4 guardrail already flags the input);
  (2) a multi-contrast **DE-table import** flow would help folders like EYG_28. Memory: `selom-eyg28-bulk-data`. **Open: push the
  9 local commits on owner's word.** **Next (P1, owner-directed):** start with broaden `deg`/`enrichment` + the pre-norm option
  (no new API, fully testable), then `pathway`/`go-graph` (KEGG/Reactome/GO APIs — scope first).
- **Last (2026-06-13) — scRNA skip-normalization landed; (a) dogfooding COMPLETE (both real datasets).** Added a `normalize`
  (bool, default true) param to umap_scrna/cluster/violin/deg (+ a real `bool` cast in `contract` via `_engine.to_bool`; UMAP
  methods-text reflects whether it ran) — `normalize=false` skips re-normalization for already-normalized inputs (`7df4db6`).
  Dogfooded `rpgrip1_merged.h5ad` with normalize=false: **DEG by genotype** → sensible retinal markers (NRL / RCVRN / RHO-axis),
  **RHO violin** across WT/C3/PT/FS; figure specs saved to `D:/selom-data/rpgrip1/processed/figures/`. Bulk EYG_28 already
  dogfooded via volcano. pytest 50; ruff clean. **All session commits PUSHED — `main` ↔ `origin/main` in sync; Docker/WSL work parked in `agent_handoff/on-hold/`.** **Next (b) P1:** broaden `deg` (no-rep /
  time-course) + `enrichment` (ORA) — no new API, build now; then scope `pathway`/`go-graph` (KEGG/Reactome/GO/QuickGO/STRING
  APIs) per charter before building new skills. Remaining scRNA nicety: use-existing-embedding (plot stored UMAP/t-SNE rather than recompute).
- **Last (2026-06-14) — P1 #1 DONE: `enrichment` full GO gene-set library (owner approved Decision #1 + the sequence).**
  Built `scripts/build_gene_sets.py` (dev-time, MIT `obonet`) → `gene_sets_go.json` (gitignored, ~5.2 MB, regenerable):
  **7,727 GO sets** (BP 5576 / MF 1346 / CC 805), 21,198 genes, from go-basic.obo + goa_human.gaf (CC-BY 4.0, license-clean —
  no gseapy/MSigDB per DECISIONS #9). `enrichment._load_gene_sets` prefers it, else the committed sample. Also broadened
  `volcano` + `enrichment` gene-column detection for real DE headers (`GeneID`). Validated on EYG_28 RPGRIP1_cpdHet Sig: overlap
  **14 → 850 / 1173**, real terms (ER lumen, basement membrane, V-ATPase, brush border) vs the prior immune noise. Commits
  `f01ddbc` / `a9d16f1` / `102d9ec` / `01d32c8`; scope in `docs/p1-skills-scope.md`. **3rd dataset assessed (owner-pointed): EYG_05**
  (`…/RNA seq/Analysis/20220318_…EYG_05`) — 2022 RUV/limma, PDE6B + RPGRIP1 **mutation** contrasts (NOT time-course); raw counts
  likely in its 478 MB `.RData` (→ could unblock pyDESeq2 `deg`); ships GO/KEGG enrichment results (= an enrichment oracle).
  **Open: push 5 commits.** **Next P1 (approved sequence):** DE-table import + use-existing-embedding (quick wins) → `pathway`
  (Reactome live API; KEGG academic-only) → `go-graph` → `deg` time-course (still blocked — no count+time dataset across the three).
- **Last (2026-06-14) — P1 quick wins DONE: DE-table-aware enrichment + use-existing-embedding (`fd8b3c2`).** `enrichment`
  now derives the query from the SIGNIFICANT rows of a full DE table (new fdr/fc params) — dogfooded on EYG_28 DEGs_All
  (16,760 → 6,266 sig@FDR0.05 → real GO terms). `umap_scrna` gained an `embedding` param to plot a stored UMAP/t-SNE with no
  recompute — dogfooded on rpgrip1 (`X_tsne_all` → the ShinyCell **retinal cell-type** t-SNE: Rods/Cones/Bipolar/Amacrine/Glial),
  saved to `…/rpgrip1/processed/figures/`. pytest 53; ruff clean. **Open: push 1 commit.** **Next P1 (bigger, NEW skills):**
  `pathway` (Reactome ContentService live; **open design Q** — Reactome pathway diagrams aren't natively editable-Plotly, so the
  render representation needs a decision) → `go-graph` (GO DAG node-link via obonet+networkx — cleaner to render). `deg`
  time-course still data-blocked; EYG_05's 478 MB `.RData` raw counts could unblock pyDESeq2 `deg` if extracted.
- **Last (2026-06-14) — `go_graph` skill DONE (`835f50e`) + ALPK1 datasets scouted & verified.** New **`go_graph`** draws the top
  enriched GO terms as an editable **DAG node-link** (ORA → transitive-reduced is_a/part_of hierarchy → networkx layout → Plotly);
  `build_gene_sets.py` also emits `go_dag.json` (gitignored). Validated on EYG_28 RPGRIP1 Sig (20 nodes / 10 edges); pytest 55; ruff
  clean. **3 ALPK1 folders scouted (subagents) + headline claims self-verified** → cataloged in **`docs/real-datasets.md`** (memory
  `selom-real-datasets`): biochemical = phospho-proteomics n=1 (not a fit; future proteomics fixture); single-cell = downstream-only
  (no cell object shared; 201 MB Seurat DEG CSV → volcano/heatmap/enrichment, no UMAP/cluster); **bulk = goldmine — the mouse
  P14/P30/P90 set has raw counts (verified 32,285 × 77 integer) + an 18-contrast DE table over 3 timepoints → UNBLOCKS `deg`
  time-course AND raw-count pyDESeq2.** NB real bulk data is `.xlsx`; skills read CSV (openpyxl now in the venv; xlsx-ingestion in
  skills is a follow-up — EYG29 has CSV raw-count pairs that work today). **Open: push `go_graph` + these docs.** **Next P1:**
  `pathway` (Reactome, approach A — last P1 skill) OR pivot to the now-unblocked `deg` raw-count / time-course on the mouse data.
- **Last (2026-06-14) — `deg` raw-count pyDESeq2 + time-course + design-sheet input DONE (`8700937`); push pending.** Owner-directed "option 2".
  pyDESeq2 was **not actually installed** (bulk DEG had only ever hit the CPM fallback) → installed it surgically (`uv pip install pydeseq2`,
  pandas-3 compatible, NOT the multi-GB `[omics]` stack). **Bulk:** robust group inference (strip trailing replicate suffix, replacing the
  naive `split("_")[0]` that mislabelled real names) + explicit `reference`/`treatment` contrast selection (clear error listing groups when
  ambiguous) + low-count filter + ≥2-rep guard; pyDESeq2 Wald is now the real path. **Time-course** (`mode=timecourse`): time as a continuous
  covariate, Wald-test the time coefficient (0.5.4 has **no LRT**; numpy unit-contrast vector over the design matrix), optional covariate
  adjustment + stratum filter; figure = per-gene log2-CPM trajectories. **Design sheet = first-class input:** optional `design` upload on
  `POST /run`, joined on sample id (overrides name inference; QC-dropped count cols excluded); threaded as reserved `_design_path`, filtered
  out of provenance/methods via `resolved_params` dropping `_`-keys. xlsx counts/design now read via **openpyxl (added to core dep + re-locked)**.
  **Found the mouse design sheet** (the blocker): rawCounts cols are opaque `S1…S80`; the sample→condition map was in the analysis `output/`
  as `RUV_K2_variates.tsv` (Genotype/Age/Tissue/Strain) — staged to `D:/selom-data/alpk1/mouse_P14P30P90/sample_design_RUV_K2_variates.csv`.
  **Validated on REAL data:** EYG29 AK1-vs-SCR (TTR/KLK4, n=3v3); ALPK1 mouse contrast via design sheet (xlsx; NR_P90 Homo-vs-WT, n=4v3);
  mouse time-course (39 NR samples, Genotype-adjusted, **10,458 genes FDR<0.05** across P14/P30/P90). **pytest 72** (55 + 17 new:
  `test_deg_bulk` grouping/contrast/time-parse/error-contracts/fallbacks + `test_run_design` endpoint design-upload + reserved-key filtering);
  ruff clean. **FE follow-up (not built):** a `design`-sheet upload affordance for bulk/time-course DE — backend accepts it, FE still drives
  matrix-only (column-name inference works for well-named bulk like EYG29). **Next P1:** `pathway` (Reactome ContentService, approach A — the
  last P1 skill); deg **no-replicate** (NOISeq-style) still has no fitting dataset.
- **Last (2026-06-14 03:07 +10:00) — FRONTEND REDESIGN on the owner's screenshot direction DONE (9 commits, LOCAL, NOT pushed; `main` ahead of `origin/main` by 9).** Owner fed 4 reference ads + iterated live; built via the 3 mandated design skills (impeccable / frontend-design / ui-ux-pro-max). All verified in-browser (mock `:3010`, desktop), `tsc` + console clean each step. Commits, in order: `6f61fa2` redesign (pipeline metaphor + App-Store catalog + bold accent type + breathing room + **root-cause CSS fix** — the unlayered `*` reset in `globals.css` beat Tailwind v4's layered utilities, zeroing ALL `px-*`/`m-*`/`gap-*`/`border-*` app-wide; moved to `@layer base`) · `e9decc0` animated `Dropzone` (idle/hover/dragging) + design-sheet upload wired to `runSkill(...,design)` (item **b**) + 3 apply affordances · `dba4117` Overview = entry point (empty project → drop-to-start in the pipeline box → hands file to Data tab intake; clickable stages) · `f0bbaef` inline skill params (`lib/catalog/params.ts`) + quick-apply favourites + data-focused Data tab · `56c043d` connector = static solid line (moving-dot version PARKED for a future hero page — memory `selom-pipeline-flow-animation`, recoverable from `cb813cf`) · `2deb361` before/after cleaning view (`components/project/cleaning-report.tsx`; raw vs cleaned + toggleable steps; `QcReport` gained `nObsRaw`/`nVarRaw`/`cleaningSteps`, `mock.ts` `buildQc`) + switch-thumb fix · `c627994` reserve after-cleaning delta line (no toggle jitter) · `3dd436f` done-badge = single `CheckCircle2` glyph (centered tick). **New shared FE:** `components/pipeline.tsx`, `components/ui/motion.tsx` (+ `motion`/Framer Motion dep), `components/project/dropzone.tsx`, `components/project/cleaning-report.tsx`, `lib/catalog/modality.ts` (`CATEGORY_GROUPS` + colours + icons + `humanizeCategory`). **Colour system:** card hue = analysis CATEGORY (matches its Store shelf); omics chips = DATA modality. `DESIGN.md` NOT yet updated to the evolved system (follow-up). **REMAINING of "build all four": ⌘K command palette** (the bigger one — see resume). **Open: push the 9 commits on owner's word.** Owner also flagged a preference for **Phosphor Icons** for new icon work (memory `selom-icon-library`); FE is currently all lucide-react — a Phosphor migration is its own task, not started.
- **Last (2026-06-14 15:37 +10:00) — ⌘K COMMAND PALETTE DONE (FE, committed `7285054`, NOT pushed; `main` ahead of `origin/main` by 1).** The LAST of the owner's "build all four" (guided Overview, inline params, quick-apply already done). New **`components/shell/command-palette.tsx`** — a global fuzzy launcher over **projects · figures · catalog skills · actions**, opened from the header search (now a trigger button) or **⌘K/Ctrl-K**. a11y = combobox→listbox via `aria-activedescendant`; ArrowUp/Down + Home/End nav, Enter runs, Esc closes; focus trapped to the input and restored to the trigger on close; **tiered matcher** (label-substring > extra-substring > label-only-subsequence — keeps results on-target, no long-keyword-blob noise). New **`lib/workspace/intent.ts`** = a tiny subscribable intent bus: the palette dispatches a deep-link intent (open tab / install+pre-select a skill) that the workspace consumes whether it's about to mount (pending → mount effect) or already on screen (subscriber fires, since a same-route `router.push` is a no-op) — the workspace owns its tab state internally, not via the URL. Wired into `app-shell` (⌘K keydown + header trigger), `project-workspace` (consumes intents → setTab + `installSkill` + pre-select), `workbench-panel` (new `preselect` prop). **Owner-refined skill rows:** in a project → `Apply` / `Add & apply` / `Queued`; elsewhere → **"Installed"** + small **project-colour swatches** showing *where* it's installed (hover-title names; caps at 4 + `+N`) or **"In Store"** (Enter jumps to the owning project, else the Store). **Verified in-browser (mock :3010, desktop):** ⌘K + header trigger open it; fuzzy filter (tightened — "volcano" → only Volcano plot); kbd nav; Enter apply-a-skill installs + opens Workbench with the skill pre-selected (PBMC went 2→3 skills); swatches render (Differential expression → cyan PBMC + orange Tumor); Esc closes. `tsc` clean; console clean (only MSW + a pre-existing form-field DevTools issue, now also cleared on the palette input via `id`/`name`). **DESIGN.md refresh + Phosphor-icon migration remain separate follow-ups (not started).** **Also this session:** refreshed graphify per owner — `python -m graphify update .` (exit 0; 3677 nodes / 6629 edges / 227 communities; graph.json/html + GRAPH_REPORT.md in `graphify-out/`; palette files indexed). NB graphify warned skill 0.8.36 vs package 0.8.33 (benign; `graphify install` to align). **Open: push the 1 FE commit on owner's word. Next: BE `pathway` (Reactome, approach A — the last P1 skill).**
- **Last (2026-06-14 16:24 +10:00) — `pathway` DONE = LAST P1 skill; + biomaRt gene-col fix; + Fidelle dogfood data ingested. 3 commits LOCAL/UNPUSHED (`1a9604c`/`6ec924f`/`95b366d`; `main` ahead of `origin/main` by 3; the 2 held ⌘K commits were pushed first on owner's word).** **`pathway` (`1a9604c`):** sibling of `go_graph` — POSTs the gene list + log2FC to the live Reactome **AnalysisService** (ORA, identifier projection to human; open CC0 — no gseapy/MSigDB per DECISIONS #9), keeps the top-N pathways, fetches the Reactome **eventsHierarchy** once for parent→child edges (transitive-reduced), lays them out as an editable Plotly node-link **coloured by mean log2 fold change** (explicit diverging scale: blue=down, red=up). Resolved the OPEN design Q this way (one POST + one hierarchy GET; stdlib `urllib`, no new dep; real deps pandas+networkx already present). Dep-free deterministic stub for the golden test; real engine raises a clear error on a Reactome network failure (never fakes). `methods.py` `_pathway` template; auto-surfaces in `GET /skills`. Real engine smoke-verified on a synthetic ciliopathy DE table (phototransduction blue ~-2.2, cell-cycle red ~+1.9, correct hierarchy edges, clean JSON). **Gene-col fix (`6ec924f`):** dogfooding pathway on the real ALPK1 RO DE tables exposed that every DE skill picked the composite `GeneID` (`ENSG…~SYMBOL`, unmappable) over the clean `external_gene_name` → empty figures; now `volcano`/`enrichment`/`go_graph`/`pathway` prefer clean symbol columns over id columns (+ a regression + drift-guard test). pathway went **0→16 nodes** on the real data. **Data (`95b366d` docs + `D:\selom-data`, outside repo):** 4 CMRI share folders scouted (4 subagents + self-verification); staged the small/new subset (~75 MB; left ~13 GB TIFFs + MSigDB `.gmt` on the share): `alpk1\ro_irpe` (EYG_05 RO DE time-course + curated gene lists + IPA/Reactome oracle), **`alpk1\irpe_rawcounts` (EYG_08 iRPE RAW COUNTS + derived design sheets + DE oracle — FIRST human raw counts; `deg` pyDESeq2 dogfooded: ALPK1 n=6 vs Control n=3 d311, 21,173 genes, sensible hits)**, `rpgr` (NEW gene: RPGR iRPE raw counts + design + RO GSEA/GO oracle + custom ciliopathy/proteostasis gene lists — **dogfood only, licensing TBC before shipping**), `alpk1\mouse_proteomics` (**new proteomics modality**) + `alpk1\mouse_enrichment_oracle`. pytest **76**; ruff clean. **Open: push the 3 commits on owner's word. Next: dogfood `deg` on RPGR + ALPK1 d207, `volcano` on the mouse proteomics + RO DE time-course, `enrichment`/`pathway` vs the staged GSEA/GO oracles (numeric cross-check); FE follow-ups (DESIGN.md refresh, Phosphor migration); or owner direction.**
- **Last (2026-06-14 19:02 +10:00) — GENE-SET BUILDER PHASE A SHIPPED (both lanes; owner-approved: top-level surface + enrichment & volcano-highlight apply).** **BE `c7ce724`:** `gene_sets/library.py` (unified loader + tiered search over GO · WikiPathways · curated; `load_collection` = ORA library by source) + `GET /gene-sets[/{id}]`; **WikiPathways CC0 ingest** (`scripts/build_wikipathways.py` — GMT Entrez→HGNC via NCBI gene_info → committed `gene_sets/corpus/gene_sets_wikipathways.json`, **921 sets / 8,999 symbols / 0 unmapped**) + 2 owned `gene_sets_curated.json` panels; apply = `enrichment` `gene_sets` source-select (go/wikipathways/curated/all; old `GO_Reactome` default still → GO) + `volcano` `highlight` panel (`_assemble` byte-identical when no panel → golden unchanged). **FE `c5f9fbe`:** top-level `/gene-sets` (search · color-coded source filter · card grid · members dialog), first-class **`GeneSet`** in ProjectStore (save/remove/select, migration-safe for pre-geneSets state), intent-bus apply (Highlight-in-volcano / Enrich-against-source) with param prefill (new `select` ParamControl + enrichment/volcano param schemas) + Save/saved-strip; sidebar nav + MSW fixture. pytest **87** (76+11); ruff/tsc/next-build clean (5 routes incl. /gene-sets static); browser-verified (mock :3010, desktop) — both apply paths land in the Workbench correctly prefilled, Save persists/re-applies; real-engine smoke green (enrichment(curated)→phototransduction; volcano→RHO/PDE6B/GNAT1). **Licensing finding:** the staged Fidelle `gene_lists` are named third-party compilations (CiliaCarta = van Dam 2013 / RD_GeneList = RetNet-style / Proteostasis) → **NOT shipped**; Phase A ships GO+WikiPathways + 2 GO/textbook-derived owned panels instead (`docs/gene-set-builder-design.md` §8.1) — **confirm provenance before shipping the Fidelle lists** (memory `selom-real-datasets`). **Open: push 8 commits on owner's word. Next: gene-set builder Phase B (present plan first) OR P2 single-cell depth; real-data dogfooding backlog; FE follow-ups.**
- **Last (2026-06-14 19:37 +10:00) — GENE-SET BUILDER PHASE B SHIPPED (both lanes; owner-approved focused scope: compile over the current corpus + attributed Fidelle).** **BE `65cd120`:** `gene_sets/normalize.py` (HGNC-style symbol normalize from NCBI gene_info → `corpus/hgnc_symbols.json`, gitignored/regenerable via `scripts/build_symbols.py`; remaps aliases, keeps unrecognized, degrades to upper+dedup) + `library.compile_sets(set_ids, op)` (union/intersect→normalize→provenance) + `POST /gene-sets/compile`. **BE `4b8c8a8`:** attributed reference panels — new `reference` source using the **rich per-set format** (`{genes, license, attribution}`; loader `_entry_genes`/`_overrides` accepts flat + rich, per-set license/attribution flow to cards + provenance); `scripts/build_reference_panels.py` → committed `corpus/gene_sets_reference.json` (5 panels: Ciliopathy/CiliaCarta cited van Dam 2013 · Cilium assembly · Proteostasis UPS/ALP/Chaperones, CMRI Fidelle). **FE `8b9b973`:** multi-select compile (per-card checkboxes + compile bar: union/intersect, name, Compile & save → provenance-stamped GeneSet saved to project) + attribution on cards/dialog + `reference` filter + `/api/gene-sets/compile` MSW mock. pytest **91**; ruff/tsc/next-build clean; browser-verified (mock :3010, desktop) — attribution renders; compile 2 cilium sets → union deduped to 18 → saved + applyable; console clean. **Pushing now (owner: yes to push).** **Next: Phase C (grounded LLM free-text→genes) OR P2 single-cell depth; B.2 disease/phenotype (HPO + Open Targets) parked per owner; confirm Fidelle-list provenance for the messy ones not yet ingested.**
- **Last (2026-06-14 21:04 +10:00) — P2 SINGLE-CELL DEPTH SHIPPED + `pca`/`composition` skills + JEV multiomics paper reproduced as Selom figures (dogfood). 4 commits LOCAL/UNPUSHED.** **P2 (DECISIONS #10), all Scanpy BSD-3, no new deps:** `markers` (rank_genes_groups → dotplot; colour=mean expr, size=%expressing; dendrogram-ordered), `annotate` (Mode A `score_genes` per cluster → embedding recoloured by cell type; bundled **attributed retinal panel** `skills/annotate/panels.json`, Lu et al. 2021), `trajectory` (diffmap→PAGA→DPT; embedding by pseudotime + PAGA node-link). Each = stub+real engines, `methods.py` template (+PAGA/DPT/Tirosh citations), guardrail coverage (`_INTERNAL_NORMALIZE`), stub-pinned golden. Commits `96490c6` feat(backend) + `06e86b1` feat(frontend). **Robustness fix found dogfooding:** rank_genes_groups/paga need a categorical groupby → coerce (real label columns are object). Real-engine validated on RPGRIP1 (**annotate 7/7 vs author celltypes**) and Fadl GSE153674 (annotate 6 retinal types, rod-dominated 5620/8699). **+ `pca`** (sklearn PCA of a feature×sample table → PC1/PC2 by group) **+ `composition`** (proportion/deconvolution bars) — `0063b88` feat(backend) + `abf66f2` feat(frontend); methods (+scikit-learn cite) + goldens. **pytest 101; ruff clean; FE tsc + next build clean (5 routes).** **JEV dogfood (owner-directed; artifacts OUTSIDE repo under `D:\selom-data\jev*`):** reproduced **"Multiomic integration… gliotic responses in degeneration" (Cioanca/Natoli, J Extracell Vesicles 2023, doi 10.1002/jev2.12393)** from `C:\Users\seamegdool\Desktop\Claude code and website tips\Data` (PDF + supp ST1–ST11). Full figure→table map; reproduced AS Selom editable Plotly specs (`D:\selom-data\jev\figures\`): proteome volcano (Fig4e/ST6, 130↑125↓ B2m-top), miRNA volcano (Fig3a/ST2), proteome heatmap (Fig4d), proteome PCA (Fig4c), deconvolution (Fig6a/ST8, Müller 2.6→22%), + scRNA umap/annotate/markers/trajectory on the **Fadl reference GSE153674** (Fig8 — the study's own PRJNA990691 has NO public data: ENA 0 runs → exact Fig8 blocked at source). Memory added: `eamos-pdf-extraction-tooling` (default pypdf, avoid AGPL PyMuPDF on the shipped path). **Open: push 4 commits on owner's word. NEXT: P2 browser-verify (mock :3010); optional JEV miRNA/group heatmaps (Fig1c/Fig5) via existing `heatmap`; gene-set Phase C / B.2; FE follow-ups (DESIGN.md, Phosphor).**
- **Resume:**
  ```
  # Resume · 2026-06-17 18:26 +10:00 (Australia/Sydney) · Selom · Claude (FE+BE, acting) · Opus 4.8 (xhigh)
  Selom build repo D:/selom. CLAUDE.md auto-loads. Read FIRST: agent_handoff/README.md + CURRENT.md (this, session-13 live state) + docs/reproduction-engine/spec.md (REVISED, PENDING approval) + docs/reproduction-engine/figure-extraction-subsystem.md (NEW sub-spec) + docs/reproduction-engine/figure-repro-sop.md + docs/rpgrip1-figrepro.md + RISKS.md (#10 GSEA) + memory (selom-figure-repro-mission, selom-repro-edge-cases, selom-scrna-batch-genotype-confound, selom-gsea-engine-sensitivity, selom-r-validation-oracle, commercial-gated-tools-build-now-gate-later, selom-backend-python-exec, ask-before-docker-wsl) + git status.
  ROLE: Codex away → Claude owns BOTH lanes; explicit per-lane scoped commits (NEVER git add -A). Opus 4.8 (xhigh). ASK before Docker/WSL. ASK before every push.
  STATE: Reproduction Engine SPEC APPROVED + FIRST SLICE R0+R1+R2 SHIPPED. Docs `9f35398` (spec rewrite + figure-extraction-subsystem.md + build plan R0–R5) committed LOCAL. Engine `e69d695` LOCAL (both push pending owner OK, per-push rule): `app/backend/reproduction.py` (R0 = typed Ledger/verdict/blame/findings-first scorecard/JSON I/O) + `reproduction_guards.py` (R1 = 14-guard registry + pure helpers) + tests (R2 = run_panel + 28 tests; blame cases = the two dogfoods as fixtures). pytest 219; ruff clean (EDR-workaround uv-3.12 interp). Library-only — NO HTTP surface (v1 internal-first, D12). Session-12's 3 commits already pushed.
  NEXT: (1) push `e69d695` + this handoff (owner leaning yes); then (2) **R3** — oracle runner (R edgeR/fgsea recapitulation) + the sweep grid = the blame instrument on real data (dev-only, ADR 0002), OR **R4** — extraction subsystem X1 (PDF → panel inventory + methods digest + golden-target table via papers.py). R5 (FE read-only view + Track A/B reconstruction) deferred per D12. Goal after R3/R4 = drive the real RPGRIP1 Fig 5/6 ledger as the engine's first end-to-end integration dogfood.
  ENV (don't guess): BE python (EDR workaround) PY=C:\Users\seamegdool\AppData\Roaming\uv\python\cpython-3.12.13-windows-x86_64-none\python.exe ; PYTHONPATH='D:/selom/app/backend;D:/selom/app/backend/.venv/Lib/site-packages' ; PYTHONIOENCODING=utf-8 ; cd app/backend ; SELOM_SKILLS_ENGINE=real. R oracle (validation-only): RS=C:\Users\seamegdool\AppData\Local\Programs\R\R-4.6.0\bin\Rscript.exe (not on PATH; edgeR+fgsea installed; redirect output to a file). Harmony = harmonypy 0.0.10 in scratch D:/tmp-thl/pylibs/. No dev servers running. End clear-safe.
  ```
  <!-- prior resume (2026-06-14 21:04):
  Selom build repo D:/selom. CLAUDE.md auto-loads. Read agent_handoff/README.md + CURRENT.md (this) + DECISIONS.md + RISKS.md + agent_handoff/on-hold/README.md + docs/build-charter.md + docs/gene-set-builder-design.md.
  ROLE: Codex away → Claude owns BOTH lanes; explicit per-lane scoped commits (NEVER git add -A; stage only FE files for feat(frontend:), only BE files for feat(backend:)); keep `## Codex` section drop-in-ready. Selom is DESKTOP-ONLY. Model = Opus 4.8 (xhigh), pinned in .claude/settings.json. Push ONLY when owner asks. For FE work, invoke the 3 design skills (impeccable / frontend-design / ui-ux-pro-max) per CLAUDE.md; verify in a real browser (chrome-devtools MCP) at desktop widths.
  FE FOUNDATION (built 2026-06-14, all pushed): redesign on the owner's 4 reference ads. Pipeline metaphor = `components/pipeline.tsx` (feature mode on Home + live progress tracker on project Overview; static solid connector; done-badge = `CheckCircle2`). App-Store Skill Store = `lib/catalog/modality.ts` `CATEGORY_GROUPS` shelves + category pills (card hue = analysis CATEGORY, omics chip = DATA modality). Motion primitives = `components/ui/motion.tsx` (+ `motion`/Framer Motion dep; reduced-motion-safe). Animated `components/project/dropzone.tsx` + design-sheet upload (item b) → `runSkill(...,design)`. Overview = entry point (empty project → drop-to-start in the pipeline box → hands file to Data tab intake). Inline skill params = `lib/catalog/params.ts`; quick-apply favourites; before/after cleaning view = `components/project/cleaning-report.tsx` (raw vs cleaned + toggleable steps; `QcReport` gained `nObsRaw`/`nVarRaw`/`cleaningSteps`, `mock.ts` `buildQc`). Root-cause fix: the unlayered `*` reset in `globals.css` was zeroing all Tailwind v4 spacing → moved to `@layer base`. `DESIGN.md` NOT yet updated to the evolved system (follow-up). **⌘K COMMAND PALETTE DONE 2026-06-14 (`7285054`, NOT pushed → `main` ahead 1):** `components/shell/command-palette.tsx` (global fuzzy launcher over projects/figures/skills/actions; combobox→listbox a11y, kbd nav, focus trap, tiered matcher) + `lib/workspace/intent.ts` (subscribable intent bus — palette deep-links into the workspace; apply-a-skill = install + Workbench + pre-select) + wiring in app-shell / project-workspace / workbench-panel; skill rows show real install state ("Installed" + project-colour swatches, or "In Store"). **All four "build all four" FE items are now done** (guided Overview · inline params · quick-apply · ⌘K).
  DONE LAST SESSION — GENE-SET BUILDER PHASE A **and** PHASE B SHIPPED (both lanes, owner-approved). Phase A (pushed): `gene_sets/library.py` + `GET /gene-sets[/{id}]` + WikiPathways CC0 ingest + first-class `GeneSet` + apply (enrichment source-select / volcano highlight). Phase B: `gene_sets/normalize.py` (HGNC-style, gitignored `corpus/hgnc_symbols.json`) + `library.compile_sets` + `POST /gene-sets/compile` + attributed `reference` panels (rich `{genes,license,attribution}` format; CiliaCarta cited, Proteostasis CMRI Fidelle) + FE multi-select/compile bar + attribution display. Committed corpus: WikiPathways + curated + reference (`gene_sets/corpus/*.json`); GITIGNORED/regenerable: GO library + hgnc_symbols.json. pytest 91; ruff/tsc/build clean; browser-verified. pydeseq2 still needs `uv pip install pydeseq2` on a fresh venv.
  PHASE: P1 COMPLETE; **gene-set builder Phase A + B SHIPPED** (DECISIONS #11); **P2 single-cell depth SHIPPED** (markers/annotate/trajectory) + `pca`/`composition` skills; **JEV multiomics paper reproduced as Selom figures** (dogfood, `D:\selom-data\jev`). build-charter **B4 active** (only Docker-gated Kaleido export remains — ASK owner before any Docker/WSL). NEXT (owner picks): (1) **gene-set builder Phase C** — grounded LLM/literature free-text→genes (propose, user confirms) — PRESENT plan before building. (2) **B.2 disease/phenotype adapters — HPO + Open Targets** (owner likes this; parked for later refinement/hardening; `docs/gene-set-builder-design.md` §8.2). (3) **P2 single-cell depth** (markers/annotate/trajectory — DECISIONS #10) — owner said next session. (4) **Licensing:** the messy multi-header Fidelle lists (RD_GeneList/RetNet, the Ciliopathy Table S1, Cilia-by-category/Vasquez) weren't ingested in Phase B — ingest with attribution when wanted (the clean ones ARE shipped). (5) real-data dogfooding backlog + FE follow-ups (DESIGN.md, Phosphor).
  FE FOLLOW-UPS (not started, own tasks): `DESIGN.md` refresh to the evolved system; Phosphor-icon migration (FE is all lucide-react today — memory `selom-icon-library`).
  ON-HOLD (Docker/WSL — ASK owner first; agent_handoff/on-hold/): Kaleido export, arq Redis status store, OmicVerse worker, deploy image, community sandbox. Parked FE: moving-dot pipeline animation for a hero page (memory `selom-pipeline-flow-animation`).
  GRAPHIFY: owner wants `python -m graphify update .` run at session start AND end (code-graph AST refresh, no LLM, ~seconds → `graphify-out/`; the LLM-backed doc/semantic pass `/graphify --update` is heavier — run only when docs/papers change). Enforcement TBD (SessionStart/Stop hook in `.claude/settings.json` is the reliable path — a memory alone won't trigger it). NB graphify warns skill 0.8.36 vs package 0.8.33 (benign; `graphify install` to align).
  ENV: FE — `cd app/frontend; npm run dev:mock -- --port 3010` (mock MSW; /gene-sets has an offline fixture incl. compile); KILL the dev server before ending. BE — user py3.12 (uv at C:/Users/seamegdool/.local/bin/uv.exe); `uv run --directory app/backend python -m pytest` (91) / `-m uvicorn`; SELOM_SKILLS_ENGINE stub|real. Gene-set corpus regen scripts (network): `scripts/build_wikipathways.py`, `scripts/build_symbols.py` (HGNC map, gitignored), `scripts/build_reference_panels.py` (reads D:\selom-data; output committed). Real CMRI data + staged copies under `D:\selom-data` (outside repo); Bash CANNOT reach \\cmri.com.au UNC — use PowerShell.
  GIT: `main` AHEAD of `origin/main` by 4 UNPUSHED (push on owner's word): P2 `96490c6`(BE)+`06e86b1`(FE), pca/composition `0063b88`(BE)+`abf66f2`(FE); gene-set Phase A+B already pushed. pytest 101; ruff clean; FE tsc + next build clean. Memory: eamos-pdf-extraction-tooling (NEW), selom-gene-set-builder, selom-real-datasets, selom-pipeline-flow-animation, selom-icon-library, ask-before-docker-wsl, graphify-keep-current. End clear-safe.
  ```
  -->

## Codex — Last Task & Resume

> **Role coverage (2026-06-11 22:10):** Codex is away on another project; **Claude is acting backend owner.**
> This section is maintained by Claude *on Codex's behalf* and kept accurate to true backend state so Codex (or
> any agent) can drop in and take over from the `**Next**` line below with zero re-derivation. When Codex
> returns, take this section back and review the backend-lane decisions locked in the interim (arq #6).

- **Last (done by Claude, acting BE, 2026-06-12):** **B1 + B2 backend complete.** B1 = real scanpy UMAP behind
  the one-shot `POST /skills/umap_scrna/run` (`6e37829`). **B2 = all 6 wedge skills** (`eded0db`):
  `cluster · violin · deg · volcano · heatmap · enrichment`, each `skills/<slug>/{skill.json, run.py (stub) +
  run_real.py}`, dispatched by `SELOM_SKILLS_ENGINE` (auto/stub/real) via `skills/_engine.py`; output via the
  shared `skills/_plotly.jsonable` (factored out of `run_scanpy`). DEG: scRNA `rank_genes_groups` + bulk pyDESeq2
  (CPM fallback). **Enrichment: in-house hypergeometric ORA + BH FDR vs a bundled GO/Reactome `gene_sets.json` —
  NO gseapy/MSigDB (DECISIONS #9).** `main.py` run-endpoint generalized to ANY Verified skill (query params +
  preserved upload suffix). Tests `tests/test_skills_golden.py` + `tests/golden/*.json` (regen `tests/regen_golden.py`)
  → `python -m pytest` = 14 passed; real engines smoke-verified on synthetic data.
- **Last (done by Claude, acting BE, 2026-06-12) — B3 backend DONE + integration quick wins; ALL PUSHED (`main` @ `5138aec`).**
  **Registry:** `skills/registry.py` maps each `skill.json` (+ a new optional `catalog` block on `SkillSpec`) to the FE
  `SkillCatalogEntry` shape; `GET /skills` returns the live Verified set. **Async jobs API:** `POST /skills/{id}/jobs` →
  `GET /jobs/{id}` (poll) / `/events` (SSE) / `/result`; new `config.py`, `jobs/{store,queue,worker}.py`,
  `storage/results.py`; one `execute_job` shared by the inline executor and the arq worker. **Default = inline + local
  filesystem result store (ZERO infra).** `SELOM_QUEUE=arq`+Redis (DECISIONS #6) and `SELOM_R2_*`+R2 (boto3 + the RISKS #5
  checksum fix) are **wired but DORMANT**; new optional `[jobs]` extra. **heatmap** got scipy hierarchical row-ordering
  (real-engine only); **`gseapy` DROPPED** from `[omics]` (DECISIONS #9 / RISKS #6/#9; uv re-locked). `pytest` = **21 passed**.
  arq #6 confirmed in your absence — review on return. **`/run` stays synchronous** (the proven light-skill path); only
  heavy skills use `/jobs`. The one open BE item: **arq cross-process job *status*** needs a Redis-backed JobStore
  (`jobs/worker.py` documents the caveat — success propagates via the shared result store, but queued/running/error states don't yet).
- **Last (done by Claude, acting BE, 2026-06-12 02:45) — B4 publish-confidence backend slice-1 DONE; COMMITTED `2ab7c43`, NOT pushed**
  (`main` ahead 2 of `origin/main`). New `provenance.py` = per-figure reproducibility bundle (skill+version, resolved+typed params,
  input **SHA-256**+size, environment = python/platform/engine-policy + scientific-stack versions via `importlib.metadata`). New
  `methods.py` = auto methods-text (deterministic per-skill templates quoting params + canonical citations; generic fallback).
  `contract.resolved_params()` = typed param coercion (runners untouched). **Additive wire change:** `POST /skills/{id}/run` +
  `GET /jobs/{id}/result` now return `{figure, provenance, methods}` (same shape both paths); `Job` gained `filename`, `submit`/
  `execute_job` thread it, the shared `execute_job` stores the full bundle (`result_store.put` param figure→payload). `pytest` =
  **28 passed** (21 + `test_provenance.py` + `test_methods.py` + jobs-bundle asserts); ruff clean; live uvicorn smoke green.
  **No new deps** (stdlib hashlib/platform/importlib.metadata). FE half is committed separately (`84ae54e`).
- **Last (done by Claude, acting BE, 2026-06-16) — Pillar-1 S2.1: skills emit a Statistics `table` (`af03583`, NOT pushed; `main` ahead 9).** Decision D7, additive. A runner attaches `figure["table"]`; `contract._execute` **pops it BEFORE theming** so the goldens stay **byte-identical**; new `run_skill_with_table(skill_id, path, params) → (figure, table)`; `/run` (`main.py`) + the job bundle (`jobs/queue.py`) gain `"table"` (None for purely-visual skills). `skills/_table.py` = shared `table()`/`de_table()` (JSON-safe cell coercion). Wired: **volcano** (full DE table gene·log2FC·padj·direction, most-significant-first, capped 300) · **deg** (gene·log2FC) · **enrichment** (terms + up/down split). **markers DEFERRED** (the FE fallback `lib/lineage/derive-table.ts` covers it). New `tests/test_table.py` (5; stub-pinned via the `_force_stub` fixture). **pytest 171 (166+5), ruff clean, goldens unchanged.** ⚠️ **Running backend pytest/ruff FROM THE AGENT TOOL CONTEXT:** the project `.venv\Scripts\python.exe` launcher is **EDR-blocked** (`Access is denied — os error 5`); plain `uv run` from the agent also fails (it spawns that stub). Workaround (the owner's own interactive `uv run` is unaffected): `$uvpy="C:\Users\seamegdool\AppData\Roaming\uv\python\cpython-3.12-windows-x86_64-none\python.exe"; $env:PYTHONPATH="D:\selom\app\backend\.venv\Lib\site-packages"; Set-Location D:\selom\app\backend; & $uvpy -m pytest -q; & $uvpy -m ruff check .` (memory `selom-backend-python-exec`).
- **Last (done by Claude, acting BE, 2026-06-17) — Reproduction Engine first slice R0+R1+R2 (`e69d695`, LOCAL/push-pending).** New `app/backend/reproduction.py` (typed Pydantic Ledger + verdict/tolerances **D4** + the blame decision procedure **D10** + findings-first scorecard + JSON load/save under `settings.data_dir/repro/<slug>/`) + `reproduction_guards.py` (the 14 SOP edge-case guards as a registry, **D11**) + `run_panel` (run via `run_skill_with_table` + `provenance.build` + `methods.build` → validate + blame, rebuild scorecard). **Headless library — NO new routes/contract change** (v1 internal-first, **D12**). Composes existing modules; **no new deps**. pytest **219** (+28 in `tests/test_reproduction*.py`); ruff clean. Spec + sub-spec live in `docs/reproduction-engine/` (`spec.md` APPROVED, build plan R0–R5). Next backend repro step = **R3** oracle runner (R edgeR/fgsea, dev-only/ADR 0002) + sweep, or **R4** extraction `extract/` package (X1).
- **Next (finish B4, then integration backlog):** **B4 remaining** — (a) **statistical-guardrail expansion DONE** by Claude
  (`guardrails.py` adds a `guardrails` list to the bundle: multiple-testing / FDR method checks + best-effort `.h5ad` low-cell /
  pre-normalized / uncorrected-batch; cluster silhouette stays in the runner; commit `edd3615`), (b) **Kaleido journal export**
  PNG/SVG/PDF — needs **Docker + system Chromium** (RISKS #2 / DECISIONS #7), so it lands with the deploy image (B8-adjacent);
  scope a CPU-Docker path or defer — **ASK OWNER before any Docker/WSL work**. **Integration backlog (`docs/integrations.md` + `plans/v2-backend.md`):**
  OmicVerse **isolated** 2nd engine/Foundry source — pins `pandas<3`, CANNOT share this venv (RISKS #9); run out-of-process (its MCP
  server / a worker), SCA-gate 50 deps; the **arq Redis status store**; R4DS/Quarto + R-oracle (RISKS #7); full GO/Reactome GMT;
  catalog-count true-up (≈418). sklearn is core (silhouette in `cluster`). Explicit staging only (never `git add -A`); `feat(backend:)` scope.
- **Resume:**
  ```
  # Resume · 2026-06-12 02:45 +10:00 · Selom · Codex (backend) — reclaiming from Claude
  Selom build repo D:/selom. CODEX.md auto-loads. Read agent_handoff/README.md + CURRENT.md + DECISIONS.md + RISKS.md + docs/build-charter.md + plans/v2-backend.md + docs/integrations.md + git status.
  Delta: B1 + B2 + B3 + B4-slice-1 backend DONE by Claude, ALL PUSHED (main ↔ origin/main in sync; B4 backend 2ab7c43 + FE half 84ae54e). B4 = provenance.py (per-figure reproducibility bundle: skill+ver, resolved+typed params, input SHA-256+size, env=python/platform/engine-policy+pkg versions) + methods.py (auto methods-text, per-skill templates + citations) + contract.resolved_params(). /run + /jobs/{id}/result now return {figure, provenance, methods}; Job.filename added; shared execute_job stores the full bundle; result_store.put param figure→payload. No new deps. pytest 28 passed; ruff clean; uvicorn smoke green.
  Open BE items: (1) arq cross-process job STATUS needs a Redis-backed JobStore (jobs/worker.py caveat). Next: finish B4 — Kaleido export ONLY (guardrail expansion DONE — guardrails.py edd3615; Kaleido needs Docker+Chromium, RISKS #2 — ASK OWNER before any Docker/WSL) — then integration backlog (OmicVerse isolated worker, R-oracle, full GMT, catalog true-up). Env: user-managed py3.12 (uv); system 3.10 IT-locked; `uv run --directory app/backend python -m pytest`/`-m uvicorn`. Stay on Opus 4.8 (xhigh). End clear-safe.
  ```
