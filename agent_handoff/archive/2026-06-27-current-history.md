# CURRENT.md — archived history (2026-06-27)

> Prior-live-state comments swept out of `agent_handoff/CURRENT.md` to keep the live
> file lean ([[archive-currentmd-history-weekly]]). Verbatim; full history also in git log.

## Prior live-state comments (sessions T24 ↓ T7) — archived 2026-06-27

<!-- prior live-state (session T24): _Last updated: 2026-06-27 18:03 +10:00 (session "T24" — **ARCHITECTURE-CONSISTENCY GATE** = the active lane: review docs + agile roadmap + **Tasks B1, B2, B3 & B4 shipped & browser live-verified**) · Claude (acting FE+BE) — **MODE = architecture-consistency gate (owner-chosen active lane "until it is mostly complete"; ALL other pillars/tangents ON HOLD — ERG parked, figure-editor §11/Prism on hold).** B1 code **PUSHED** (commit `322228c`). **B2 (`a44724c`) + B3 (`28c7035`) + B4 (`f77e0c1`) code + all verify-doc updates are COMMITTED locally, NOT pushed — owner pushes by default.** **The gate (read `docs/architecture-consistency-gate/{plan,inventory,roadmap}.md` + `[[selom-architecture-consistency-gate]]`):** keep the bespoke-per-skill panes (the MOAT), put ONE consistent spine under them; the switch-crashes = a bespoke pane meets a shape it doesn't expect. 4-part spine keyed off ONE content hash (canonical table contract · skill/section registry · stable-skeleton+crash-isolation · cache/source split). **DB-materialization line (owner "don't let it block"):** B · C · D1–D3 · E are **DB-free, run now**; **C4 · D4 · F DEFERRED + flagged**. **✅ Task B1 (`322228c`):** `ErrorBoundary`+`PaneBoundary` (`components/ui/error-boundary.tsx` + `lib/ui/reset-keys.ts`) wrap each inspector tab/canvas/preview/Figure-data/stats — a pane that THROWS shows a local fallback, resetKeys auto-clear on switch. **✅ Task B2 (`a44724c`) — fail-safe at the spec seam (stops the throw happening):** `lib/figure/contract.ts` `validateFigureContract` coerces ANY input to a render-safe `{data,layout}`, wired into figure-store `init` so init NEVER throws; `normalizeSpec` hardened too; `heatmapColorscaleState` → `ready|empty|hidden` (a non-heatmap marker colorscale stays hidden — no trajectory/markers regression); Style renders "No heatmap trace…" + threshold-editor "No points yet…". **✅ SHIPPED THIS SESSION — Task B3 (`28c7035`) — pane state machine + stable skeletons:** new pure **`lib/ui/pane-state.ts`** (`PaneState` union `idle|loading|empty|partial|stale|error|ready` + `hasData`/`isPending`/`isDegraded`) + **`components/ui/pane-shell.tsx`** `<PaneShell>` (stable outer shape per tag: loading→skeleton · empty→note · error→msg+Retry · partial/stale→content+note; pairs with B1's PaneBoundary — boundary catches a throw, PaneShell renders the typed state). **`useSkillParams` gained an 8s fetch timeout + AbortController + `error` status + `retry()`** (failed entry evicted so Retry re-fetches) — the fix for the infinite "Loading inputs…" spinner; `{fields,loading}` kept back-compat for sweep-form/workbench-panel. Figure-data **Inputs** renders loading/empty/error/ready via `<PaneShell>`; `property-panel`'s `!spec` `return null` → a stable empty shell. **GATES:** FE tsc clean · eslint 0-err (1 pre-existing warn in the unrelated selection effect — NOT mine) · **vitest 288** (+4 pane-state). **✅ B3 BROWSER LIVE-VERIFIED 2026-06-26** on the B5 iRPE project (`p_fb5166e1`, webpack dev, next.config temp→:8010 then REVERTED to :8000): with the proxy on an empty :8010 the Inputs pane showed `data-pane-status="error"` → "Couldn't load this skill's inputs (the backend didn't respond). · Retry" (NOT an infinite spinner, same Inputs card = stable shape); starting the Selom BE on :8010 + clicking **Retry** flipped it to `ready` with the real param controls (Genes shown=70, Clustering, …) + Re-run. **No store-mutating probe this pass** (opened real figures only) → B5 fixture untouched (8 figures intact). **⚠ INCIDENT (T24, B2 verify — RESOLVED): the B2 `?b2probe` called `figure.init`, which the debounced `updateFigureSpec` auto-persisted to localStorage → overwrote 2 B5 clustermap fixtures; I restored them to valid clustermaps (deep-copied the 2 intact siblings — valid, not byte-identical originals). LESSON [[selom-fe-probe-autopersist-gotcha]]: a store-mutating probe persists to the fixture — snapshot+restore, or use a render-only probe (like B1's throw).** **OWNER DECISION (2026-06-26): the "no backend → /api/skills/{id} fails" dependency is reducible (param_spec is immutable per skill_version) → FILED as roadmap C5 (FE localStorage cache, or stamp param_spec into figure provenance); NOT done in B3 — B3's error/Retry is the floor.** **⚠ ENV:** Turbopack `0xc0000142` panic recurs → use **`npx next dev --webpack`**; BE on :8010 via the python workaround (PY=uv 3.12 + PYTHONPATH incl `.venv\Lib\site-packages`, NOT `uv run` — EDR-blocked) [[selom-backend-python-exec]] [[selom-turbopack-webpack-workaround]]. **✅ SHIPPED — Task B4 (`f77e0c1`) — skill-scoped staged params + switch hygiene:** the reset-from-figure `useEffect` (keyed only on `activeFigureId`, eslint-disabled, one-frame flash) → a **derive-don't-sync DURING-render reset** keyed on `fdScope = dataset:skill:figure` (React's "store info from previous renders" pattern, the same idiom `NumRow`/`MarkRow` already use); `key={fdScope}` remounts the bespoke `FigureDataPanel` on any switch + the preview/inputs `PaneBoundary` resetKeys widen to `[datasetId, skillId, figureId]`. So a stale fc from skill A can't carry into a skill-B run, and a failed re-run doesn't strand another skill's staged value. **GATES:** tsc clean · eslint **0-err** (27 warns, **−1 vs HEAD** — the removed effect's setState-in-effect warning) · **vitest 288**. **✅ B4 BROWSER LIVE-VERIFIED 2026-06-27** (webpack dev, real localStorage fixtures, **no backend**): in the 2-version volcano `p_3384a01f` — staged **fc 1→2** (live re-bucket 607/334→142/53) → **Re-run FAILED** (no session bytes → re-upload halt; staged fc=2 KEPT; **no new version**) → switched to the **fc=3** version (reset to 3, no banner) → **back** to fc=1 (**reset to 1, NO stale fc**, counts back to 607/334); plus a genuine **cross-skill** switch in `p_7821cab4` (`erg_intensity_response` ↔ `erg_traces`) remounted the inputs to the correct skill, **no crash/boundary trip**, console clean (only the expected param-spec 404 = B3's error state). **No store-mutating probe** (drove the real UI) → fixtures intact (22 figures, fc=[1,3] unchanged) [[selom-fe-probe-autopersist-gotcha]]. **⚑ NEXT: B5** (registry-completeness CI gate — a test asserting every `params.ts` overlay key + each declared `_capabilities` tool has a backing `skill.json`; promote `warnDeadKnob` from a dev console warn to a failing audit; ~0.5); then **Task B EXIT** (open every skill's figure, switch dataset↔skill 20× → zero crashes/stale-param leak/heap growth — overlaps E1); then Task C (cache, incl. **C5** param-spec cache) → D1–D3 (table contract) → E (perf/leak). **⚠ DISCUSSION GATE (owner 2026-06-27): complete every DB-FREE bucket (B5 · Task B exit · C1–C3 · C5 · D1–D3 · E1–E2), then STOP and raise it with the owner BEFORE starting any materialization bucket (C4 · D4 · F1–F3) — "there may be a change of plans in that regard"; do not begin that work or pre-commit to the Supabase/R2/DuckDB shape until the conversation happens** (recorded in `roadmap.md` → DB-materialization line). **HEATMAP CLUSTERMAP COMPLETE** (T23 below). All dev servers KILLED (FE :3000, mine; BE :8010 not started this session; ⚠ eamos holds :8000 — NOT mine, never kill); next.config untouched (at :8000). memory [[selom-architecture-consistency-gate]] [[selom-fe-probe-autopersist-gotcha]] [[selom-turbopack-webpack-workaround]] [[selom-backend-python-exec]] [[full-app-smoke-test-before-handoff]] [[kill-dev-servers-on-handoff]] [[stamp-prompts-with-datetime]]. _(T23 below = PRIOR session.)_ -->
<!-- (T24 full text in git @ this commit^) -->

<!-- prior live-state (session T23): _Last updated: 2026-06-25 18:35 +10:00 (session "T23" — heatmap v2 clustermap slice 7b: COLOURED DENDROGRAM BRANCHES via a staged `cut_k` cut — the last deferred piece) · Claude (acting FE+BE) — **MODE = Selom proper (off the ERG tangent).** **SHIPPED + LIVE-VERIFIED end-to-end + COMMITTED** (BE `7690674` · FE `65d7407` · docs = this; OWNER pushes — do NOT push unless told). Finished slice 7b of `docs/figure-data-capabilities/heatmap-clustermap-spec.md` → **the heatmap clustermap is now COMPLETE (one chart fully deep, all slices 1–7).** **(7b) Coloured branches (STAGED):** new `cut_k` param (skill.json `param_spec` int 0–8; FE range in the Figure-data Inputs). When ≥2, `run_real._order_rows` passes a `color_threshold` to SciPy's `dendrogram` (the distance of the (k−1)-th largest merge = exactly k clusters) with `set_link_color_palette(_CLUSTER_PALETTE)` + `above_threshold_color=_TRUNK_COLOR`, captures the per-link `color_list` into `dendro["colors"]` (global palette reset in a `finally` → no cross-figure leak); `run._dendro_trace`→`_dendro_traces` (plural) groups links into ONE scatter trace per cluster colour (grey trunk drawn first, clusters layered over), else the single grey trace as before; `heatmap_spec` `data +=` the list. Threaded for BOTH trees via `_cluster`; `cut_k=0` (default) = byte-identical single-trace tree. **NO new dependency — scipy was already imported** by the heatmap clustering path; only `set_link_color_palette` + the `color_threshold`/`above_threshold_color` args of the already-called `dendrogram` (owner asked, confirmed). **Leaf-tips interaction RESOLVED (the flagged risk):** splitting a tree into N coloured traces broke the leaf-tip projection's one-trace-per-axis assumption → `lib/heatmap/dendrogram.ts` is now multi-trace-aware (`traceCoords`→`traceCoordsAt(index)` + `axisTraceIndices`; `maxDistance`/`leafStubs` aggregate across ALL axis traces; `applyDendrogramTips` extends leaf bases per-trace + widens the gutter range ONCE per axis using the global max-E). Single-trace behaviour identical. **VERIFIED LIVE end-to-end on REAL iRPE bulk** (B5 `p_fb5166e1`, BE :8010 + FE :3000 real not mock; a `file://` Plotly pre-check de-risked the real backend spec's geometry first): re-attached data + sheet (data bytes cleared on the fresh BE, as expected) → Figure-data `cut_k=3`+cluster=both+annotations=condition → **Re-run** drew the row tree as green/red/blue clusters under a grey trunk + the column tree as blue/red clusters (condition strip + column reorder composing cleanly); the Row leaf-tip lever then grew stubs across all FOUR coloured row traces (the multi-trace projection); no console errors. **GATES:** BE 16 heatmap_dendrogram (+4 cut_k) · golden+table-contract 80 byte-identical (default path untouched) · registry/contract 35 + ruff clean; FE tsc clean · eslint 0-err (2 pre-existing liveRef warns) · vitest **265** (+3 multi-trace dendrogram). Dev servers KILLED (FE :3000 + BE :8010; ⚠ eamos holds :8000 — NOT mine). next.config.ts reverted to :8000. **⚑ NEXT = a MEETING with the owner to discuss the next plans (owner: "finish off A, then we have a meeting to discuss the next plans").** **(follow-up SHIPPED right after, owner "just quickly build it":** the `cut_k`-driven unsupervised block-split — new `split_by_cut` bool [skill.json + FE switch]: on + cut_k≥2 + no categorical `split_by` → cut the COLUMN dendrogram into k clusters [`scipy fcluster`] + block-split the columns into them, no sample sheet. Shared `_block_split` core factored out of `_split_columns`; cut path forces column clustering, drops the column tree [blocks replace it], keeps the row tree + cut_k colours; categorical `split_by` wins when both set. BE `3a7e486` · FE `195bfd4`; 4 more heatmap tests [99 BE pass] + ruff/tsc/eslint clean; verified on real iRPE via a file:// render of the live spec [cut_k=3 → 3 column blocks, condition strip splits in tandem, row tree coloured, no console errors]. ⚠ the in-app SWITCH→re-run wasn't re-clicked live [proven switch plumbing + the parent re-run path was live-verified minutes before]; do a quick toggle next live pass if wanted.)** Heatmap is DONE. memory [[selom-figure-edit-ux-pattern]] [[stack-browser-verification]] [[full-app-smoke-test-before-handoff]] [[verify-on-real-data-not-mock]] [[selom-uv-sync-footgun]]. _(T22 below = PRIOR session.)_ -->
<!-- (T23 full text in git @ this commit^) -->

<!-- prior live-state (session T22): _Last updated: 2026-06-25 16:00 +10:00 (session "T22" — heatmap v2 clustermap slices 4–6 [annotation tracks · quant side bar · block-split] + slice 7a [coloured tick labels by group]) · Claude (acting FE+BE) — **MODE = Selom proper (off the ERG tangent).** **SHIPPED + LIVE-VERIFIED end-to-end + COMMITTED** (BE `746e901` · FE `b3e5d86` · docs = this; OWNER pushes). Built the rest of `docs/figure-data-capabilities/heatmap-clustermap-spec.md`, every staged piece driven by the optional **design/sample sheet** (`runSkill(...design)` → `_design_path`; new shared `skills/_design.load_design`, deg's private loader refactored onto it). **(4) Annotation tracks (STAGED):** `annotations` param (comma-sep design columns) → `_col_tracks` maps each ordered column → category code → `heatmap_spec._col_track_layer` paints a thin 1-row heatmap strip per track ABOVE the columns (own x4/y4… axes, `matches:x`, discrete step-colorscale + legend proxy scatters). **(5) Block-split (STAGED):** `split_by` param → `_split_columns` regroups columns by a categorical, inserts a **`None` spacer** column (NaN is invalid JSON — jsonable doesn't sanitise) + a centred bold header per block; column clustering + cross-block tree dropped (categorical order imposed), row tree/tracks/quant bar still align. **(6) Quant row side bar (STAGED):** `quant_track ∈ {none,variance,mean,logfc}` → `_row_quant` keyed by gene, projected onto the clustered order; `logfc` = log₂ mean-count FC between the sheet's 2 condition groups → `_row_quant_layer` draws a horizontal bar left of the heatmap (diverging→0-centred+sign colours). A quant bar OR a row tree pushes gene labels right. **(7a) Coloured tick labels by group (INSTANT/FE):** `lib/heatmap/labels.colorLabelsByGroupOps` reads group→colour off the strip legend proxies + per-column category off the strip customdata → wraps each sample label in its group colour span (highlight mechanism generalised) + stamps `meta.selom.labelColorBy`; Style "Colour labels by group" select (gated on tracks); instant+undoable. **(7b) Coloured dendrogram BRANCHES = DEFERRED** (needs SciPy `color_threshold`/`fcluster` topology = a STAGED backend change + interacts with the leaf-tips projection/block-split; lowest-value visual; scoped for a focused `cut_k` follow-up). **VERIFIED LIVE end-to-end on REAL iRPE bulk + a 2-factor sample sheet** (project B5 `p_fb5166e1`, BE :8010 + FE :3000 real not mock; 4 file:// pre-checks de-risked the geometry on the ACTUAL backend specs first): re-attached data + added the sheet (`graphify-out/scratch/irpe_sample_sheet.csv`) + Skip intake → fresh heatmap → Figure-data set cluster=both/annotations=condition/quant=logfc → **Re-run** drew the full clustermap (column tree + condition strip CE4-4/CE4-5 + row tree + `log2FC CE4-5/CE4-4` diverging bar + gene labels right + legend, columns reordered); then in the styler the slice-7 "Colour labels by group"→condition recoloured the sample labels red/teal to MATCH the strip (ticktext spans confirmed) + Undo cleared it. **TWO bugs the live pass caught (unit tests missed):** ① the slice-7 select used `value:""` for None → shared Radix `SelectItem` forbids empty-string → runtime crash on the tracks figure → fixed with a `__none__` sentinel. ② **owner-reported:** a clustermap's title-less furniture axes (gutters/strips/quant bar) made Plotly paint a "Click to enter … axis title" placeholder over EACH (8+) in the editor → `figure-canvas` drops `edits.axisTitleText` when secondary axes are present (empty-string titles do NOT suppress it; the main sample/gene titles are auto-set). **GATES:** FE tsc clean · eslint 0-err (2 pre-existing liveRef warns) · vitest **262** (+4); BE 178 focused (golden byte-identical — default no-furniture path untouched) + ruff clean. Known minor (not fixed): a clustermap's Style panel shows Markers/Lines sections (the legend proxies/dendrogram lines) — harmless. Dev servers KILLED (FE :3000 + BE :8010; ⚠ eamos holds :8000 — NOT mine). next.config.ts reverted to :8000. **⚑ NEXT (pick WITH owner): heatmap is DONE (one chart deep) — slice 7b coloured branches if wanted (the `cut_k` follow-up), OR continue the §11 capability-generalization to OTHER chart kinds (UMAP color-remap/lasso · bar/box direct-manip — heatmap/volcano/ERG done) [[selom-figure-edit-ux-pattern]]; figure-editor sits in the Prism UX pillar [[selom-prism-pillar-phases]].** On-hold backlog: intake-questionnaire rethink · accession auto-fetch · BAM ingest (ASK before async infra) · journal styles. Parked: ERG tangent (owner SIGNALS return). memory [[selom-figure-edit-ux-pattern]] [[stack-browser-verification]] [[full-app-smoke-test-before-handoff]] [[verify-on-real-data-not-mock]]. _(T21 below = PRIOR session.)_ -->
<!-- (T22 full text in git @ d6adb02) -->

<!-- prior live-state (session T21): _Last updated: 2026-06-25 14:26 +10:00 (session "T21" — dendrogram LEAF-TIP editing [uniform lever + per-line drag/hover-highlight] + VOLCANO gene-label-persistence live-verify [pending from T19, DONE]) · Claude (acting FE+BE) — **MODE = Selom proper (off the ERG tangent).** **SHIPPED + LIVE-VERIFIED + COMMITTED + PUSHED** (FE `90083b4`; FE-ONLY, NO backend change; origin synced — pushed the 16 prior commits first per owner "ok push and go", then this). Built the owner-requested dendrogram leaf-tip feature per `docs/figure-data-capabilities/dendrogram-tips-spec.md` (design was complete): lengthen the too-short dendrogram leaf END-STUBS between a merge bar and the heatmap. **MECHANISM (the spec's core insight) = meta-pref + render-time PROJECTION:** editing the dendrogram TRACE coords is a server re-run + insert-not-replace under JSON-Patch (the wall the rename hit), so tip length is a `meta.selom.dendrogramTips` pref the canvas applies to the trace at RENDER time via pure `applyDendrogramTips` (mirrors `lib/figure-model.ts::projectOverlay`) — the canonical leaves-at-0 trace is untouched, the view grows the stubs; FE-only. **Built (each mirrors the instant-vs-staged plug-in pattern [[selom-figure-edit-ux-pattern]]):** (1) `lib/heatmap/dendrogram.ts` — `applyDendrogramTips` (move each leaf base to −e=gap+tips[leafKey], widen the gutter distance-axis range to fit, merge bars/shape preserved) + `tipLengthOps` (uniform lever) + `setLeafTipOps` (per-leaf override) + `leafStubs`/`maxDistance`/`leafKeyOf` (+10 unit tests). (2) `components/figure/dendrogram-drag.ts` — hover-HIGHLIGHT (HTML overlay on the stub, NOT a restyle — can't colour one polyline segment) + per-leaf DRAG with a FROZEN grab-time axis mapping for direct manipulation; instant undoable store edit, NO re-run (mirrors `colorbar-drag`/`mark-drag`). (3) `figure-canvas.tsx` — compose `applyDendrogramTips` into the render spec (sibling of `projectOverlay`, identity when no pref) + wire the drag, gated on a present tree + editable store. (4) `style-panel.tsx` — "Row/Column leaf tip length" levers in the Dendrogram Style section (0–100% of ½·max_d). **VERIFIED LIVE on REAL iRPE bulk** (`D:\selom-data\alpk1\irpe_rawcounts`, project B5 `p_fb5166e1`, cluster=both, BE :8010 + FE :3000 real not mock): a file:// Plotly pre-check de-risked the geometry first (numeric: leaf bases→−e, gutter widened, no heatmap overlap, shape kept); then live — the LEVER grew all column stubs INSTANT (no /run POST); the per-leaf DRAG grew one leaf (x=5 → −1.6687 vs −0.8962 others, gutter range widened to fit), the hover HIGHLIGHT overlay appeared, the gesture SURVIVED mid-drag re-renders, Undo reverted it as ONE entry. **ALSO (pending from T19, NOW DONE): VOLCANO gene-label PERSISTENCE** live-verified end-to-end on real eyg28 DE (`PDE6B_FS_d210`, project `p_3384a01f`): hand-pick UBC (canvas click → annotation) → re-run FC 1→3 → the auto top-N labels re-seeded ENTIRELY (RMST/TRIM4/… via the text-trace) but the hand-picked UBC annotation CARRIED via `carryLabels` (re-anchored to its point, even as UBC fell to the n.s. bucket). **GATES:** FE tsc clean · eslint 0-err (2 pre-existing liveRef warns) · vitest **258** (+10 dendrogram-tips). Dev servers KILLED (FE :3000 + BE :8010; ⚠ eamos holds :8000 — NOT mine). next.config.ts reverted to :8000. **⚑ NEXT — heatmap v2 remaining (`heatmap-clustermap-spec.md` slices 4–7): categorical annotation tracks (sample sheet → type/dataset strips + DR/PD group bar) · block-split · quant side track (mean log₂FC) · coloured branches + tick labels by group.** DON'T TUNNEL (owner steer): the §11 capability-generalization continues to OTHER chart kinds (UMAP color-remap/lasso · bar/box direct-manip — heatmap/volcano/ERG done) [[selom-figure-edit-ux-pattern]]; the figure-editor sits in the Prism UX pillar [[selom-prism-pillar-phases]]. Parked: ERG tangent (owner SIGNALS return). memory [[selom-figure-edit-ux-pattern]] (updated: render-time projection from a meta-pref = the instant mechanism when an instant edit would otherwise hit a /data trace) [[stack-browser-verification]] [[full-app-smoke-test-before-handoff]] [[verify-on-real-data-not-mock]]. _(T20 below = PRIOR session.)_ -->
<!-- (T21 full text in git @ this commit^) -->

<!-- prior live-state (session T20): _Last updated: 2026-06-25 12:58 +10:00 (session "T20" — heatmap v2 publication clustermap: COLUMN dendrogram + provenance-safe RENAME + gene HIGHLIGHT + label-SIDE + owner's DENDROGRAM-SIZE control) · Claude (acting FE+BE) — **MODE = Selom proper (off the ERG tangent).** **SHIPPED + LIVE-VERIFIED + COMMITTED** (BE `fe3bb57` · FE `399f6ab` · docs = this; 10 ahead of origin, OWNER pushes). Built slices 1–3 of `docs/figure-data-capabilities/heatmap-clustermap-spec.md` + an owner mid-session add, each mirroring the instant-vs-staged capability plug-in pattern ([[selom-figure-edit-ux-pattern]]). **(1) Column clustering + top dendrogram (STAGED):** new `cluster` param `none|row|column|both` (replaces `dendrogram`); `_order_rows` gained `orientation`; `_cluster` transposes the z-matrix to cluster samples; `heatmap_spec` now takes `row_dendro`+`col_dendro` and lays out 4 cases (left gutter x2/y2, top gutter x3/y3). Rows always cluster for ordering; the mode picks which trees draw + whether samples reorder. **(2) `heatmapLabels` capability + RENAME (INSTANT):** backend stamps the CANONICAL pre-symbol labels at `meta.selom.heatmapLabels` (render-inert) + declares `heatmapLabels`; rename writes the axis **`ticktext`** (a LAYOUT edit → client/instant/undoable — NOT data `x`/`y`, which is a server re-run AND insert-not-replace under JSON-Patch), so data stays canonical, hover shows the original, provenance intact; `lib/heatmap/labels.ts` (pure) + a Figure-Styling "Labels" panel surfacing the canonical "was ENSG…~SYMBOL". **(3) HIGHLIGHT + label SIDE (INSTANT):** highlight wraps a label's ticktext in an inline colour span (`<span style=color:#dc2626>` — a uniform tickfont.color can't colour one tick); side=`yaxis.side`; the panel shows a "pinned right" note when a row tree owns the left. **(4) OWNER MID-SESSION ADD — DENDROGRAM-SIZE (INSTANT):** owner asked for a way to give the trees room so connections read clearly → `lib/heatmap/dendrogram.ts` (pure) re-proportions the gutter vs heatmap DOMAINS (layout edit → instant, no re-run); a "Dendrogram" Style section with Row-tree-width / Column-tree-height sliders (per present tree). **VERIFIED LIVE end-to-end on REAL iRPE bulk** (`D:\selom-data\alpk1\irpe_rawcounts`, project "Heatmap re-tone test (B5)", BE :8010 + FE :3000 real not mock): upload→classify→run→editor; cluster=both drew BOTH dendrograms + reordered samples (CE4_5 vs CE4_4 groups); rename ENPP2→"ENPP2 (autotaxin)" on the axis; STRIP2 highlighted RED; "was ENSG…" provenance surfaced; the side toggle flipped to the "pinned right" note under a row tree; the Column-tree-height slider spread the top tree to 45% live. A fast Plotly file:// pre-check first de-risked the categorical-ticktext rename + inline-span highlight + dendrogram alignment. **GATES:** FE tsc clean · eslint clean · vitest **248** (+17: labels 11 · dendrogram 6); BE focused subsets (heatmap_dendrogram/golden/methods/capabilities/contract/table-contract **135**) + ruff clean; heatmap.json golden regen = render-inert meta only (an erg_intensity_response regen-drift was pure key-reorder, REVERTED). Dev servers KILLED (FE :3000 + BE :8010; ⚠ eamos holds :8000 — NOT mine). **⚑ NEXT — PRIORITY (owner-requested, DESIGN COMPLETE this session, BUILD next): dendrogram leaf-tip editing** per `docs/figure-data-capabilities/dendrogram-tips-spec.md` — a uniform **"Leaf tip length" lever** + **per-line select+drag with hover highlight** to lengthen the (too-short) leaf end-stubs. Owner clarified across 4 msgs + 3 zoom screenshots: NOT the T20 gutter-spread sliders (those scale the whole tree + steal heatmap height — at 45% they squashed the map = the owner's "why is it compressed" screenshot, a slider I left at max; Undo/16% restores it). KEY DESIGN: editing the dendrogram TRACE coords is a server re-run + insert-not-replace (the same wall the rename hit), so tip length is driven by a `meta.selom.dendrogramTips` pref + a render-time PROJECTION (mirroring `figure-model.projectOverlay`) — **FE-ONLY, no backend change for v1** (leaf bases identified by their distance-0 baseline). Spec has signatures (`applyDendrogramTips`/`tipLengthOps`/`setLeafTipOps`), geometry, the drag/highlight plug-in (`components/figure/dendrogram-drag.ts`, mirrors `mark-drag.ts`), test plan + ordered build steps; internal-branch drag deferred to v2. **THEN heatmap v2 remaining (spec slices 4–7): categorical annotation tracks (from a sample sheet — refs 035134/035636 type/dataset strips + DR/PD group bar) · block-split · quant side track (mean log₂FC) · coloured dendrogram branches + coloured tick labels by group.** Also PENDING (carried from T19): live-verify the VOLCANO gene-label PERSISTENCE (label→re-run→survive; unit-tested+wired, not browser-confirmed) — fold into the next volcano live pass; plus a heatmap label-persistence-across-re-run analog if the owner wants it (today a re-run resets the cosmetic ticktext, like volcano before carryLabels). memory [[selom-figure-edit-ux-pattern]] [[stack-browser-verification]] [[full-app-smoke-test-before-handoff]] [[verify-on-real-data-not-mock]]. _(T19 below = PRIOR session.)_ -->
<!-- (T20 full text in git @ 90083b4^) -->

<!-- prior live-state (session T19): _Last updated: 2026-06-25 04:15 +10:00 (session "T19" — heatmap colour RE-TONE [3rd capability plug-in] + gene-label persistence + figure polish + v2 clustermap spec) · Claude (acting FE+BE) — **MODE = Selom proper (off the ERG tangent).** **SHIPPED + VERIFIED LIVE + COMMITTED** (BE `d1bc3c6` · FE `de8767a` · docs `2ff361d`; tree CLEAN, 7 ahead of origin — OWNER pushes). Owner interview chose: **HEATMAP, one chart DEEP**, + fold in the **gene-label persistence** follow-up first. Built + live-verified on REAL iRPE bulk counts (`D:\selom-data\alpk1\irpe_rawcounts`): **(A) gene-label persistence** — `lib/volcano/labels.ts` `captureLabels`/`carryLabels` re-anchor the hand-picked label set onto a re-run's fresh spec by gene name (wired into `rerunFigure`/`rerunFigureWithParams`); unit-tested, **live-verify of the volcano path PENDING** (low-risk; carry to next combined pass). **(B) HEATMAP colour re-tone** = the 3rd capability plug-in (after ERG marks + volcano thresholds/labels): a declared `heatmapTones` capability (`_capabilities.py` stamp → `theme.apply`; `resolveContract`) gates ① Style **Midpoint/Saturation sliders** + ② a **draggable colour bar** (`components/figure/colorbar-drag.ts`: top→zmax · bottom→zmin · middle→zmid) — both re-map the existing z-matrix via pure `lib/heatmap/colorscale.ts toneOps` → an **INSTANT undoable figure-store edit, NO re-run** (genes/clustering recompute stays STAGED via the existing Figure-data Inputs+banner — free). VERIFIED LIVE: colour-bar drag set `zmax`→0.569; the Saturation slider mirrored the drag (shared state); `heatmapTones` stamped on the live layout. **(C) Owner live-feedback polish (all verified):** ① gene labels were **clipped** → `theme.py` now preserves `automargin` through axis restyling (also fixes corr_heatmap/ssgsea/enrichment), AND gene labels show the **SYMBOL** not `ENSG…~SYMBOL` (`_genes.display_symbols`, fallback to ID; stub/scRNA/clean labels pass through → stub golden unchanged); ② **white space** when the side rails collapse → editor artboard `maxWidth` 64rem→88rem so the figure fills the freed width (1024→1288px); ③ NAME test projects. **OWNER PROVENANCE PRINCIPLE (confirmed):** a per-user **rename** of gene/sample labels belongs in **Figure Styling** as a cosmetic figure-store edit — never touches data/params/methods, so canonical IDs stay the source of truth (don't auto-rewrite data; a smart default like symbol-display is fine, keep the original in hover). **⚑ NEXT (owner-requested, SPECCED): heatmap v2 publication clustermap** — `docs/figure-data-capabilities/heatmap-clustermap-spec.md` (from 5 owner reference screenshots in `graphify-out/scratch/heatmap-v2-refs/`): the provenance-safe RENAME (a `heatmapLabels` capability + a Figure-Styling "Labels" panel) · COLUMN dendrogram (we have ROW only) · categorical annotation tracks (DR/PD group bars, type/dataset strips, from a sample sheet) · quant side track (mean log₂FC) · gene-of-interest highlight (red labels) · labels-right · block-split. Build plan in the spec; START with the rename + column dendrogram. **GATES:** FE tsc clean · eslint 0-err · vitest **231**; BE focused subsets (genes/capabilities/golden/styles/table-contract **111**, + **72**) + ruff clean; ⚠ a stub-FORCED full run "failed" only the 5 real-engine opt-in tests (iwx/gsea-library/reproduction-cold-drive) = invocation artifact (confirmed passing individually with the real engine — do NOT force stub on the full suite). Dev servers KILLED (FE :3000 + BE :8010; ⚠ eamos holds :8000 — NOT mine). memory [[selom-figure-edit-ux-pattern]] (updated: heatmap = 3rd example + rename/provenance principle) [[stack-browser-verification]] [[full-app-smoke-test-before-handoff]] [[selom-figure-editor-architecture]]. _(T18 below = PRIOR session.)_ -->

<!-- prior live-state (session T18): _Last updated: 2026-06-25 02:50 +10:00 (session "T18" — §11 capability generalization: VOLCANO threshold flagship + global gesture flip + GENE LABELLING + paintbrush icon) · Claude (acting FE+BE) — **MODE = Selom proper (OFF the ERG tangent: owner "relax on tangent … continue working on selom"; owner will signal when back on it).** **SHIPPED + VERIFIED LIVE + COMMITTED.** Combined live browser pass done on REAL DE data (`D:\selom-data\eyg28` PDE6B_FS_d210, 16,760×9): drop→classify "Differential-expression results"→volcano; (a) **threshold staged-edit** — set |log2FC|≥ 1→2 → live recount up 607→142 · down 334→53 · n.s.→16565 + "Pending changes" banner → re-run = new version with the recomputed DE table; (b) **gene labelling** — click a dot adds its gene as an arrow annotation (+ hover gene·log2FC·adj p), click the SAME dot removes it (toggle verified GALNT5 add→remove), labels are draggable annotations (`annotationPosition`), the Statistics-table **Label** column + gene search toggles the SAME shared set; (c) **global gesture flip** — the volcano renders `dragmode:false` + `scrollZoom:false` + Zoom/Pan buttons (live-confirmed); capabilities `{thresholds, geneLabels}` + per-point gene `customdata` confirmed on the live spec. Owner asked mid-session (all already-built behaviours, confirmed): labels CAN be moved (annotations) · clicking a dot again hides it (toggle). Owner ALSO asked: the **figure-styling icon → paintbrush, not stars** (stars reserved for the future Selom AI) — DONE across the pipeline stage · rail figure rows + collapsed dot · "Style/Open figure" · figure lists · home "Jump back in". Implements §11 of `docs/figure-data-capabilities/spec.md` via a new spec `docs/figure-data-capabilities/generalization-spec.md` (2 owner interviews locked the scope). **Owner decisions:** (1) shape = **volcano flagship (deep)**; (2) gesture default = **flip GLOBALLY to no-op**; (3) gene labelling = **both surfaces (canvas click + Statistics-table toggle), one shared `label_genes` set** — finish the threshold drag FIRST, then labelling. **Built (slices 1–5), each a direct parallel to the ERG dot-drag plug-in (a capability flag gates a chart-specific module):** ① **global gesture flip** — `lib/figure-model.ts resolveContract` now defaults EVERY figure to `dragmode:false` (no-op) + zoom/pan as modebar buttons + wheel-zoom off; a figure opts back into box-zoom via `gesture.default:"zoom"`; canvas reads `gesture.scrollZoom` (hardcode fixed). ② **`thresholds` capability** (declared-only) in the resolver + a central `app/backend/skills/_capabilities.py` stamp injected once in `theme.apply` (`_theme_for_kind` extracted) → volcano stamped (only its golden regen, render-inert; never clobbers an ERG stamp). ③ **`lib/volcano/thresholds.ts`** — pure `applyStagedThresholds` (re-bucket the UNION of up/down/n.s. by new FC/p cuts + move the 3 lines + live counts), idempotent + tolerant; `bucketCounts`/`readThresholds`/`gatherPoints`; 7 unit tests. ④ **`components/project/threshold-editor.tsx`** (numeric FC≥/p≤ + live up/down/n.s. count strip) gated on `capabilities.thresholds`; `project-workspace previewSpec` re-buckets live; staged edits trip the existing pending-changes banner. ⑤ **`components/figure/threshold-drag.ts`** — magnet-grab the FC (drags horizontal, both ± mirror) or p-value (drags vertical) line, axis-constrained, rAF-throttled live staging → points re-colour AS YOU DRAG, commit on release; wired in `figure-canvas` gated on `capabilities.thresholds` + an `onThresholdChange` handler, ONLY on the Figure-data preview canvas (a data edit lives in the Figure-data stage). ⑥ **GENE LABELLING (slices 7–9, §H):** `volcano/run.py`+`run_real.py::_assemble` stamp each up/down/n.s. point's gene SYMBOL (+ adj p) as `customdata` + a `hovertemplate` (customdata optional → proteomics_de that reuses `_assemble` stays byte-identical); `geneLabels` capability (declared-only) stamped for volcano in `_capabilities.py`; **`lib/volcano/labels.ts`** (pure) — `gatherLabelablePoints`/`findPoint`/`labeledGenes`/`toggleLabelOps`/`toggleGeneLabelOps`/`pointFromClick` (a label = ONE `layout.annotations` entry, identified by its `text`=gene; the volcano draws no other annotations); **canvas click-to-label** (`figure-canvas onToggleLabel`, gated on `geneLabels`, threaded via `EditorWorkspace`) → `project-workspace.onToggleLabel` commits to the figure store (instant, undoable, NO re-run); **Statistics-table Label column + gene search** (`stats-panel` `labeling` prop) → `onToggleGeneLabel` writes the SAME set; `openStats` now `figure.init`s so table+canvas share one editable spec; `applyStagedThresholds`/`gatherPoints` carry per-point customdata through a re-bucket. ⑦ **paintbrush icon** (Sparkles→Paintbrush for the figure-styling identity: `pipeline.tsx` stage · `workrail.tsx` rows+dot · `project-workspace.tsx` Style/Open/list · `app/page.tsx` recent-figures). **GATES:** FE tsc clean · eslint 0-err (30 pre-existing warns) · **vitest 218** (208 +10: labels 8 · thresholds customdata 1 · figure-model geneLabels 1); BE **full suite exit 0** + ruff clean; ONLY `tests/golden/volcano.json` regen (capabilities + customdata, render-inert). **COMMITTED** (BE/FE/docs scopes). Dev servers KILLED (BE :8010 + FE :3000; ⚠ eamos PID holds :8000 — NOT mine). **⚑ NEXT (owner-flagged, §11 roadmap): extend the capability + staged-edit model to the NEXT chart kinds** — heatmap colorscale-midpoint drag · UMAP color-remap/lasso · bar/box direct-manip (each = a chart-specific plug-in behind a declared capability, mirroring volcano/ERG). Out-of-scope-but-noted: persisting the gene-label set across a re-run (today labels reset on a new version; `top_n` re-seeds). memory [[selom-figure-edit-ux-pattern]] [[stack-browser-verification]] [[full-app-smoke-test-before-handoff]] [[selom-figure-editor-architecture]]. _(T17 below = PRIOR session.)_ -->

<!-- prior live-state (session T17): _Last updated: 2026-06-25 00:55 +10:00 (TANGENT session "T17" — capability-driven figure-editing surface + staged-edit live-drag UX) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision, due 12 Aug 2026); Selom pillars COMPLETE+parked.** **SHIPPED + PUSHED + VERIFIED LIVE on real Diagnosys `453_AAV`.** Hardened `docs/figure-data-capabilities/spec.md` from stub via an owner interview (AskUserQuestion — contract lives in `meta.selom`, gesture=select/no-op + zoom buttons, per-figure capability FLAGS, ERG-first, role-coloured dots), owner-confirmed, then built. **① Capability contract:** `_tracegrid.grid_spec` stamps `meta.selom.capabilities` (`gesture{default:"none",zoomTools,scrollZoom:false}` + `tools{landmarkMarks,scaleBar}`); `lib/figure-model.ts deriveFigureModel` resolves it → `model.capabilities{landmarkMarks,modelFit}` + `model.gesture`; canvas/inspector/Figure-data read the RESOLVED model (one source, never raw `meta.selom`). **② Gesture:** `dragmode=false` (no box-zoom default), zoom/pan = modebar buttons, **wheel-zoom OFF** (owner: a stray scroll on a multi-panel grid is fiddly to undo); dot-drag gated on `capabilities.landmarkMarks` (NEVER a UMAP scatter) + a 24px **magnet** proximity grab (`mark-drag.ts`). **③ Dot UX:** role colours (a=blue/b=amber/n1=teal/p1=violet, `_erg.ROLE_COLORS`↔`lib/erg/marks.ts`) + per-role pinned labels + role legend + `mark_labels` toggle. **④ STAGED-EDIT UX (the big owner win — live review; the canonical model now, see [[selom-figure-edit-ux-pattern]]):** a dot drag OR a numeric a/b time edit STAGES into ONE lifted `fdParams` (project-workspace) + moves the dot LIVE via `applyStagedMarks` (client-side, NO re-run per action); a prominent TOP **"Pending changes — Re-run" banner** replaced the per-control chips (kept ONE `LIVE` tag on the labels toggle); label show/hide = instant `hideDotLabels` restyle; **Figure data** (amber `--stage-figuredata`) vs **Figure styling** (cyan) — distinct stage colours + the "Figure"→"Figure styling" rename; scale-bar input alignment fixed. **VERIFIED LIVE** (BE :8010 + FE :3000, real not mock): drag/edit a b-time → the dot moves 105→60 ms live + the banner appears, ZERO re-runs; `gd._fullLayout.dragmode===false` + `scrollZoom===false`; role-coloured, per-role-positioned dots. **GATES:** FE tsc clean · eslint 0-err · vitest **199**; BE focused subset (golden/tracegrid/erg_units **112**, earlier full **209**) + ruff clean; 2 trace-grid goldens regen (render-inert capabilities block). Commits `96c975b`(BE) · `9d61ecb`(FE) · docs. No dev servers (killed :8010+:3000; ⚠ eamos PID 45792 holds :8000 — NOT mine, never kill). **⚑ NEXT (owner-flagged roadmap/pillar item): §11 GENERALIZATION** — extend the capability contract + the staged-edit live-preview UX across ALL skills/omics (UMAP · volcano · heatmap · bar · box/violin): classify each control LIVE vs RE-RUN, stamp `meta.selom.capabilities` per skill, flip the global gesture default. memory [[selom-figure-edit-ux-pattern]] [[selom-erg-manual-marks]] [[selom-figure-editor-architecture]] [[full-app-smoke-test-before-handoff]]. _(T16 below = PRIOR session.)_ -->

<!-- prior live-state (session T16): _Last updated: 2026-06-24 22:40 +10:00 (TANGENT session "T16" — IMPLEMENTED both specs: skill-references [FULL] + erg-manual-marks v1 [marks-only]) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision, due 12 Aug 2026); Selom pillars COMPLETE+parked.** **SHIPPED + PUSHED.** **① skill-references (FULL):** `background:str` + structured `references:list[dict]` on `SkillSpec` (`skills/contract.py`) + `skills/references.py` (type enum + tolerant `validate_reference`/`clean_references`, contract-test gated) + emitted in `registry.to_catalog_entry` (present-only) → a **"Skill Information" provenance card** in the Store detail (`components/store/skill-detail.tsx`; DOI→doi.org links + type tags; pure logic `lib/catalog/references.ts` +tests). **Seed wave backfilled with WEB-VERIFIED DOIs** (no guesses): ERG family (ISCEV 2022 `10.1007/s10633-022-09872-0`, Naka-Rushton 1966, Lyubarsky 1999, Bush 2019, Saszik 2002, iWorx/Diagnosys), gsea (Subramanian 2005 + gseapy/blitzgsea), melody (Korsunsky 2019), cepo (Kim 2021). **② erg-manual-marks v1 (operator override of ERG landmark TIMES):** `_erg.landmarks(manual=)`/`flicker_landmarks(manual=)` re-measure amplitude AT a supplied a/b (or N1/P1) time per ISCEV + `a_source/b_source∈{auto,manual}`; `metrics_from_waveforms(marks=)` keyed by `"{condition}|{stimulus_type}|{intensity_group}|{eye}"` (empty=wildcard); `parse_manual_marks`; `manual_marks` JSON param threaded through `erg_{traces,bwave_bar,intensity_response,flicker}` (+caption provenance count); `erg_traces` emits `meta.selom.marks` (auto times+source+µV, +trace/point when dots drawn) gated by a `marks` bool (default off). Default-off byte-identical (goldens stub-based, untouched). FE = **numeric Marks panel** (Figure-data; per-cell time inputs + measured µV + reset-to-auto + show-dots toggle → re-run, `components/project/marks-editor.tsx` + pure `lib/erg/marks.ts` +tests) + a **draggable-dot** layer (sticky-to-line HTML overlay + crosshair, `components/figure/mark-drag.ts`). **VERIFIED LIVE on REAL Diagnosys `453_AAV*.TXT`** (BE :8010 + FE :3000, real not mock): edit a b-wave time → re-run → the **Statistics table** picks up the new b-wave µV+time (120µV@40.5ms → 113µV@60ms) — the data-flow the owner wanted. **GATES:** BE focused subset green + ruff; FE tsc clean · eslint 0-err · vitest **187**. **⚠ DRAG/GESTURE = ROUGH EDGE (owner deferred to next session):** live demo surfaced (a) a `plotly.js` raw-import that 500'd SSR [FIXED — HTML-overlay dot, no plotly import; keep WebGL behind `dynamic(ssr:false)`] · (b) marks-editor param clobber + toggle-checked bugs [FIXED, `cddadd6`]. **⚑ OWNER STEER (NEXT SESSION, design-first): a CAPABILITY-DRIVEN figure-editing surface** — gate dot-drag/zoom/tools off the figure's `meta.selom` data+skill contract (drag for ERG trace/line, NOT a UMAP scatter; zoom = a BUTTON mode, not the default drag; default drag = select). Spec STUB `docs/figure-data-capabilities/spec.md` (research→design→spec→build, don't patch live). No dev servers (killed :8010+:3000; ⚠ eamos PID 45792 holds :8000 — NOT mine). Commits: `1594219`(BE refs)·`6bcda8e`(FE refs)·`7dc13d2`(BE marks)·`abdbf76`(FE marks panel)·`df25459`(FE drag)·`cddadd6`(FE marks fixes)·docs. memory [[selom-erg-manual-marks]] [[full-app-smoke-test-before-handoff]]. _(T15 below = PRIOR session.)_ -->




<!-- prior live-state (session T15): _Last updated: 2026-06-24 20:25 +10:00 (TANGENT session "T15" — photopic/cone a/b-wave landmark windows [shipped + literature-corrected] · 2 specs written for next session: erg-manual-marks · skill-references) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision, due 12 Aug 2026); Selom pillars COMPLETE+parked. BE tip `c312613` (feat) + this docs commit.** **Shipped (goal ① — photopic cone a/b metric):** the photopic FLASH figure decoded+rendered (T14) but the a/b *metric* (`_erg.landmarks`, used by `erg_bwave_bar`/`erg_intensity_response`/`erg_traces` when measuring FROM waveforms) used SCOTOPIC windows. `landmarks` now takes `mode` (scotopic|photopic) — chosen PER SEGMENT from `stimulus_type` (`metrics_from_waveforms`, which also adds `stimulus_type` to its grouping keys so the two modes' shared `GroupN` never merge) and per skill from `adaptation` (new `_erg.adaptation_mode`). **Windows anchored to PUBLISHED mouse cone-ERG timing + corroborated on the REAL .iwxdata:** cone a-wave EARLY (~14 ms, Lyubarsky 1999) → tight a(0–25 ms) window isolates it (the scotopic 0–40 ms window had mis-latched a ~27 ms noise dip as the a-wave); cone b-wave ~40–45 ms (Bush 2019 IOVS) → b(12–80 ms); smoothing kept HEAVY (3/16 ms) to null the ~50 Hz mains hum. ⚠ DATA-ONLY tuning first widened b to 130 ms to capture a dominant 104 ms C57 peak — the owner-requested literature backfill then showed that peak is the dim-flash **ROD** b-wave / hum, NOT cone (Saszik 2002 ~110 ms) → narrowed back to 12–80 ms. LESSON: tune on the data AND check the value vs the domain's published norms; the dominant peak isn't always the feature you're measuring. Default/unknown mode → scotopic, so every existing µV figure is **byte-identical** (goldens NOT regenerated). **Flicker unchanged** — 30 Hz intrinsically isolates cones, measured N1→P1 not the flash windows (re-confirmed real `453_AAV` 10 Hz 19.96 / 30 Hz 7.08 µV device). **VERIFIED on REAL data:** Rd10#288 a-wave now 7 ms (early cone) vs scotopic's wrong 27 ms; the cone window reports cone-range b-waves (rd10 49.8 µV @ 56.8 ms · C57 43.8 µV at the 80 ms edge = an honest "review this" flag) NOT the 156 µV/104 ms rod-hum artifact; the real photopic feed auto-picks cone windows even at the scotopic default; Diagnosys photopic-flash from-traces runs e2e. **GATES:** 165 ERG-family green (+4 erg_units: cone-window timing · `adaptation_mode` · per-segment mode · e2e bar) + ruff clean; goldens unchanged. No dev servers (pure pytest + Plotly file://; ⚠ eamos PID holds :8000 — NOT mine). **⚑ 2 SPECS WRITTEN this session — IMPLEMENT NEXT SESSION (owner: "give me an updated prompt … we will implement the specs/plans then"):** (1) `docs/records/erg-manual-marks/spec.md` — **MANUAL a/b + flicker N1/P1 OVERRIDE** (scientist drags the landmark points on the trace grid; bar/intensity-response measure AT those points; seeded from the auto-detection; extends the M3 `_tracegrid` markers overlay + the FE Marks panel). **v1 = MARKS ONLY** (owner-set) — provenance-log (R6) + blinding (R7) DEFERRED to v2. (2) `docs/skill-references/spec.md` — a **"Skill Information"/references** block on `SkillSpec` (`background` + structured `references[]`) → a Store provenance card. **Scope = FULL** (owner-set): schema + serialize + UI + the seed wave (ERG family · `gsea` · `melody` · `cepo`). Both backed by the 2 owner-requested web-research agents (BOTH BACK + applied: timing literature CORRECTED the photopic b-window to 12–80 ms [already applied]; manual marking + auto-seed CONFIRMED field-standard — Espion auto-seed+manual-adjust · ISCEV defs · ERGAssist hybrid). **NEXT (owner-set): IMPLEMENT both specs** (skill-references is the more self-contained/additive start), THEN goal ② = plan next Selom pillars (P1–P5 complete+parked). Spec build-log **T15 (D12–D14)** in `docs/records/erg-module/spec.md`; the 2 new specs in `docs/{erg-manual-marks,skill-references}/spec.md`. _(T14 detail below = PRIOR session; full text git @ `2eaff5d`.)_ -->

<!-- prior live-state (session T14): _Last updated: 2026-06-24 19:32 +10:00 (TANGENT session "T14" — protocol-GENERIC photopic .iwxdata · fully-dynamic group count · intensity-response shared `_charts` vocabulary) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision, due 12 Aug 2026); Selom pillars COMPLETE+parked. ALL pushed to main — BE tip `9d7d8b0`.** Owner asked: solve the photopic group-count blocker GENERICALLY (don't fork a skill), then commit; confirmed photopic intensity LABELS are a non-issue ("as long as it graphs correctly"). **Shipped:** **① photopic / protocol-generic `.iwxdata` (`76d581e`)** — `skills/_iwx.py` no longer hard-rejects non-7-group files; the intensity-group COUNT is read from the file (SWEEP_INFO block-ID index: scotopic 7 / photopic 5 / any N) + a `Protocol` registry (`resolve_protocol`, filename-hinted, tolerates the "Potopic" typo) fills the calibration blanks — known protocol → its cd·s/m² ladder + adaptation, unknown → ordinal `GroupN` + intensity `None` (honest, never fabricated). iWorx now emits `stimulus_type` (`scotopic_flash`/`photopic_flash`, the Diagnosys vocab) so a combined scotopic+photopic cohort filters to ONE mode via `_erg.resolve_flash_mode` (no shared-`GroupN` collision); scotopic byte-identical. **② fully-dynamic group count (`bcf618f`)** — only obviously-aborted SHORT runs fold; FULL groups never force-collapsed to a protocol count, so a different lab's 10/12-intensity "Scotopic"-named ladder survives intact (proven 3/5/7/10/12 named+unnamed; 28-file real scan = every scotopic exactly 7 `[10,10,10,10,10,5,5]`, photopic 5 `[10,10,10,5,5]`, the fold never fires on the owner's data). **③ intensity-response shared vocabulary (`9d7d8b0`)** — `erg_intensity_response` now speaks `_charts` (item ① of the line rollout): selectable `error`(sem|sd|ci95|minmax) via `spread_stats` · individual eye `points` via `point_markers` (the reviewer's "individual data points in all quantitative graphs" — was MISSING here) · optional ± `band` via `band_traces` (`spread`=error_bars|band|both|none); markers + Naka-Rushton fit idiom preserved; stub golden BYTE-IDENTICAL (no regen). **VERIFIED on REAL CMRI data** (photopic `Rd10#288_LE Photopic UV.iwxdata` → 5-group "Representative photopic ERG"; combined 7+5 cohort renders 7 OR 5 rows by `adaptation`, never 12; intensity-response on `erg_metrics_long.csv` 210 rows/6 cond → points+band+error-metric confirmed, sd≈√n×sem). **GATES:** BE focused subsets (iwx 10 · golden+contract 80 · erg/charts/methods 120 · ingest/data_inspect) + ruff clean; goldens NO regen. Spec build-log **T14 (D9/D10/D11)** in `docs/records/erg-module/spec.md`. No dev servers (pure-stdlib probes + pytest; Plotly file:// only; ⚠ eamos PID holds :8000 — NOT mine). **NEXT (owner-set, in order): ① finish PHOTOPIC cone-specific a/b/flicker metrics — the photopic FIGURE works, but `_erg.landmarks` windows (`_AWAVE_WIN_MS=(0,40)`, `_BWAVE_WIN_MS=(40,120)`) are SCOTOPIC-tuned → cone responses faster, so add photopic/cone-timing windows keyed off `stimulus_type`/`adaptation`, wired through `erg_bwave_bar` + `erg_intensity_response`; confirm flicker is photopic-correct (Diagnosys `erg_flicker` does N1→P1). ② plan next Selom phases/pillars (P1–P5 COMPLETE+parked; weigh Prism UX vs deeper figure-repro/Hani vs modality breadth vs launch gates).** _(T13 detail below = PRIOR session; full text git @ `a43043a`.)_ -->

<!-- prior live-state (session T13, full text in git @ a43043a): _Last updated: 2026-06-24 18:40 +10:00 (TANGENT session "T13" — ERG TRACE/line styling half · GENERIC line builder · C6 multi-file combine · C5 re-attach) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision); Selom pillars COMPLETE+parked. ALL pushed (BE `84da24a` · FE `7bc61fb` · docs = tip).** Owner: "you can do 1, 2, 3" + gave 3 new real data folders (Experiments 10/11, incl. **PHOTOPIC .iwxdata**). Shipped: **① TRACE/line styling half** — `erg_traces` `central`=representative|mean|**none** + `spread`=band|error_bars|individual|both|none + `error`(sem|sd|ci95|minmax) + `band_alpha`/`boundary_lines`/`error_every`/`band_color`; replicate-aware (sample_id×eye) → single-eye byte-identical, multi-eye/cohort = real mean+spread (also FIXED a latent jagged multi-eye representative). **② GENERIC line builder** (`_charts.py`, owner **D12** — mirrors `bar_figure`): `aggregate_replicates` (the ONE replicate-avg math, shared with the grid) · `band_traces`/`error_markers`/`individual_lines`/`point_markers` · `spread_line_traces` · **`line_figure`** · `LINE_PALETTE`; the grid keeps its `_tracegrid` overlay emitter but shares the math+vocabulary. **③ Owner asks D13:** band colour now MATCHES the trace (was grey ONLY for unmapped C57/Rd10 → every condition gets a stable explicit colour, line+band share it; `band_color` overrides) · **individual data points** on lines (`points`, with/without the mean) · `central=none` (individual traces only). **④ C6 multi-file** `engine.ingest_many([paths],labels)` (each file = a condition: explicit > file's `condition` col > stem; `sample_id` namespaced on collision; deterministic `condition_order`) + **`POST /data/combine`** (merged CSV + `X-Combine-Summary` header) + FE `combineData` + the data dropzone is `multiple` (drop several → one cohort); pandas in-memory ([[selom-data-substrate-decision]]). **⑤ C5 re-attach** — the lost-bytes banner REFILLS the existing dataset (`reattachDatasetId`/`onReattach`), no duplicate (built+gated, **not** live-verified). **GATES:** BE focused 189 + 127 + ruff clean; FE tsc/eslint(0-err)/vitest **173**. **VERIFIED on REAL cohort** (`\\cmri.com.au\…\ERG files\6. 29 Oct 2020 - Experiment 10` scotopic → `ingest_many` → C57 n=2 vs Rd10 n=6, "Mean scotopic ERG (±SEM, n=2–6)"; browser-rendered mean+**coloured**-band grid · individual-traces grid · generic dose-response line WITH points). **⚠ `_iwx` still rejects PHOTOPIC .iwxdata (7-group scotopic only)** → **C7 photopic-.iwxdata** is the next data-unblocked item (Diagnosys already covers photopic). **NEXT:** roll `_charts.line_figure`/`points` into `erg_intensity_response` + other line skills (per-golden, incremental) · C7 photopic .iwxdata · live-verify C5 re-attach. **Spec D12/D13** in `docs/records/erg-module/mean-spread-styling-spec.md`. No dev servers (Plotly file:// renders only; ⚠ eamos PID holds :8000 — NOT mine). **READ FIRST:** `docs/records/erg-module/mean-spread-styling-spec.md` · `skills/_charts.py` (line builders) · `skills/proprietary/erg_traces/run_real.py` · `engine/ingest.py` (`ingest_many`) · `main.py` (`/data/combine`) · `app/frontend/lib/intake/inspect.ts` (`combineData`) · `app/frontend/components/project/{data-panel,project-workspace}.tsx` · `D:\selom-data\diagnosys-erg\synthetic\erg_synth_multi.csv` + the 3 new CMRI ERG-files folders · memory [[selom-erg-module]] [[selom-diagnosys-erg]] [[selom-data-substrate-decision]] [[verify-on-real-data-not-mock]] [[selom-backend-python-exec]] [[kill-dev-servers-on-handoff]] [[compound-capability-each-task]]. _[prior T12 ↓]_ -->

<!-- prior live-state (session T12): _Last updated: 2026-06-24 04:12 +10:00 (TANGENT session "T12" — ERG flicker device markers · tracegrid overlay hook · GENERIC bar/line styling) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision); Selom pillars COMPLETE+parked. ALL pushed (origin ↔ main @ `8a5f385`).** Pushed the 5 prior T11 commits, then shipped: **① M2b (`239304a`)** `erg_flicker` prefers DEVICE N1→P1 — `_celeris.flicker_device_metrics` joins `device_n1p1_uv`/`device_p1_implicit_ms` onto the flicker waveform rows (source="device", folded-cycle fallback); real 453_AAV.TXT 10 Hz 19.96 / 30 Hz 7.08 µV (exact device). **② M3 (`4dc4d38`)** shared `_tracegrid` per-panel OVERLAY HOOK (markers/band/error/extra_lines on the panel axis; series-grouping now tracks the real line index) + N1/P1 DOTS on the flicker grid (`_erg.flicker_first_cycle_marks`, `marks` param). **③ Fig 1E BAR styling (`2c06bf0`)** from the owner's GraphPad target: `erg_bwave_bar` gains `error`(sem|sd|ci95|minmax) · `bar_fill`(pattern|filled|open hatch) · `show_error`+`points` toggles · significance brackets (computed welch/student/mwu AND overridable `A~B:**`) · `hline`/`vline` reference line · `legend`. **④ GENERIC `_charts.py` (`547dc3f`)** owner steer: styling applies to ALL bar/line graphs, not ERG — extracted `spread_stats`/`sig_stars`/`compare_groups`/`rgba`/`jitter`/`sig_brackets`/`ref_line`/`bar_figure`; `erg_bwave_bar.bar_spec`→thin wrapper; `_erg` re-exports; `test_charts.py` non-ERG proof. **⑤ Fixes (`8a5f385`)** owner-caught: trace-grid title no longer clipped (margin t=48 when titled) + every patterned bar visibly filled (light tint + denser hatch); goldens regen (erg_traces/flicker/bwave_bar). **GATES:** BE 883 + ruff; FE tsc/eslint/vitest 170; **browser-rendered** real Fig 1E bar (matches the target screenshot) + flicker N1/P1 dots. **Spec decisions D7–D11** in `docs/records/erg-module/mean-spread-styling-spec.md`. **NEXT:** TRACE/line styling half (`central=mean` + band/individual reps on the grid — test on `erg_synth_multi.csv` which has 6 eyes/cond; add a flat-line band builder in `_charts`) · C6 multi-file combine (`engine.ingest_many`; synthetic `by_condition/` + erg-fig1e CSVs ready) · C5 re-attach bytes (FE) · C7 photopic .iwxdata (DATA-BLOCKED — no photopic .iwxdata staged) · roll `_charts.bar_figure` into other categorical-bar skills (incremental, per-golden). **READ FIRST:** `docs/records/erg-module/mean-spread-styling-spec.md` · `skills/_charts.py` · `skills/_tracegrid.py` (overlay) · `skills/_celeris.py` (flicker_device_metrics) · `skills/proprietary/erg_{bwave_bar,flicker}/` · `D:\selom-data\erg-fig1e\` (real Fig 1E: metrics has per-eye n; waveforms 1-rep/cond) · memory [[selom-erg-module]] [[selom-diagnosys-erg]] [[verify-on-real-data-not-mock]] [[selom-backend-python-exec]] [[compound-capability-each-task]]. No dev servers started (Plotly file:// renders, tabs closed; ⚠ eamos PID holds :8000 — NOT mine). _[prior T11 ↓]_ -->
<!-- (T12 full text in git @ 8a5f385) -->

<!-- prior live-state (session T11): _Last updated: 2026-06-24 02:26 +10:00 (TANGENT session "T11" — ERG fan-out bars + intensity-response + styling spec + synthetic dogfood data) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision); Selom pillars COMPLETE+parked. 4 commits LOCAL, NOT pushed — ASK before push; origin ↔ main @ `5242d09`, +`8d223c4`/`d27ba46`/`2072118`/`78ec520`.** **① M1 (`8d223c4`+`d27ba46`):** `erg_bwave_bar` → scotopic+photopic, **a-wave AND b-wave** bars off ONE dropped recording — new `wave` param (a|b); when handed `erg_waveforms_long` it MEASURES the a/b peak from the traces via `_erg.landmarks` (the owner's "max b-wave peak from the traces at a settable intensity"); device markers win when present (D2); source captioned ("measured from traces" vs "device markers"). FE `wave` select + a/b-wave card. **② M2a (`2072118`):** `erg_intensity_response` ALSO fans out off the dropped waveforms (shared `_erg.metrics_from_waveforms`+`fs_from`, refactored out of the bar) → **the ERG materialize gap is CLOSED for the waveforms path: drop ONE file → trace grid + a/b bar + intensity-response all run, no metrics CSV, no re-dragging** (the fan-out / AnnData-Seurat model the owner picked over manual chaining). **③ Styling spec (`78ec520`):** `docs/records/erg-module/mean-spread-styling-spec.md` — owner's mean±spread / individual-points / trace-styles ask SCOPED (build next session); one orthogonal param vocab across bar+traces; shared `_tracegrid` overlay hook (also powers M3/A). **④ Synthetic dogfood data** (`D:\selom-data\diagnosys-erg\synthetic\`, gitignored): `erg_synth_multi.csv` (6 conditions × 6 eyes; scotopic+photopic+flicker) → the **multi-condition bar + trace means run TODAY** (bar groups by condition within one file); `by_condition/*.csv` for the C6 multi-file work. **VERIFIED LIVE** (real `453_AAV.TXT`, HTTP→ingest→skill): bars scotopic b 109.5 / a 53.4, photopic b 49.1 / a 6.9 µV (n=2 eyes); intensity-response Naka-Rushton Vmax 130.7 µV R² 0.929. Synthetic 6-cond bar 282/16/39/15/14/116 µV (n=6, matches Fig-1E ordering). **GATES:** BE units+golden+table-contract+celeris **106** + ruff clean; FE tsc/eslint clean · vitest **170**. Dev servers KILLED (eamos :8000 untouched). **NEXT (owner: "finish remaining next session"):** M2b (B) device N1/P1 feed for flicker (materialize `erg_markers_long` sibling; real-data join check) · M3 (A) N1/P1 dots on the flicker grid (build the shared `_tracegrid` overlay) · M4 (C) C5 re-attach bytes / C6 multi-file → one grid (unlocks the real 6-cond bar + real-n means; synthetic `by_condition/` ready) / C7 photopic .iwxdata · then the styling-feature build. **READ FIRST:** `docs/records/erg-module/mean-spread-styling-spec.md` · `docs/records/diagnosys-erg/spec.md` · `skills/_erg.py` + `skills/_celeris.py` (markers_long, M2b) + `skills/_tracegrid.py` (overlay, M3) + `skills/proprietary/erg_{bwave_bar,intensity_response,flicker}/run_real.py` · `app/frontend/lib/catalog/{seed,params}.ts` · `D:\selom-data\diagnosys-erg\synthetic\README.md` · memory [[selom-diagnosys-erg]] [[selom-erg-module]] [[verify-on-real-data-not-mock]] [[selom-backend-python-exec]] [[kill-dev-servers-on-handoff]] [[compound-capability-each-task]]. _[prior T10 ↓]_ -->

<!-- prior live-state (session T10): _Last updated: 2026-06-23 23:49 +10:00 (TANGENT session "T10" — Diagnosys ERG Phases 2–4, owner's real work) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision); Selom pillars COMPLETE+parked. ALL of this session's work is SHIPPED + PUSHED (origin ↔ main @ `3932d72` + the docs commit carrying this).** **The Diagnosys/Celeris ERG skill is now Phases 1–4 COMPLETE.** **① Phase 2 photopic (`f9be40b`):** `adaptation` hint (auto|scotopic|photopic) on erg_traces/erg_intensity_response/erg_bwave_bar via shared `_erg.resolve_flash_mode` (explicit `stimulus_type` wins); dynamic scotopic/photopic title; erg_bwave_bar gained the stimulus filter it lacked (multi-mode Group-collision fix). **② Phase 3 NEW skill `skills/proprietary/erg_flicker`:** two views of one skill — a steady-state waveform trace grid (reuse `_tracegrid`) + an N1→P1 amplitude-vs-frequency summary (NO Naka-Rushton); N1→P1 re-derived from the phase-folded steady-state cycle (`_erg.flicker_cycle`/`flicker_landmarks`), matches the device markers at 10 Hz (17.4 vs ~19 µV) + tracks the ~2× drop at 30 Hz; native `spec["table"]`; table-contract guard + golden registered. **③ `theme.apply`** now prefers `layout.meta.selom.figureKind` over per-id `_KIND` (waveform→trace_grid axes hidden · summary→base axes visible; backward-compatible — erg_traces unchanged). **④ Phase 4 methods `_erg_flicker`** + adaptation-aware scotopic/photopic wording on the 3 flash builders. **⑤ FIX:** `_save_upload` preserves the upload's ORIGINAL filename (per-upload temp subdir, mirrors `_save_capped`) → an ERG figure's condition/sample label reads as the file stem not `tmpXXXX` (all skills). **FE (`3932d72`):** seed `erg_flicker` card (Electrophysiology shelf) + params overlay (`view` waveform|summary + `adaptation` on the 3 flash skills) + MSW spec fixture. **GATES:** BE affected subsets (golden/table-contract/celeris/cleaning/data_inspect/erg_units **127** · methods/registry/litsynth/legends **55** · erg_flicker/erg_units incl. real opt-in **11** · run-endpoint suite **38**) pass + ruff clean; FE tsc/eslint clean · vitest **170**. **VERIFIED LIVE** (real BE :8010 + FE :3000, NOT dev:mock; full HTTP→ingest→theme→table on the real `453_AAV.TXT`): flicker waveform (trace_grid) + N1/P1 table (10 Hz 17.4 µV/54.6 ms · 30 Hz 4.2 µV) + summary (visible axes); photopic traces title+methods; Store ERG-Flicker card (runnable 31); condition label now the file stem. Dev servers KILLED (:8010 + :3000; ⚠ eamos PID 45792 holds :8000 — NOT mine, untouched). **NEXT:** named Diagnosys ERG scope (Phases 1–4) DONE → await owner steer; optional follow-ups = (a) N1/P1 marker DOTS on the flicker waveform grid (would extend `_tracegrid`; today in the table+summary, metric Selom-re-derived not device) · (b) device-marker feed for flicker (materialize `erg_markers_long`) · (c) T8 leftovers C5/C6/C7. **READ FIRST:** `docs/records/diagnosys-erg/spec.md` · `app/backend/skills/proprietary/erg_flicker/{run,run_real}.py` + `skills/_celeris.py` + `skills/_erg.py` + `skills/theme.py` · `app/frontend/lib/catalog/{seed,params}.ts` · memory [[selom-diagnosys-erg]] [[selom-erg-module]] [[stack-browser-verification]] [[verify-on-real-data-not-mock]] [[selom-backend-python-exec]] [[kill-dev-servers-on-handoff]]. _[prior T9 state ↓]_ -->

<!-- (T10 full text also in git @ 5242d09) -->

<!-- prior live-state (session T9): confidence-chip unification (shared ConfidenceChip/TintChip — lib/ui/confidence.ts + components/ui/confidence-chip.tsx; reproduction byte-identical + DataTypeStrip + skill-match tier chip consume it) · ERG-by-format classification (engine.ingest ERG loaders record meta["erg_format"] + kind=GENERIC_TABLE, profile_data emits ERG-certain-by-format, R-ingest-1) · handoff prune (CURRENT.md −70%). Commits 64a13b7+0937ccf (T8 display-unit) · fd5f67b (confidence-chip) · b33486a (ERG-by-format) · 15ded87+e725030 (prune+refresh). Full detail in git log @ e725030. -->

<!-- prior live-state (session T8): _Last updated: 2026-06-23 20:08 +10:00 (TANGENT session "T8 + Diagnosys ERG" — owner's real work) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision); Selom pillars COMPLETE+parked. This session: (1) T8 SHIPPED+PUSHED; (2) the Diagnosys/Celeris ERG skill — SPEC APPROVED + Phase 1 BUILT (committed locally, NOT pushed — ASK before push).** **① T8 (PUSHED, origin ↔ main @ `96dd932`):** generalized + hardened the ingest engine across ALL modalities — `engine/cleaning.py::profile_data()` rebuilt to a ranked candidate model (`Candidate{code,label,confidence,source,reason}` + `DataProfile.candidates[]`/`.mismatch`; content OUTRANKS filename; name-vs-content disagreement → a mismatch nudge, never overrules) + `main.py` `/data/inspect` honours a Kind override; FE `DataTypeStrip` mismatch nudge + "Or:" alt chips + Reset-to-auto; **+ the ERG overlay/axis toggle** (`figure-model.projectOverlay` + layout/overlay ops; `figure-canvas` + data-panel `LayoutControl`). Commits `abb0aea` (BE) + `96dd932` (FE). **② Diagnosys/Celeris ERG skill — SPEC APPROVED (`docs/records/diagnosys-erg/spec.md`) + Phase 1 BUILT (LOCAL, not pushed):** owner-approved tri-modal scope (scotopic + photopic + flicker from a raw Diagnosys export; greenfield; subsumes the C7 photopic optional). **Data-format lineage mapped:** native `.BAK` = proprietary `ESP`-magic Espion DB (OUT of scope) → **full multi-table `.TXT`** (TAB; Contents/Header/Marker/Summary/Stimulus/Data tables — the PRIMARY target) → reduced `.CSV` (COMMA; Marker+Data only — the friend's export) → processed `.xlsx` (out). One real `.TXT` carries ALL THREE modes (DA 0.01–10 cd·s/m² scotopic · LA Single 3.0/10.0 Flash photopic · LA 10/30 Hz Flicker) → **no data gate.** **Phase 1 (parser + scotopic reuse) BUILT + validated on real data:** `skills/_celeris.py` (pure-stdlib: delimiter sniff · Contents-Table locate + banner-scan fallback · split-line repair · Stimulus-Table read [DA/LA/Flash/Flicker + cd·s/m² + Hz] else marker-vocab inference · `b`≡`B` case-fold · channel→eye · nV→µV · baseline-correct) → emits `erg_markers_long` (a/B/b/c/OP1-3/N1/P1) + `erg_metrics_long` (the a/b view the existing skills read) + `erg_waveforms_long` (primary ERG channel per eye; OP/secondary excluded). **Ingest wired:** `engine/ingest.py` `diagnosys_erg` loader (magic-header recognize, BEFORE the csv loader, `materialize=True`) + `engine/cleaning.py` recognizes the ERG waveform shape (time+voltage). **Scotopic REUSE:** `erg_traces` + `erg_intensity_response` run UNCHANGED on the parser output — only addition = a backward-compatible `stimulus_type` mode-filter (no-op when the column is absent → iWorx unchanged). **FE:** `components/project/data-panel.tsx` dropzone now accepts `.txt`. **VALIDATED** on real Will-Y exports (`453_AAV`: 9 steps = 5 scotopic / 2 photopic / 2 flicker classified; device a/b extracted; nV↔µV reconciles vs the µV markers; e2e — `erg_intensity_response` Naka-Rushton Vmax 164 µV + `erg_traces` 5-intensity grid both ran on the parser output). Samples staged → `D:\selom-data\diagnosys-erg\` (+ README). **⚠ NOT yet LIVE-verified in the browser** (drop a `.TXT` → `/data/inspect` ERG → run `erg_traces`); validated at the engine/skill level via pytest + direct calls — the live FE drop→run is the immediate next check ([[verify-on-real-data-not-mock]]). **GATES:** BE **850 passed / 1 skip** + ruff clean (new `tests/test_celeris.py`: helper units + synthetic reduced-CSV fixture + opt-in real-file). FE tsc clean · eslint 0 err (1 pre-existing warn) · vitest **170**. **PROPOSED commits (NOT pushed): `feat(backend)` (`_celeris` + ingest + cleaning + the 2 skills + test) · `feat(frontend)` (.txt dropzone) · `docs` (spec + this handoff).** **NEXT:** ASK before push; then **LIVE-verify Phase 1** in the browser; **Phase 2** (photopic — `adaptation` param wiring + validate the LA Single Flash steps); **Phase 3** (flicker — NEW `erg_flicker` skill + N1/P1 graph + amplitude-vs-frequency); **Phase 4** (`methods._erg_flicker` three-mode ISCEV + FE polish). **No dev servers started this session** (validated via direct PY/pytest + npx gates — none to kill; Eamos :3001 untouched). **ENV:** BE PY+PYTHONPATH per [[selom-backend-python-exec]]; FE `npm run dev` NOT dev:mock ([[verify-on-real-data-not-mock]]); real samples at `D:\selom-data\diagnosys-erg\{full-txt-exp8,full-txt-exp9,reduced-csv}`. **READ FIRST:** `docs/records/diagnosys-erg/spec.md` · `app/backend/skills/_celeris.py` + `engine/{ingest,cleaning}.py` + `skills/proprietary/erg_{traces,intensity_response}/run_real.py` · `D:\selom-data\diagnosys-erg\README.md` + memory [[selom-erg-module]] [[selom-engine-ingest-tabular-gap]] [[selom-figure-editor-architecture]] [[verify-on-real-data-not-mock]] [[layered-deterministic-extraction]] [[selom-backend-python-exec]] [[kill-dev-servers-on-handoff]]. _[prior T7 state ↓]_ -->

<!-- prior live-state (session T7): _Last updated: 2026-06-23 11:40 +10:00 (TANGENT session "T7" — ERG figure / engine-ingest, owner's real work) · Claude (acting FE+BE) — **MODE = ERG tangent (Eamegdool PDE6B *Gene Therapy* revision, due 12 Aug 2026); NOT the Selom pillars (complete+parked). 9 commits SHIPPED + PUSHED (origin ↔ main @ `fea54c1`; T7 core `2e80ef2`/`15698aa`/`fe2ecfa`/`8fca2d0`/`a4a57ab`/`ecb44f3` + the two owner-requested follow-ups `9386f49`+`a3a60f1` (.iwxdata ingest) + `fea54c1` (lost-bytes re-upload gate); tree clean; dev servers KILLED — real BE :8000 + FE :3000 stopped, Eamos :3001 untouched).** **⊕ T7 FOLLOW-UPS (owner: "push then do the natural follow-ups"):** (A) **`engine.ingest` now DECODES the native `.iwxdata` ERG format** — `skills/_iwx.py` (pure-stdlib, ported from `iwx_parse.py`; CH000.dat big-endian float64 + per-intensity grouping) → canonical `erg_waveforms_long` DataFrame; `_Loader.materialize` writes the decoded table to a temp CSV so the path-based `erg_traces` runs unchanged (`source` keeps the original). A dropped `.iwxdata` is **ERG-certain by FORMAT (L1)** + drop-and-run. Verified on REAL CMRI files (`\\cmri.com.au\Storage\Shared\Eye Genetics\PDE6B\ERG files`, 516 files; C57 157–264 µV vs Rd10 15–80 µV). FE dropzones accept `.iwxdata`. Tests: synthetic-ZIP (CI-safe) + opt-in real-file. (B) **Lost-File-bytes run path FIXED (T6 #11)** — `resolveRunFile()` returns null when there are no session bytes outside mock mode → a calm "Re-upload it to run" banner + "Re-upload in Data" button across all four run paths, instead of POSTing a 4-byte placeholder that fails confusingly. Verified live (Apply with no bytes → banner, 0 run POSTed). Gates: BE 121 + ruff clean; FE tsc/eslint 0 err/vitest 164. **⚑ The T6 HEADLINE BLOCKER ([[selom-engine-ingest-tabular-gap]]) is RESOLVED — and the T6 root-cause was WRONG.** It was NOT `engine.ingest` (that classified the ERG metrics CSV correctly as `generic_table`, columns intact, all along). The bug was the **FE intake MOCK**: `lib/intake/mock.ts::detectModality(filename)` mapped every `.csv` → "bulk RNA-seq" and `mockQcReport` returned a hardcoded `24 × 22,000` − "drop low-count genes" = the bogus **"24 × 18,102" + gene-subset cleaning**, never the real `/data/inspect`. (Lesson [[verify-on-real-data-not-mock]]: verify against the engine DIRECTLY before trusting the FE's displayed verdict.) **Fixed per the owner's layered steer (no false classification; format → keywords → user; dynamic cleaning pane):** **① BACKEND (`2e80ef2`):** new `engine/cleaning.py` — `profile_data()` (layered honest data-type label: L1 format `.iwxdata`=certainly ERG · L2 high-precision columns a-/b-wave+intensity=ERG · L3 modality; an untypable table stays a neutral "Data table", NEVER a false guess) + `plan_cleaning()` (DYNAMIC cleaning keyed off modality: count matrix → real steps w/ orientation-correct cheap deltas; results/generic/ERG → honest EMPTY plan "used as-is", no gene cleaning). `engine/route.py::route_profile()` routes a recognized ERG table to the electrophysiology skills. `/data/inspect` (+ a `profile=` override) and the run `data_check` now carry `profile` + `cleaning_plan`. Run endpoint catches a runner `ValueError` → **HTTP 400 with the real cause** (was a misleading 5xx "service unavailable"). **② FRONTEND (`15698aa`):** `lib/intake/inspect.ts` (fail-soft `inspectData` + `qcFromInspect`/`modalityFromKind`) — intake now classifies the REAL payload via `/data/inspect`, not the filename; new `DataTypeStrip` (label + confidence + reason + the L3 override = the shared Radix Select) atop a DYNAMIC `CleaningReport` (honest empty-state for non-matrix); `store.updateDatasetProfile` persists it; rail/list show the profile label; MSW `/data/inspect` mock (header-only) so dev:mock matches. **VERIFIED LIVE end-to-end** (real BE + real `erg_metrics_long.csv`, NOT dev:mock): drop → "ERG / electrophysiology (likely)", NO gene cleaning, 210×11, routes to ERG skills; run `erg_intensity_response` → the real Naka-Rushton "b-wave intensity-response" figure; a plain table → 400 "missing required columns […]" (honest). Waveforms CSV → honest "Data table" (no a/b-wave signature → no false ERG claim). Screenshots `graphify-out/scratch/t7-*`. **③ FIGURE-EDITOR P3 + P4 (`fe2ecfa` + `8fca2d0`) — the restructure is now COMPLETE.** P3 (now that `_tracegrid` emits `meta.selom`): **click-to-select** (canvas `plotly_click` → focuses the trace's series in the Data tab) · **Marks tab** (scale-bar show/hide + resize, resolved from `meta.selom.primitives`; annotations list edit/hide) — `figure-model.ts` gained `scalebar` + `scalebar*Ops`/`annotation*Op`. P4 (impeccable audit + ui-ux-pro-max + frontend-design per CLAUDE.md): one real finding fixed — the `DataTypeStrip` override was a one-off native `<select>` → shared Radix Select (component-vocabulary consistency); other new surfaces passed. Verified LIVE on the real ERG trace grid (Marks tab, scale-bar hide/restore, 15 annotations). **GATES:** BE 89 tests + ruff clean (test_cleaning, test_data_inspect, test_run_error_surface added; PY/PYTHONPATH per [[selom-backend-python-exec]], ruff binary copied OUT of the EDR-blocked .venv). FE tsc clean · eslint 0 err · vitest 164 (+10: inspect.test 4 + figure-model P3 6). **NEXT:** all named ERG-tangent items DONE + the two owner-requested follow-ups DONE + PUSHED — **await the owner's next direction.** Optional remaining nice-to-haves only: re-attach re-uploaded bytes to the EXISTING dataset (today re-upload via the banner creates a new dataset — the run then works, but it's a fresh dataset record); scale-bar resize repeated-edit edge cases; multi-`.iwxdata` ingest → a multi-condition grid (today one file = one eye/condition); a real photopic-protocol `.iwxdata` path (currently errors honestly). **ENV:** BE = `$PY -m uvicorn main:app --port 8000` with `SELOM_SKILLS_ENGINE=real` + the PYTHONPATH ([[selom-backend-python-exec]]); FE = `npm run dev` (real, NOT dev:mock — [[verify-on-real-data-not-mock]]); staged real data `graphify-out/scratch/erg_metrics_long.csv` + `erg_waveforms_long.csv`. **READ FIRST:** `docs/records/erg-module/findings-2026-06-23.md` (Resolved section) · `app/backend/engine/{cleaning.py,route.py}` + `main.py` (/data/inspect + run) · `app/frontend/lib/intake/inspect.ts` + `components/project/{data-type-strip,cleaning-report}.tsx` + `components/figure/panels/marks-panel.tsx` + `lib/figure-model.ts` + memory [[selom-engine-ingest-tabular-gap]] [[selom-figure-editor-architecture]] [[selom-data-fit-scorer]] [[selom-erg-module]] [[verify-on-real-data-not-mock]] [[layered-deterministic-extraction]] [[kill-dev-servers-on-handoff]]. _[prior T6 state ↓]_ -->

## Prior live-state comments (session T25) — archived 2026-06-27

<!-- prior live-state (session T25): _Last updated: 2026-06-27 20:27 +10:00 (session "T25" — **ARCHITECTURE-CONSISTENCY GATE**: Task B FULLY COMPLETE [B1–B5 + exit smoke] + **Task C C1·C2·C3 SHIPPED this session** [content-addressed result cache · source/render split + ETag/304 · input cache + param-range 400 + exec timeout]) · Claude (acting FE+BE) — **MODE = architecture-consistency gate** (owner-chosen active lane "until mostly complete"; all other pillars/tangents ON HOLD). **OWNER STEER 2026-06-27 20:27: stop before C5 — "can do that next session." C5 + D + E are queued; the DB-materialization DISCUSSION GATE still stands before any C4/D4/F bucket.** **PUSH STATE (2026-06-27 20:27 +10:00): all of Task B is PUSHED — origin/main = `58bb9dd` (pushed at this session's start, owner-cleared "you can push before continuing"). Task C **C1 `c8747ca` · C2 `7f841a9` · C3 `909264e`** are COMMITTED locally, **NOT pushed** (3 ahead of origin) — OWNER pushes by default; all green + ready to push. Verify vs origin at session start.** **✅ SHIPPED THIS SESSION — Task B5 (registry-completeness CI gate, cross-lane):** promotes the `params.ts` `warnDeadKnob` dev console warning to a FAILING test, on BOTH lanes. **Spine** = the backend `skill.json` param_spec + `list_skill_ids()`; two FE/BE mirrors validated against it, each in its OWN lane's fast gate (reachability picks the home). **FE** new `lib/catalog/registry-completeness.test.ts` reads the REAL skill.json via `node:fs` (the cross-lane read IS the point — it exists to catch FE↔BE contract drift) → every `params.ts` PRESENTATION overlay key backs a real param_spec entry · every overlay skill has a backing skill.json · the dev:mock fixture renders the same controls as live · no stale fixture key; new export `params.ts::overlayParamKeys()`. **BE** `tests/test_capabilities.py` (+2) → every `_capabilities._PROFILES` skill id ∈ the registry · every declared capability tool ∈ the FE-known allow-list (mirrors `figure-model.ts` `SelomCapabilities.tools`). **DRIFT FIXED en route (the gate's first catch):** the `mocks/skill-spec-fixture.ts` `SKILL_PARAM_SPECS` dev:mock fixture was stale — missing the heatmap clustermap controls (`cut_k`/`annotations`/`quant_track`/`split_by`/`split_by_cut`) + the ERG mean/spread (`central`/`spread`/`error`/`band_*`/`boundary_lines`/`error_every`), bar-significance (`wave`/`show_error`/`error`/`bar_fill`/`legend`/`comparisons`/`sig_test`/`hline`/`hline_label`) and flicker `marks` controls → brought current so dev:mock matches live. **GATES:** FE tsc clean · eslint 0-err · **vitest 293** (+5); BE `test_capabilities` **8** (+2). **RED-THEN-GREEN VERIFIED (the verify for a pure-test bucket — no browser needed):** `volcano.highlight`→`highlightz` → FE red ("overlay keys absent from the backend param_spec: volcano.highlightz"); volcano tool `thresholds`→`thresholdz` → BE red ("declares capability tool(s) {'thresholdz'} the FE can't resolve"); both reverted, green. ruff **clean** (both `-m ruff` and the direct `.venv/Scripts/ruff.exe` are EDR-blocked → ran via the documented copy-to-TEMP workaround: `cp .venv/Scripts/ruff.exe $TEMP/ruff.exe && $TEMP/ruff.exe check`) [[selom-backend-python-exec]]. **✅ SHIPPED THIS TURN — Task B EXIT smoke (capstone) PASSED** (browser-driven via chrome-devtools MCP, 2026-06-27, on the cleaned localStorage fixtures, webpack `npx next dev` on :3000, **no backend** → every analysis-input pane correctly shows B3's `error` slot [param-spec describe 404] = the floor, not a crash). **~150 dataset/skill/view switches** through the REAL UI (programmatic clicks → real React handlers) across **all 5 skills**: `erg_traces` + `erg_intensity_response` (p_7821cab4, SVG, 2 skills × multiple ds), `volcano` (p_3384a01f, **WebGL scattergl**), `heatmap`/clustermap (p_fb5166e1, SVG + dendrogram + strips), legacy no-spec `umap` (demo-pbmc → B2 "Figure spec not stored" empty state). **All four acceptance met:** (a) **zero crashes / zero boundary trips** (`role=alert` "rest of the editor is fine" never appeared; console = only the expected param-spec 404s, **no `[error-boundary]`, no React/Plotly/WebGL errors, no "too many WebGL contexts"**); (b) **every pane a stable slot** (`data-pane-status` loading→error→ready; empty states "Figure spec not stored"/"Re-run to generate"/"No points yet"; never `null`); (c) **no stale-param leak (B4)** — staged a distinctive `fc=5` on the volcano fc=1 version, switched to the fc=3 version → it showed its OWN base (3) with NO pending banner, return to v1 reset to 1 (`fdScope` reset + `key={fdScope}` remount confirmed); (d) **heap flat across switches (E1 overlap)** — delta between TWO post-GC measurements *after* Plotly's one-time module load (the 87→190 MB jump = that load, permanent, NOT a leak): **SVG/ERG 190.65→189.53 MB** over 60 switches, **WebGL/volcano 195.52→195.68 MB** over 48 switches; scattergl canvas capped at 3 and back to **0 at home** every time (react-plotly purges on unmount); snapshot files flat (76.2→74.4 MB). Fixtures intact (7 projects / 18 figs / 16 ds, no re-runs since no backend). Heap snapshots + backup in this session's scratchpad. **✅ SHIPPED 2026-06-27 19:59 +10:00 — Task C1 (content-addressed result cache, BE; pytest-verified, no browser; COMMITTED — owner pushes):** new pure `skills/_result_cache.py` = two-tier (in-proc LRU + local-disk JSON under `data/result_cache/`) keyed by `(skill_id+version, canonical(params), input_sha256)`. `canonical_params` overlays skill defaults, type-coerces (a string query arg ≡ the typed default), **drops defaults**, rounds floats to a stable precision, hashes `_`-prefixed file params (design sheets) by content. Wired into `contract.py::_execute` (the ONE chokepoint every `run_skill*`/`run_bundle*` funnels through) — caches the **pre-theme** compute + re-applies `theme.apply` per hit (deterministic; it deep-copies → no mutation; forward-compatible with C2's source/render split). **Invalidation automatic** (version in the key → a bump is a clean miss; no TTL). Caller-isolated (deep-copy on set+get). Config `SELOM_RESULT_CACHE=on|off` + `_MEM_MAX`. **Dependency-free deviation** from the roadmap's named `diskcache` (owner asked "should it be installed?" → NO: content-addressing makes diskcache's eviction/concurrency unneeded for an immutable cache + it'd mutate the EDR-fragile venv; the R2 tier C4 swaps the disk backend behind the same interface). New `tests/conftest.py` disables the cache **suite-wide** (autouse) so golden tests still recompute (a disk-surviving cache would false-green a skill changed without a version bump); the cache's own tests opt back in. ⚠ a missing input path (the golden harness's `"unused"`) is unhashable → cache skipped entirely (no spurious collisions). **GATES:** `test_result_cache.py` **15** (canonicalization · key stability/version-sensitivity/input-byte tracking · two-tier round-trip + disk-survives-restart + LRU + caller-isolation + disabled-flag · **ACCEPTANCE**: an identical re-run = a measured `mem_hits` with the runner NOT re-entered [a call-counter proves no recompute], a non-default param + a `skill_version` bump each miss cleanly, a real stub skill caches end-to-end through the genuine `_execute`/`theme.apply`/stamp path); contract+capabilities **58**, golden+integration **67** all green; ruff clean (TEMP-copy workaround). **✅ SHIPPED 2026-06-27 — Task C2 (source/render split + ETag/304, BE; pytest-verified, no browser; COMMITTED):** the split = C1 caches the **pre-theme** compute (source) + theming is a separable render step. C2 adds the **figure-envelope cache**: new `theme.py::render(spec, skill_id, style, source_key=None)` wraps `apply`, caches the themed figure keyed by `(source identity, skill_id, style, THEME_VERSION)` → a theme/style change re-renders from the cached source **without re-running the skill**; a repeat render of the same source+style hits. `source_key` lets `_execute` reuse its compute key (no re-hash of a big figure); `/figures/style/apply` hashes the figure by content. New `THEME_VERSION` const (bump on any theme/style-token change → envelopes miss clean). Render cache reuses the C1 store via new generic `put`/`fetch` (`get`/`set` now wrappers); envelope keys `render-…` prefixed (filename-safe — `:` would silently break the NTFS disk tier, **a bug the cold-disk test caught + an `errors==0` assert now guards**). Wired into `_execute` + `POST /figures/style/apply`. **ETag/304:** `GET /jobs/{job_id}/result` emits a strong content-hash `ETag`, honors `If-None-Match` → **304**. ⚠ the export/rasterize `theme.apply` left as-is (terminal raster, not an editable re-render). **GATES:** `test_result_cache.py` 22 (+3 render: envelope hit · style+THEME_VERSION bump miss · acceptance style-change does NOT re-enter runner; end-to-end now asserts BOTH tiers hit + errors==0), `test_jobs.py` (+1 ETag/304), contract/capabilities/export/legends/data_inspect/guardrails 110, golden+integration green, ruff clean. **✅ SHIPPED 2026-06-27 — Task C3 (input cache + param-range 400 + exec timeout, BE; pytest-verified, no browser; COMMITTED):** three run-path guards. **(1) Parsed-input cache** — new `engine.ingest_cached(src,hint,sheet,sep)` memoizes `engine.ingest` in-process keyed by input **content hash** → same bytes not re-parsed across `/data/inspect` + `/run` (each uploads its own temp path, same bytes = same key). In-process only (live AnnData/DataFrame payload). On a hit payload+kind **shared read-only** (QC/profile read them; skill runs from `path`, a file), `source`/`qc`/`path` **rebound per request** (materialized parse keeps its decoded temp; direct file rebinds to the current upload) so no cross-request corruption. Wired into `/data/inspect` + `_inspect_for_run`. **(2) Param-range validation** — new `contract.validate_param_ranges(spec,params)` enforces `param_spec` type/`[min,max]`/`options` at the API → **400** `param_out_of_range` (e.g. `fc_threshold=100` on max:5) on `/run` + `/skills/{id}/jobs`, before any heavy work; unknown/`_` keys ignored. **(3) Exec timeout** — `/run` runs the skill in a worker thread under `SELOM_SKILL_TIMEOUT_S` (default 120) via `asyncio.wait_for(run_in_executor)` → hung skill returns **504** promptly, event loop stays live (also fixes the prior sync event-loop block). ⚠ true kill needs a subprocess worker (deferred infra); abandoned thread finishes on its own (documented). New config `SELOM_INPUT_CACHE`/`_MAX` + `SELOM_SKILL_TIMEOUT_S`; conftest disables the input cache suite-wide (settings singleton built at import → flip the attribute). **GATES:** `test_input_cache.py` 3 + `test_run_guards.py` 7 (range unit numeric/option/type + ignore unknown; endpoint /run & jobs 400 · happy 200 · hung 504), engine+endpoint+cache regression 124, golden+integration green, ruff clean. **⚑ NEXT = Task C C5 (param-spec caching — FE localStorage cache keyed skill_id+version + stamp param_spec into figure provenance at run; B3's error/Retry stays the floor) → D1–D3 (table contract) → E1–E2. ⚠ then HARD STOP → owner DISCUSSION GATE before ANY materialization bucket (C4 · D4 · F1–F3).** **⚠ DISCUSSION GATE (owner 2026-06-27): complete every DB-FREE bucket (Task B exit · C1–C3 · C5 · D1–D3 · E1–E2), then STOP and raise it with the owner BEFORE any materialization bucket (C4 · D4 · F1–F3) — "there may be a change of plans."** **OWNER ASK — ERG control discoverability (SHIPPED `57d3249`, live-verified):** owner wanted the ERG spread / SEM-vs-SD / individual controls more discoverable. ROOT CAUSE: they were gated behind **"Trace shows" = Mean of replicates** (`central=mean`) via `showWhen` (HIDDEN until then) — NOT behind n; with the default "Representative" they were hidden by design (a single representative trace has no spread to shade). FIX = a new reusable **`enabledWhen`** gate (companion to `showWhen`) in `params.ts` — the control always RENDERS but is shown DISABLED (opacity-50 + cursor-not-allowed + semantic `disabled`, per the `ui-ux-pro-max` review = `progressive-disclosure` + `disabled-states`) until its gate matches. Applied to the two capability-signaling controls (`spread`, `error`) so SEM/SD/band is discoverable on a single trace; the cosmetic fine-tuning (`band_alpha`/`band_color`/`boundary_lines`/`error_every`) stays on `showWhen` (hidden) = no clutter in the common case. `ParamControl` got a `disabled` prop (all 4 widgets) + new `isFieldDisabled`/`gateMatches` in `params.ts`; `figure-data-panel` + `workbench-panel` pass it. The bar graph (`erg_bwave_bar`) already shows individual-eyes (`points`) + error-bar (`show_error`) + SEM/SD/CI95/range (`error`) ALWAYS (un-gated). **LIVE-VERIFIED** (webpack `dev:mock`, real 453_AAV ERG figure in `p_3a7a966a`): Representative → Spread+Error VISIBLE+greyed (help "Needs Trace shows = Mean of replicates"); switch to Mean → both ENABLE + the cosmetic controls appear; staged change reverted (no persist). tsc/eslint 0-err, **vitest 298** (+5). **PROJECT FOLDERS cleaned** (owner ask "delete irrelevant/duplicate folders"; localStorage `selom.projects.v1`, DEV FIXTURES only — not in the repo; pre-cleanup backup in this session's scratchpad). **10→7 projects.** DELETED: `p_4285918c` ("Volcano alt/dup" — duplicate of `p_3384a01f`), `p_2df871f8` ("ERG scratch", 0 figures), `demo-phospho` (empty seed, owner-confirmed), + the 2 stray `erg_traces` cruft figures in `demo-pbmc` (its UMAP `f1` intact). KEPT (7): `p_3384a01f` "Volcano — eyg28 PDE6B DE (B4 fixture)", `p_fb5166e1` "Heatmap re-tone test (B5)", `p_3a7a966a` "ERG — 453_AAV trace grid", `p_7821cab4` "ERG — cross-skill switch fixture", `p_7cab3397` "ERG — metrics & waveforms (intensity + grid)", `demo-pbmc`, `demo-tumor`. Owner chose to KEEP both overlapping ERG fixtures (p_7821cab4 + p_7cab3397). Residual within-fixture cruft noted, not touched: orphan duplicate datasets inside the kept heatmap/ERG fixtures (e.g. 3× `heatmap_irpe_rawcounts.csv` in p_fb5166e1, 4× `erg_waveforms_long.csv` in p_7821cab4) — flag, dedupe only if asked. **ENV:** Turbopack `0xc0000142` → `npx next dev --webpack`; BE via uv-python + PYTHONPATH (NOT `uv run` — EDR) [[selom-backend-python-exec]] [[selom-turbopack-webpack-workaround]]. **FE dev :3000 started this turn** for the Task B exit browser pass (`npx next dev --webpack`, NO mock, no backend) — **KILL it before handoff** [[kill-dev-servers-on-handoff]]; ⚠ eamos holds :8000 — NOT mine, never kill. memory [[selom-architecture-consistency-gate]] [[selom-fe-probe-autopersist-gotcha]] [[selom-backend-python-exec]] [[selom-turbopack-webpack-workaround]] [[full-app-smoke-test-before-handoff]] [[stamp-prompts-with-datetime]]. _(T24 below = PRIOR session.)_ -->
