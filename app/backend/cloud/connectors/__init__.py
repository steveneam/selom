"""Cloud connectors — one ``CloudConnector`` per provider (``url`` works today; ``google`` /
``onedrive`` / ``dropbox`` are scaffolded against Nango). Import the concrete module you need; the
provider table lives in ``cloud.registry``."""
