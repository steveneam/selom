{
  "id": "erg_bwave_bar",
  "version": "0.1.0",
  "title": "ERG a/b-wave bar (individual points)",
  "engine": "python",
  "omics": "electrophysiology",
  "omics_type": ["general"],
  "entrypoint": "skills.proprietary.erg_bwave_bar.run:run",
  "origin": "proprietary",
  "inputs": [{ "name": "table", "kind": "csv", "required": true }],
  "param_spec": {
    "wave": { "type": "str", "default": "b", "options": ["a", "b"] },
    "intensity_group": { "type": "str", "default": "Group4" },
    "value_col": { "type": "str", "default": "" },
    "adaptation": { "type": "str", "default": "auto", "options": ["auto", "scotopic", "photopic"] },
    "stimulus_type": { "type": "str", "default": "" },
    "points": { "type": "bool", "default": true },
    "display_unit": { "type": "str", "default": "uV", "options": ["auto", "nV", "uV", "mV", "V"] }
  },
  "outputs": [
    { "name": "figure", "kind": "plotly_spec" },
    { "name": "table", "kind": "csv" }
  ],
  "catalog": {
    "name": "Selom ERG a/b-wave Bar",
    "summary": "Peak a-wave or b-wave at one flash intensity per condition (scotopic or photopic) — bar of the group mean ± SEM with every eye overlaid as an individual point (the reviewer ask for quantitative graphs). Runs straight off the dropped recording: reads the device markers when present, else measures the peak from the same traces the grid draws, at the intensity you pick. Condition colours + order match the trace grid; attaches a per-condition n / mean / SEM table.",
    "category": "electrophysiology",
    "tier": "verified",
    "status": "beta",
    "input_formats": [".csv"],
    "chains_with": ["erg_traces", "erg_intensity_response"],
    "license": "proprietary",
    "popularity": 1
  }
}
