import { ChartExtractor } from "@/components/extract/chart-extractor";

export const metadata = { title: "Recover data — Selom" };

export default function ExtractPage() {
  return <ChartExtractor />;
}
