# Milestone review — `campaign/parallel-lanes` (`60df628..693dd49`, 12 commits)

Run 2026-07-25 over the whole campaign branch as one milestone diff: **review-gauntlet** (correctness/invariants, 39 agents) + **fe-review** (interaction/layout/affordance, 38 agents). Every finding below survived an adversarial verify pass; the workflows dropped 11 more as refuted. This file is the durable backlog — the raw run outputs were session-scoped.

Status legend: **FIXED** (commit noted) · **OPEN** · **DECISION** (needs an owner call).


## Status ledger — updated 2026-07-25 after the fix pass

| Finding | Status | Where |
|---|---|---|
| **A1 + A2** (blocker) DE significance flipped to raw p while still claiming adjusted | **FIXED** | `b73bd9b` — adjusted-tier-first resolver. Verified on the real EYG_28 export **and** through the live API: 1008 plotted = ground truth; the raw-only path prints `-log10 raw p`, a `pvalue` column, no BH claim or citation, and a `raw_pvalues_only` QC warn that does not block |
| **A3** (blocker) clicking a new annotation silently deletes it | **FIXED** | `b1a49e8` — every gene-label matcher skips `selom`-tagged items; reads stay index-true so patch paths cannot go off-by-N |
| A4 downstream DE query sets filtered raw p at `fdr_threshold` | **FIXED** (same root as A1) | `b73bd9b` |
| A5 GSEA ranking metric could resolve to a gene-id column (bare `t`) | **FIXED** | `1d2aa81` — short tokens match the whole header only, plus a numeric sanity check that names the failing column |
| A6 FACS compensation claimed when it may have degraded to raw events | **FIXED** | `1d2aa81` — the runner declares the outcome in `layout.meta`; the prose states it |
| A7 · A22 · A28 FACS attributed to FlowKit, which is not a dependency | **FIXED** | `1d2aa81` — cites FlowIO + FlowUtils; the gate claim no longer asserts GatingML conformance |
| A8 `/data/assemble-scrna` recorded no lineage artifact | **FIXED** | `1d2aa81` — materializes the cohort, parents by content SHA, fail-soft |
| A11 Nango Elastic License 2.0 unrecorded on the shipped path | **FIXED** | `1d2aa81` — verified at source, recorded in `LAUNCH-GATES.md` with the internal-use assessment and the two binding obligations |
| A20 cloud provider registry forked FE↔BE | **DOCUMENTED, OPEN** | needs a providers endpoint the FE reads instead of a hardcoded literal — lands with the cloud import/export slice |
| B4 · B9 Connect buttons looked live but could never succeed | **FIXED** | `1d2aa81` — disabled with a visible "Soon" chip instead of an sr-only note |
| A9 · B1–B3 · B5–B8 · B10–B12 · B17–B24 annotation-layer debt | **DEFERRED PENDING A PROPER PLAN, NOT FIXED** | `b1a49e8` — `NEXT_PUBLIC_ANNOTATION_LAYER` off by default is the **holding mechanism, not the decision**. Owner clarified 2026-07-25: the layer is *deferred to be fixed later once a proper plan exists*, **not** shelved or dropped. So the owed deliverable is **the plan** (selection model · carry-through on re-run · the computed-p bracket path), and the flag flips when that plan is executed — not before |
| A14–A19 · A21 · A23–A27 · B13–B16 remainder | **OPEN** | entries below; none are merge blockers |

**Not verified visually.** No browser was drivable this session, so the §D layout predictions
(the 7-column tab strip, the clipped artboard) remain *unobserved*. The tab strip is back to six
columns by construction now that Annotate is gated, but that is code-reading, not a render. What
*was* verified live: all five top-level routes load 200 with no SSR or runtime error markers, and
both volcano paths (adjusted and raw-only) were driven end-to-end through a real backend on real
data, confirming the honesty chain survives the theme layer.

## A. review-gauntlet — 30 confirmed (5 dropped as refuted)

### A1. [BLOCKER · repro-integrity] WS3.1 vocab convergence silently flips the significance axis from adjusted p to RAW p — while the axis, table and methods text still say "adjusted"

- **Where:** `app/backend/skills/volcano/run_real.py:26 (root: app/backend/engine/vocab.py:28; ratchet inverted at app/backend/tests/test_volcano_columns.py:31,42)`
- **What:** The six runners were converged from EXACT-match forked lists onto substring matching over `engine.vocab.DE_PVAL_SYNONYMS = ("padj", "pvalue", "p_val", "p.value", "pval", "adj.p.val", "fdr", "qvalue")`. The raw-p tokens sit BEFORE the adjusted tokens, so for every non-DESeq2 convention the chosen column changes. Simulated with the exact runner `_pick` logic: edgeR topTags [logFC, PValue, FDR] OLD='FDR' -> NEW='PValue'; limma [logFC, P.Value, adj.P.Val] OLD='adj.P.Val' -> NEW='P.Value'; scanpy rank_genes_groups [pvals, pvals_adj] OLD='pvals_adj' -> NEW='pvals'; Seurat [p_val, p_val_adj] OLD=None (honest raise) -> NEW='p_val'. Only DESeq2 ('padj') is unaffected. Meanwhile the figure hardcodes `"yaxis": {"title": {"text": "-log10 adjusted p"}}` (skills/volcano/run.py:134), the attached table column is named `padj` (skills/_table.py:44), `fdr_threshold` is applied as if the column were an FDR, and the auto-methods text asserts "FDR <= 0.05 ... Adjusted p-values reflect Benjamini-Hochberg correction" (companions/methods.py:175-177). So a limma/edgeR volcano now plots and thresholds RAW p-values on an axis labelled adjusted, with a methods paragraph claiming BH correction — a printed-vs-computed mismatch shipped as a pass. Worse, the pre-existing guard `assert _pick(cols, _P_COLS) == "FDR"  # FDR preferred over raw P.Value` was DELETED and inverted to `assert _pick(cols, DE_PVAL_SYNONYMS) == "P.Value"` with a comment calling it "a deliberate WS3.1 convergence" — the ratchet that would have caught this was rewritten to bless it. The stub-only goldens (tests/test_skills_golden.py pins SELOM_SKILLS_ENGINE=stub) cannot see any of it, which is why the commit could truthfully say "no goldens changed". This regresses the documented EYG_28/RPGRIP1 limma repro path, whose exact header is the fixture in that test.
- **Fix:** Split the significance vocabulary into adjusted-first and raw tiers and select in that order: `DE_PADJ_SYNONYMS = ("padj", "p_val_adj", "pvals_adj", "adj.p.val", "fdr", "qvalue", "q.value")` tried BEFORE `DE_PVAL_RAW_SYNONYMS = ("pvalue", "p_val", "p.value", "pval")` (substring order alone is not enough — 'pval' substring-matches 'pvals_adj', so match the adjusted tier first and, within a tier, prefer the longest matching header). Restore the deleted `== "FDR"` assertion as the regression test, and add cases for edgeR/limma/scanpy/Seurat headers. Then make the claim honest rather than implicit: return the resolved column name plus an `adjusted: bool` from the resolver, drive the y-axis title ("-log10 adjusted p" vs "-log10 raw p") and the methods sentence from that flag, and emit a QC WARN in engine/qc.py:_qc_de_columns when only a raw p column is present.
- **Status:** OPEN

### A2. [BLOCKER · spine-consistency] WS3.1 consumes the presence-only DE_PVAL_SYNONYMS as a selection-priority list — 6 runners now pick the RAW p-value over FDR/padj

- **Where:** `app/backend/engine/vocab.py:28 (consumed at app/backend/skills/volcano/run_real.py:26 + enrichment/go_graph/pathway/string_network/gsea)`
- **What:** engine/vocab.py's synonym tuples are documented as SUBSTRING PRESENCE sets — every pre-existing consumer is order-independent (compat._has_group, databundle._has_any, frame_schema._NUMERIC_SYNONYM_SETS identity, qc's exact membership). WS3.1 repurposed DE_PVAL_SYNONYMS as the ORDERED selection list in all six runners' _pick, but left it in its presence order: ('padj','pvalue','p_val','p.value','pval','adj.p.val','fdr','qvalue') — raw-p tokens BEFORE 'fdr'. The author saw this exact hazard for the gene half and gave GENE a deliberate selection order (engine/columns.py:27-32, 'Ordered by runner-selection priority'), but did not apply the same treatment to the p-value half. Verified live: `run('limma.csv', {})` on a header `GeneID,external_gene_name,logFC,AveExpr,t,P.Value,B,FDR` (P.Value 0.001/0.004, FDR 0.30/0.42 — nothing significant) returns table rows [['RHO',2.5,0.001,'up'],['ACTB',-2.1,0.004,'down']] under a column literally named `padj`, on a y-axis hardcoded '-log10 adjusted p' (skills/volcano/run.py:134). The pre-WS3.1 fork's exact-match order (padj, adj.p.val, fdr, …) returned FDR → both n.s. Same defect in edgeR (`PValue`+`FDR` → PValue) and Seurat (`p_val`+`p_val_adj` → p_val) headers. Blast radius beyond volcano: enrichment/run_real.py:44, go_graph:46, pathway:65, string_network:49 all filter the query gene set with `sub[fdr_col] <= fdr_threshold (0.05)` — now unadjusted p, so far more genes enter every ORA/Reactome/STRING query. The repo's own real DE corpora (EYG_28, ALPK1, RPGRIP1) are limma/edgeR exports carrying both columns. The goldens are blind ('no goldens changed') because their fixtures carry a single p-like column.
- **Fix:** Give p-value SELECTION an explicit adjusted-first order and keep presence semantics intact. Minimal: reorder the literal in engine/vocab.py:28 to ('padj','p_val_adj','pvals_adj','adj.p.val','fdr','qvalue','pvalue','p.value','pval') — adjusted tokens must precede the raw substrings they contain; safe because no consumer depends on the current order (verified: _has_group/_has_any/_NUMERIC_SYNONYM_SETS/_ROLE_BY_SYNONYMS_ID are all order-independent, and the single tuple object survives a literal edit). Cleaner: add `engine.columns.PVAL_SELECTION` alongside GENE and have the runners read that, leaving vocab as the pure presence set. Then update the deliberate-edit assertion at engine/test_vocab_drift_guard.py:146 and add a guard case asserting the resolver returns the ADJUSTED column for the limma (`P.Value`+`FDR`), edgeR (`PValue`+`FDR`) and Seurat (`p_val`+`p_val_adj`) headers — the ratchet that would have caught this.
- **Status:** OPEN

### A3. [BLOCKER · design] Clicking a new annotation on a volcano figure silently deletes it

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/figure-canvas.tsx:293-298`
- **What:** The `plotly_clickannotation` handler removes ANY clicked annotation by index whenever the figure declares `geneLabels` (`st.commit([remove(`/layout/annotations/${idx}`)])`) — it was written when every annotation on a volcano was a gene label. Slice 5 now lets the user add Selom-tagged annotations into that same array on every figure type, and the backend stamps `tools.geneLabels: true` on every volcano (app/backend/skills/_capabilities.py:20-21). VERIFIED IN BROWSER: added a Text label from the rail on /p/demo-pbmc?demo=1, instrumented the graph div, clicked the rendered label once → `plotly_clickannotation` fired with `{index: 0, text: "Text", hasSelomTag: true}`. It survived only because the UMAP is not a geneLabels figure; on a volcano the same click executes the remove. Note the annotation does NOT set `captureevents`, which is irrelevant: plotly.js emits this event from an unconditional `.on('click')` (node_modules/plotly.js/src/components/annotations/draw.js:176-179) and `pointer-events` is enabled because `edits.annotationPosition/annotationTail` are on. So on the product's most common figure type, the first click on a bracket/label/arrow destroys it, silently.
- **Fix:** Gate the delete on the item being a gene label: read `spec.layout.annotations[idx]` in the handler and bail when `isSelomAnnotation(...)` is true (import from lib/figure/annotations) — click-to-delete is a gene-label affordance only. Add an e2e assert on a geneLabels-stamped spec that a Selom annotation survives a click while a gene label still deletes.
- **Status:** OPEN

### A4. [HIGH · repro-integrity] Downstream DE skills now build their "significant genes" query set by filtering RAW p at fdr_threshold — a silent change to enrichment/GO/pathway/STRING inputs

- **Where:** `app/backend/skills/enrichment/run_real.py:40 (same at go_graph/run_real.py:42, pathway/run_real.py:61, string_network/run_real.py:45)`
- **What:** Same root cause as the volcano finding, different blast radius. All four skills do `sub = sub[to_numeric(sub[fdr_col]) <= fdr_threshold]` where `fdr_col` is now the raw-p column for edgeR/limma/scanpy/Seurat tables. Filtering at raw p <= 0.05 instead of FDR <= 0.05 admits an order of magnitude more genes into the ORA query set, so the enriched-term list, the dotplot, the GO subgraph, the Reactome projection and the STRING network all change materially — with no verdict, no warning, and the variable still named `fdr_col`. The methods text (companions/methods.py:212-216) describes only the BH correction applied ACROSS gene sets, so a reader is told nothing about the input filter having become unadjusted. This also compounds RISKS #10 (gseapy term-count conservatism): term counts now move for two independent reasons at once.
- **Fix:** Fix the shared resolver as described above so `fdr_col` is again the adjusted column when one exists. Additionally, since the input filter is a scientific choice, surface it: rename to `sig_col`, record `{column, adjusted, threshold, n_in, n_kept}` in the skill's provenance/params, and have the methods template state which column and whether it was adjusted ("genes with FDR <= 0.05" vs "genes with unadjusted p <= 0.05") rather than asserting FDR unconditionally.
- **Status:** OPEN

### A5. [HIGH · repro-integrity] GSEA ranking metric can resolve to a gene-id or string column — single-letter "t" as a substring synonym

- **Where:** `app/backend/skills/gsea/run_real.py:35,76`
- **What:** `RANK_EXTRA = ("stat", "score", "t", "signed_rank", "metric")` is consumed by the new substring `_pick`, so the synonym "t" matches ANY header containing the letter t. Verified with the runner's own logic: ['GeneID','entrezgene_id','t','P.Value'] -> metric_col = 'entrezgene_id' (numeric, so `pd.to_numeric` succeeds and GSEA ranks genes by their Entrez ID — a plausible-looking, meaningless enrichment result with no error); ['external_gene_name','AveExpr','t','P.Value','B','FDR'] -> 'external_gene_name' (string -> to_numeric coerces to NaN -> dropna -> an EMPTY ranked frame handed to gseapy); ['Protein.Ids','t','P.Value'] -> 'Protein.Ids'. Before this commit the exact-match list picked the column literally named 't'. Neither failure mode produces an honest verdict — no needs_recipe / data_unmatched, no "ranking column is not numeric" error.
- **Fix:** Never substring-match a synonym shorter than ~3 chars: match `RANK_EXTRA` exactly (or anchor it with word boundaries, e.g. the lowered header equals 't' / 'stat' / 'score' / 'metric' or matches `(?:^|[._-])t(?:$|[._-])`). Then validate the pick: after `to_numeric`, if the metric column yields zero finite values, raise a needs_recipe RunError naming the column instead of running GSEA on an empty ranking. Record the resolved metric column name in the run provenance so the ranking basis is auditable.
- **Status:** OPEN

### A6. [HIGH · repro-integrity] FACS compensation silently degrades to raw events while the auto-methods text asserts the spillover matrix was applied

- **Where:** `app/backend/skills/facs_gating/run_real.py:43 (helper at :101-131); claim at app/backend/companions/methods.py:689-694`
- **What:** `_compensate` returns `(events, compensated?)` and every failure path degrades to uncompensated raw events: malformed `comp_matrix` JSON, a detector name that doesn't map to a PnN label (`any(c is None for c in cols)`), a matrix whose shape != (len(cols), len(cols)), or a `$SPILLOVER` string FlowUtils can't parse (bare `except Exception: pass`). The caller then throws the status away: `comp, _compensated = _compensate(...)`. Nothing in the figure, the population table, or the provenance records that compensation did not happen — the layout title carries only the transform name (skills/_flow.py:figure_layout). Meanwhile `companions/methods.py:694` states flatly "an operator-supplied spillover matrix was applied" whenever `compensate == "matrix"`, because the methods template only sees the requested params, never the run result. Uncompensated fluorescence changes every gate membership and every median MFI in the table, so the published counts, %-parent, %-total and MFIs are wrong while the methods paragraph vouches for a correction that silently didn't run.
- **Fix:** Make the degrade an honest verdict, not a fallback: when `compensate == "matrix"` and the matrix cannot be applied, raise a bad-input RunError naming the mismatch (unknown detector / wrong shape / unparseable JSON) — the operator explicitly asked for it. For `auto`, keep the fallback but return the resolved state (`compensation: "embedded" | "operator" | "none"`, plus the detector list) in the skill result, stamp it into provenance, note it in the figure title/table subtitle, and derive the methods sentence from that resolved value rather than from the requested param.
- **Status:** OPEN

### A7. [HIGH · repro-integrity] FACS provenance attributes the computation to FlowKit, which is deliberately not a dependency, and claims a "GatingML-compliant gating strategy" for clean-room numpy geometry

- **Where:** `app/backend/companions/methods.py:49,720,732 and app/backend/skills/facs_gating/skill.json:32,36`
- **What:** The commit's whole point is that FlowKit is NOT used (it pins pandas<3) and that the gate tree + population report were reimplemented clean-room in numpy. The provenance artifacts say otherwise. `methods.py:732` returns `[FLOWKIT, *xform_cite, GATINGML]`, so every generated methods paragraph cites White et al., FlowKit — a tool that never touched the data. `methods.py:720` asserts the gate tree "was applied with a GatingML-compliant gating strategy", but `run_real._gating_rows` is an even-odd point-in-polygon plus boolean masks over display-space bounds; nothing implements the GatingML 2.0 gate/transform semantics. `skill.json:36` (the catalog summary a user reads when picking the skill) says "Built on the BSD-3 FlowKit stack for compute", and `skill.json:32` says GatingML "Defines the ... semantics FlowKit implements". The auto-methods text is a published provenance artifact — citing an uninvolved engine and claiming standards compliance the code doesn't have is a citation-integrity breach, and it also makes the license ledger wrong about what ships.
- **Fix:** Replace FLOWKIT with FLOWIO + FLOWUTILS citations in `_facs_gating`'s reference list (both are already documented in skill.json references). Change the gating sentence to state what actually ran: "rectangle, polygon and quadrant gates defined in the transformed display space were evaluated by Selom's in-house gating implementation following the Gating-ML 2.0 gate definitions" — and keep GATINGML only as the definitional reference, not a compliance claim. Fix skill.json:36 to "Built on the BSD-3 FlowIO + FlowUtils libraries with Selom clean-room gating" and drop "FlowKit implements" from the :32 note.
- **Status:** OPEN

### A8. [HIGH · repro-integrity] /data/assemble-scrna records no lineage artifact, unlike its sibling /data/combine — the assembled cohort's design provenance is unrecoverable

- **Where:** `app/backend/routers/data.py:153-207 (compare /data/combine at app/backend/routers/data.py:139-145)`
- **What:** `/data/combine` captures each input file's content SHA while the uploads still exist (`lineage.source_parent`) and calls `lineage.materialize(..., kind=KIND_COMBINED, parents=parent_refs, recipe_note=...)`, returning `artifact_id` + `receipt` in its summary — the D3 record that renders "merged from {A, B, C}". The new endpoint, described in its own comment as "the scRNA sibling of /data/combine", does none of this: no `source_parent` capture, no `lineage.materialize`, no `artifact_id`/`receipt`. It deletes the per-sample uploads in `finally` and streams back the .h5ad bytes, which the FE then re-uploads as an ordinary dataset. The assembled AnnData is the input to every downstream scRNA figure, and its per-cell design (`sample_id`/`line`/`condition`) was synthesized here from filenames plus an operator `obs_map`. After the response, nothing records which per-sample files went in, which obs_map produced the design, or the outer-join gene union — so no figure built on it can be traced back to its inputs.
- **Fix:** Mirror the combine path: capture `parent_refs = [lineage.source_parent(p, f.filename or "") for p, f in zip(paths, files)]` before the `finally` unlink, then `lineage.materialize(..., kind=KIND_ASSEMBLED (new), filename="assembled_scrna.h5ad", parents=parent_refs, recipe_note=f"assembled {n_samples} sample(s); design keys {sorted(obs_map or {})}")`, and put `artifact_id` + `receipt` into the `X-Assemble-Summary` header. Include the resolved per-sample obs mapping in the recipe so the design is reconstructible.
- **Status:** OPEN

### A9. [HIGH · repro-integrity] Significance stars are hand-set with no p-value and no basis recorded — an unprovenanced statistical claim exported on the figure via the cosmetic edit path

- **Where:** `app/frontend/lib/figure/annotations.ts:35,247 (picker at app/frontend/components/figure/panels/annotations-panel.tsx:155-186)`
- **What:** The Annotate panel offers a quick-pick over `STAR_TIERS = ["*", "**", "***", "ns"]`, and `addSigBracketOps` defaults to `const stars = opts.stars ?? (typeof opts.p === "number" ? starsForP(opts.p) : "*")` — i.e. absent a p-value it stamps "*" (p <= 0.05) onto the figure. The commit message concedes the state: "Semi-data-aware stars wired, pending a Statistics-table p-value contract". The `selom` tag written onto the shape/annotation carries only `{kind, id, role}`; it records neither the p-value, the test, the compared groups, nor whether the tier was computed or asserted. So an operator-typed "***" is byte-indistinguishable in the persisted spec from a computed one, and it exports WYSIWYG into the publication figure. Because the ops are classified `"client"` (annotations.ts:9, patch.ts:61), the claim also rides the cosmetic/live lane, which by the project's own figure-edit model is reserved for changes that carry no data semantics — a significance assertion is not cosmetic. `bracketGeometry` compounds it by spanning "the first two groups" from trace extents, so even the p-aware path would not bind the stars to the comparison the p-value came from.
- **Fix:** Extend the tag to carry the claim's basis and refuse to render a bare assertion silently: `selom: { kind:"sigBracket", id, role, stars, basis: "computed" | "operator_asserted", p?: number, test?: string, groups?: [string, string] }`. Default `basis` to `"operator_asserted"` when no `p` is supplied, show that state in the Annotate panel row and in the figure's provenance/methods surface ("1 significance bracket is operator-asserted, not computed"), and gate export-as-final on either a computed p or an explicit operator acknowledgement. Bind `groups` to the two categories the bracket spans so a later computed p can be verified against the pair.
- **Status:** OPEN

### A10. [HIGH · spine-consistency] Gene-label selection switched from exact match to substring, so a non-label column containing a gene token is chosen as the gene label

- **Where:** `app/backend/skills/volcano/run_real.py:88 (_pick, forked into 5 more runners) + app/backend/engine/columns.py:33`
- **What:** The pre-WS3.1 runners matched gene columns by EXACT lower-cased name against an explicit label list; _pick now returns the first column whose name merely CONTAINS a GENE member. GENE includes the bare tokens 'gene', 'feature' and 'names', so any header containing them is selected as the row label. Verified live: `run()` on `gene_biotype,logFC,FDR` yields table rows [['protein_coding',2.5,0.001,'up'],['lncRNA',-2.1,0.004,'down']] — the biotype string is now the gene label on the figure, in the top-N callouts and in the statistics table. The old exact list returned None for the same header, so the runner correctly fell back to the frame index. Same class of hazard for headers like `n_features`/`feature_count` or `colnames`. Substring is the right semantics for a PRESENCE gate (compat D1 only asks 'does a gene-ish column exist') and the wrong semantics for SELECTION ('which column IS the label'); WS3.1 collapsed the two.
- **Fix:** Split the two semantics in the resolver: try an exact normalized match against the GENE members and the known concrete label headers first (external_gene_name, gene_symbol, gene_name, symbol, gene, genes, feature, names, geneid, gene_id, ensembl_gene_id, entrezgene_id), and only fall back to substring when nothing matches exactly — presence checks keep using the substring set unchanged. Add guard cases to tests/test_volcano_columns.py for a `gene_biotype`-only header (expect None → index fallback) and for `gene_biotype` alongside `external_gene_name` (expect external_gene_name).
- **Status:** **FIXED** (L1-01) — selection is tiered in `engine/columns.py` (`GENE_EXACT` exact tier → `GENE_STRONG` substring tier via `pick_gene`); `GENE` stays the unchanged substring PRESENCE set. Guard cases in `tests/test_volcano_columns.py` + `tests/test_enrichment_columns.py`. Verified on the real EYG_28/ALPK1 exports (same columns as before) and on the `gene_biotype` hazard built from those rows (now `None` → frame index).

### A11. [HIGH · license] Nango is Elastic License 2.0 (non-OSI, source-available) — now on the shipped path with zero license record

- **Where:** `app/frontend/package.json:20 · deploy/nango/docker-compose.yaml:50`
- **What:** Two new Nango surfaces land in 8fb2bad: `"@nangohq/frontend": "^0.71.2"` bundled into the client (lazy-imported at app/frontend/lib/cloud/nango.ts:26, so it ships as a Next chunk to every browser) and `image: nangohq/nango-server:hosted` self-hosted as product infrastructure (already live at nango.swordfish.cfd per agent_handoff/CURRENT.md:38). The npm package's own `license` field is the non-answer `"SEE LICENSE IN LICENSE FILE IN GIT REPOSITORY"` and the published tarball ships NO license file (verified: app/frontend/node_modules/@nangohq/frontend/ contains only `dist/` + `package.json`). Fetched the real terms 2026-07-25 — raw.githubusercontent.com/NangoHQ/nango/master/LICENSE is the **Elastic License 2.0**: (1) no providing the software to third parties as a hosted or managed service exposing a substantial set of its features, (2) no circumventing license-key functionality, (3) **no altering, removing, or obscuring the licensor's notices**. This is Selom's first non-OSI dependency and it is undocumented everywhere: `grep -rniE 'elastic license|elv2'` over the repo returns zero hits; LAUNCH-GATES.md §1 (the license/SCA gate) still names only gseapy + MSigDB; docs/records/external-integrations.md (the license ledger) has no Nango entry. This breaks the repo's own convention — every backend dep in app/backend/pyproject.toml carries an inline license annotation, and this FE dep carries none. Limitation (1) is arguable in Selom's favour (users get an OAuth consent flow, not Nango's integration-platform functionality) but that is exactly why it must be a recorded decision rather than silence; limitation (3) is concretely at risk because minified Next chunks carry no notice and the tarball supplied none to preserve.
- **Fix:** Add a Nango entry to docs/records/external-integrations.md: ELv2 (source-available, NOT OSI open source), both surfaces (`@nangohq/frontend` distributed in the client bundle; `nango-server` self-hosted), the three limitations verbatim, and the recorded decision that Selom self-hosts Nango as internal OAuth infrastructure and does not resell its functionality. Add an ELv2 line to LAUNCH-GATES.md §1 alongside gseapy/MSigDB, marked `source-available — re-verify the hosted-service limitation before public launch`. Satisfy limitation (3) by vendoring the ELv2 text + Nango copyright into a notice the bundle can't strip (e.g. app/frontend/public/licenses/nango-elv2.txt referenced from a THIRD-PARTY-NOTICES file). Pin `nangohq/nango-server` to an image digest (deploy/nango/docker-compose.yaml:13 already flags this) so the licensed artifact is identifiable.
- **Status:** OPEN

### A12. [HIGH · design] Significance bracket splits apart on drag — the bracket moves, its stars stay

- **Where:** `/home/deploy/work/selom/app/frontend/lib/figure/annotations.ts:245-275`
- **What:** A bracket is built as TWO independent items sharing an id: a `path` shape in `layout.shapes` and a stars annotation in `layout.annotations`. `edits.shapePosition` drags the path (plotly emits `shapes[i].path`, allow-listed at lib/figure/plotly-edits.ts:17-25 → persisted), and `edits.annotationPosition` drags the stars — but nothing re-pairs them, so moving either half leaves the other behind and the asterisks detach from the bracket. The module doc claims 'a coordinated pair … so show/hide and remove act on both' (it deliberately excludes move), while the panel copy tells the user the opposite: 'Each annotation lives in the figure — drag it on the artboard to place it' (annotations-panel.tsx:56-59). The only recovery is undo or hand-dragging the second half back.
- **Fix:** Make the bracket ONE object: put the stars in the shape's own `label: { text: stars, textposition: 'top center', font }` — plotly.js ^3.6 supports shape labels — so a single `shapePosition` drag carries both, and `annotationTextOps`/`annotationVisibilityOps` collapse to one item. If the pair must stay split, extend the relayout path with a bracket-aware step that derives dx/dy from the new `path` and applies the same delta to the paired annotation's x/y in the SAME commit (one undo step).
- **Status:** OPEN

### A13. [HIGH · design] Repeated adds stack invisibly at identical coordinates, and the panel rows are indistinguishable

- **Where:** `/home/deploy/work/selom/app/frontend/lib/figure/annotations.ts:286-330`
- **What:** Every add uses fixed defaults: text label always paper (0.02, 0.98) with text 'Text', arrow always paper (0.5, 0.5), and `bracketGeometry` is deterministic for a given spec. VERIFIED IN BROWSER: two clicks on the rail's 'Text label' produced `[{x:0.02,y:0.98,text:'Text'},{x:0.02,y:0.98,text:'Text'}]` — pixel-identical overlap — while the panel header read 'On the figure · 2'. So the second click looks like a no-op on the artboard (the classic 'did that work?' failure) and the two panel rows are literally identical (same 'Text label' caption, same text, no ordinal, no row↔canvas linkage), leaving the user no way to tell which row edits/hides/removes which item. Same for a second bracket. `bracketGeometry` also falls back to yb=1.06/yt=1.12 in DATA coords when no trace carries a numeric `y` (annotations.ts:199-203), which can place the bracket outside the visible y-range — another add with no visible result.
- **Fix:** Cascade the default position by the number of existing Selom items of that kind (e.g. paper offset of ~2.5% per item, wrapped/capped inside the artboard; bracket yt raised one tier per existing bracket) so each add lands somewhere visibly new. Differentiate the rows (append an ordinal, and show the item's text) and highlight the corresponding on-canvas item while its row is hovered or focused. Clamp the bracket into the resolved axis range instead of the 1.06/1.12 data-coord fallback.
- **Status:** OPEN

### A14. [MEDIUM · repro-integrity] Multi-sample assembly zero-fills genes absent from a sample's reference, fabricating hard zero counts with no overlap verdict

- **Where:** `app/backend/engine/assemble.py:208`
- **What:** `ad.concat(parts, join="outer", fill_value=0)` unions the gene axis and writes 0 for genes a sample's features.tsv never carried. Downstream those 0s are indistinguishable from measured zero counts, so a gene present in only one sample's reference becomes a perfect sample-specific marker — which, with `obs['sample']` mirrored to the batch key, is exactly the batch-confounded-as-biology failure mode. Nothing measures or reports the overlap: `summarize()` (engine/assemble.py:215) returns `n_genes` (the union size) with no `n_genes_shared` / per-sample gene count, and no flag when the intersection is small. The gotcha is live for real deposits mixing references or prefixed feature names (e.g. `GRCh38_`-prefixed symbols), and it is untested — `tests/test_assemble.py:49-50` builds both samples with identical gene sets, so the disjoint case never runs.
- **Fix:** Compute the intersection before concat and report it honestly in `summarize()`: `n_genes_union`, `n_genes_shared`, and `per_sample_n_genes`. Emit a QC WARN (or raise) when `n_genes_shared / n_genes_union` falls below a threshold (~0.9) with the fix text "samples appear to use different references — re-run Cell Ranger against one reference or assemble with join='inner'". Expose the join as a parameter defaulting to `"inner"` for count matrices, with `"outer"` opt-in and the zero-fill disclosed in the lineage recipe note.
- **Status:** **FIXED** (L1-03) — `join` defaults to `inner` (no fabricated count), `outer` is opt-in and discloses its zero-fill, and `gene_overlap()` measures the axis before the concat and reports `n_genes_union` / `n_genes_shared` / `per_sample_n_genes` / `shared_fraction` + a level/message/fix through `uns['selom_gene_overlap']` and `summarize()`. WARN below 0.9. The disjoint case now runs in `tests/test_assemble.py`. Verified on the real Hani GSE201356 triplets, including the documented `GRCh38_`-prefix mismatch (67 076 of 98 129 genes were previously zero-filled in silence).

### A15. [MEDIUM · repro-integrity] FACS gate bounds are compared in a transform space anchored to a data-dependent t_top that is never recorded

- **Where:** `app/backend/skills/facs_gating/run_real.py:45,137-155`
- **What:** `t_top = float(max(1.0, float(np.nanmax(np.abs(comp)))))` derives the GatingML transform's `t` parameter from the data's global maximum absolute value, and that single `t_top` scales every channel for both display AND gate evaluation. Gate bounds travel in transformed display space (`_flow.parse_gates`), so the meaning of a saved gate depends on an extremum of the specific event array: one saturating outlier event shifts the whole transform and therefore every population count, %-parent and MFI in the table. The recorded params (`skill.json` param_spec) capture transform/cofactor/bins/gates but NOT the resolved `t_top`, so the provenance is insufficient to recompute or audit the numbers independently, and a gate drawn on one export is not portable to a re-acquisition of the same panel. The docstring's claim that this "is the same single t_top FlowKit's per-run transform used, so gate bounds keep their meaning across engines" does not hold — FlowKit derives `t` from the FCS `$PnR` range keyword per channel, not from the observed maximum.
- **Fix:** Anchor the transform to metadata, not data: default `t` to the FCS `$PnR` for each channel (available via `flowio.FlowData.channels`), falling back to a fixed 262144 for 18-bit acquisitions, and expose it as an explicit `transform_t` param. Whatever is resolved, return it in the skill result and stamp it into provenance alongside `m`/`w`/`a` so the gate space is reconstructible, and record which channel ranges were used.
- **Status:** **FIXED** (L1-05) — `t` resolves from the `transform_t` param → each channel's FCS `$PnR` → a fixed 262144, never from the events, and is recorded in `layout.meta.transform` (name, `t_source`, `t_per_channel`, `t_default_channels`, m/w/a or cofactor) with the methods prose stating it. arcsinh records `not_applicable` rather than a `t` it never used. Measured: the old data-anchored `t` moved a gate's count from 0 to 7 on one saturating event; `$PnR` gives 7 on both. Guards in `tests/test_flow_gating_honesty.py` (deliberately not `@slow`, so they run in `scripts/verify.sh`). **Real-data gap:** the corpus carries no `.fcs`, so this was verified on a FlowIO-written synthetic.

### A16. [MEDIUM · repro-integrity] FACS silently drops gates it cannot resolve — the population vanishes from the table with no verdict

- **Where:** `app/backend/skills/facs_gating/run_real.py:220,235,242 (parser at app/backend/skills/_flow.py:parse_gates)`
- **What:** Three silent drops on the population-stats path. `_gating_rows` does `if cxi is None or cyi is None: continue` when a gate names a channel absent from the FCS, and `continue` again for any unrecognized `type`; `_flow.parse_gates` drops a gate with no geometry, fewer than 3 polygon vertices, or a non-finite split ("Tolerant: a malformed or geometry-less entry is dropped, never raised"). The result is a population table that is simply missing rows the operator defined, with %-parent and %-total for the surviving gates computed against parents that may themselves have silently vanished (an unresolved parent falls back to `all_mask`/`total`, so a child gate's %-parent is silently computed against ALL events instead of its parent). Nothing distinguishes "this population was 0 events" from "this gate never ran". For a gating table where the counts ARE the result, a missing row is a wrong result presented as complete.
- **Fix:** Keep the tolerant parse but make the loss visible: have `parse_gates` return `(gates, dropped)` and `_gating_rows` collect unresolved gate ids, then either raise a needs_recipe RunError naming the unresolved channel/gate (the operator supplied it explicitly, so this is the honest default) or emit a row per dropped gate with `count=None` and a `status` column ('channel_not_in_fcs' / 'malformed_geometry' / 'unknown_type'). Never fall back to the root mask for an unresolved parent — mark descendants unresolved too rather than reporting a %-parent against ALL events.
- **Status:** **FIXED** (L1-06) — `parse_gates` returns `(gates, dropped)` with a reason + actionable fix per drop; the population table gained a `status` column and every unresolvable gate is a row with `count=None` naming why. The root-mask parent fallback is gone — a descendant of an unresolved gate is marked `parent_unresolved` rather than counted against ALL events. Also surfaced in `layout.meta.gates_unresolved` and in the auto-methods prose. Tolerance kept: nothing raises.

### A17. [MEDIUM · repro-integrity] ERG oscillatory potentials report 0.0 amplitude for "not measurable", conflating an unmeasurable trace with absent inner-retinal activity

- **Where:** `app/backend/skills/_erg.py:378,383,387 (function at :347)`
- **What:** `oscillatory_potentials` returns `empty = {"op_amplitudes_uv": [], "op_times_ms": [], "op_sum_uv": 0.0, "op_rms_uv": 0.0, "n_ops": 0}` for three distinct situations: a trace too short to band-pass (`n < 24`), a filter that could not run (`sosfiltfilt` ValueError — "signal shorter than the filter padding -> not measurable", per the code's own comment), and a window containing fewer than 5 samples. Those are all "could not measure", but the returned SumOP/RMS of 0.0 is a scientific claim of zero oscillatory activity — the reading that indicates inner-retinal dysfunction. The docstring even conflates them: "Degrades honestly to an all-zero / empty result (never raises) when the trace is too short to filter OR carries no OP-band deflection". Figure/table wiring is a follow-up, so the numbers are not yet published, but the API shape is what the follow-up will consume, and a downstream mean over eyes will silently absorb unmeasurable traces as zeros. The same pattern applies to `photopic_negative_response`, which at least returns None rather than 0.
- **Fix:** Distinguish the two states in the return shape before anything consumes it: return `None` (as `photopic_negative_response` does) for not-measurable, or add an explicit `"measurable": False, "reason": "trace_too_short" | "filter_failed" | "window_too_small"` alongside `None`-valued amplitudes, and reserve 0.0 for a trace that filtered successfully but carried no OP-band deflection. When wiring the figure/table, render unmeasurable segments as a blank cell with the reason, never as 0, and exclude them from group means.
- **Status:** **FIXED** (L1-07) — the three not-measurable cases return `None` amplitudes with `measurable: False` + a `reason` (with a human label for rendering); `0.0` is reserved for a successfully-filtered trace with no OP-band deflection. `_erg.op_group_mean()` excludes unmeasurable eyes from a group mean and says how many. Verified on a real Diagnosys trace: a 20-20.5 ms operator OP window now reads not-measurable where the old code reported SumOP = 0.0, while 20-25 ms is a genuine measured 0.0.

### A18. [MEDIUM · repro-integrity] The opt-in robust a/b detector changes measured amplitudes but is not disclosed in the auto-methods text

- **Where:** `app/backend/companions/methods.py:599-631 (param added at app/backend/skills/proprietary/erg_traces/skill.json:30)`
- **What:** `ab_detector: "windowed" | "robust"` is now a param on all three ERG skills and selects a materially different measurement: `_robust_auto_ab` (skills/_erg.py:178) applies Savitzky-Golay smoothing plus prominence-gated peak picking with a noise gate at 2 x 1.96 x SD of the pre-stimulus baseline, and falls back to the windowed argmin/argmax when nothing clears the gate. The a-/b-wave amplitudes and implicit times it produces differ from the windowed path. The methods templates (`_erg_traces` and siblings) describe only the windowed construction — "the a-wave was measured from baseline to the initial cornea-negative trough and the b-wave from that trough to the following peak" — with no mention of the detector, the SavGol window/polyorder, the prominence gate, or the fallback. A reader given that methods paragraph cannot reproduce numbers produced under `robust`. The param itself is recorded, so re-running inside Selom reproduces; the gap is the published recipe.
- **Fix:** Branch the ERG methods templates on `p.get("ab_detector")`: for `robust`, add a sentence naming the detector and its parameters ("landmarks were seeded by Savitzky-Golay smoothing (second-order, <win> ms) with prominence-gated peak detection above 2 x 1.96 x SD of the pre-stimulus baseline, falling back to the windowed extremum when no deflection cleared that gate"). Also record, per segment, whether the gate was cleared or the fallback fired, so a reader can tell which measurements were near the noise floor.
- **Status:** **FIXED** (L1-08) — `landmarks()` records `detector` + (robust only) the per-segment `detector_gate`; `_erg.detector_summary()` aggregates it to `layout.meta.ab_detector` from all three ERG runners, and the methods templates state the SavGol construction, the 2x1.96xSD gate, the windowed fallback and how many segments fell back. `None` for `windowed`, so every existing figure and golden is byte-identical. Verified on the real CMRI .iwxdata: robust differs from windowed on 7 of 7 segments, with 7/7 a-waves and 5/7 b-waves measured at the noise floor.

### A19. [MEDIUM · spine-consistency] The column resolver is still forked verbatim into 6 runners, and the drift guard cannot see a forked matcher

- **Where:** `app/backend/skills/volcano/run_real.py:88, enrichment/run_real.py:135, go_graph/run_real.py:24, pathway/run_real.py:32, string_network/run_real.py:27, gsea/run_real.py:334`
- **What:** engine/columns.py:10-14 states the invariant this refactor was meant to satisfy: 'column-picking is duplicated in each runner's _pick and the override has to be honoured at every resolution site at once … One resolver, consulted everywhere.' WS3.1 single-sourced the VOCABULARY but left the RESOLVER forked six ways — six copies of the identical loop, plus six copies of the `{str(c).strip().lower(): c}` normalization and the `override_column(...) or _pick(...)` composition. The drift guard only AST-scans for collection-literals of synonyms (_FORK_THRESHOLD = 3 members, engine/test_vocab_drift_guard.py), and the new identity test only asserts `mod.GENE is columns.GENE` — neither fails if one runner's _pick silently diverges (exact vs substring, a different tie-break, a dropped override). A third semantics already exists in-tree: engine/qc.py:162 resolves the p column by EXACT tuple membership (`low in _PVAL`) while the runners use substring and compat._has_group uses substring-presence, so QC and the runner can read different columns from the same table (e.g. a `padj_bh` header matches in the runner, misses in qc).
- **Fix:** Add `engine.columns.resolve(role, df_columns, override=None) -> str | None` that owns normalization + override + the role's selection order, delete all six `_pick` functions, and route engine/qc.py:162 through it too. Extend engine/test_vocab_drift_guard.py with an AST check that no file under skills/** defines a local column-matching helper (a function whose body iterates a synonyms parameter over lower-cased columns), so the next fork fails on exit code rather than passing every guard.
- **Status:** **FIXED, with one residue** (L1-02) — `engine.columns.resolve` + `normalize` added, all six `_pick` forks deleted, and `engine/test_vocab_drift_guard.py` now locks the RESOLVER by identity plus an AST scan that fails on a re-forked matcher under `skills/**`. **Residue:** `engine/qc.py:162` still resolves the p column by exact-tuple membership — `engine/qc.py` is outside Lane 1's territory, so re-pointing it at `resolve` is still owed.

### A20. [MEDIUM · spine-consistency] Cloud provider registry forked FE↔BE: the FE hardcodes a `comingSoon` flag that cannot track the backend's declared per-provider flags

- **Where:** `app/frontend/lib/cloud/providers.ts:22 (mirror of app/backend/cloud/registry.py:30)`
- **What:** cloud/registry.py is exemplary — a declared table with per-provider `flag` names and `is_enabled(provider, settings)` so no handler branches on provider identity. But the FE re-declares the same registry by hand (ids, labels, providerConfigKeys) with enablement as a hardcoded literal `comingSoon: true`, and there is NO providers-listing endpoint (routers/cloud.py exposes only POST /uploads/intake/remote and POST /export/cloud). The declared flag therefore has two homes that cannot agree: set SELOM_CLOUD_GOOGLE=1 and the backend accepts Google imports while the FE still renders 'coming soon' — the capability is enabled server-side and unreachable client-side. lib/cloud/nango.ts:17 adds a third independent gate (`CLOUD_CONNECT_ENABLED = false`). Nothing fails when the two registries drift: no parity test, and no test asserts every `_PROVIDERS` id has a `_CONNECTORS` entry (routers/cloud.py:109 calls `connector.fetch_to_store` on a possibly-None connector).
- **Fix:** Add `GET /cloud/providers` to routers/cloud.py returning `[{id, label, kind, provider_config_key, enabled}]` derived from registry._PROVIDERS + registry.is_enabled(p, settings); have the FE menu render from that response and delete the hardcoded `comingSoon` + CLOUD_CONNECT_ENABLED literals (keep a static fallback shape only for types). Add a backend test asserting `set(_PROVIDERS) == set(_CONNECTORS)` so a provider added without a connector fails rather than 500ing at import time.
- **Status:** OPEN

### A21. [MEDIUM · spine-consistency] engine/assemble.py re-declares the 10x format detector and re-implements unit loading instead of consulting engine/ingest's declared loader registry

- **Where:** `app/backend/engine/assemble.py:54 (_is_10x_dir) and :95 (_read_unit)`
- **What:** engine/ingest.py already owns the single format-decision point: a declared `_Loader` registry (:199) with `_is_10x` (:162), `_load_10x` (:54) and `_load_h5ad` (:45), resolved through `_pick_loader(path)` (:208). engine/assemble.py — a sibling module in the SAME engine package, declaring itself 'the scRNA sibling of C6 ingest_many' — re-declares `_is_10x_dir` with a comment that admits it 'mirrors engine.ingest._is_10x' (byte-identical logic), and `_read_unit` re-implements the 10x-directory and .h5ad reads with its own direct `sc.read_10x_mtx` / `ad.read_h5ad` calls. Two loader decision points, currently coincidentally identical, with no guard test: the day engine/ingest gains a format or changes 10x detection (a v2 `genes.tsv`-only layout, a `.h5` matrix), /data/assemble-scrna silently rejects inputs the rest of the engine accepts, and the error is the hard `ValueError('unrecognized file(s)')` at :154.
- **Fix:** Have assemble.py consume the ingest registry rather than fork it — export a narrow helper from engine/ingest (e.g. `is_10x_dir(path)` and a `load_unit(path)` that reuses `_pick_loader`) and call it from `_is_10x_dir`/`_read_unit`; keep only assemble's genuinely new logic (loose-triplet grouping by filename prefix, the canonical-name staging dir, obs materialization). Add a guard test asserting the single 10x detector (e.g. `assemble._is_10x_dir is ingest.is_10x_dir`) so a re-fork fails.
- **Status:** **FIXED** (L1-04) — `engine.ingest` exports `is_10x_dir` (identity alias) + `load_unit`; `assemble._is_10x_dir is ingest.is_10x_dir` and `_read_unit` reads only through the registry, keeping just the canonical-name staging dir. Guarded in `tests/test_assemble.py` (identity + an AST assertion that no direct `read_10x_mtx`/`read_h5ad` call survives + an end-to-end registry-recognized 10x directory).

### A22. [MEDIUM · license] facs_gating attributes its compute to FlowKit — a library that is not a dependency — in both the user-facing catalog and the emitted methods citation

- **Where:** `app/backend/skills/facs_gating/skill.json:36 · app/backend/companions/methods.py:49,732`
- **What:** e90203a explicitly drops FlowKit ("FlowKit is no longer a dependency"; absent from uv.lock) and runs on flowio + flowutils. But skill.json:36 `catalog.summary` — the user/marketplace-facing provenance string — still says "Built on the BSD-3 **FlowKit** stack for compute", and skill.json:32's GatingML note says "the semantics **FlowKit** implements". Worse, companions/methods.py:732 returns `[FLOWKIT, *xform_cite, GATINGML]` as the citation list for a **publication-facing auto-methods paragraph** whose own prose (methods.py:727-730) correctly says "read with FlowIO and compensated and transformed with FlowUtils" — so the generated Methods section tells the researcher to cite a tool the pipeline never ran. The same file's `background` field gets it right, which shows the drift is an unupdated leftover rather than a considered family-citation choice. This breaches LAUNCH-GATES.md §4 ("Every skill carries license + provenance metadata — which library/method it wraps, under what license, citation … expose provenance to users"): the pre-launch SCA inventory driven off the catalog would go hunting for `flowkit` (not in uv.lock) and would MISS `flowio`/`flowutils`, the two packages actually shipped and actually needing BSD-3 notice on any redistribution.
- **Fix:** In skills/facs_gating/skill.json change catalog.summary to "Built on the BSD-3 FlowIO + FlowUtils stack for compute" and reword line 32's note to attribute the gate/transform semantics to the Gating-ML 2.0 standard itself, not to FlowKit. In companions/methods.py rename/retarget the citation so it matches the code path — either cite FlowIO/FlowUtils directly, or keep the FlowKit paper as the family citation but say so explicitly in the constant (e.g. `FLOWIO_UTILS = "White, S. et al. FlowKit … (2021). [cited for FlowIO/FlowUtils, the BSD-3 components used here]"`), so no reader concludes FlowKit ran.
- **Status:** OPEN

### A23. [MEDIUM · design] Inspector tab strip overflows once the Annotate tab makes seven tabs

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/property-panel.tsx:72-95`
- **What:** Annotate is added unconditionally, so figures that also have Marks (a scale bar, or any skill-emitted label — heatmap, ERG trace grid, violin with unit labels) render 7 grid cells inside the fixed 330px dock. MEASURED at 1512×1050: tablist clientWidth 305px; at 6 tabs each cell is 48px and the 'Annotate' label is 42.8px wide, leaving 3px gutters where the trigger's `px-2.5` asks for 10px; at 7 tabs (simulated by cloning a trigger and setting `repeat(7, minmax(0,1fr))` on the real list) cells drop to 40.7px and the same label measures 42.8px → gutters of −1.1px per side, i.e. the label spills out of its own pill into the 2px gap and butts against its neighbours. `TabsTrigger` sets `whitespace-nowrap` with no `truncate`/`overflow-hidden`, so the text overflows rather than clipping.
- **Fix:** Either shorten the label (e.g. 'Notes'/'Anno') and add `truncate`/`overflow-hidden` to the label span so it clips instead of spilling, or switch to icon-only triggers with the name in an accessible tooltip once `tabs.length > 6`. A cleaner structural option: put the annotation layer INSIDE the Marks tab (both are 'things drawn on the figure') and keep the strip at six.
- **Status:** OPEN

### A24. [MEDIUM · design] The artboard hero is clipped inside its own stage

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/shell/artboard-host.tsx:59-64`
- **What:** The white card keeps the pre-shell `height: min(74vh, 720px)` while it is now a flex child of a host whose height is only what the shell's four bands leave over. MEASURED at 1512×1050: card height 720px inside a host of 548px (host scrollHeight 752) → ~24% of the figure sits below the visible stage and is reachable only by scrolling inside the stage; at a 720px content height the card is 533px in a 304px host (43% clipped). The document itself does not scroll, so nothing signals that the bottom of the figure (x-axis title, legend) exists. This directly contradicts the code's own claim on the same lines ('Fill the available width … the figure gets every remaining pixel') and the milestone's 'artboard is the hero' framing — and the two new inert bands (below) made it 64px worse.
- **Fix:** For the non-`fixed` branch, let the flex host size the card: `{ width: '100%', maxWidth: '88rem', height: '100%', maxHeight: '100%' }` (or drop the inline height and use `h-full max-h-full` with `min-h-0` on the wrapper). Keep the pixel height only for `fixed` layouts, where the figure has a declared size and scrolling is expected.
- **Status:** OPEN

### A25. [MEDIUM · design] Inert 'coming soon' scaffolding takes 64px of the vertical budget and reads as unfinished

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/shell/palette-strip.tsx:11-25`
- **What:** One milestone ships four placeholders around the editor: the PaletteStrip (MEASURED 34px tall, a read-only swatch row plus 'Palette board — coming soon' — while the real colourway swap already lives in Style, as its own comment admits), the ToolContextStrip (MEASURED 30px of static prose that never changes because Select is the only mode), two permanently disabled rail tools (Shape/Guide, whose `title` hint is unreachable because a `disabled` button takes no hover/focus for many users), and a disabled 'Export to cloud · Coming soon' row (export-menu.tsx:223-233) whose client `exportToCloud` (lib/cloud/api.ts:55) has no caller anywhere. The 64px of dead bands come straight out of an artboard that is already clipped (previous finding). The export row's 'Coming soon' text also computes to ≈2.4:1 contrast (`text-muted-foreground/70` inside an `opacity-70` container over `bg-background/40`), unreadable at 11px.
- **Fix:** Delete the palette strip until the live board lands (Style covers it today) and delete the export-to-cloud row until the endpoint is wired — the `exportToCloud` client can sit unused in lib without a dead button advertising it. Fold the Select hint into the rail buttons' tooltips (or render the strip only when a tool with real context is active). For Shape/Guide, keep them enabled-looking only if they do something; otherwise drop them from the rail rather than shipping two dead slots. If any 'coming soon' text stays, raise it to at least 4.5:1 and drop the compounding `opacity-70`.
- **Status:** OPEN

### A26. [MEDIUM · design] Cloud Connect buttons look live but their only outcome is an error message

- **Where:** `/home/deploy/work/selom/app/frontend/components/intake/cloud-import-menu.tsx:77-87`
- **What:** `connect()` calls `connectProvider`, which throws unconditionally because `CLOUD_CONNECT_ENABLED === false` (lib/cloud/nango.ts:40-42), and the catch block renders that thrown message as the UI copy. So a fully enabled-looking outline button's entire behaviour is: click → an error string appears in a muted note. The provider registry already declares the truth (`comingSoon: true`, lib/cloud/providers.ts:24-26) and the component never reads it. The disabled-state affordance exists in the data model and is ignored in the view. The sr-only explanation ('… other providers are coming soon') is also placed at the very END of the popover (line 181), after the Connect buttons, so a screen-reader user hears it only after having tried them.
- **Fix:** Read the registry: `disabled={p.comingSoon}` on the Connect button with a small 'Soon' badge on the row, and delete the throw→`connectNote` path (keep `connectProvider`'s guard as a programming-error backstop, not as UI copy). Move the sr-only sentence to the top of the popover, before the provider list.
- **Status:** OPEN

### A27. [MEDIUM · design] Multi-sample scRNA assembly ships with no way for a user to reach it

- **Where:** `/home/deploy/work/selom/app/backend/routers/data.py:153`
- **What:** `POST /data/assemble-scrna` (11a115f) has zero references anywhere in app/frontend — no client, no MSW handler, no entry point — while its sibling `/data/combine` is wired into intake (lib/intake/inspect.ts:158-174, plus mocks/handlers.ts:149). The motivating scenario (per-sample 10x matrices whose design lives in the filenames — the RPGRIP1/Hani case) therefore still cannot be done from the app: a multi-file drop either fails soft to single-file ingest or goes to the ERG/tabular `combineData` path, which is the wrong endpoint for 10x matrices. Shipped ≠ reachable.
- **Fix:** Add a 'these files are per-sample' branch to the multi-file drop flow that posts to `/data/assemble-scrna` (mirroring `combineData`'s fail-soft shape), surface the derived obs columns (sample_id/line/condition) for confirmation before the dataset is created, and add the MSW handler in the same change so the mock mirrors the backend contract.
- **Status:** OPEN

### A28. [LOW · spine-consistency] facs_gating auto-methods cites the FlowKit paper for software that is deliberately not a dependency, and the compensation-applied outcome is computed then discarded

- **Where:** `app/backend/companions/methods.py:732 and app/backend/skills/facs_gating/run_real.py:43`
- **What:** The commit's stated goal is 'honest provenance … closing the honesty gap (RISKS #12)', but the publication-facing methods text emits `[FLOWKIT, *xform_cite, GATINGML]` on every facs_gating run while pyproject.toml states 'FlowKit itself is deliberately NOT a dependency' and run_real.py reimplements the gate tree + population report in clean-room numpy. This is a software-attribution error, distinct from the acceptable algorithm citation precedent (_integration cites HARMONY for the algorithm Melody implements): FlowIO and FlowUtils are the software actually used and are the correct software citations. Separately, `_compensate` returns a `(events, compensated?)` pair but the caller discards the flag (`comp, _compensated = ...`), and `_compensate` degrades to RAW uncompensated events on a malformed $SPILLOVER string (:115), a detector/label mismatch (:123) or a shape mismatch (:126) — while the methods text asserts 'the acquisition spillover matrix embedded in the FCS ($SPILLOVER) was applied where present'. Fail-soft here degrades the DATA silently, with nothing on the figure, the population table or the provenance to say so.
- **Fix:** Replace FLOWKIT with FLOWIO + FLOWUTILS citation strings in companions/methods.py (keep LOGICLE/GATINGML for the algorithm and standard). Thread the `compensated` boolean out of run() — into the figure title/axis note (the pattern normalization_qc/run_real.py:82 already uses) and the population-table title — and key the methods sentence off the actual outcome rather than the requested `compensate` param.
- **Status:** OPEN

### A29. [LOW · design] New tools rail skips the house focus ring and declares role=toolbar without roving focus

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/shell/tools-rail.tsx:44-78`
- **What:** MEASURED with real Tab-key focus: the focused rail button matches `:focus-visible` but its only indicator is the UA default `outline: auto 1px rgb(230,237,243)` with `box-shadow: none` — the repo's established pattern is an explicit `outline-none focus-visible:ring-2 focus-visible:ring-ring/50` (workrail.tsx:246/295/352/480, export-menu.tsx:151), so the newest interactive surface is the one place keyboard focus looks different and faintest on 36px icon buttons. Same omission on the Annotate panel's AddButton, IconButton and StarPicker (annotations-panel.tsx:86-100, 171-186, 241-257) — only the text Input carries a focus style. Separately, `role="toolbar"` + `aria-orientation="vertical"` is declared while all six buttons keep their own tab stop (measured: 4 enabled stops, every `tabindex` null); ARIA APG expects a toolbar to be a single tab stop with arrow-key navigation, so the announced semantics don't match the behaviour.
- **Fix:** Add `outline-none focus-visible:ring-2 focus-visible:ring-ring/50` to the rail buttons and to AddButton/IconButton/StarPicker. Then either implement roving tabindex + ArrowUp/ArrowDown on the rail, or drop `role="toolbar"`/`aria-orientation` down to a plain labelled `<div role="group" aria-label="Drawing tools">`, which matches what the markup actually does.
- **Status:** OPEN

### A30. [LOW · design] Icon glyphs collide across meanings, and new work continues on lucide against the stated preference

- **Where:** `/home/deploy/work/selom/app/frontend/components/intake/cloud-import-menu.tsx:13-19`
- **What:** `Cloud` is used both for the menu trigger ('Import from cloud') and as OneDrive's provider glyph, so the same mark means two different things eight lines apart; Dropbox gets `Package` (a generic box) and Google Drive `HardDrive` (a local disk — the opposite of cloud). In the editor, `Asterisk` marks BOTH the Annotate tab (property-panel.tsx:72) and the 'Significance bracket' tool (tools-rail.tsx:29), so a tab and one of its child actions share a glyph while the neighbouring Marks tab has its own (`Shapes`). All of this new icon work is lucide, while the owner's stated preference for new icon work is Phosphor — the mismatch keeps getting re-litigated per slice because the decision has no home.
- **Fix:** Give each provider a distinct mark (brand SVGs, or at minimum distinct generic glyphs) and keep `Cloud` for the trigger only; pick a different glyph for the Annotate tab than for the bracket tool. Record the icon-set decision once — either 'lucide remains the FE standard, Phosphor deferred' in the FE conventions doc, or a planned wholesale swap — so new components stop deciding ad hoc.
- **Status:** OPEN


## B. fe-review — 24 confirmed (6 dropped as refuted)

### B1. [HIGH · V-visible] Repeat annotation adds are invisible — identical default geometry, no cue at the control, no surfaced count

- **Where:** `app/frontend/lib/figure/annotations.ts:286`
- **What:** V-check breached: "every user-initiated state change must have a visible cue AT the control" + "a 'pending' state with no surfaced count" — a literal repeat of the S5 changed-control miss.

All three add builders place the new item at a FIXED position that ignores what is already on the figure:
- `addTextLabelOps` (annotations.ts:288-299) → always `x:0.02, y:0.98` paper, text `"Text"`.
- `addArrowOps` (annotations.ts:310-328) → always `x:0.5, y:0.5, ax:-55, ay:45`, text `""`.
- `addSigBracketOps` (annotations.ts:245-275) via `bracketGeometry` (annotations.ts:187-227), which reads only `spec.data` — never the existing `layout.shapes`/`layout.annotations`.

So the SECOND click of any draw tool appends an item that lands exactly on top of the first: pixel-identical text/path, no visible change on the artboard.

And nothing at the control compensates:
- tools-rail.tsx:55 `onClick={() => store.commit(build(spec))}` — the button has no pressed/flash/count state; its className (tools-rail.tsx:65-72) is a pure function of `kind`/`enabled`, so it renders identically before and after the commit.
- annotations-panel.tsx:41,47,53 `AddButton` — same, no post-commit state.
- The only count in the product is `On the figure · N` (annotations-panel.tsx:63), INSIDE the Annotate tab. `PropertyPanel` defaults to `tab="style"` (property-panel.tsx:43) and the Annotate `TabsTrigger` (property-panel.tsx:90-95) carries no badge — so with the inspector on Style (the default) the entire add is unacknowledged.
- There is no toast path: the only toast in the app is `UndoToast`, scoped to figure/project deletes (components/shell/undo-toast.tsx).
- Even the FIRST add can be invisible: `ArtboardHost` scrolls (`overflow-auto`, artboard-host.tsx:45) and a fixed-width tall figure scrolled down hides the paper-0.98 text label entirely.

The e2e masks this exactly — annotation-layer.spec.ts:43 opens the Annotate tab BEFORE any add, and asserts counts via `gd.layout`, never a user-visible cue.

Failure walkthrough: open a figure (inspector on Style) → click "Text label" in the rail → a label appears top-left. Click "Text label" again → nothing changes anywhere on screen; two stacked labels now exist, both will export, and the user's next Cmd-Z appears to do nothing either.
- **Fix:** Two parts, both idiomatic to the repo:
(1) Cascade the default so the artboard ALWAYS changes: in `addTextLabelOps`/`addArrowOps`/`bracketGeometry`, count existing same-kind items via the already-exported `selomAnnotations(spec).filter(a => a.kind === kind).length` and step the default (e.g. paper `x += n*0.03, y -= n*0.05` for text/arrow; `yb/yt += n*span*0.07` for brackets), clamped into range.
(2) Surface the state at the control: add a count badge to the Annotate `TabsTrigger` (property-panel.tsx:90-95) and to each tools-rail draw button (tools-rail.tsx:57-77), mirroring the existing precedent at project-workspace.tsx:460-475 ("Carries the pending-✨ count so attribution is visible without opening it"); and ring the just-added Annotate row with the established amber vocabulary `changedRingClass("user")` from lib/ui/changed-ring.ts (`--stage-figuredata`) for a few seconds after the commit.
- **Status:** OPEN

### B2. [HIGH · R-reversible] Annotate panel's Remove is a one-way door on /extract — that surface has no undo at all

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/panels/annotations-panel.tsx:129`
- **What:** R check breached: a destructive per-item action with NO per-change revert and NO global undo/reset in the surface it ships into.

Evidence chain: `property-panel.tsx:72,76` adds `annotateTab` **unconditionally** to `tabs` (no capability/host gate), and `TabsContent value="annotate"` at `property-panel.tsx:125-129` is likewise unconditional. PropertyPanel is rendered by `InspectorDock` → `EditorWorkspace`, and `components/extract/chart-extractor.tsx:242` renders `<EditorWorkspace store={store} />`. So the Annotate panel — including `store.commit(removeAnnotationOps(spec, item.id))` at annotations-panel.tsx:129 — is live on `/extract`.

`/extract` has zero reversibility infrastructure:
• `grep -n "undo|redo|Undo|Redo" components/extract/chart-extractor.tsx` → **no matches**. The top bar (chart-extractor.tsx:225-240) offers only "Re-calibrate" and "Recover another".
• The app's ONLY Cmd-Z/Cmd-Shift-Z handler is `project-workspace.tsx:251-263`, gated by `if (view !== "figure" || frozen) return;`. `/extract` is a different route (`app/extract/page.tsx`), so the handler never mounts. Confirmed: `grep -rn "metaKey"` returns exactly two hits — this handler and the Cmd-K palette in `app-shell.tsx:72`.
• `store.canUndo` / `store.undo` exist on the store (`hooks/use-figure-store.ts:141-153`) but are never surfaced on /extract.

Concrete loss: on a digitized figure the user adds a significance bracket, drags it into place on the artboard (a real positioning gesture, ~10s of work), then mis-clicks Remove — which sits 4px from the benign Hide toggle (annotations-panel.tsx:119, `gap-1`, `size-7` buttons). The bracket is gone. Re-adding via `addSigBracketOps` yields a DEFAULT-geometry bracket (annotations.ts:245-249 recomputes from `bracketGeometry(spec)`), so the hand-placed position and any edited stars are unrecoverable. Same for a positioned text label / arrow (`addTextLabelOps` always lands at paper 0.02/0.98, `addArrowOps` always at 0.5/0.5).

Compounding on the same surface: "Recover another" (chart-extractor.tsx:236) calls `loadFileReset()` → `store.reset()` (chart-extractor.tsx:134), which sets the store to `EMPTY` (use-figure-store.ts:105-106) — every annotation plus all history discarded, no confirm, no restore. Before this slice /extract's inspector was non-destructive (Marks only offers hide + inline text edit); slice 5 introduces the first irreversible operation there.
- **Fix:** Make recovery independent of the host surface OR give /extract undo. Preferred (cheap, one place): route the Remove through the app's existing undo-toast pattern — `pushUndo(label, restore)` in `lib/workspace/undo.ts:29`, already used for figure/dataset/project/paper deletes (`use-figure-crud.ts:84,97`, `project-workspace.tsx:504`, `library-view.tsx:222`). In `AnnotationRow`, snapshot the pre-remove spec and call `pushUndo(`Removed ${item.label}`, () => store.commit(restoreOps))` (or expose a `store.restore(spec)` action, since the reducer already keeps whole-spec snapshots). Additionally add an Undo/Redo pair to the chart-extractor top bar bound to `store.canUndo/canRedo/undo/redo` (chart-extractor.tsx:232-239) — the store already exposes them, it is ~8 lines — and gate `loadFileReset`'s `store.reset()` behind a confirm when `store.canUndo` is true.
- **Status:** OPEN

### B3. [HIGH · R-reversible] A re-run silently destroys the whole annotation layer and clears the undo history that could restore it

- **Where:** `/home/deploy/work/selom/app/frontend/components/project/hooks/use-figure-run.ts:223`
- **What:** R check breached: an accepted change with no path back — the revert affordance (Undo) is explicitly disabled at the exact moment the work is lost.

Every re-run path carries gene labels forward but NOT the slice-5 annotation layer:
• `use-figure-run.ts:122-126` — `captureCarryLabels()` returns labels only when `deriveFigureModel(spec).capabilities.geneLabels`; `lib/volcano/labels.ts:170-186` `carryLabels()` re-anchors only gene-name annotations.
• `use-figure-run.ts:217,223,240` (staleness re-run), `:340,346,363` (`rerunFigureWithParams`, the Figure-data staged re-run), and `:400-423` (the AI-apply re-run) all build `nextSpec = carryPrev.length ? carryLabels(res.figure, carryPrev) : res.figure` — the fresh backend spec has no `selom`-tagged shapes/annotations, and nothing re-appends them. Brackets, free text labels and arrows are dropped.
• Then `figure.init(nextSpec)` runs (`:240`, `:363`, `:423`), and the reducer's `init` case (`hooks/use-figure-store.ts:44-49`) returns `{ past: [], future: [], checkpoint: null }`. So `canUndo` is false (`use-figure-store.ts:145`) → the Undo button in the command bar is `disabled` (`figure-view.tsx:140`) and Cmd-Z is inert.

Failure scenario: user opens a bar figure, adds 3 significance brackets and 2 panel-letter text labels via the Annotate panel, drags each into place. The dataset is marked stale, so they click the prominent "Re-run" in the command bar (`figure-view.tsx:152-162`). The new version renders with all 5 annotations gone; Cmd-Z does nothing; the Undo button is greyed out. The toolbar copy two elements away still reads "Editing live — every change is a JSON-Patch" (`figure-view.tsx:146`), which is now false for this loss.

Mitigation exists but is not a revert and is unsignposted: `projectStore.addFigure(..., parentFigureId: fig.id, variantLabel: "re-run")` retains the previous version, and `project-workspace.tsx:269-274` had persisted the annotated spec (350ms debounce), so the parent version in the VersionBar still holds the annotations. Nothing tells the user that, and there is no way to bring the annotations forward onto the re-run version — they would have to re-draw all 5.
- **Fix:** Add a `carryAnnotations(nextSpec, prev)` peer of `carryLabels`, in `lib/figure/annotations.ts` (it already owns `isSelomAnnotation`/`tagOf`): before the run, collect every `selom`-tagged entry from `/layout/shapes` + `/layout/annotations`; after it, re-append them to `nextSpec`. Paper-referenced items (`textLabel`, `arrow` — `xref/yref: "paper"`) carry verbatim; data-referenced brackets re-anchor by recomputing `bracketGeometry(nextSpec)` for y while keeping the user's x1/x2 and stars text. Wire it into all three re-run paths beside the existing `carryLabels` call (use-figure-run.ts:223, :346, and the AI-apply path ~:410). Minimum viable alternative if the carry is deferred: count `selomAnnotations(figure.spec).length` and, when > 0, make the Re-run button open a confirm that names the count and states the annotations will not carry, plus a post-run toast pointing at the retained parent version.
- **Status:** OPEN

### B4. [HIGH · D-discoverable] Cloud "Connect" buttons look fully enabled but can never succeed — the only pre-click disclosure is sr-only

- **Where:** `/home/deploy/work/selom/app/frontend/components/intake/cloud-import-menu.tsx:164-171`
- **What:** D-check breached: a control must look like what it does. The three OAuth rows (Google Drive / OneDrive / Dropbox) render a live `<Button variant="outline">Connect</Button>` with normal enabled styling, pointer cursor and hover — indistinguishable from the working "Import" button 30px above it. `connectProvider` throws unconditionally (`lib/cloud/nango.ts:40-42`, `CLOUD_CONNECT_ENABLED = false`), so every click is a guaranteed dead end. The registry ALREADY carries the honest state — `CloudProvider.comingSoon` (`lib/cloud/providers.ts:19`, true for all three at :24-26) — and the menu never reads it. Worse, the pre-emptive disclosure exists but is hidden from sighted users: `<p className="sr-only">…other providers are coming soon.</p>` (line 181). This also breaks the repo's own established idiom for an unavailable control: `export-menu.tsx:224-233` (same commit) ships `disabled` + a visible "Coming soon" chip, and `skill-match-results.tsx:535-543` does the same. Failure: a user opens "Import from cloud", clicks Connect on Google Drive expecting an OAuth popup, gets a grey note explaining it was never possible.
- **Fix:** Gate the row on the flag that already exists: `disabled={p.comingSoon}` on the Button, label it `{p.comingSoon ? "Coming soon" : "Connect"}` (or keep "Connect" and add an `ml-auto text-[11px] text-muted-foreground/70` "Coming soon" chip like export-menu.tsx:232), dim the row, and delete the `onClick` for coming-soon providers. Promote the sr-only sentence (line 181) to visible helper text under the "Connect an account" header so the state is legible before the click, and drop the now-dead `connectNote` catch path.
- **Status:** OPEN

### B5. [HIGH · A-attributable] Hand-typed significance stars export as an unattributed statistical claim

- **Where:** `app/frontend/components/figure/panels/annotations-panel.tsx:156-181 (StarPicker) + app/frontend/lib/figure/annotations.ts:245-273 (addSigBracketOps / the `selom` tag)`
- **What:** ATTRIBUTABLE — "a change must say WHO made it" — breached for the strongest claim the new layer can put on a figure. The Annotate panel and the tools rail both call `addSigBracketOps(spec)` with NO options (annotations-panel.tsx:44 `store.commit(addSigBracketOps(spec))`; tools-rail.tsx:41 `build: addSigBracketOps`, invoked as `build(spec)`), so `opts.p` is never supplied and the stars default to `"*"` (annotations.ts:247); the operator can then freely pick `*` / `**` / `***` / `ns` by hand in StarPicker. `starsForP` (annotations.ts:166) is dead from the UI. The resulting item is tagged `selom: { kind, id, role }` (annotations.ts:259,272) — the tag carries NO author and NO source, so nothing in the spec, the panel row (labelled only "Significance", annotations.ts:38-42 KIND_LABEL), the artboard, or the export distinguishes a hand-typed asterisk from a computed one. Export stamps no provenance either (`lib/figure/export.ts` has no provenance/caption path), and the annotation is a client-tier spec edit, so it never reaches `provenance.params` or `provenance.actions[]` — meaning the figure asserts p<0.001 while the Publish-confidence "reproducibility bundle" directly above it (components/project/publish-confidence.tsx:352 `Reproducibility`) lists params/input-hash/environment that reproduce a figure WITHOUT that claim. The repo's own discipline says otherwise in two places: docs/pillar-2-direct-manipulation/spec.md:70 ("mark the cell as a manual override — a hand-edited point is a *claim about the data*, never silent analysis output") and spec.md:109 ("so the asterisks are *correct*, not hand-typed"). The precedent already exists for the weaker case: components/project/marks-editor.tsx:348-361 `SourceTag` renders a textual manual/auto/device chip per ERG landmark, plus a summary line "N markers operator-set" (marks-editor.tsx:229).
- **Fix:** Stamp the source on the tag and surface it as text, not just a doc comment: extend `SelomTag` (annotations.ts:44) to `{ kind, id, role?, source: "manual" | "derived", p?: number }`; in `addSigBracketOps` set `source: typeof opts.p === "number" ? "derived" : "manual"` and carry `p`; expose `source` on `SelomAnnotation` (annotations.ts:76-90) and render a `SourceTag`-style chip in `AnnotationRow` ("HAND-SET" vs "p = 0.003") next to the row label so it is glyph+text, never tone alone; add the same statement under StarPicker ("Hand-set — not read from the statistics table"). Then include the manual-annotation count in the Publish-confidence reproducibility block so a hand-set claim cannot ride out of a figure that advertises itself as reproducible-from-params.
- **Status:** OPEN

### B6. [HIGH · R-recorded] Committed annotation layer is silently dropped by the sanctioned re-run (only volcano gene labels are carried)

- **Where:** `/home/deploy/work/selom/app/frontend/components/project/hooks/use-figure-run.ts:120-125 (applied at :223, :346, :405)`
- **What:** R-check breached: a COMMITTED figure change must survive the one recorded re-run path. Slice 5 adds a whole new class of committed client edits — `selom`-tagged `layout.shapes` + `layout.annotations` (brackets/stars, text labels, arrows) written via `store.commit` and persisted by the 350ms effect at project-workspace.tsx:272 → `projectStore.updateFigureSpec` → `PATCH /figures/{id}` — but it does NOT extend the capture/carry seam that exists precisely for this problem. `captureCarryLabels()` (use-figure-run.ts:120-125) captures ONLY volcano gene labels and only when `deriveFigureModel(spec).capabilities.geneLabels` is true; all three re-run paths then do `carryPrev.length ? carryLabels(res.figure, carryPrev) : res.figure` (:223 rerunFigure, :346 rerunFigureWithParams, :405 rerunFigureWithAi) and `figure.init(nextSpec)`. Failure scenario: user adds a significance bracket + two text labels to a bar-chart figure (persisted in that figure's spec), then edits a data param and clicks the ONE explicit re-run. The new variant (`addFigure` with `parentFigureId`/`variantLabel:"re-run"`) is POSTed with a spec containing none of them; `figure.init()` resets past/future so Cmd-Z cannot bring them back on the new version, and the annotations survive only on the now-superseded parent. They are also not in `provenance.params` (they are cosmetic, correctly), so nothing in the recorded run reproduces the annotated figure. The e2e (e2e/annotation-layer.spec.ts) proves render + undo only — no test covers the re-run boundary.
- **Fix:** Add `captureSelomAnnotations(spec)` / `carrySelomAnnotations(newSpec, prev)` to lib/figure/annotations.ts (capture BOTH halves — tagged `layout.shapes` and `layout.annotations` — keyed by the `selom.id`; re-append paper-referenced items verbatim, and for data-coord brackets either re-derive y from the fresh `bracketGeometry(newSpec)` keeping x1/x2, or drop with a count the caller can surface). Call it next to `carryLabels` in all three re-run paths, and add a unit test asserting a bracket + text label survive a fresh-spec re-run. If a carried item cannot be re-anchored, say so in the run result rather than dropping silently.
- **Status:** OPEN

### B7. [HIGH · R-recorded] carryLabels mis-captures a Selom annotation on a volcano and persists it with data coords under xref:"paper"

- **Where:** `/home/deploy/work/selom/app/frontend/lib/volcano/labels.ts:157 (captureLabels) and :170 (carryLabels), reached via use-figure-run.ts:124`
- **What:** R-check breached: the persisted record is rewritten into a state that does not correspond to what the user committed. `captureLabels` filters `layout.annotations` to "any entry with a non-empty string `text`" on the documented assumption that on a volcano EVERY annotation is a gene label (labels.ts:12-14). Slice 5 breaks that assumption: `addTextLabelOps` / `addArrowOps` / the bracket's stars annotation all land in `layout.annotations` with text, and the Annotate panel's own hint invites the volcano case ("Panel letter, gene name, or note"). Failure scenario: on a volcano the user adds a Text label and types `TP53` (a plotted gene), then re-runs with edited inputs. `captureCarryLabels()` captures that Selom item; `carryLabels` finds TP53's point and emits `{...a, x: point.x, y: point.y}` — overwriting the item's paper coords (0.02/0.98) with DATA coords (e.g. x=-1.8, y=4.2) while `xref`/`yref` stay `"paper"` and the `selom` tag is preserved. Result: the label is positioned ~1.8 plot-widths left and 4.2 heights above the plotting area (invisible, and unreachable by the artboard drag that is its only reposition affordance), it is still listed as present in the Annotate panel, and the corrupted item is persisted into the new figure via `addFigure` → `POST /figures`. Bracket stars ("***") and arrow captions take the sibling path — captured then dropped, so the bracket's shape half disappears too.
- **Fix:** In `captureLabels` (or in `captureCarryLabels` before it), exclude `isSelomAnnotation(a)` items — they are owned by the annotation layer, not the gene-label set — and harden `carryLabels` to skip any annotation whose `xref`/`yref` is not the data axes (only re-anchor items already in data coords). Extend lib/volcano/labels.test.ts with a Selom text label whose text equals a plotted gene, asserting it is neither captured nor re-coordinated.
- **Status:** OPEN

### B8. [HIGH · N-nonbreaking] Every annotation add lands on the same fixed default position — the 2nd add is drawn pixel-identically over the 1st (looks like a no-op, occludes its neighbour)

- **Where:** `app/frontend/lib/figure/annotations.ts:286 (addTextLabelOps), :308 (addArrowOps), :187/:245 (bracketGeometry → addSigBracketOps)`
- **What:** N breach — a new item added to an existing region must not overlay its neighbour, and a row must not lose its meaning. All three slice-5 primitives hard-code their default placement with no awareness of what is already on the figure: text label → paper (0.02, 0.98); arrow → paper (0.5, 0.5) with a fixed tail (ax -55, ay 45); bracket → bracketGeometry(spec), which is a pure deterministic function of the data, so it returns the identical path for every add. CONFIRMED RENDERED (Chrome 1440×900, /p/demo-pbmc?demo=1, driven from the tools rail): two "Text label" clicks → both spec annotations at x:0.02,y:0.98 and both drawn client rects at exactly {x:711,y:536,w:36,h:22}; two "Significance bracket" clicks → identical spec path "M -0.9422,5.843134 L -0.9422,6.218368 L -0.5619,6.218368 L -0.5619,5.843134" AND identical drawn SVG d="M 79.37,100.61 L 79.37,75 L 90.27,75 L 90.27,100.61" plus both star annotations at {x:722,y:525}; two "Arrow / callout" clicks → both at {x:760,y:788}. Net effect: the Annotate panel row count says "On the figure · 2" while the artboard shows one item, the first item is fully occluded, and the drag handle only reaches the topmost one. The e2e (e2e/annotation-layer.spec.ts) adds one of each, so it can never catch this. This is the owner's core workflow (panel letters A/B/C, a bracket per comparison) — the second add of any kind is the normal case, not the edge case.
- **Fix:** Cascade the default off the count of existing same-kind items instead of a constant. In each builder read `const n = selomAnnotations(spec).filter(a => a.kind === <kind>).length;` then: textLabel → `x: Math.min(0.02 + 0.04 * n, 0.7), y: Math.max(0.98 - 0.07 * n, 0.15)`; arrow → `x: 0.5, y: 0.5` with `ax: -55 + 18 * n, ay: 45 - 18 * n` (or offset x/y by 0.04·n); sigBracket → tier the bracket up like GraphPad by raising the geometry: `yb += span * 0.08 * n; yt += span * 0.08 * n` (pass through opts.geometry so an explicit caller still wins). Add a unit test in lib/figure/annotations.test.ts asserting that two consecutive adds of the same kind produce different x/y (and different bracket paths), which is the ratchet this bug slipped through.
- **Status:** OPEN

### B9. [MEDIUM · V-visible] Cloud "Connect" buttons render as live while `comingSoon` is declared; the only cue is a provider-agnostic note at the bottom of the popover

- **Where:** `app/frontend/components/intake/cloud-import-menu.tsx:164`
- **What:** V-check breached: a user-initiated action whose visible cue is not at the control — and which produces NO visible change at all on a second click.

The registry already declares the state: `comingSoon: true` for google/onedrive/dropbox (lib/cloud/providers.ts:24-26, documented at :18 as "OAuth providers render a Connect button that no-ops until this is false"). `CloudImportMenu` never reads that field — cloud-import-menu.tsx:164-171 renders a normal enabled `variant="outline"` Connect button for every OAuth provider, styled identically to a working action.

Clicking it calls `connect()` (cloud-import-menu.tsx:77-87), which always throws (nango.ts:40-42, `CLOUD_CONNECT_ENABLED = false`) and writes the caught message into `connectNote`, rendered at cloud-import-menu.tsx:176-180 — BELOW the entire three-item list, ~120px from the button pressed, with no `role="status"`/`aria-live`. Because the thrown message is a constant ("Cloud account connection isn't enabled yet — coming soon.") and the provider-naming fallback at :84 is only reached for non-Error throws, the note is byte-identical for all three providers: click Google, then click Dropbox, and the UI does not change in any way — the user cannot tell the second click registered.

The SAME commit (8fb2bad) gets this right elsewhere: export-menu.tsx:224-234 renders the not-yet-built "Export to cloud" entry as `disabled` with a `title="Coming soon"` and an inline "Coming soon" pill at the control.
- **Fix:** Read the declared flag and put the state on the control, matching the export-menu pattern: in the `OAUTH_PROVIDERS.map` (cloud-import-menu.tsx:153-174) render `<Button disabled={p.comingSoon} …>` plus an inline `<span className="ml-auto text-[11px] text-muted-foreground/70">Coming soon</span>` in the row (and `title={p.comingSoon ? "Coming soon" : "Connect your account"}`). That deletes the click-with-no-feedback path entirely; keep `connectNote` only for REAL connect failures once the flag flips, and give it `role="status"` so it is announced when it does appear.
- **Status:** OPEN

### B10. [MEDIUM · V-visible] Annotation rows are mutually indistinguishable and unlinked to the canvas item they control

- **Where:** `app/frontend/components/figure/panels/annotations-panel.tsx:114`
- **What:** V-check breached: acting on a control gives no visible cue about WHICH figure item changed.

`AnnotationRow` labels each row with `item.label` only — `KIND_LABEL` (annotations.ts:38-42) returns the same string for every item of a kind ("Significance" / "Text label" / "Arrow / callout"), with no ordinal and no id fragment. There is no hover/focus → canvas highlight (annotations-panel.tsx:113-152 binds only `onClick` handlers; nothing communicates back to the artboard).

Combined with the identical default geometry (finding 1), three text labels give three rows reading `TEXT LABEL` + `"Text"`, sitting on three exactly-overlapping canvas labels. Clicking the Trash2 button (annotations-panel.tsx:127-133, `aria-label={`Remove ${item.label}`}` → "Remove Text label") gives no indication which of the three vanishes, and hiding one via the Eye toggle (:120-126) also produces no visible canvas change while duplicates remain underneath. Same for `StarPicker`/`TextEditor` edits: the commit lands on an item the user cannot identify.
- **Fix:** Give each row a stable visible identity and a canvas link: (a) in `selomAnnotations` (annotations.ts:93-127) attach a per-kind ordinal and render it in the row header ("Text label 2") and in the `aria-label`s; (b) on row `onMouseEnter`/`onFocus`, highlight the corresponding canvas item — cheapest honest version is a non-undoable preview (`store.set`, not `store.commit`) of the item's `bgcolor`/`bordercolor`, or reuse the amber `changedRingClass("user")` ring on the row plus a temporary `--stage-figuredata` outline on the annotation — so the pairing is visible before a destructive click.
- **Status:** OPEN

### B11. [MEDIUM · D-discoverable] Tools-rail draw buttons read as modes but insert instantly — no hint anywhere in the shell, and a second click silently stacks an identical annotation

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/shell/tools-rail.tsx:57-77`
- **What:** D-check breached: a control's appearance must not misrepresent its action. Everything about the rail says "pick a mode, then draw": `role="toolbar"`, vertical icon rail, Select rendered as the permanently pressed member (`aria-pressed` + `bg-accent/60`, tools-rail.tsx:62,68). The three live tools carry NOUN labels — "Significance bracket", "Text label", "Arrow / callout" (lines 29-31), reused verbatim as the `title` at line 63 — yet clicking one commits an insert immediately (`store.commit(build(spec))`, line 55) and gives the button no state change afterwards. Nothing in the shell corrects the expectation: the tool-context strip, which is the shell's designated "what does the active tool do" surface, still describes ONLY Select and was not touched by slice 5 (`shell/tool-context-strip.tsx:11-14`). The one place that explains the model — "drag it on the artboard to place it… Undo (Cmd-Z) reverts any add" — lives inside the Annotate panel (`panels/annotations-panel.tsx:56-59`), which the rail user has not necessarily opened. Failure: user clicks the asterisk icon, sees no pressed state and (on a figure with an explicit y-range, where a bracket placed at yMax+12% falls outside the plotted range) nothing on the artboard, concludes the button is dead and clicks again — `addSigBracketOps` recomputes the SAME default geometry (`lib/figure/annotations.ts:186-206`), so two pixel-identical brackets are now stacked in `layout.shapes` and both export.
- **Fix:** Make the rail's action honest: prefix the labels/titles with the verb (`Add significance bracket` / `Add text label` / `Add arrow or callout`) and stop styling the group as a mode selector — drop `aria-pressed` on the inert Select button (it has no `onClick`) or give it a real role as a hint-only indicator. Update `tool-context-strip.tsx` to state the slice-5 model in one line ("Draw tools drop an annotation on the figure — drag it to place it, Cmd-Z to undo"), ideally switching copy on the last-used tool. Note the rename touches `e2e/annotation-layer.spec.ts:52,61,62`, which match rail names with `exact: true`.
- **Status:** OPEN

### B12. [MEDIUM · D-discoverable] New Annotate-panel buttons keep the browser's arrow cursor — no pointer affordance, unlike every peer control in the app

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/panels/annotations-panel.tsx:90`
- **What:** D-check breached: a clickable control needs a cursor affordance. A bare `<button>` computes `cursor: default` in Chrome (verified in headless Chrome: plain button → `default`, explicit → `pointer`), and there is no global override — `app/globals.css` has zero `cursor` rules. The codebase compensates explicitly everywhere: `components/ui/button.tsx:7`, `components/ui/tabs.tsx:31`, `ui/select.tsx`, `ui/switch.tsx`, `panels/marks-panel.tsx:136`, `shell/tools-rail.tsx:70`, and even `IconButton` inside this same new file (line 248). The two new raw-button surfaces here were missed: the three primary "Add annotation" rows (`AddButton`, line 90) and the four significance-tier buttons (`StarPicker`, line 178). Failure: the panel's headline action rows hover-highlight but keep the text/arrow cursor, so they read as decorative list items rather than buttons — the exact S5 "toggled but read as dead" miss, and visibly inconsistent with the pointer cursor on the Tabs above them and the eye/trash icons in the same row.
- **Fix:** Add `cursor-pointer` to the `AddButton` className (line 90) and to the `StarPicker` tier button base classes (line 178) — matching `IconButton` at line 248. Also give the SELECTED tier button a hover response (only the unselected branch has `hover:bg-accent/50` at line 181) so re-picking the active tier isn't inert-looking.
- **Status:** OPEN

### B13. [MEDIUM · A-attributable] Palette strip asserts the figure's colourway by colour alone, hardcoded and aria-hidden

- **Where:** `app/frontend/components/figure/shell/palette-strip.tsx:13-24`
- **What:** ATTRIBUTABLE — "attribution carried by colour with no glyph + text/tooltip (colourblind/SR unsafe)" — breached, and the claim is also wrong. The new permanent bottom region of the editor renders `COLORWAYS.okabeito.colors` unconditionally (palette-strip.tsx:15), ignoring `spec.layout.colorway`. `COLORWAYS` has five entries (lib/figure/figure-spec.ts:30-51) and the Style tab writes `/layout/colorway` on swap (components/figure/panels/style-panel.tsx:610), so as soon as the user picks Viridis/Grayscale/Tableau — or a journal style overrides it — the strip is showing a palette that is NOT this figure's, presented as if it were. The swatch row carries no name and no per-swatch title, and the whole row is `aria-hidden` (palette-strip.tsx:14), so the only accessible text is "Palette board — coming soon": a sighted user gets a colour-only (possibly false) statement about figure state, a screen-reader/colourblind user gets nothing at all. The correct pattern is three lines away: style-panel.tsx:599-627 derives `colorwayKey` from the spec, names it via the SelectField label ("Okabe–Ito (CB-safe)"), and gives every swatch a `title={c}`.
- **Fix:** Derive from the spec instead of hardcoding: read `/layout/colorway`, match it against `COLORWAYS` exactly as style-panel.tsx:599-601 does, render those swatches, and add the resolved label as visible text ("Okabe–Ito (CB-safe) · palette board coming soon") plus `title={c}` per swatch; drop the blanket `aria-hidden` in favour of `role="img"` + `aria-label={`Current palette: ${label}`}`. If deriving is out of scope for the placeholder, remove the swatches entirely and keep only the "coming soon" line — an inert row is better than a confident wrong claim about the figure.
- **Status:** OPEN

### B14. [MEDIUM · A-attributable] Cloud-import provenance is stamped server-side but dropped by the FE — an imported dataset looks hand-dropped

- **Where:** `app/frontend/lib/cloud/api.ts:37 (`return fromApiDataset(row)`) + app/frontend/lib/projects/sync.ts:28-39`
- **What:** ATTRIBUTABLE — the artifact must say where it came from — breached inside the same slice that created the record. The backend half of 8fb2bad deliberately adds import provenance: alembic 0005_datasets_source + app/backend/db/schema.py:154 add `datasets.source`, routers/cloud.py:119 stamps `{provider, ref, fetched_at}` after the stream, and `_dataset_public` returns it on EVERY dataset row (app/backend/uploads/repo.py:65 `"source": row.source`). The FE mapper ignores it — `fromApiDataset` (lib/projects/sync.ts:29-38) maps id/filename/label/modality/sha/uploaded/qc/created_at and never reads `r.source`, and the FE `Dataset` type (lib/projects/types.ts:78-118) has no `source` field — so `importFromCloud` returns a dataset stripped of its origin, and `onCloudImported` (components/project/data-panel.tsx:252-257) adds it to the list where it is pixel-identical to a locally dropped file. The user (and any reviewer reading Publish confidence) cannot tell that these bytes came from `s3://…` at a given time rather than off the operator's disk — for a reproducibility product the origin URI is the reproducible link, and it is currently recorded in Postgres and shown nowhere.
- **Fix:** Carry the field through and show it: add `source?: { provider: string; ref: string; fetchedAt?: string }` to `Dataset`, map `r.source` in `fromApiDataset` (so it also survives a reconcile), and render an origin chip on the dataset row / profile — a glyph + text (e.g. `Link2` + "URL / S3 · s3://bucket/key") with the full ref and `fetched_at` in the accessible name, not only a mouse `title`. Locally dropped files keep no chip (absence of `source` = local), which is the honest default.
- **Status:** OPEN

### B15. [MEDIUM · R-recorded] Cloud import fabricates a File stand-in that becomes lastFile and can be POSTed as the run's actual data

- **Where:** `/home/deploy/work/selom/app/frontend/components/project/data-panel.tsx:256`
- **What:** R-check breached: a run must be recorded against the input it actually computed on. `onCloudImported` synthesizes `new File(["remote"], added.filename)` as the active file. `IntakeQuestionnaire.onSubmit` passes `file: active.file` to `onAnalyze` (data-panel.tsx:481), and project-workspace.tsx:361 does `setLastFile(file)` — so a 6-byte placeholder becomes the session's run payload. `runFlow` normally avoids it (`useDataset = !step.aiActions?.length && !designFile && !!dataset?.uploaded`, and a cloud import returns `uploaded:true`), but two reachable branches flip `useDataset` false: (a) the questionnaire's time-course choice synthesizes a `designFile` (data-panel.tsx:495-497), and (b) an accepted AI action rides on the proposal → `step.aiActions`. Either then calls `resolveRunFile` (use-figure-run.ts:115) which returns the fabricated file — not null — so the deliberate real-backend guard (`setNeedData` → re-upload prompt) never fires, and `runSkill(...)`/`applyAiActions(...)` POST the ASCII bytes "remote" as the user's data. Best case the parse fails and the user sees a generic "Run failed" for a dataset whose bytes ARE server-side; worst case the skill produces a figure that `addFigure` persists with `datasetId` = the cloud dataset and `provenance.params` for the real run — a recorded run that its own record cannot reproduce. The pre-existing `new File(["mock"], …)` at data-panel.tsx:247 has the identical shape and should be fixed in the same move.
- **Fix:** Widen `AnalyzeArgs.file` to `File | null` (data-panel.tsx:51-57) and pass `file: active.real ? active.file : null` at both call sites (:481, :499), so `resolveRunFile`'s null path fires the intended `needData` prompt against a real backend. Better still, keep `Active.file` `File | null` and never fabricate bytes at all — a cloud-imported dataset should force the dataset_id run path (make the design-sheet and AI-action branches accept `dataset_id` server-side rather than falling back to multipart).
- **Status:** OPEN

### B16. [MEDIUM · R-recorded] Backend-stamped cloud-import provenance (datasets.source) is dropped by the FE dataset mapper

- **Where:** `/home/deploy/work/selom/app/frontend/lib/projects/sync.ts:28-39`
- **What:** R-check breached: the recorded origin of an input is not reachable in the product, so no committed figure can state where its data came from. 8fb2bad deliberately records import provenance server-side — `db/schema.py:154` `source` JSON `{provider, ref, fetched_at}`, written at `routers/cloud.py:119` (`repo.set_source`) and returned on EVERY dataset row by `uploads/repo.py:65` `_dataset_public`. `fromApiDataset` maps id/projectId/filename/label/modality/currentSha256/uploaded/qc/createdAt and silently discards `source`; `Dataset` (lib/projects/types.ts) has no field for it. Failure scenario: user imports counts.csv from `https://…` or `s3://…` (no local file ever existed), runs DEG, and exports methods — nothing in the dataset card, provenance surfaces, or methods text can say the data came from that URL, even though the server holds it. For a URL/S3 import that `source.ref` is the ONLY record of the input, and it is the exact fact a reader needs to reproduce the figure.
- **Fix:** Add `source?: { provider: string; ref: string; fetchedAt?: string }` to `Dataset`, map `r.source` in `fromApiDataset`, confirm `mergeDatasets` preserves it on reconcile (server-authoritative field, so server-wins is fine), and surface it where the input is described — the dataset card in data-panel and the figure's provenance/methods block — as "Imported from <ref>".
- **Status:** OPEN

### B17. [MEDIUM · R-recorded] Volcano style apply overwrites a committed significance bracket's stroke, and the FE commits the rewritten spec

- **Where:** `/home/deploy/work/selom/app/backend/skills/theme.py:131-133 (committed by /home/deploy/work/selom/app/frontend/components/figure/style-picker.tsx:52-59)`
- **What:** R-check breached: an unrelated action rewrites committed annotation state in the persisted record. `_style_volcano` loops EVERY `layout.shapes` entry and hard-assigns `sh["line"] = dict(color="#b3bcc6", width=1, dash="dash")` plus `opacity=0.9` — written for the volcano's threshold guides, with no knowledge of the new tagged annotation shapes. Failure scenario: on a volcano the user adds a significance bracket (solid INK `#0f172a`, width 1.4 per annotations.ts `addSigBracketOps`), then picks a journal style. `applyStyle` (lib/figure/styles.ts:26) POSTs to `/figures/style/apply` → `theme.render` → the returned spec has the bracket restyled as a dashed grey 1px line, and style-picker.tsx:59 commits that spec as ONE undoable edit → the debounced PATCH persists it. The recorded bracket now renders (and exports, `validate=False` keeps the tag harmless) as a dashed grey threshold guide — a different statistical claim than the one the user committed. The `selom` tag survives `copy.deepcopy`, so the item still lists in the Annotate panel, hiding the drift.
- **Fix:** In `_style_volcano`, skip Selom-owned shapes: `for sh in ... : if sh.get("selom"): continue` (or restyle only threshold shapes — those the volcano primitive emitted). Add a backend test asserting a `selom`-tagged path shape passes through `theme.apply` byte-identical, so the annotation layer is protected against every future style pack.
- **Status:** OPEN

### B18. [MEDIUM · N-nonbreaking] Tools rail gained 3 live draw tools, but its co-located state row (ToolContextStrip) and the pressed-state vocabulary were not updated

- **Where:** `app/frontend/components/figure/shell/tool-context-strip.tsx:11 · app/frontend/components/figure/shell/tools-rail.tsx:62,70`
- **What:** N breach — a control added without co-located state, so the row loses meaning. The ToolContextStrip sits 30px above the rail (measured y:414 h:30, directly over the rail row at y:444) and is documented as "a muted hint line describing what the active tool does". Slice 5 added three LIVE tools to the rail but left the strip a hard-coded string: "Select tool — click a trace to focus its series; drag legends, labels, titles, and colour bars on the artboard to reposition them." It never mentions that a rail click immediately commits an annotation, nor that the item lands at a fixed default spot (which, as measured, can be an 11px staple in the plot corner on a scatter figure, or an exact duplicate — see the stacking finding), nor that Cmd-Z reverts it. Second half: the active-mode token is `bg-accent/60` on Select (tools-rail.tsx:69) while an enabled draw tool gets `hover:bg-accent` (:70) — a MORE saturated fill than the "active" tool, so hovering Significance bracket / Text label / Arrow reads as if it had become the active mode while the strip still says "Select tool" and Select keeps `aria-pressed` (:62). Before slice 5 these buttons were disabled with `opacity-40` and no hover style, so this confusable state is new. Visible in the rendered capture after two Text-label clicks: the T button carries an accent fill, Select keeps its pressed fill, the strip still describes Select only.
- **Fix:** Two surgical changes. (1) Make the strip a function of the rail rather than a constant: lift a `lastTool` (or `mode`) value into CanvasShell and pass it to ToolContextStrip, or minimally extend the copy with the missing clause — "Draw tools drop an item at a default spot on the artboard — drag it to place it, edit it in Annotate, Cmd-Z undoes." (2) Stop letting a one-shot action out-shout the mode: give the draw buttons a distinct hover token (e.g. `hover:bg-accent/30 hover:text-foreground`) so `bg-accent/60` stays unique to the pressed Select, and give a draw click a transient acknowledgement (brief ring / "Added" in the context strip) so the click is legible even when the new item lands off-view.
- **Status:** OPEN

### B19. [LOW · V-visible] Frozen-figure draw tools are visually and textually indistinguishable from not-yet-built tools

- **Where:** `app/frontend/components/figure/shell/tools-rail.tsx:63`
- **What:** V-check breached: a disabled state whose cause is not visible at the control.

`canvas-shell.tsx:62` passes `store={readOnly ? undefined : store}`, so on a frozen "paper" version the three draw tools take the `!enabled` branch. Their tooltip is then `title={enabled ? label : kind === "soon" ? "Arrives in a later update" : label}` — i.e. just `"Significance bracket"`, no reason — and their className falls to the same `"text-muted-foreground opacity-40"` used by the genuinely-unbuilt Shape/Guide placeholders (tools-rail.tsx:71). Two different states (locked-by-freeze vs not-built) render byte-identically, so the rail cannot tell the user why the tool is dead. The freeze itself is signalled only far away, in the command bar (figure-view.tsx:131-133).
- **Fix:** Distinguish the two reasons at the control: pass an explicit `readOnly` prop to `ToolsRail` (alongside/instead of the `store`-absence signal) and set `title="Frozen version — Edit a copy to annotate"` for draw tools in that state, plus the `Lock` glyph or `text-stage-figure` tint already used by the frozen vocabulary (inspector-dock.tsx:59, figure-view.tsx:131), keeping plain opacity-40 + "Arrives in a later update" for `kind === "soon"`.
- **Status:** OPEN

### B20. [LOW · D-discoverable] A hidden annotation's controls are dimmed to the app's disabled opacity while still fully live

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/panels/annotations-panel.tsx:169`
- **What:** D-check breached: state must not be ambiguous. When an annotation is hidden, the star-tier group gets `opacity-50` (line 169) and the text field gets `line-through` + `text-muted-foreground/60` (line 222) — but both remain fully interactive and still commit patches. `opacity-50` is this app's disabled signature (`ui/button.tsx:7` `disabled:opacity-50`, `ui/tabs.tsx:32` `disabled:opacity-50`), and `line-through` reads as "deleted", not "hidden". Failure: a user hides a bracket, sees the tier buttons go half-transparent, assumes they're locked until it's shown again, and never discovers the stars are still editable while hidden.
- **Fix:** Keep a hidden row's controls at full opacity and carry the state in an explicit, non-disabled-looking marker instead — e.g. a "Hidden" TintChip next to `item.label` (line 116) plus the already-toggled EyeOff icon. If the dimming stays, pair it with `title="Hidden on the figure — still editable"` on the group so the meaning isn't guessed from opacity alone.
- **Status:** OPEN

### B21. [LOW · D-discoverable] Annotation rows of the same kind are indistinguishable — identical titles on Hide/Remove give no clue which figure item they act on

- **Where:** `/home/deploy/work/selom/app/frontend/components/figure/panels/annotations-panel.tsx:116-133`
- **What:** D-check breached: a control's label must identify its target. Every row is headed by `item.label`, which is a per-KIND constant (`lib/figure/annotations.ts:36-40`: "Significance" / "Text label" / "Arrow / callout"), and the icon buttons take their `aria-label`/`title` from it (lines 122, 128). Add three brackets and the panel shows three rows all headed "SIGNIFICANCE", each with a tooltip "Remove Significance" and no on-canvas linkage (no hover highlight, no index). The peer surface numbers them — `panels/marks-panel.tsx:132` uses "Hide annotation {index + 1}". Failure: with two brackets on a figure, the user cannot tell which row is the one they want to delete and has to remove-and-undo to find out.
- **Fix:** Number the rows per kind (`Significance 1`, `Significance 2`) — `selomAnnotations` already returns first-seen order — and flow the number into the icon-button labels. Cheap upgrade: on row hover, briefly emphasise the matching item (e.g. commit-free Plotly `relayout` of that shape's line width, or an outline) so the row↔figure mapping is visible.
- **Status:** OPEN

### B22. [LOW · D-discoverable] Cloud-import "Import" button becomes an unlabelled spinner while busy

- **Where:** `/home/deploy/work/selom/app/frontend/components/intake/cloud-import-menu.tsx:131-133`
- **What:** D-check breached: an icon-only control needs a label. During the request the button's entire content is swapped for `<Loader2 className="animate-spin"/>` (line 132), so it becomes a nameless icon-only control — no text, no `aria-label`, no `title` — for the whole duration of a streaming import (which for an S3 object can be many seconds). The peer surface in the same range keeps its word: `export-menu.tsx:220-221` renders `{busy ? "Rendering…" : …}` alongside the spinner. Failure: mid-import the primary action has no accessible name and, visually, no indication of what is spinning; a screen-reader user tabbing to it hears an unlabelled button.
- **Fix:** Keep the verb next to the spinner (`{busy ? <><Loader2 className="size-4 animate-spin"/>Importing…</> : "Import"}`), or at minimum add `aria-label="Import"` / `title="Importing…"` plus `aria-busy={busy}` on the Button.
- **Status:** OPEN

### B23. [LOW · A-attributable] Annotate and Marks split annotations by author, but neither list names the author

- **Where:** `app/frontend/components/figure/panels/annotations-panel.tsx:63 + app/frontend/components/figure/panels/marks-panel.tsx:83`
- **What:** ATTRIBUTABLE — the authorship distinction is real but invisible. This diff makes the two annotation lists mutually exclusive by AUTHOR: Annotate lists `selomAnnotations(spec)` (items the operator drew) under "On the figure · N", while Marks now lists `nonSelomAnnotationItems(spec)` (skill-emitted unit labels, condition labels, threshold callouts) under "Annotations · N" (property-panel.tsx:40 gates the Marks tab on the same predicate). The authorship is stated only in code comments (annotations-panel.tsx:22-28, marks-panel.tsx:36-38) — on screen the split is carried purely by which tab an item happens to sit in. So a user cannot tell which annotations are the analysis's own output (editing a skill-emitted "p < 0.05" callout silently rewrites what the run claimed) versus their own additions, and someone who drew a text label and later looks in Marks — where annotations have always lived — sees it missing with no explanation.
- **Fix:** Put the authorship in the section titles and one hint line each: Annotate → "Added by you · N" (keep the existing "exports with the figure" note), Marks → "From the analysis · N" with "Emitted by <skill name> — edit only to fix wording". One string change per panel makes the authorship textual instead of positional, and it sets up the manual-override marker the hand-set stars finding needs.
- **Status:** OPEN

### B24. [LOW · N-nonbreaking] The always-present Annotate tab makes the inspector tab strip a 7-column grid on any figure with Marks; "Annotate" is wider than its column

- **Where:** `app/frontend/components/figure/property-panel.tsx:72-76,86-96`
- **What:** N breach (crowding) — a new label added to a fixed-width right dock squeezes its neighbours' column. The strip is an equal-column grid (`gridTemplateColumns: repeat(${tabs.length}, minmax(0, 1fr))`) inside the 330px InspectorDock, and Annotate is appended unconditionally, so figures with a scalebar or skill-emitted labels (hasMarks) now render 7 columns instead of 6. Measured in Chrome at 1440×900 (real Geist metrics, 297px of usable strip): 6 tabs → trigger 47.8px vs the 42.8px "Annotate" label (2.5px slack per side); forced to the 7-column case → trigger 40.7px, so the 42.8px label spills 1.1px past its own active-state pill on each side and the label-to-label gap falls from 16.4px to 9.5px (Annotate→Page) and 10.2px (Data→Annotate). No literal label collision, so this is a squeeze/polish defect rather than a break — but the active tab's white pill ends up narrower than its own text.
- **Fix:** Stop squeezing at 7: either wrap instead of compress (`grid-template-columns: repeat(4, minmax(0,1fr))` with two rows, `h-auto` already allows it), or drop the label to icon-only + `title`/`aria-label` past 6 tabs, or shorten the label to "Notes" (33px, fits at 40.7px). Cheapest correct option is the 2-row wrap — it keeps every label readable on both the 6- and 7-tab figures.
- **Status:** OPEN


## C. fe-review G1 — user-task walkthrough: 31 of 35 jobs have NO affordance

The Pillar-2 annotation/drawing layer as shipped. A gap means a user cannot do the thing at all (or cannot tell that they did).

- `GAP ` **Add a significance bracket over the two groups I'm actually comparing** — Annotate panel "Significance bracket" + tools-rail asterisk (both call addSigBracketOps(spec) with no options)
    - Geometry is hardcoded to the first two x positions (lib/figure/annotations.ts:187-227) — no group pickers. AddSigBracketOptions.geometry/p exist and are unit-tested but no UI passes them. Plotly's shapePosition drags a `path` shape as a whole and gives it no resize handles, so the span can never be re-fitted to groups 3-4.
- `GAP ` **Move a bracket and its stars together** — — (drag each half separately on the artboard)
    - A bracket is two independent layout items sharing an id. annotationVisibilityOps/removeAnnotationOps act on both halves, but position does not: dragging the bracket path leaves the stars behind and vice versa. No group-drag, no re-derive of the star x from the shape path.
- `GAP ` **See the bracket I just added on a bar/box chart** — — (no post-add confirmation of any kind)
    - The shared bar/box helper sets an explicit yaxis.range=[0, y_top*1.08] where y_top includes error bars + points (app/backend/skills/_charts.py:312-315), while bracketGeometry places the bracket at (mean-only) yMax+12% — so the add lands either on top of the error bars or above the fixed range (clipped, nothing appears). Nothing selects/flashes/counts the new item, so "invisible" and "didn't work" are indistinguishable.
- `GAP ` **Put a bracket carrying a real p-value on the chart, not typed stars** — Figure-data params "Significance brackets" + "Significance test" (lib/catalog/params.ts:374-385) → staged re-run
    - The computed, provenance-recorded path already exists server-side (sig_brackets in _charts.py), but the new Annotate tool/rail is the discoverable affordance and only produces hand-typed stars. starsForP() and AddSigBracketOptions.p are built but unreachable from the UI, and the panel never points at Figure-data.
- `GAP ` **Tell whether the stars on this figure were computed or typed by hand** — — 
    - Hand-typed stars are a cosmetic client patch only — provenance.actions[] is untouched and no marker distinguishes them on the figure. Worse, computed brackets are untagged so they list under Marks while hand-added ones list under Annotate: same visual object, two tabs, no way to audit. The Marks text editor also lets a computed "*" be silently retyped to "***".
- `GAP ` **Type or edit a label's text where it sits on the figure** — Annotate-panel row inputs (StarPicker / TextEditor, commit on blur/Enter)
    - In-place editing is off: `annotationText: false` in the figure-canvas edits config (components/figure/figure-canvas.tsx:445, set for the gene-label identity model) disables Plotly's double-click text edit for every annotation, including the new free text labels. Editing is right-dock-only, 360px away from the item.
- `GAP ` **Tell which panel row is which item on the canvas (two text labels, three brackets)** — — (rows are labelled only by kind: "Text label", "Significance")
    - No hover-highlight, no click-row-to-select-on-canvas, and no canvas→row focus: Selom items don't set captureevents, so a click on the annotation falls through to onSelectTrace, which yanks the inspector to the Data tab and away from Annotate.
- `GAP ` **Place several annotations without them landing on top of each other** — — 
    - Every add uses fixed defaults (text label paper 0.02/0.98, arrow paper 0.5/0.5, bracket the same derived geometry). No cascade offset and no click-to-place, so two adds are pixel-identical and read as one item.
- `GAP ` **Place an annotation where I click on the figure** — — (rail draw tools commit immediately)
    - ToolsRail draw buttons are one-shot commits, not armed modes (onClick → store.commit). aria-pressed is hardcoded on Select and ToolContextStrip always describes Select, so nothing reflects an active tool — the Illustrator expectation (pick tool → click canvas) silently fails.
- `GAP ` **Style an annotation — colour, size, arrow head, line weight** — — 
    - INK "#0f172a" and font sizes 15/16/12 are hardcoded in lib/figure/annotations.ts with no controls. The items also don't follow the active journal-style stamp, so switching style leaves hand annotations visually off-system.
- `GAP ` **Duplicate an annotation (same bracket over the next pair)** — — 
    - Add-again is the only path, and it re-lands at the same default geometry, so every repeat is a fresh re-drag.
- `GAP ` **Delete or nudge the selected annotation from the keyboard** — Per-row trash button in the Annotate panel
    - No canvas selection model → no Delete/Backspace, no arrow-key nudge. Precision placement is drag-only, on paper coords, with no numeric x/y fields.
- `GAP ` **Keep my brackets and panel letters when I re-run with fresh data** — — 
    - rerunFigure (components/project/hooks/use-figure-run.ts:199-225) builds the new version from the fresh backend spec and carries ONLY gene labels (captureCarryLabels/carryLabels). Every Selom annotation is silently dropped, with no warning on the Re-run button or StaleBadge and no "copy annotations from the previous version".
- `GAP ` **Add a free text label naming a gene on a volcano** — Annotate panel "Text label" — but it collides with the gene-label system
    - lib/volcano/labels.ts matches annotations by `text` and ignores the `selom` tag: a label reading "RPGRIP1" makes the Statistics-table Label toggle report that gene as already labelled, and clicking the gene's point runs toggleLabelOps → removes the user's annotation instead of adding a gene label. captureLabels/carryLabels then re-anchor it to data coords while it keeps xref:"paper", flinging it off-canvas on the next re-run.
- `ok  ` **Undo a mis-clicked add** — Command-bar undo/redo + global Cmd-Z (components/project/project-workspace.tsx:251-263)
    - One commit per add, so both halves of a bracket revert together; Cmd-Z is correctly ignored while focus is in a panel input.
- `ok  ` **Hide an annotation without deleting it** — Per-row Eye/EyeOff, toggling both bracket halves
- `GAP ` **Draw a box/highlight, or snap to an alignment guide** — Shape / Guide rail buttons, disabled with "Arrives in a later update"
    - Declared-but-inert; no other path to a highlight box, which is the second most common Illustrator round-trip after brackets.
- `GAP ` **Recolour the active series by clicking a swatch** — PaletteStrip is a read-only preview labelled "Palette board — coming soon"; the real swap is the Style tab colourway
    - The strip is inert but permanently consumes bottom chrome in a height-constrained editor — it costs artboard height and returns nothing.
- `GAP ` **See the whole figure while editing** — — 
    - ArtboardHost pins the white card to height: min(74vh,720px) (components/figure/shell/artboard-host.tsx:63) while the shell added ~150px of new fixed chrome (command bar + tool-context strip + palette strip) inside the same min-h-0 main. At 1280x800 the card overflows its gutter and the bottom of the figure is clipped, reachable only by scrolling inside the artboard region.
- `GAP ` **Give the figure more room — collapse the inspector or rails, focus mode** — — 
    - ArtboardHost's own comment assumes collapsible rails ("collapsing the side rails gives the figure more room") but the shell ships no collapse/expand control: rail w-12, dock w-[330px] (also off the 360px dock token), both strips fixed.
- `GAP ` **Zoom or fit the artboard to the window** — — (Plotly modebar zoom acts on the axes, not the artboard)
    - The shell adopts the Inkscape four-region convention, where a zoom %/fit-to-window control is expected in the top strip; the strip is a static hint line instead.
- `GAP ` **Know what the active tool does** — ToolContextStrip
    - Static text — always the Select description, even right after clicking a draw tool. It fills the region rather than reporting state.
- `GAP ` **Reach the drawing tools from the keyboard** — role="toolbar" + aria-orientation on the rail
    - No roving tabindex/arrow-key navigation, so all six buttons are tab stops; the two disabled tools explain themselves via `title` only.
- `ok  ` **Click a trace on the artboard to inspect that series** — ArtboardHost onSelectTrace → PropertyPanel focuses the series and switches to Data (nonce re-fires on re-click)
    - Works, but it is the only thing a canvas click can do — which is why it hijacks the Annotate tab (see the row-identification gap).
- `ok  ` **Import a dataset from a link or an S3 URI** — CloudImportMenu URL/S3 field + Import (throws a real ApiError message inline)
- `GAP ` **Tell where a dataset came from — dropped file vs URL vs bucket** — — 
    - The backend stamps datasets.source (app/backend/routers/cloud.py:119) and the repo returns it (uploads/repo.py:65), but fromApiDataset (app/frontend/lib/projects/sync.ts:28-38) never maps `source` and the FE Dataset type has no field. The list row shows filename + dims only, so a remote import is indistinguishable from an upload and the URL is never shown again — a provenance hole on the data surface.
- `GAP ` **See progress, or cancel, a large remote import** — Spinner on the Import button only
    - No bytes/percent and no cancel for a server-side stream that can run to the tenant's storage cap. The outside-mousedown handler closes the popover while the request continues, and nothing outside the menu reports in-flight state or a later failure (the "Uploading your data…" status covers only the local drop path).
- `GAP ` **Know which cloud sources actually work before clicking** — Drive / OneDrive / Dropbox rows with live-looking "Connect" buttons
    - connectProvider throws "coming soon" only after the click; the registry already carries comingSoon: true and the UI ignores it. The export-menu scaffold in the same commit does this correctly (disabled + a visible "Coming soon" chip), so the two scaffolds contradict each other.
- `GAP ` **Recover when an imported file can't be parsed** — — 
    - The endpoint falls back to the unparsed row (no qc) on a parse error; onCloudImported opens it with real:false → DataTypeStrip canOverride=false and the right pane renders a permanent "Inspecting…" status (components/project/data-panel.tsx:445-455). No error, no retry, no manual data-type set, no re-import.
- `GAP ` **Import several remote files and combine them into one cohort** — — (single `ref` per import)
    - The local dropzone's multi-file combine has no cloud peer, so a cohort that lives in a bucket has to be pulled one file at a time and can't be combined.
- `GAP ` **Re-pull a remote file when the source changes** — — 
    - source/fetched_at are recorded server-side but there is no re-import/refresh action, so remote-origin datasets never enter the staleness → re-run loop that a re-uploaded file does.
- `GAP ` **Send the finished figure to my cloud drive** — Export-menu "Export to cloud" button — hard-disabled, "Coming soon"
    - Mislabelled seam: the wired endpoint behind it (exportToCloud / POST /export/cloud) takes a datasetId + destination and cannot export a figure at all, so the button as placed in the FIGURE export popover promises something the backend doesn't do.
- `GAP ` **Assemble my per-sample 10x matrices into one multi-sample dataset** — — 
    - 11a115f is backend-only (POST /data/assemble-scrna); a grep for assemble-scrna across app/frontend returns nothing. The capability is shipped and unreachable from the UI.
- `GAP ` **Drop several 10x files and have Selom combine them** — Multi-file dropzone → combineData → /api/data/combine (ERG-only)
    - Actively misroutes now that assembly exists: several 10x matrices fail and the user is told "they need to be ERG recordings (.iwxdata / Diagnosys / a canonical waveform table)". The dropzone `accept` list also excludes .h5/.mtx/.gz, so the files can't even be selected.
- `GAP ` **Confirm or fix the design parsed from my filenames (sample_id / line / condition)** — — 
    - Filename→obs materialization is the whole value of the assemble endpoint and there is no surface to review or correct it; the intake questionnaire's design confirm-card knows nothing about it, so a mis-parsed condition would flow silently into DE.

## D. fe-review G2 — rendered-in-context

- Live server reachable during the review: **False** — the layout findings below are arithmetic-backed predictions from the CSS, NOT observed renders. Re-verify in the running app before closing them.

- **Inspector dock tab strip (Style · Axes · Legend · Data · Annotate · Marks · Page)** (330px dock → 40.7px per tab track) — verified=False
    - HIGHEST-RISK, arithmetic-backed cramping prediction. property-panel.tsx:86-96 renders `gridTemplateColumns: repeat(tabs.length, minmax(0,1fr))`; tabs.length went 6→7 (Annotate added, always present). Chain: InspectorDock w-[330px] incl. border-l → 329 content − px-3 (24) = 305 − TabsList p-1 (8) = 297 − gap-0.5 × 6 (12) = 285 / 7 = 40.7px per track; TabsTrigger carries px-2.5 (20px) from ui/tabs.tsx:31 → ~20.7px for the label. Labels are `text-[10px] leading-none` with `whitespace-nowrap` and NO truncate/overflow-hidden → "Annotate" (~43px), "Legend" (~32px), "Marks" (~29px) all overflow their track and will visually collide with neighbours. The 6-tab case (hasMarks=false) is 47.8px/track → 27.8px content — "Annotate" still overflows. Check BOTH states (a figure with a scalebar or skill-emitted labels = 7 tabs; a plain volcano/UMAP = 6).
- **Annotate panel body (new AnnotationsPanel: 3 AddButton rows, annotation rows, StarPicker, TextEditor)** (330px dock → 297px content (329 − ScrollArea inner p-4 = 32)) — verified=False
    - AddButton (annotations-panel.tsx:86-100): icon 32 + gap 12 → ~253px for the two-line label; the hint span IS `truncate` — confirm "Bracket + stars over two groups" / "Panel letter, gene name, or note" / "Pointer or leader line" aren't clipped mid-word at 11px. StarPicker (:169-187): `flex flex-wrap gap-1` with STAR_TIERS buttons at `min-w-9 flex-1` — confirm the tier row doesn't wrap to a ragged second line inside 253px (row is nested in a p-1.5 card). AnnotationRow header (:115-134): uppercase 11px label + 2 × size-7 IconButtons at ml-auto — confirm a long item label doesn't push the Hide/Remove buttons out. TextEditor is a transparent-bordered Input (h-8) — confirm the focus-visible border actually reads as an editable field (it looks like static text at rest).
- **ExportMenu popover + new disabled "Export to cloud" row** (288px (w-72), anchored `absolute right-0 mt-2` — NOW INSIDE CanvasShell's overflow-hidden box) — verified=False
    - NEW clipping exposure from 3db8f0b. Before: figure-view's toolbar was a SIBLING above `<div class="... overflow-hidden rounded-xl">`, so the popover escaped. After: the command cluster moved into CommandBar INSIDE canvas-shell.tsx:57 `flex min-h-[520px] ... overflow-hidden` → the absolutely-positioned popover is now clipped by that box with no scroll. My estimate is ~318px of popover in a ≥520px shell starting at y≈54-95, so it probably fits — but this MUST be eyeballed, because the clipped region would be the primary `Export PNG/SVG/PDF` button and the new cloud row. Check with the command cluster WRAPPED (1280 width pushes the ml-auto action group to a second row) and with the VersionBar present, which is the worst case. Also confirm the row's 3-part layout (Cloud 14px + "Export to cloud" 12px + "Coming soon" 11px at ml-auto ≈ 206px of 264px content) reads as intentionally-disabled, not broken.
- **CanvasShell four-region frame (CommandBar · ToolContextStrip · ToolsRail · ArtboardHost · InspectorDock · PaletteStrip)** (944px @1280 / 1104px @1440 (viewport − 56 page px-7 − 256 workrail w-64 − 24 gap-6); 1152 / 1312 with the rail collapsed to w-12) — verified=False
    - Whole-shell composition check. CommandBar (px-3) gives the cluster 918px @1280 / 1078px @1440 — confirm how many rows the toolbar wraps into, because CommandBar is `shrink-0` and every wrapped row steals height from the artboard inside a min-h-[520px] box. ToolContextStrip's one-line hint is ~130 chars at 11px (~715px) — fits one line at 918px, confirm it doesn't wrap to two and eat another ~16px. PaletteStrip (8 Okabe–Ito swatches + "Palette board — coming soon") is tiny/low-risk but confirm it doesn't read as dead space. Two placeholders (ToolContextStrip, PaletteStrip) plus two disabled rail tools plus a disabled "Export to cloud" row now stack in one screen — confirm the shell doesn't read as mostly-unbuilt.
- **ArtboardHost / the figure artboard (the hero)** (564px region @1280 · 724px @1440 · 932px @1440 rail-collapsed → plot ≈ 506 / 666 / 874px) — verified=False
    - The tools rail took 48px and the gutter tightened p-6→p-3 lg:p-4 to pay for it (artboard-host.tsx:41-44 documents exactly this trade). Net vs the old EditorWorkspace is ≈ −24px at 1280, so roughly neutral — but CONFIRM the figure is still legible at 1280 (axis tick labels, legend, colour bar at ~506px of plot width). Also confirm the fixed-width branch: `fixed ? undefined : {width:100%, maxWidth:88rem, height:min(74vh,720px)}` — a spec with a numeric layout.width (ERG trace grid, multi-panel) gets NO constraint and must scroll inside the `overflow-auto` region rather than blowing out the shell.
- **ToolsRail (Select · Significance bracket · Text label · Arrow/callout · Shape · Guide)** (48px (w-12), 6 × size-9 (36px) buttons) — verified=False
    - Fits by arithmetic. Confirm the disabled Shape/Guide at `opacity-40` are visibly distinct from the enabled draw tools without reading as rendering failures, that Select's `bg-accent/60 aria-pressed` reads as the active mode, and that the icon-only rail is decipherable (title attr only — no visible labels). Also check read-only: figure-view passes `readOnly={frozen}` → canvas-shell.tsx:62 passes NO store → all three draw tools disable; confirm that state doesn't look identical to the never-coming Shape/Guide.
- **Marks tab visibility change (nonSelomAnnotationItems)** (330px dock) — verified=False
    - property-panel.tsx:40 now computes hasMarks from `nonSelomAnnotationItems(spec)`. Render-check the transition: on a figure with NO scalebar and NO skill labels, add a significance bracket via the rail → the tab count must STAY at 6 (Marks must not appear) and the new item must appear ONLY in Annotate, never double-listed in Marks. Then verify the reverse on an ERG/microscopy figure that does have a scalebar (7 tabs). This is the one place where a wrong result changes the tab-strip width class from the previous surface.
- **CloudImportMenu trigger + popover in the Data stage** (trigger in a 462px grid column @1280 (main 944 − gap-5 20, lg:grid-cols-2); popover 320px (w-80) `absolute left-0 z-50`) — verified=False
    - Popover fits inside the 462px column, so no viewport overflow. Check: (a) the URL row — 320 − p-3 (24) = 296 − gap 6 − Import button (~72) leaves ~218px of Input for the placeholder "https://… or s3://bucket/key" (~209px incl. Input px-3) — it fits by ~9px, so confirm no ellipsis/clip; (b) the popover is anchored below the trigger and z-50 over the dataset list — confirm it doesn't cover a primary action the user needs while the menu is open (the Dropzone above, the dataset rows below); (c) the trigger sits in a bare `<div class="flex items-center gap-2">` between the Dropzone and the upload-status text (data-panel.tsx:290-292) with no heading — confirm it reads as a peer of the drop-zone rather than a stray control; (d) the `connectNote` "coming soon" line appears BELOW the provider list and must not push the popover past the fold.
- **AI panel overlay vs the new inspector dock** (fixed right-0 top-14 z-40 w-[360px] over a 330px InspectorDock) — verified=False
    - Pre-existing owner-directed overlay (not a regression) but the NEW consequence must be eyeballed: a 360px fixed panel fully occludes the 330px inspector dock plus ~30px of the artboard gutter, so the whole new Annotate tab is unreachable while the AI panel is open. Confirm the AI panel's close affordance is reachable and the left ToolsRail stays clear. Second combination to check: export-open sets ArtboardHost `elevated` → `z-[45]`, which is ABOVE the AI panel's z-40 — with BOTH open, confirm the artboard doesn't poke through the AI panel (the same class of bug export-menu.tsx:115-117 already calls out for its trigger).
- **Frozen / read-only inspector (FrozenNotice, relocated into InspectorDock)** (330px dock) — verified=False
    - Moved verbatim from editor-workspace.tsx into shell/inspector-dock.tsx:56-75 — no style change, but it now renders inside the CanvasShell rather than the classic workspace. Confirm the centred lock chip + `max-w-[18rem]` copy + "Edit a copy" button still centre correctly in the 330px dock at full shell height, and that the frozen command cluster ("Frozen — read-only" + "Edit a copy" in the top bar) doesn't duplicate-affordance confusingly with the dock's button.
- **Chart-extractor editor (EditorWorkspace, now ArtboardHost + InspectorDock)** (/extract page container (no workrail) — measure at 1280 and 1440) — verified=False
    - editor-workspace.tsx was gutted to compose the two extracted regions; the doc comment says the chart-extractor renders `<EditorWorkspace store={store}/>` and wants exactly artboard + inspector with no rails. The artboard gutter changed p-6 lg:p-10 → p-3 lg:p-4 in the extracted ArtboardHost, so /extract's editor gained ~48px of figure width and lost its generous gutter. Confirm /extract still looks deliberate (the artboard no longer floats with breathing room on the dark stage) and that the 7-tab inspector strip is checked here too — this is a SECOND host of the same cramped tab strip.
- **Multi-sample scRNA assemble intake (11a115f)** (None) — verified=True
    - No FE surface to check — `git show --stat 11a115f -- app/frontend` returns EMPTY. That commit is backend-only (/data/assemble-scrna); there is no intake UI in this range. Nothing for the main loop to render-verify here.
